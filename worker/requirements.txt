vtk>=9.4
# torch ohne Obergrenze — Hebel 4 (Shape Foundation Model) ist aktuell via
# SHAPE_DISABLE=1 deaktiviert (CPU-Inferenz war unzuverlässig), daher brauchen wir
# torch-cluster nicht mehr und können die neueste torch-Version verwenden.
# Falls Hebel 4 reaktiviert wird: torch auf <2.10 pinnen, sonst gibt's keine
# pre-compiled PyG-Wheels von data.pyg.org.
torch>=2.6
torchvision>=0.18
transformers>=4.41
Pillow>=10.0
numpy>=1.26
psycopg2-binary>=2.9
pgvector>=0.3
boto3>=1.34
python-dotenv>=1.0
# Phase 3: FastAPI Worker-Endpunkte + Celery Queue
fastapi>=0.136.0,<1.0.0
uvicorn>=0.30.0,<1.0.0
celery>=5.6.0,<6.0.0
redis>=7.0.0,<8.0.0
# Hebel 1: Background-Removal für Foto-Vorverarbeitung (rembg = U²Net via onnxruntime)
rembg>=2.0.50,<3.0.0
onnxruntime>=1.16.0,<2.0.0
# Hebel 4 (Shape Foundation Model) — Stack wird nur bei
# `docker build --build-arg ENABLE_SHAPE=1` installiert (siehe Dockerfile):
#   trimesh, gmsh (STEP-Mesh-Loading)
#   torch-geometric, torch-cluster, torch-scatter (MAGNO-Encoder radius search)
#   git+https://github.com/simd-ai/shape (Modell-Code)
#   huggingface_hub (Checkpoint-Download — kommt sonst implizit via transformers)
