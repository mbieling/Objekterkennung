vtk>=9.4
# torch auf 2.9.x gepinnt — PyG (torch-cluster, torch-scatter) hat aktuell keine
# pre-compiled Wheels für torch ≥ 2.10. Hebel 4 (Shape Foundation Model) braucht
# torch-cluster für radius-search im MAGNO-Encoder. Bei Update bitte
# https://data.pyg.org/whl/ prüfen, welche torch-Versionen Wheels haben.
torch>=2.6,<2.10
torchvision>=0.18
transformers>=4.41
Pillow>=10.0
numpy>=1.26
psycopg2-binary>=2.9
pgvector>=0.3
boto3>=1.34
python-dotenv>=1.0
# Phase 3: FastAPI Worker-Endpunkte + Celery Queue
fastapi>=0.136.0,<1.0.0
uvicorn>=0.30.0,<1.0.0
celery>=5.6.0,<6.0.0
redis>=7.0.0,<8.0.0
# Hebel 1: Background-Removal für Foto-Vorverarbeitung (rembg = U²Net via onnxruntime)
rembg>=2.0.50,<3.0.0
onnxruntime>=1.16.0,<2.0.0
# Hebel 4: STEP-Mesh-Loading für Shape Foundation Model
# trimesh nutzt OCC (bereits via conda installiert) als STEP-Backend
trimesh>=4.0.0
# gmsh als robuster STEP-Tessellation-Fallback wenn trimesh scheitert
gmsh>=4.11.0
huggingface_hub>=0.24.0
