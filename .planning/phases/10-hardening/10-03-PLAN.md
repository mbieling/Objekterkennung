---
phase: 10-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/parts/route.ts
  - src/app/admin/CatalogTable.tsx
autonomous: true
requirements:
  - SC-4
must_haves:
  truths:
    - "GET /api/parts akzeptiert ?page=N&limit=20 und gibt nur 20 Einträge pro Seite zurück (LIMIT/OFFSET im SQL)"
    - "GET /api/parts gibt total_count im Response zurück, damit CatalogTable die Gesamtseitenzahl berechnen kann"
    - "CatalogTable lädt immer nur die aktuelle Seite vom Server — kein Fetch aller Datensätze mehr"
    - "Admin-Katalog bleibt unter 2s Ladezeit bei 1.000+ Teilen durch serverseitige Pagination"
  artifacts:
    - path: "src/app/api/parts/route.ts"
      provides: "Pagination-Support: ?page=N&limit=20, LIMIT/OFFSET SQL, total_count in Response"
      contains: "LIMIT"
    - path: "src/app/admin/CatalogTable.tsx"
      provides: "Serverseitige Pagination: fetch mit ?page=N&limit=20, totalPages aus total_count"
      contains: "total_count"
  key_links:
    - from: "CatalogTable.tsx fetchParts"
      to: "GET /api/parts?page=N&limit=20"
      via: "fetch mit page-Parameter"
      pattern: "api/parts.*page="
    - from: "GET /api/parts SQL"
      to: "LIMIT 20 OFFSET n"
      via: "Zod-validierter page-Parameter"
      pattern: "LIMIT.*OFFSET"
---

<objective>
Serverseitige Pagination für den Admin-Katalog implementieren:

1. **API:** GET /api/parts bekommt `?page=N&limit=20` Support mit echtem SQL LIMIT/OFFSET und `total_count` im Response-Body
2. **UI:** CatalogTable refactored: weg von "lade alles, filtere im Client" hin zu "lade Seite N vom Server"

Purpose: SC-4 — Der Admin-Katalog muss bei 1.000+ Teilen unter 2 Sekunden laden. Aktuell werden alle Teile auf einmal geladen; mit serverseitiger Pagination werden nur 20 Zeilen pro Request abgerufen.
Output: Geänderte /api/parts route.ts + geändertes CatalogTable.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hardening/10-UI-SPEC.md
</context>

<interfaces>
<!-- Aktuelle GET /api/parts route.ts (vollständige Datei, 17 Zeilen): -->
<!-- import { NextResponse } from 'next/server' -->
<!-- import { db } from '@/lib/db' -->
<!-- export async function GET(): Promise<NextResponse> { -->
<!--   const rows = await db`SELECT id, name, part_number, project, status, thumbnail_count, created_at FROM parts ORDER BY created_at DESC` -->
<!--   return NextResponse.json({ parts: rows }) -->
<!-- } -->

<!-- Neuer Response-Typ nach dieser Phase: -->
<!-- { parts: Part[], total_count: number, page: number, limit: number, total_pages: number } -->

<!-- CatalogTable.tsx — aktueller Fetch (Zeilen 175-181): -->
<!-- useEffect(() => { -->
<!--   setIsLoading(true) -->
<!--   fetch('/api/parts') -->
<!--     .then(res => res.json()) -->
<!--     .then(data => setParts(data.parts ?? [])) -->
<!--     .catch(() => toast.error('Katalog konnte nicht geladen werden.')) -->
<!--     .finally(() => setIsLoading(false)) -->
<!-- }, []) -->

<!-- Aktueller Pagination-State (Zeilen 159, 220-224): -->
<!-- const [currentPage, setCurrentPage] = useState(1) -->
<!-- const totalPages = Math.max(1, Math.ceil(filteredParts.length / ROWS_PER_PAGE)) -->
<!-- const paginatedParts = filteredParts.slice(...) -->
<!-- Problem: filteredParts enthält ALLE Teile — slice ist clientseitig -->

<!-- Wichtige Architektur-Einschränkung: -->
<!-- Tab-Filter (all/ready/pending/failed/archived) und Suchfeld (searchQuery) werden aktuell -->
<!-- clientseitig auf dem kompletten parts-Array angewendet. -->
<!-- Nach der Umstellung werden Tab-Filter und Suchfeld als Query-Parameter an die API übergeben. -->
<!-- Das ist die einzige Möglichkeit, serverseitige Pagination korrekt zu implementieren. -->

<!-- db-Hilfsfunktion (aus @/lib/db): -->
<!-- Neon postgres tagged template literal — db`SELECT ...` gibt Row[] zurück -->
<!-- Parametrisierte Queries: db`SELECT ... WHERE id = ${id}` -->
<!-- Für LIMIT/OFFSET: db`SELECT ... LIMIT ${limit} OFFSET ${offset}` -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: GET /api/parts — Pagination-Support (SC-4)</name>
  <files>src/app/api/parts/route.ts</files>
  <read_first>
    - src/app/api/parts/route.ts (aktuelle vollständige Implementierung lesen)
    - .planning/phases/10-hardening/10-UI-SPEC.md (Abschnitt "Performance-Standards (SC-4)" und "Pagination-Implementierungsregel")
  </read_first>
  <action>
GET /api/parts vollständig ersetzen. Neue Implementierung:

```typescript
// src/app/api/parts/route.ts
// GET /api/parts — ADMIN-01: Alle Teile für Admin-Katalog abrufen
// Phase 10 — SC-4: Serverseitige Pagination (?page=N&limit=20)
// Server-only — KEIN "use client", keine Browser-Imports.

import { NextRequest, NextResponse } from 'next/server'
import { db } from '@/lib/db'
import { z } from 'zod'

const querySchema = z.object({
  page: z.coerce.number().int().min(1).default(1),
  limit: z.coerce.number().int().min(1).max(100).default(20),
  status: z.enum(['all', 'pending', 'processing', 'ready', 'failed', 'archived']).optional(),
  search: z.string().max(200).optional(),
})

export async function GET(req: NextRequest): Promise<NextResponse> {
  const { searchParams } = req.nextUrl
  const parsed = querySchema.safeParse({
    page: searchParams.get('page') ?? undefined,
    limit: searchParams.get('limit') ?? undefined,
    status: searchParams.get('status') ?? undefined,
    search: searchParams.get('search') ?? undefined,
  })

  if (!parsed.success) {
    return NextResponse.json({ error: 'Ungültige Parameter' }, { status: 400 })
  }

  const { page, limit, status, search } = parsed.data
  const offset = (page - 1) * limit

  // WHERE-Bedingungen dynamisch aufbauen
  // Neon tagged template literals unterstützen keine dynamischen WHERE-Klauseln direkt;
  // daher separate Queries je nach Kombination der Filter.
  // Vereinfachter Ansatz: alle Filter optional, separate Count-Query für total_count.

  let rows: Record<string, unknown>[]
  let countResult: Record<string, unknown>[]

  if (status && status !== 'all' && search) {
    const searchPattern = `%${search}%`
    rows = await db`
      SELECT id, name, part_number, project, status, thumbnail_count, created_at
      FROM parts
      WHERE status = ${status}
        AND (name ILIKE ${searchPattern} OR part_number ILIKE ${searchPattern})
      ORDER BY created_at DESC
      LIMIT ${limit} OFFSET ${offset}
    `
    countResult = await db`
      SELECT COUNT(*)::int AS total
      FROM parts
      WHERE status = ${status}
        AND (name ILIKE ${searchPattern} OR part_number ILIKE ${searchPattern})
    `
  } else if (status && status !== 'all') {
    rows = await db`
      SELECT id, name, part_number, project, status, thumbnail_count, created_at
      FROM parts
      WHERE status = ${status}
      ORDER BY created_at DESC
      LIMIT ${limit} OFFSET ${offset}
    `
    countResult = await db`
      SELECT COUNT(*)::int AS total FROM parts WHERE status = ${status}
    `
  } else if (search) {
    const searchPattern = `%${search}%`
    rows = await db`
      SELECT id, name, part_number, project, status, thumbnail_count, created_at
      FROM parts
      WHERE name ILIKE ${searchPattern} OR part_number ILIKE ${searchPattern}
      ORDER BY created_at DESC
      LIMIT ${limit} OFFSET ${offset}
    `
    countResult = await db`
      SELECT COUNT(*)::int AS total
      FROM parts
      WHERE name ILIKE ${searchPattern} OR part_number ILIKE ${searchPattern}
    `
  } else {
    rows = await db`
      SELECT id, name, part_number, project, status, thumbnail_count, created_at
      FROM parts
      ORDER BY created_at DESC
      LIMIT ${limit} OFFSET ${offset}
    `
    countResult = await db`SELECT COUNT(*)::int AS total FROM parts`
  }

  const totalCount = Number(countResult[0]?.total ?? 0)
  const totalPages = Math.max(1, Math.ceil(totalCount / limit))

  // embedding wird NICHT zurückgegeben — 768-dim, zu groß für Admin-UI
  return NextResponse.json({
    parts: rows,
    total_count: totalCount,
    page,
    limit,
    total_pages: totalPages,
  })
}
```

Hinweis zur Architektur: Der Ansatz mit mehreren if-Zweigen ist explizit und vermeidet dynamisches SQL-Building. Das ist für 4 Kombinationen (status+search, nur status, nur search, keine Filter) vollständig abgedeckt und wartbar.
  </action>
  <verify>
    <automated>grep -n "LIMIT\|OFFSET\|total_count\|total_pages" /Users/mbieling/claude/Objekterkennung/src/app/api/parts/route.ts</automated>
  </verify>
  <done>
    - Datei enthält `LIMIT ${limit} OFFSET ${offset}` in allen Zweigen
    - Response enthält `total_count`, `total_pages`, `page`, `limit`
    - Zod-Schema validiert `page`, `limit`, `status`, `search`
    - Funktion-Signatur ist `GET(req: NextRequest)` (nicht mehr `GET()`)
  </done>
</task>

<task type="auto">
  <name>Task 2: CatalogTable.tsx — serverseitige Pagination refactoring (SC-4)</name>
  <files>src/app/admin/CatalogTable.tsx</files>
  <read_first>
    - src/app/admin/CatalogTable.tsx (vollständige Datei lesen — insbesondere Zeilen 153-234 für State, Fetch und Computed-Werte)
    - .planning/phases/10-hardening/10-UI-SPEC.md (Abschnitt "Performance-Standards (SC-4)")
  </read_first>
  <action>
CatalogTable.tsx refactoring auf serverseitige Pagination. Ziel: Fetch mit ?page=N&limit=20&status=X&search=Y, `total_count` aus Response für Pagination-Berechnung.

**Änderung A — State erweitern (nach den vorhandenen State-Deklarationen ~Zeile 153-164):**
```tsx
const [totalCount, setTotalCount] = useState(0)
const [totalPages, setTotalPages] = useState(1)
```

**Änderung B — Fetch-Funktion extrahieren und generalisieren:**
Den bisherigen initialen `useEffect` (Zeilen 175-181) durch eine `fetchParts`-Funktion ersetzen, die alle Filter-Parameter übergeben kann:

```tsx
// Zentraler Fetch — wird bei Seiten-, Tab- und Suchwechsel aufgerufen
const fetchParts = useCallback(
  async (page: number, tab: TabValue, search: string) => {
    setIsLoading(true)
    const params = new URLSearchParams({
      page: String(page),
      limit: String(ROWS_PER_PAGE),
    })
    if (tab !== 'all') params.set('status', tab)
    if (search) params.set('search', search)
    try {
      const res = await fetch(`/api/parts?${params.toString()}`)
      if (!res.ok) throw new Error('Fetch failed')
      const data = await res.json()
      setParts(data.parts ?? [])
      setTotalCount(data.total_count ?? 0)
      setTotalPages(data.total_pages ?? 1)
    } catch {
      toast.error('Katalog konnte nicht geladen werden.')
    } finally {
      setIsLoading(false)
    }
  },
  []
)
```

`useCallback` benötigt den Import: `import { useState, useRef, useEffect, useCallback } from 'react'`

**Änderung C — Initialer Fetch und Re-Fetch bei Parameterwechsel:**
Den alten `useEffect` (Zeilen 175-181) ersetzen durch:
```tsx
useEffect(() => {
  fetchParts(currentPage, activeTab, searchQuery)
}, [currentPage, activeTab, searchQuery, fetchParts])
```

**Änderung D — Tab-Counts: separater Count-Fetch ODER lokal aus zurückgegebenen Daten:**
Tab-Counts (tabCounts) können mit serverseitiger Pagination nicht mehr clientseitig aus dem `parts`-Array berechnet werden (nur 20 Einträge statt alle).

Lösung: Tab-Counts deaktivieren — Badges zeigen keinen Count mehr an. Stattdessen nur aktiver Tab-Name ohne Zahl. Das ist ein akzeptabler Kompromiss: exakte Tab-Counts würden 5 separate COUNT-Queries benötigen.

Tab-Badges-Logik ersetzen durch leere Counts (oder Badges ganz entfernen):
```tsx
// tabCounts-Computed entfernen (Zeilen 228-234)
// Alle Badge-Anzeigen in TabsTrigger entfernen oder durch leere Spans ersetzen
```

Konkret: In den `<TabsTrigger>`-Elementen (~Zeilen 366-396) die `<Badge>` Komponenten innerhalb der Triggers entfernen. Der Text bleibt ("Alle", "Bereit", "Ausstehend", "Fehler", "Archiviert").

**Änderung E — Suchfeld: debounce triggert fetchParts (nicht mehr clientseitig):**
Der `handleSearchInput`-Handler setzt bereits `searchQuery` via debounce — durch Änderung C wird der neue Wert automatisch einen Re-Fetch triggern. Keine weitere Änderung nötig.

**Änderung F — Computed-Werte `filteredParts` und `paginatedParts` entfernen:**
Da die API nun paginiert, sind `filteredParts` (Zeilen 207-216) und `paginatedParts` (Zeilen 221-224) nicht mehr notwendig. Alle Stellen, die `filteredParts` oder `paginatedParts` referenzieren, durch `parts` ersetzen:
- Tabelle rendert `parts.map(part => ...)` statt `paginatedParts.map`
- Pagination-Bedingung `filteredParts.length > 0 && totalPages > 1` → `parts.length > 0 && totalPages > 1`
- Paginierungstext: `{Math.min((currentPage - 1) * ROWS_PER_PAGE + 1, totalCount)}–{Math.min(currentPage * ROWS_PER_PAGE, totalCount)} von {totalCount} Teilen`
- `parts.length === 0` für den Empty-State bleibt korrekt

**Änderung G — Pagination-Controls: statische Seitennummern bei > 10 Seiten:**
Bei 1.000+ Teilen mit 20/Seite gibt es 50+ Seiten. Die aktuelle Implementierung rendert alle Seiten als Links. Das ist unbrauchbar.

Bestehende Pagination-Logik (~Zeilen 544-594) ersetzen durch ein "fensterbasiertes" Rendering: immer nur 5 Seiten rund um die aktuelle Seite + erste/letzte Seite:

```tsx
{totalPages > 1 && parts.length > 0 && (
  <div className="mt-4 space-y-2">
    <Pagination>
      <PaginationContent>
        <PaginationItem>
          <PaginationPrevious
            href="#"
            onClick={e => { e.preventDefault(); if (currentPage > 1) setCurrentPage(p => p - 1) }}
            aria-disabled={currentPage <= 1}
            className={currentPage <= 1 ? 'pointer-events-none opacity-50' : ''}
          />
        </PaginationItem>
        {/* Immer Seite 1 zeigen */}
        {currentPage > 3 && (
          <>
            <PaginationItem>
              <PaginationLink href="#" onClick={e => { e.preventDefault(); setCurrentPage(1) }}>1</PaginationLink>
            </PaginationItem>
            {currentPage > 4 && <PaginationItem><span className="px-2 text-muted-foreground">…</span></PaginationItem>}
          </>
        )}
        {/* Fensterpages: max 5 um currentPage */}
        {Array.from({ length: totalPages }, (_, i) => i + 1)
          .filter(p => p >= Math.max(1, currentPage - 2) && p <= Math.min(totalPages, currentPage + 2))
          .map(page => (
            <PaginationItem key={page}>
              <PaginationLink
                href="#"
                isActive={page === currentPage}
                onClick={e => { e.preventDefault(); setCurrentPage(page) }}
              >
                {page}
              </PaginationLink>
            </PaginationItem>
          ))}
        {/* Immer letzte Seite zeigen */}
        {currentPage < totalPages - 2 && (
          <>
            {currentPage < totalPages - 3 && <PaginationItem><span className="px-2 text-muted-foreground">…</span></PaginationItem>}
            <PaginationItem>
              <PaginationLink href="#" onClick={e => { e.preventDefault(); setCurrentPage(totalPages) }}>{totalPages}</PaginationLink>
            </PaginationItem>
          </>
        )}
        <PaginationItem>
          <PaginationNext
            href="#"
            onClick={e => { e.preventDefault(); if (currentPage < totalPages) setCurrentPage(p => p + 1) }}
            aria-disabled={currentPage >= totalPages}
            className={currentPage >= totalPages ? 'pointer-events-none opacity-50' : ''}
          />
        </PaginationItem>
      </PaginationContent>
    </Pagination>
    <p className="text-xs text-muted-foreground text-center">
      Zeige {Math.min((currentPage - 1) * ROWS_PER_PAGE + 1, totalCount)}–{Math.min(currentPage * ROWS_PER_PAGE, totalCount)} von {totalCount} Teilen
    </p>
  </div>
)}
```

**Änderung H — handleTabChange und handleSearchClear setCurrentPage(1) beibehalten:**
Diese Handler sind bereits korrekt implementiert — keine Änderung nötig.
  </action>
  <verify>
    <automated>grep -n "total_count\|fetchParts\|useCallback\|params.set" /Users/mbieling/claude/Objekterkennung/src/app/admin/CatalogTable.tsx</automated>
  </verify>
  <done>
    - `fetchParts` ist als `useCallback` deklariert und mit page, tab, search-Parametern aufgerufen
    - `useEffect` mit `[currentPage, activeTab, searchQuery, fetchParts]` Dependencies triggert Re-Fetch
    - `total_count` und `total_pages` werden aus der API-Response gelesen und in State gesetzt
    - Tabelle rendert `parts.map` (nicht mehr `paginatedParts.map`)
    - Pagination zeigt Fenster-basierte Seitenzahlen (nicht alle 50+)
    - `npm run build` läuft ohne TypeScript-Fehler durch
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/parts?page=N&limit=20&status=X&search=Y | Query-Parameter kommen vom Client — alle müssen serverseitig validiert werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering | ?page Parameter: coerce.number().int().min(1) | mitigate | Zod-Validierung mit `.safeParse()` — ungültige Werte geben HTTP 400 zurück; kein SQL-Injection möglich durch parametrisierte Neon-Queries |
| T-10-03-02 | Tampering | ?search Parameter: ILIKE ${searchPattern} | mitigate | Neon tagged template literals sind parametrisierte Queries — SQL-Injection ausgeschlossen; Maximallänge 200 Zeichen via Zod |
| T-10-03-03 | DoS | ?limit Parameter: max(100) durch Zod | mitigate | Limit auf maximal 100 beschränkt — verhindert "LIMIT 1000000"-Angriff |
| T-10-03-04 | Information Disclosure | GET /api/parts gibt alle Parts ohne Auth zurück | accept | Admin-Route ohne Auth in Phase 5 bewusst entschieden (kein Auth-Scope in v1) — bleibt in Phase 10 unverändert; interne Werkstatt-App ohne öffentlichen Zugang |
</threat_model>

<verification>
```bash
cd /Users/mbieling/claude/Objekterkennung
npm run build 2>&1 | tail -20
```

Build muss ohne TypeScript-Fehler durchlaufen.

```bash
grep -n "LIMIT\|OFFSET\|total_count\|total_pages" src/app/api/parts/route.ts
grep -n "fetchParts\|total_count\|useCallback" src/app/admin/CatalogTable.tsx
```

Alle sechs Strings müssen in den jeweiligen Dateien vorhanden sein.
</verification>

<success_criteria>
- GET /api/parts?page=1&limit=20 gibt genau 20 Zeilen + `total_count` + `total_pages` zurück (LIMIT/OFFSET im SQL)
- GET /api/parts?page=1&limit=20&status=failed gibt nur failed-Parts zurück
- GET /api/parts?search=flansch gibt nur Parts mit "flansch" in name oder part_number zurück
- CatalogTable lädt beim Tab-Wechsel oder Suchfeld-Eingabe neu vom Server
- Pagination-Controls zeigen max 5 Seitennummern (kein "50 Buttons für 50 Seiten")
- `npm run build` läuft ohne Fehler
</success_criteria>

<output>
Nach Abschluss SUMMARY erstellen unter:
`.planning/phases/10-hardening/10-03-SUMMARY.md`
</output>
