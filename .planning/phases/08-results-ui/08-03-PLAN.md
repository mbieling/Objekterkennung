---
phase: 08-results-ui
plan: 03
type: execute
wave: 2
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - src/app/search/CameraCapture.tsx
autonomous: true
requirements:
  - SEARCH-03
  - SEARCH-04
  - SEARCH-05

must_haves:
  truths:
    - "CameraCapture.tsx ruft POST /api/search mit threshold=0&limit=Math.max(50, displayLimit) auf"
    - "result-State zeigt SearchResults-Komponente statt <pre>-JSON-Placeholder"
    - "Neu aufnehmen setzt searchResult NICHT zurück — searching-State prüft ob searchResult vorhanden"
    - "Bei Re-Suche (searchResult !== null im searching-State): Grid + Spinner-Overlay sichtbar (D-11)"
    - "Bei Erst-Suche (searchResult === null im searching-State): reiner Spinner"
    - "displayThreshold-State (default 0.5) und displayLimit-State (default 10) sind in CameraCapture vorhanden"
    - "Limit-Select-Wechsel via onLimitChange triggert neue API-Anfrage mit neuem Limit"
  artifacts:
    - path: "src/app/search/CameraCapture.tsx"
      provides: "Erweiterte State-Machine mit displayThreshold, displayLimit, SearchResults-Integration"
      contains: "displayThreshold"
  key_links:
    - from: "src/app/search/CameraCapture.tsx"
      to: "src/app/search/SearchResults.tsx"
      via: "import { SearchResults } from './SearchResults'"
      pattern: "SearchResults"
    - from: "CameraCapture.tsx handleSearch"
      to: "/api/search?threshold=0&limit=..."
      via: "fetch URL mit Query-Params"
      pattern: "threshold=0"
---

<objective>
CameraCapture.tsx erweitern: State-Machine um displayThreshold/displayLimit ergänzen, handleSearch auf threshold=0-URL umstellen, result-State durch SearchResults-Komponente ersetzen, searching-State für D-11-Spinner-Overlay anpassen.

Purpose: Dies ist die Integrations-Schicht — SearchResultCard und SearchResults aus Wave 1 werden hier verkabelt. Nach diesem Plan ist die vollständige Results-UI funktionsfähig (ohne E2E-Tests).
Output: Geändertes CameraCapture.tsx. `npm test` grün (inkl. CameraCapture.test.tsx).
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-results-ui/08-CONTEXT.md
@.planning/phases/08-results-ui/08-PATTERNS.md
@.planning/phases/08-results-ui/08-02-SUMMARY.md

<interfaces>
<!-- Aus src/app/search/SearchResults.tsx (Wave 1 erstellt) -->
```typescript
// SearchResultsProps — exakt diese Props muss CameraCapture.tsx übergeben:
interface SearchResultsProps {
  searchResult: {
    results: SearchResultItem[]
    query: { threshold: number; limit: number; results_count: number }
  }
  displayThreshold: number      // 0.0–1.0; Slider-Wert; Default: 0.5
  displayLimit: number          // 10 | 20 | 50; Default: 10
  onThresholdChange: (val: number) => void
  onLimitChange: (val: number) => void  // triggert neue API-Anfrage
}
```

<!-- Aus src/app/search/CameraCapture.tsx (AKTUELLER STAND — vor Phase-8-Änderungen) -->
<!-- Zeile 78–83: existierende State-Variablen -->
```typescript
const [phase, setPhase] = useState<SearchPhase>('idle')
const [errorMessage, setErrorMessage] = useState<string | null>(null)
const [previewUrl, setPreviewUrl] = useState<string | null>(null)
const [searchResult, setSearchResult] = useState<SearchResponse | null>(null)
const [capturedBlob, setCapturedBlob] = useState<Blob | null>(null)
```

<!-- Zeile 169–197: handleSearch (aktueller Stand — wird erweitert) -->
```typescript
async function handleSearch() {
  if (!capturedBlob) return
  setPhase('searching')
  setErrorMessage(null)
  const formData = new FormData()
  formData.append('image', capturedBlob, 'capture.jpg')
  const controller = new AbortController()
  const timeoutId = setTimeout(() => controller.abort(), 30_000)
  try {
    const res = await fetch('/api/search', {  // <-- wird zu fetch(`/api/search?threshold=0&limit=...`)
      method: 'POST',
      body: formData,
      signal: controller.signal,
    })
    if (!res.ok) throw new Error(`HTTP ${res.status}`)
    const data: SearchResponse = await res.json()
    setSearchResult(data)
    setPhase('result')
  } catch (err) { ... }
  finally { clearTimeout(timeoutId) }
}
```

<!-- Zeile 299–328: searching- und result-Block (werden ersetzt) -->
```typescript
// SEARCHING (Zeilen 299–305): wird durch D-11-Overlay-Logik ersetzt
{phase === 'searching' && (
  <div className="flex flex-col items-center gap-4 py-8" aria-live="polite">
    <Loader2 className="animate-spin h-8 w-8" aria-label="Suche läuft" />
    <p className="text-sm text-muted-foreground">Suche läuft...</p>
  </div>
)}

// RESULT (Zeilen 307–328): <pre>-Placeholder wird durch <SearchResults /> ersetzt
{phase === 'result' && (
  <div className="flex flex-col gap-4">
    <pre className="text-xs overflow-auto bg-muted p-4 rounded-lg max-h-96">
      {JSON.stringify(searchResult, null, 2)}
    </pre>
    <Button variant="outline" className="w-full" onClick={() => {
      setPhase('idle')
      setSearchResult(null)
      ...
    }}>
      <RotateCcw className="mr-2 h-4 w-4" />
      Neu aufnehmen
    </Button>
  </div>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CameraCapture.tsx — State + handleSearch + searching-Block + result-Block erweitern</name>
  <files>src/app/search/CameraCapture.tsx</files>

  <read_first>
    - src/app/search/CameraCapture.tsx (VOLLSTÄNDIG lesen — aktuelle State-Machine, alle Handler, alle Render-Blöcke)
    - src/app/search/SearchResults.tsx (SearchResultsProps-Interface prüfen — korrekte Props-Namen)
    - .planning/phases/08-results-ui/08-PATTERNS.md (Abschnitt "CameraCapture.tsx MODIFY" — exakte Änderungsstellen)
  </read_first>

  <action>
Die Datei `src/app/search/CameraCapture.tsx` wird an 5 präzisen Stellen geändert:

**Änderung 1 — Import hinzufügen** (nach Zeile 12, nach dem lucide-react Import):
```typescript
import { SearchResults } from './SearchResults'
```

**Änderung 2 — State-Variablen hinzufügen** (nach Zeile 83, nach `setCapturedBlob`-State):
```typescript
// Phase 8: Threshold + Limit für SearchResults (D-06, D-08)
const [displayThreshold, setDisplayThreshold] = useState<number>(0.5)
const [displayLimit, setDisplayLimit] = useState<number>(10)
```

**Änderung 3 — handleSearch umstellen** (Zeile 178 ersetzen — nur die fetch-URL ändern):
```typescript
// VORHER:
const res = await fetch('/api/search', {
  method: 'POST',
  body: formData,
  signal: controller.signal,
})

// NACHHER (D-07: threshold=0 holt alle Kandidaten; Slider filtert lokal):
const res = await fetch(
  `/api/search?threshold=0&limit=${Math.max(50, displayLimit)}`,
  {
    method: 'POST',
    body: formData,
    // KEIN Content-Type-Header — Browser setzt multipart Boundary automatisch
    signal: controller.signal,
  }
)
```

**Außerdem:** Eine neue Funktion `handleSearchWithLimit` hinzufügen (nach der `handleSearch`-Funktion), die eine neue Suche mit einem spezifischen Limit auslöst (für D-08 Limit-Select):
```typescript
// D-08: Limit-Wechsel triggert neue API-Anfrage
async function handleSearchWithLimit(newLimit: number) {
  if (!capturedBlob) return
  setPhase('searching')
  setErrorMessage(null)
  const formData = new FormData()
  formData.append('image', capturedBlob, 'capture.jpg')
  const controller = new AbortController()
  const timeoutId = setTimeout(() => controller.abort(), 30_000)
  try {
    const res = await fetch(
      `/api/search?threshold=0&limit=${Math.max(50, newLimit)}`,
      { method: 'POST', body: formData, signal: controller.signal }
    )
    if (!res.ok) throw new Error(`HTTP ${res.status}`)
    const data: SearchResponse = await res.json()
    setSearchResult(data)
    setPhase('result')
  } catch (err) {
    const msg = err instanceof DOMException && err.name === 'AbortError'
      ? 'Suche hat zu lange gedauert. Bitte erneut versuchen.'
      : 'Suche fehlgeschlagen. Bitte überprüfe deine Verbindung und versuche es erneut.'
    setErrorMessage(msg)
    setPhase('error')
  } finally {
    clearTimeout(timeoutId)
  }
}
```

**Änderung 4 — searching-Block ersetzen** (Zeilen 299–305, D-11: Spinner-Overlay bei Re-Suche):
```typescript
{/* searching-State (D-11: Overlay über altem Grid wenn Re-Suche; reiner Spinner bei Erst-Suche) */}
{phase === 'searching' && (
  searchResult ? (
    // Re-Suche: Grid bleibt sichtbar, Spinner-Overlay darüber
    <div className="flex flex-col gap-4">
      <div className="relative">
        <div className="absolute inset-0 bg-background/70 flex items-center justify-center rounded-lg z-10">
          <Loader2 className="animate-spin h-8 w-8" aria-label="Neue Suche läuft" />
        </div>
        <SearchResults
          searchResult={searchResult}
          displayThreshold={displayThreshold}
          displayLimit={displayLimit}
          onThresholdChange={setDisplayThreshold}
          onLimitChange={() => {}}
        />
      </div>
    </div>
  ) : (
    // Erst-Suche: reiner Spinner
    <div className="flex flex-col items-center gap-4 py-8" aria-live="polite">
      <Loader2 className="animate-spin h-8 w-8" aria-label="Suche läuft" />
      <p className="text-sm text-muted-foreground">Suche läuft...</p>
    </div>
  )
)}
```

**Änderung 5 — result-Block ersetzen** (Zeilen 307–328, `<pre>` durch `<SearchResults>` ersetzen):
```typescript
{/* result-State — SearchResults ersetzt den pre-JSON-Placeholder (Phase 8) */}
{phase === 'result' && searchResult && (
  <div className="flex flex-col gap-4">
    <SearchResults
      searchResult={searchResult}
      displayThreshold={displayThreshold}
      displayLimit={displayLimit}
      onThresholdChange={setDisplayThreshold}
      onLimitChange={(newLimit) => {
        setDisplayLimit(newLimit)
        // D-08: Limit-Wechsel triggert neue API-Anfrage
        handleSearchWithLimit(newLimit)
      }}
    />
    <Button variant="outline" className="w-full" onClick={handleRetry}>
      <RotateCcw className="mr-2 h-4 w-4" />
      Neu aufnehmen
    </Button>
  </div>
)}
```

**Wichtig bei handleRetry:** Die bestehende `handleRetry`-Funktion setzt `setSearchResult(null)` — das ist korrekt (User kehrt zu idle zurück, alle Ergebnisse verworfen). Nicht ändern.

**Wichtig: `<pre>` vollständig entfernen** — Der alte pre-Block muss aus dem result-State verschwinden. Nach der Änderung darf kein `<pre>` mehr in CameraCapture.tsx stehen (Phase-7-E2E-Test wurde dafür angepasst).
  </action>

  <verify>
    <automated>npm test -- --reporter=verbose src/app/search/CameraCapture.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -v '^//' src/app/search/CameraCapture.tsx | grep -c '<pre'` gibt 0 zurück (kein pre-Element mehr)
    - `grep "threshold=0" src/app/search/CameraCapture.tsx` findet den API-Call mit threshold=0
    - `grep "displayThreshold" src/app/search/CameraCapture.tsx` findet State-Variable
    - `grep "displayLimit" src/app/search/CameraCapture.tsx` findet State-Variable
    - `grep "SearchResults" src/app/search/CameraCapture.tsx` findet Import und Verwendung
    - `grep "handleSearchWithLimit" src/app/search/CameraCapture.tsx` findet die neue Funktion
    - `grep "searchResult.*&&\|&&.*searchResult" src/app/search/CameraCapture.tsx` findet D-11-Kondition im searching-Block
    - `npm test -- src/app/search/CameraCapture.test.tsx` — alle bestehenden Tests grün (kein Regressionen)
    - `npm run build` — TypeScript-Kompilierung ohne Fehler
  </acceptance_criteria>

  <done>CameraCapture.tsx vollständig erweitert. Kein &lt;pre&gt; mehr. SearchResults integriert. D-11-Overlay implementiert. npm test grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capturedBlob → FormData → POST /api/search | Blob kommt von getUserMedia (Kamera) oder File-Input; MIME-Typ-Check bereits in handleFileSelect |
| API-Response → setSearchResult | JSON aus validiertem API-Endpoint; similarity ist parseFloat-gesicherter Float |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | handleSearch: displayLimit in URL | accept | `Math.max(50, displayLimit)` — displayLimit ist ein kontrollieter State-Wert (10/20/50); kein User-Input direkt in URL; keine SQL-Injection möglich (Query-Param wird von route.ts mit parseInt verarbeitet) |
| T-08-03-02 | Information Disclosure | searchResult State in Browser | accept | Ergebnisse sind nicht sensibel (Teil-Namen und Ähnlichkeitswerte); kein Benutzer-PII; kein localStorage |
</threat_model>

<verification>
Nach Wave 2:
- `npm test` — vollständige Vitest-Suite grün
- `grep -v '^//' src/app/search/CameraCapture.tsx | grep '<pre'` — keine Treffer
- `grep "SearchResults" src/app/search/CameraCapture.tsx` — 2+ Treffer (Import + Verwendung)
- `npm run build` — TypeScript kompiliert ohne Fehler
</verification>

<success_criteria>
- CameraCapture.tsx hat kein &lt;pre&gt; mehr (D-10-Placeholder entfernt)
- handleSearch ruft `/api/search?threshold=0&limit=...` auf (D-07)
- handleSearchWithLimit-Funktion vorhanden (D-08)
- result-State zeigt SearchResults (SEARCH-03)
- searching-State zeigt Grid + Overlay bei Re-Suche (D-11)
- npm test vollständig grün
- npm run build ohne TypeScript-Fehler
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08-results-ui/08-03-SUMMARY.md` erstellen.
</output>
