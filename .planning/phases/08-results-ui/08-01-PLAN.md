---
phase: 08-results-ui
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/ui/slider.tsx
  - src/app/search/SearchResultCard.test.tsx
  - src/app/search/SearchResults.test.tsx
  - tests/phase-08-results-ui.spec.ts
  - tests/phase-07-camera-ui.spec.ts
autonomous: true
requirements:
  - SEARCH-03
  - SEARCH-04
  - SEARCH-05

must_haves:
  truths:
    - "shadcn Slider-Komponente ist unter src/components/ui/slider.tsx installiert"
    - "Vitest-Stubs für SearchResultCard und SearchResults existieren (alle Tests als it.todo)"
    - "Playwright-Stubs für Phase 8 existieren (alle Tests als test.skip)"
    - "Phase-7 E2E-Tests brechen nicht mehr durch locator('pre')-Referenzen"
  artifacts:
    - path: "src/components/ui/slider.tsx"
      provides: "shadcn Slider-Primitiv (Radix-basiert)"
    - path: "src/app/search/SearchResultCard.test.tsx"
      provides: "Unit-Test-Stubs für SEARCH-03 (Badge-Farben, Thumbnail-Skeleton)"
    - path: "src/app/search/SearchResults.test.tsx"
      provides: "Unit-Test-Stubs für SEARCH-04 (Slider-Filterung), SEARCH-05 (Limit-Select)"
    - path: "tests/phase-08-results-ui.spec.ts"
      provides: "Playwright E2E-Stubs für Phase 8 Results UI"
    - path: "tests/phase-07-camera-ui.spec.ts"
      provides: "Gefixter Phase-7 E2E ohne locator('pre')-Assertions"
  key_links:
    - from: "src/app/search/SearchResultCard.tsx (Wave 1)"
      to: "src/app/search/SearchResultCard.test.tsx"
      via: "named export { SearchResultCard }"
      pattern: "import.*SearchResultCard"
    - from: "src/app/search/SearchResults.tsx (Wave 1)"
      to: "src/app/search/SearchResults.test.tsx"
      via: "named export { SearchResults }"
      pattern: "import.*SearchResults"
---

<objective>
Wave-0-Blocker für Phase 8: Slider-Komponente installieren, Test-Gerüste anlegen und Phase-7-E2E-Breaking-Change beheben — bevor eine einzige Produktionszeile geschrieben wird.

Purpose: Wave 1 (SearchResultCard + SearchResults) kann nicht starten, solange slider.tsx fehlt. Die Phase-7 E2E-Suite muss grün bleiben, nachdem Phase 8 das `<pre>`-Element entfernt.
Output: slider.tsx installiert, 4 Test-Dateien angelegt/gefixt, `npm test && npm run test:e2e` grün.
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-results-ui/08-CONTEXT.md
@.planning/phases/08-results-ui/08-RESEARCH.md
@.planning/phases/08-results-ui/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn Slider installieren + Vitest-Test-Stubs anlegen</name>
  <files>
    src/components/ui/slider.tsx
    src/app/search/SearchResultCard.test.tsx
    src/app/search/SearchResults.test.tsx
  </files>

  <read_first>
    - src/app/search/CameraCapture.test.tsx (Boilerplate-Muster: describe, it.todo, vi.fn, beforeEach/afterEach)
    - src/components/ui/badge.tsx (prüfen ob shadcn-Slider-Format konsistent ist)
    - src/app/search/CameraCapture.tsx (SearchResponse-Interface — wird in Stubs als Typ gebraucht)
  </read_first>

  <action>
1. Slider installieren:
   ```bash
   npx shadcn@latest add slider --yes
   ```
   Nach der Installation: `src/components/ui/slider.tsx` lesen und Props-Interface prüfen (Assumption A1 aus RESEARCH.md: `value={[number]}`, `onValueChange={([val]) => ...}`). Falls das Interface abweicht, das korrekte Interface notieren — Wave 1 wird es brauchen.

2. `src/app/search/SearchResultCard.test.tsx` anlegen:
   ```typescript
   // src/app/search/SearchResultCard.test.tsx
   // Phase 8 — Unit Tests für SearchResultCard (SEARCH-03)

   import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
   import { render, screen, waitFor } from '@testing-library/react'

   global.fetch = vi.fn()
   global.URL.createObjectURL = vi.fn().mockReturnValue('blob:fake-url')
   global.URL.revokeObjectURL = vi.fn()

   describe('Phase 8: SearchResultCard', () => {
     beforeEach(() => { vi.clearAllMocks() })
     afterEach(() => { vi.restoreAllMocks() })

     it.todo('SEARCH-03: zeigt Skeleton während Thumbnail lädt')
     it.todo('SEARCH-03: Badge bg-green-500 bei similarity >= 0.80')
     it.todo('SEARCH-03: Badge bg-amber-500 bei similarity 0.60–0.79')
     it.todo('SEARCH-03: Badge bg-red-500 bei similarity < 0.60')
     it.todo('SEARCH-03: Link href="/parts/{id}" korrekt gesetzt')
     it.todo('SEARCH-03: Name mit truncate dargestellt')
     it.todo('SEARCH-03: Thumbnail erscheint nach erfolgreichem fetch')
     it.todo('SEARCH-03: Skeleton bleibt sichtbar bei Thumbnail-404')
   })
   ```

3. `src/app/search/SearchResults.test.tsx` anlegen:
   ```typescript
   // src/app/search/SearchResults.test.tsx
   // Phase 8 — Unit Tests für SearchResults (SEARCH-04, SEARCH-05)

   import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
   import { render, screen, fireEvent, waitFor } from '@testing-library/react'

   global.fetch = vi.fn()
   global.URL.createObjectURL = vi.fn().mockReturnValue('blob:fake-url')
   global.URL.revokeObjectURL = vi.fn()

   describe('Phase 8: SearchResults', () => {
     beforeEach(() => { vi.clearAllMocks() })
     afterEach(() => { vi.restoreAllMocks() })

     it.todo('SEARCH-04: Slider-Filterung — threshold 0.5 zeigt nur Treffer mit similarity >= 0.5')
     it.todo('SEARCH-04: Slider-Filterung — threshold 0.9 zeigt Leer-Zustand wenn kein Treffer >= 0.9')
     it.todo('SEARCH-04: Leer-Zustand zeigt "Keine ähnlichen Teile gefunden." und Slider-Hinweis')
     it.todo('SEARCH-05: Limit-Select-Wechsel ruft onLimitChange mit neuem Wert auf')
     it.todo('SEARCH-05: Limit-Select zeigt Optionen 10, 20, 50')
     it.todo('SEARCH-03: Ergebnis-Liste ist nach similarity DESC sortiert (API-Reihenfolge beibehalten)')
     it.todo('SEARCH-03: aria-live="polite" auf Ergebnis-Container gesetzt')
   })
   ```
  </action>

  <verify>
    <automated>npm test -- --reporter=verbose src/app/search/SearchResultCard.test.tsx src/app/search/SearchResults.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `src/components/ui/slider.tsx` existiert: `ls src/components/ui/slider.tsx` gibt Datei zurück
    - Slider-Props-Interface bestätigt (nach Installation lesen): enthält `value`, `onValueChange`, `min`, `max`, `step`
    - `src/app/search/SearchResultCard.test.tsx` existiert mit 8 it.todo-Einträgen: `grep -c 'it.todo' src/app/search/SearchResultCard.test.tsx` gibt 8
    - `src/app/search/SearchResults.test.tsx` existiert mit 7 it.todo-Einträgen: `grep -c 'it.todo' src/app/search/SearchResults.test.tsx` gibt 7
    - `npm test -- src/app/search/SearchResultCard.test.tsx src/app/search/SearchResults.test.tsx` läuft ohne Fehler (alle Tests als "todo" markiert)
  </acceptance_criteria>

  <done>slider.tsx installiert. Beide Vitest-Test-Stub-Dateien existieren. npm test für die Stubs grün (todo).</done>
</task>

<task type="auto">
  <name>Task 2: Phase-7 E2E-Breaking-Change fixen + Phase-8 Playwright-Stubs anlegen</name>
  <files>
    tests/phase-07-camera-ui.spec.ts
    tests/phase-08-results-ui.spec.ts
  </files>

  <read_first>
    - tests/phase-07-camera-ui.spec.ts (aktuelle Datei — Zeilen 44–46 und 113–115 enthalten locator('pre'), die Phase 8 brechen)
    - tests/phase-08-results-ui.spec.ts (existiert noch nicht)
    - .planning/phases/08-results-ui/08-PATTERNS.md (Abschnitt "phase-07-camera-ui.spec.ts MODIFY" für exakte Ersetzungen)
  </read_first>

  <action>
1. `tests/phase-07-camera-ui.spec.ts` — zwei Stellen fixen:

   **Zeilen 44–46** (Test "SEARCH-02: Datei-Upload via File-Input löst Suche aus"):
   Ersetze:
   ```typescript
   await expect(page.locator('pre')).toBeVisible({ timeout: 10_000 })
   await expect(page.locator('pre')).toContainText('results_count')
   ```
   Durch:
   ```typescript
   // Phase 8: pre-Block entfernt — Leer-Zustand prüfen (mock gibt leere results zurück)
   await expect(page.getByText('Keine ähnlichen Teile gefunden.')).toBeVisible({ timeout: 10_000 })
   ```

   **Zeilen 112–116** (Test "D-10: JSON-Ergebnis in pre-Block nach erfolgreicher Suche"):
   - Test-Name umbenennen: `'D-10: Ergebnis-Grid nach erfolgreicher Suche'`
   Ersetze:
   ```typescript
   await expect(page.locator('pre')).toBeVisible({ timeout: 10_000 })
   await expect(page.locator('pre')).toContainText('Testbauteil')
   await expect(page.locator('pre')).toContainText('"similarity": 0.87')
   ```
   Durch:
   ```typescript
   // Phase 8: Ergebnis-Grid statt pre-Block
   await expect(page.getByText('Testbauteil')).toBeVisible({ timeout: 10_000 })
   await expect(page.getByText('87%')).toBeVisible()
   ```
   Außerdem muss der Thumbnail-Mock zum Phase-7-Test hinzugefügt werden, damit der Thumbnail-Fetch nicht zu 404 führt:
   ```typescript
   await page.route('/api/parts/*/thumbnail', async route => {
     await route.fulfill({ status: 404 })
   })
   ```
   Diesen Mock direkt nach dem `/api/search`-Mock-Block im D-10-Test einfügen.

2. `tests/phase-08-results-ui.spec.ts` anlegen:
   ```typescript
   // tests/phase-08-results-ui.spec.ts
   // E2E-Tests für Phase 8 Results UI (SEARCH-03, SEARCH-04, SEARCH-05)
   // Stubs — werden in Wave 3 (Plan 04) aktiviert

   import { test, expect } from '@playwright/test'

   // Mock-Ergebnis-Fixture mit 3 Treffern verschiedener Similarity-Werte
   const mockSearchResponse = {
     results: [
       { id: 'id-01', name: 'Flanschplatte', part_number: 'FP-001', project: null,
         status: 'ready', similarity: 0.92, created_at: '2026-01-01T00:00:00Z' },
       { id: 'id-02', name: 'Schraubenring', part_number: null, project: 'Motor',
         status: 'ready', similarity: 0.67, created_at: '2026-01-01T00:00:00Z' },
       { id: 'id-03', name: 'Dichtungsring', part_number: 'DR-42', project: null,
         status: 'ready', similarity: 0.45, created_at: '2026-01-01T00:00:00Z' },
     ],
     query: { threshold: 0, limit: 50, results_count: 3 },
   }

   test.describe('Phase 8: Results UI', () => {

     test.skip('SEARCH-03: Ergebnis-Grid sichtbar nach Suche', async ({ page }) => {})
     test.skip('SEARCH-03: Badge-Farben korrekt (grün/amber/rot)', async ({ page }) => {})
     test.skip('SEARCH-04: Slider-Filterung reduziert angezeigte Karten', async ({ page }) => {})
     test.skip('SEARCH-05: Limit-Select-Wechsel triggert neue Suche', async ({ page }) => {})
     test.skip('D-10: Leer-Zustand bei hohem Threshold', async ({ page }) => {})
     test.skip('D-11: Spinner-Overlay bei Re-Suche, Grid bleibt sichtbar', async ({ page }) => {})

   })
   ```
  </action>

  <verify>
    <automated>npm run test:e2e -- tests/phase-07-camera-ui.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "locator('pre')" tests/phase-07-camera-ui.spec.ts` gibt 0 zurück
    - `grep "Keine ähnlichen Teile gefunden" tests/phase-07-camera-ui.spec.ts` findet einen Treffer
    - `grep "87%" tests/phase-07-camera-ui.spec.ts` findet einen Treffer
    - `grep "Testbauteil" tests/phase-07-camera-ui.spec.ts` findet einen Treffer
    - `tests/phase-08-results-ui.spec.ts` existiert: `ls tests/phase-08-results-ui.spec.ts`
    - `grep -c 'test.skip' tests/phase-08-results-ui.spec.ts` gibt 6 zurück
    - `npm run test:e2e -- tests/phase-07-camera-ui.spec.ts` läuft grün (alle bestehenden Tests bestehen)
  </acceptance_criteria>

  <done>Phase-7 E2E ohne locator('pre')-Assertions. Phase-8 Playwright-Stubs existieren mit 6 test.skip-Einträgen. Phase-7 E2E grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Dateisystem | npx shadcn@latest schreibt in src/components/ui/ — nur slider.tsx erwartet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | npx shadcn@latest add slider | accept | shadcn CLI ist projekt-etabliert (alle anderen ui/*.tsx via CLI installiert); kein User-Input involviert |
</threat_model>

<verification>
Nach Wave 0:
- `ls src/components/ui/slider.tsx` — Datei vorhanden
- `npm test -- src/app/search/SearchResultCard.test.tsx src/app/search/SearchResults.test.tsx` — grün (todo)
- `npm run test:e2e -- tests/phase-07-camera-ui.spec.ts` — grün (kein locator('pre') mehr)
- `grep -c "locator('pre')" tests/phase-07-camera-ui.spec.ts` — gibt 0
</verification>

<success_criteria>
- slider.tsx installiert und Props-Interface verifiziert
- SearchResultCard.test.tsx und SearchResults.test.tsx existieren mit todo-Stubs
- phase-08-results-ui.spec.ts existiert mit skip-Stubs
- phase-07-camera-ui.spec.ts läuft grün ohne pre-Assertions
- npm test grün
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08-results-ui/08-01-SUMMARY.md` erstellen.
</output>
