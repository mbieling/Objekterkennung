---
phase: 06-search-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/app/api/search/route.ts
autonomous: true
requirements:
  - SEARCH-03
  - SEARCH-04
  - SEARCH-05

must_haves:
  truths:
    - "POST /api/search akzeptiert multipart/form-data mit einem 'image'-Feld"
    - "Threshold und Limit werden per Zod z.coerce.number() validiert (D-05, D-06, D-07)"
    - "Das Suchbild wird temporär in S3 unter search-temp/{uuid}.jpg gespeichert (D-03)"
    - "Der Worker /embed wird synchron aufgerufen mit AbortSignal.timeout(28_000) (D-04, D-02)"
    - "S3 Cleanup (DeleteObjectCommand) erfolgt in ALLEN Fehler-Pfaden und nach Embedding-Erhalt"
    - "pgvector-Query nutzt embeddingLiteral als String mit ::vector-Cast (Pitfall 1 adressiert)"
    - "Similarity-Alias wird NICHT im WHERE verwendet — Ausdruck vollständig wiederholt (Pitfall 3)"
    - "Response entspricht D-11-Shape mit parseFloat(row.similarity)"
    - "export const maxDuration = 30 als Module-Level-Export vorhanden (D-02)"
    - "Filter: WHERE status = 'ready' — KEIN is_archived (D-12)"
  artifacts:
    - path: "src/app/api/search/route.ts"
      provides: "POST /api/search Handler"
      min_lines: 100
      contains: "export const maxDuration = 30"
  key_links:
    - from: "src/app/api/search/route.ts"
      to: "BUCKET_THUMBNAILS"
      via: "PutObjectCommand + DeleteObjectCommand"
      pattern: "search-temp/"
    - from: "src/app/api/search/route.ts"
      to: "workerUrl/embed"
      via: "fetch mit AbortSignal.timeout(28_000)"
      pattern: "AbortSignal.timeout"
    - from: "src/app/api/search/route.ts"
      to: "parts (Neon)"
      via: "db tagged template mit ::vector cast"
      pattern: "::vector"
---

<objective>
`POST /api/search` Route implementieren: multipart/form-data empfangen, Zod-Validierung für Query-Parameter, Suchbild temporär in S3 hochladen, Worker `/embed` synchron aufrufen, S3-Cleanup auf allen Pfaden, pgvector-Cosine-Query mit korrektem `::vector`-Cast, gerankete Treffer als JSON zurückgeben.

Purpose: Das Herzstück der Search Pipeline — verbindet S3-Transfer, Python Worker und pgvector-Datenbank zu einer synchronen Suchoperation.
Output: `src/app/api/search/route.ts` — vollständig implementierter Route Handler.
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-search-pipeline/06-CONTEXT.md
@.planning/phases/06-search-pipeline/06-RESEARCH.md
@.planning/phases/06-search-pipeline/06-PATTERNS.md
</context>

<interfaces>
<!-- Aus src/lib/s3.ts: -->
```typescript
export const s3: S3Client  // S3Client-Instanz (forcePathStyle für MinIO)
export const BUCKET_THUMBNAILS: string  // process.env.AWS_S3_BUCKET_THUMBNAILS!
```

<!-- Aus src/lib/db.ts: -->
```typescript
// db = Neon Tagged Template — Verwendung: db`SELECT ... FROM parts WHERE ...`
import { db } from '@/lib/db'
```

<!-- D-11 Response-Shape (aus 06-CONTEXT.md): -->
```typescript
{
  results: Array<{
    id: string           // UUID
    name: string
    part_number: string | null
    project: string | null
    status: "ready"
    similarity: number   // 0–1 Float (parseFloat wegen Neon Decimal-String)
    created_at: string   // ISO 8601
  }>
  query: {
    threshold: number    // 0.0–1.0, Default 0.7
    limit: number        // 1–50, Default 10
    results_count: number
  }
}
```

<!-- Neon pgvector Pitfall-Fix (aus 06-RESEARCH.md Pitfall 1 + 3): -->
```typescript
// KORREKT: String-Literal mit ::vector Cast
const embeddingLiteral = `[${embedding.join(',')}]`
// Im SQL: ${embeddingLiteral}::vector
// Threshold-Filter NICHT als Alias: 1 - (embedding <=> ${embeddingLiteral}::vector) >= ${threshold}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/app/api/search/route.ts implementieren</name>
  <files>src/app/api/search/route.ts</files>
  <read_first>
    - src/app/api/upload/confirm/route.ts (Worker-HTTP-Call-Pattern, Zod-safeParse-Muster)
    - src/app/api/upload/init/route.ts (S3 PutObjectCommand-Pattern)
    - src/lib/s3.ts (BUCKET_THUMBNAILS-Export verifizieren)
    - src/lib/db.ts (db Tagged Template verifizieren)
    - .planning/phases/06-search-pipeline/06-RESEARCH.md (Pattern 1–6 vollständig lesen)
    - .planning/phases/06-search-pipeline/06-PATTERNS.md (Abschnitt "route.ts")
    - .planning/phases/06-search-pipeline/06-CONTEXT.md (D-01 bis D-12)
  </read_first>
  <behavior>
    - Test "gibt HTTP 200 mit geranketen Treffern zurück" → results-Array mit similarity als Float
    - Test "threshold=0.6 schließt Treffer mit similarity=0.5 aus" → WHERE-Bedingung mit ::vector funktioniert
    - Test "limit=3 begrenzt auf 3 Ergebnisse" → LIMIT $n in pgvector-Query
    - Test "kein image-Feld → HTTP 400"
    - Test "Worker nicht erreichbar → HTTP 502 + S3-Cleanup"
  </behavior>
  <action>
Neue Datei `src/app/api/search/route.ts` erstellen. Die Datei ist ein Next.js App Router Route Handler (server-only, kein "use client").

**Vollständige Implementierung:**

```typescript
// src/app/api/search/route.ts
// POST /api/search — Suche per Foto-Embedding gegen pgvector-Datenbank
// Ablauf: Query-Params validieren → FormData parsen → S3 Upload → Worker /embed → S3 Cleanup → pgvector Query → Response
// Server-only — KEIN "use client", keine Browser-Imports.

import { NextResponse } from 'next/server'
import { type NextRequest } from 'next/server'
import { PutObjectCommand, DeleteObjectCommand } from '@aws-sdk/client-s3'
import { z } from 'zod'
import { db } from '@/lib/db'
import { s3, BUCKET_THUMBNAILS } from '@/lib/s3'

// D-02: 30s Vercel-Timeout — muss als Module-Level-Export stehen (Next.js liest beim Build)
export const maxDuration = 30

// Zod-Schema für Query-Parameter (D-05, D-06, D-07)
// z.coerce.number() konvertiert URL-Strings zu Zahlen
const SearchQuerySchema = z.object({
  threshold: z.coerce.number().min(0).max(1).optional().default(0.7),
  limit: z.coerce.number().int().min(1).max(50).optional().default(10),
})

// Hilfsfunktion: S3 Temp-Cleanup — fire-and-forget mit .catch(warn)
// Wird auf ALLEN Fehler-Pfaden aufgerufen (Worker-Fehler, Network-Fehler, etc.)
async function cleanupTempS3(key: string): Promise<void> {
  await s3.send(new DeleteObjectCommand({
    Bucket: BUCKET_THUMBNAILS,
    Key: key,
  })).catch(err => console.warn(`[search] S3 Cleanup fehlgeschlagen für ${key}: ${err}`))
}

export async function POST(request: NextRequest): Promise<NextResponse> {
  // 1. Query-Parameter validieren (threshold, limit) — vor FormData-Parsing
  const rawThreshold = request.nextUrl.searchParams.get('threshold')
  const rawLimit = request.nextUrl.searchParams.get('limit')

  const parsedQuery = SearchQuerySchema.safeParse({
    threshold: rawThreshold ?? undefined,
    limit: rawLimit ?? undefined,
  })
  if (!parsedQuery.success) {
    return NextResponse.json(
      { error: 'Ungültige Query-Parameter', details: parsedQuery.error.flatten() },
      { status: 400 }
    )
  }
  const { threshold, limit } = parsedQuery.data

  // 2. FormData parsen und Bild extrahieren
  let formData: FormData
  try {
    formData = await request.formData()
  } catch {
    return NextResponse.json({ error: 'FormData konnte nicht gelesen werden' }, { status: 400 })
  }

  const file = formData.get('image')

  if (!(file instanceof File)) {
    return NextResponse.json({ error: 'image-Feld fehlt oder ist kein File' }, { status: 400 })
  }

  // Pitfall 5: MIME-Type-Validierung — verhindert PIL.UnidentifiedImageError im Worker
  if (!file.type.startsWith('image/')) {
    return NextResponse.json({ error: 'Nur Bilddateien erlaubt (image/*)' }, { status: 400 })
  }

  const bytes = await file.arrayBuffer()
  const buffer = Buffer.from(bytes)

  // 3. Suchbild temporär in S3 hochladen (D-03)
  // Bucket: BUCKET_THUMBNAILS mit Prefix search-temp/ (Wiederverwendung — kein neuer Bucket)
  // Key: crypto.randomUUID() — kein User-Input im S3-Key (Schutz gegen Path-Traversal)
  const tempKey = `search-temp/${crypto.randomUUID()}.jpg`

  try {
    await s3.send(new PutObjectCommand({
      Bucket: BUCKET_THUMBNAILS,
      Key: tempKey,
      Body: buffer,
      ContentType: file.type,
    }))
  } catch (err) {
    return NextResponse.json(
      { error: 'S3 Upload fehlgeschlagen', detail: String(err) },
      { status: 500 }
    )
  }

  // 4. Worker /embed synchron aufrufen (D-04)
  // Worker-URL ist server-only (kein NEXT_PUBLIC_) — Pflicht für Suche (kein Dev-Bypass)
  const workerUrl = process.env.WORKER_URL
  if (!workerUrl) {
    await cleanupTempS3(tempKey)
    return NextResponse.json({ error: 'Worker nicht konfiguriert (WORKER_URL fehlt)' }, { status: 503 })
  }

  let embedResponse: Response
  try {
    // AbortSignal.timeout(28_000) — 2s Puffer vor maxDuration=30 (Pitfall 4)
    embedResponse = await fetch(`${workerUrl}/embed`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ s3_key: tempKey }),
      signal: AbortSignal.timeout(28_000),
    })
  } catch {
    // AbortError (Timeout) oder TypeError (Network unreachable)
    await cleanupTempS3(tempKey)
    return NextResponse.json({ error: 'Worker nicht erreichbar' }, { status: 502 })
  }

  if (!embedResponse.ok) {
    await cleanupTempS3(tempKey)
    return NextResponse.json({ error: 'Worker Embed-Fehler' }, { status: 502 })
  }

  const { embedding } = await embedResponse.json() as { embedding: number[] }

  // 5. S3 Cleanup — direkt nach Embedding-Erhalt, vor pgvector-Query
  // Temp-Objekt wird nicht mehr benötigt sobald das Embedding vorliegt
  await cleanupTempS3(tempKey)

  // 6. pgvector Cosine Similarity Query (Pitfall 1 + 3)
  // KRITISCH: embeddingLiteral als String — Neon serialisiert number[] als PG-Array {0.1,...},
  //           pgvector erwartet Literal-Format [0.1,...]::vector
  // KRITISCH: Threshold-Filter NICHT als Alias (Pitfall 3) — Ausdruck vollständig wiederholen
  // Filter: WHERE status = 'ready' — KEIN is_archived (D-12, Phase-5-Downstream-Constraint)
  const embeddingLiteral = `[${embedding.join(',')}]`

  const rows = await db`
    SELECT
      id,
      name,
      part_number,
      project,
      status,
      created_at,
      1 - (embedding <=> ${embeddingLiteral}::vector) AS similarity
    FROM parts
    WHERE status = 'ready'
      AND embedding IS NOT NULL
      AND 1 - (embedding <=> ${embeddingLiteral}::vector) >= ${threshold}
    ORDER BY embedding <=> ${embeddingLiteral}::vector
    LIMIT ${limit}
  `

  // 7. Response serialisieren (D-11 Shape)
  // parseFloat(row.similarity): Neon gibt berechnete Float-Ausdrücke manchmal als Decimal-String zurück
  return NextResponse.json({
    results: rows.map((row: {
      id: string
      name: string
      part_number: string | null
      project: string | null
      status: string
      similarity: string | number
      created_at: string
    }) => ({
      id: row.id,
      name: row.name,
      part_number: row.part_number,
      project: row.project,
      status: row.status,
      similarity: parseFloat(String(row.similarity)),
      created_at: row.created_at,
    })),
    query: {
      threshold,
      limit,
      results_count: rows.length,
    },
  })
}
```

**Kritische Punkte die NICHT geändert werden dürfen:**
- `embeddingLiteral` muss als String mit `[...]`-Format definiert werden (Neon Pitfall)
- Der Threshold-Filter im WHERE muss den vollen Ausdruck wiederholen, NICHT den `similarity`-Alias
- `AbortSignal.timeout(28_000)` ist Pflicht (nicht `setTimeout` + `Promise.race`)
- `cleanupTempS3()` muss bei JEDEM Fehler-Return aufgerufen werden der nach dem S3-Upload kommt
- `export const maxDuration = 30` muss ein Module-Level-Export sein
- `WHERE status = 'ready'` — KEIN `is_archived = false`
  </action>
  <verify>
    <automated>cd /Users/mbieling/claude/Objekterkennung && npx tsc --noEmit 2>&1 | grep "search/route" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert: `ls src/app/api/search/route.ts` gibt Datei aus
    - export const maxDuration = 30 vorhanden: `grep -c "export const maxDuration = 30" src/app/api/search/route.ts` gibt `1`
    - AbortSignal.timeout(28_000) vorhanden: `grep -c "AbortSignal.timeout(28_000)" src/app/api/search/route.ts` gibt `1`
    - ::vector-Cast vorhanden: `grep -c "::vector" src/app/api/search/route.ts` gibt mindestens `2` (3× in pgvector-Query)
    - search-temp/ Prefix vorhanden: `grep -c "search-temp/" src/app/api/search/route.ts` gibt `1`
    - cleanupTempS3 bei Worker-Fehler aufgerufen: `grep -c "cleanupTempS3" src/app/api/search/route.ts` gibt mindestens `4`
    - parseFloat vorhanden (Neon Decimal-String): `grep -c "parseFloat" src/app/api/search/route.ts` gibt `1`
    - WHERE status = 'ready' vorhanden: `grep -c "status = 'ready'" src/app/api/search/route.ts` gibt `1`
    - KEIN is_archived: `grep -c "is_archived" src/app/api/search/route.ts` gibt `0`
    - KEIN similarity im WHERE (Alias-Pitfall): `grep "WHERE.*similarity\|AND.*similarity\|>= threshold" src/app/api/search/route.ts` gibt keine Ausgabe (der Threshold-Filter muss den vollen Ausdruck wiederholen)
    - TypeScript-Kompilierung ohne Fehler in der Datei: `npx tsc --noEmit 2>&1 | grep "search/route"` gibt keine Fehler
  </acceptance_criteria>
  <done>src/app/api/search/route.ts ist vollständig implementiert. Alle pgvector-Pitfalls adressiert. TypeScript kompiliert ohne Fehler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/search | Untrusted multipart/form-data + Query-Parameter vom Client |
| Next.js Handler → S3 | Server-Credentials (AWS_ACCESS_KEY_ID etc.) — server-only, kein NEXT_PUBLIC_ |
| Next.js Handler → Python Worker | Internes Netz; s3_key vom Handler generiert (nicht User-Input) |
| Next.js Handler → Neon (pgvector) | Embedding-Literal ist generierter Float-String — kein User-Input in SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-03 | Spoofing | route.ts file.type MIME-Check | mitigate | `file.type.startsWith('image/')` validiert MIME-Type; tieferes Magic-Byte-Checking in Phase 10 Hardening |
| T-6-04 | DoS | route.ts große Bilddateien | mitigate | DINOv2 resized auf 224×224 im Worker; implizites S3-Limit; Phase 10 kann explizites Byte-Limit ergänzen |
| T-6-05 | Tampering | route.ts SQL Injection via Embedding | accept | embeddingLiteral ist generierter Float-String aus Worker-Response (number[].join(',')) — kein User-Input in SQL; threshold/limit via Zod validiert |
| T-6-06 | Tampering | route.ts S3-Key Path-Traversal | mitigate | tempKey = `search-temp/${crypto.randomUUID()}.jpg` — komplett serverseitig generiert, kein User-Input im Key |
| T-6-07 | DoS | route.ts Worker-Timeout | mitigate | AbortSignal.timeout(28_000) + export const maxDuration = 30 verhindert unbegrenzte Wartezeiten |
</threat_model>

<verification>
```bash
cd /Users/mbieling/claude/Objekterkennung

# Datei existiert
ls src/app/api/search/route.ts

# Kritische Implementierungsdetails
grep "export const maxDuration = 30" src/app/api/search/route.ts
grep "AbortSignal.timeout(28_000)" src/app/api/search/route.ts
grep "::vector" src/app/api/search/route.ts
grep "search-temp/" src/app/api/search/route.ts
grep "WHERE status = 'ready'" src/app/api/search/route.ts
grep "parseFloat" src/app/api/search/route.ts

# Pitfall-Checks (darf keine Ausgabe geben)
grep "is_archived" src/app/api/search/route.ts  # Muss leer sein

# TypeScript-Kompilierung
npx tsc --noEmit 2>&1 | grep "search/route"  # Muss leer sein
```
</verification>

<success_criteria>
- src/app/api/search/route.ts existiert und kompiliert ohne TypeScript-Fehler
- export const maxDuration = 30 als Module-Level-Export vorhanden
- Zod SearchQuerySchema mit threshold (0–1, default 0.7) und limit (1–50, default 10)
- S3 Temp-Upload nach search-temp/{uuid}.jpg in BUCKET_THUMBNAILS
- Worker /embed via fetch mit AbortSignal.timeout(28_000)
- S3 cleanupTempS3() auf allen Fehler-Pfaden nach dem Upload
- pgvector-Query mit embeddingLiteral als String + ::vector-Cast (Pitfall 1)
- Threshold als vollständiger WHERE-Ausdruck (Pitfall 3) — kein similarity-Alias im WHERE
- WHERE status = 'ready' — kein is_archived (D-12)
- parseFloat(row.similarity) für Neon Decimal-String (Pitfall Neon)
- Response entspricht D-11-Shape
</success_criteria>

<output>
Nach Abschluss `.planning/phases/06-search-pipeline/06-03-SUMMARY.md` erstellen.
</output>
