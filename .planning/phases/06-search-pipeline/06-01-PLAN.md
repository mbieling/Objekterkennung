---
phase: 06-search-pipeline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/app/api/search/route.test.ts
autonomous: true
requirements:
  - SEARCH-03
  - SEARCH-04
  - SEARCH-05

must_haves:
  truths:
    - "src/app/api/search/route.test.ts existiert mit 9 it.todo-Stubs"
    - "npm test -- src/app/api/search/route.test.ts läuft durch ohne Compilation-Fehler"
    - "Alle 9 Test-Cases decken SEARCH-03, SEARCH-04, SEARCH-05 ab"
  artifacts:
    - path: "src/app/api/search/route.test.ts"
      provides: "Vitest-Test-Stubs für die Search-Route"
      min_lines: 60
      contains: "it.todo"
  key_links:
    - from: "src/app/api/search/route.test.ts"
      to: "src/app/api/search/route.ts"
      via: "dynamischer import('./route')"
      pattern: "import.*route"
---

<objective>
Vitest-Teststubs für die Search-Route anlegen — alle neun Test-Cases als `it.todo` mit Mocks für `db`, `s3` und `fetch` vorbereiten, bevor die Implementierung existiert.

Purpose: Die Nyquist-Regel verlangt, dass Teststubs in Wave 0 existieren, bevor Implementation beginnt. Alle SEARCH-03/04/05-Anforderungen werden hier verankert.
Output: `src/app/api/search/route.test.ts` mit 9 `it.todo`-Stubs, korrekten Mock-Deklarationen und `beforeEach`-Reset.
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-search-pipeline/06-CONTEXT.md
@.planning/phases/06-search-pipeline/06-VALIDATION.md
</context>

<interfaces>
<!-- Analog-Quelle: src/app/api/upload/confirm/route.test.ts -->

Mock-Setup-Pattern (exakt übernehmen):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

vi.mock('@/lib/db', () => ({
  db: vi.fn(),
}))
vi.mock('@/lib/s3', () => ({
  s3: { send: vi.fn() },
  BUCKET_THUMBNAILS: 'mock-bucket-thumbnails',
}))

const mockFetch = vi.fn()
global.fetch = mockFetch

import { db } from '@/lib/db'
import { s3 } from '@/lib/s3'
const mockDb = vi.mocked(db)
const mockS3Send = vi.mocked(s3.send)
```

beforeEach-Pattern:
```typescript
beforeEach(() => {
  vi.clearAllMocks()
  process.env.WORKER_URL = 'http://localhost:8000'
})
```

it.todo-Pattern (Vitest):
```typescript
it.todo('gibt HTTP 200 mit geranketen Treffern zurück (SEARCH-03)')
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Vitest-Stubs für route.test.ts anlegen</name>
  <files>src/app/api/search/route.test.ts</files>
  <read_first>
    - src/app/api/upload/confirm/route.test.ts (exaktes Mock-Muster übernehmen)
    - .planning/phases/06-search-pipeline/06-PATTERNS.md (Abschnitt "route.test.ts")
    - .planning/phases/06-search-pipeline/06-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
Datei `src/app/api/search/route.test.ts` neu erstellen. Kein `describe`-Block mit Implementierungs-Code — nur Stubs.

Struktur der Datei:

1. Imports und Mock-Setup (exakt analog zu confirm/route.test.ts):
```typescript
import { describe, it, vi, beforeEach } from 'vitest'
import { type NextRequest } from 'next/server'

vi.mock('@/lib/db', () => ({
  db: vi.fn(),
}))
vi.mock('@/lib/s3', () => ({
  s3: { send: vi.fn() },
  BUCKET_THUMBNAILS: 'mock-bucket-thumbnails',
}))

const mockFetch = vi.fn()
global.fetch = mockFetch

import { db } from '@/lib/db'
import { s3 } from '@/lib/s3'
// Typen-only — Variablen werden in Wave 3 (Plan 06-04) aktiviert
// eslint-disable-next-line @typescript-eslint/no-unused-vars
const _mockDb = vi.mocked(db)
// eslint-disable-next-line @typescript-eslint/no-unused-vars
const _mockS3Send = vi.mocked(s3.send)
```

2. describe-Block mit 9 it.todo-Stubs:
```typescript
describe('POST /api/search', () => {
  beforeEach(() => {
    vi.clearAllMocks()
    process.env.WORKER_URL = 'http://localhost:8000'
  })

  // SEARCH-03: Gerankete Treffer mit similarity-Score
  it.todo('gibt HTTP 200 mit geranketen Treffern zurück wenn Treffer vorhanden (SEARCH-03)')
  it.todo('gibt HTTP 200 mit leerem results-Array zurück wenn keine Treffer gefunden (SEARCH-03)')
  it.todo('gibt HTTP 502 zurück wenn Worker nicht erreichbar ist (SEARCH-03)')
  it.todo('gibt HTTP 502 zurück wenn Worker einen Fehler zurückgibt (SEARCH-03)')

  // SEARCH-04: Threshold-Filter
  it.todo('schließt Treffer unter dem Schwellwert aus (SEARCH-04)')
  it.todo('gibt HTTP 400 zurück bei ungültigem threshold-Parameter (SEARCH-04)')

  // SEARCH-05: Limit-Parameter
  it.todo('begrenzt die Anzahl der Treffer per limit-Parameter (SEARCH-05)')
  it.todo('gibt HTTP 400 zurück bei ungültigem limit-Parameter (SEARCH-05)')

  // Eingabe-Validierung
  it.todo('gibt HTTP 400 zurück wenn kein image-Feld im FormData vorhanden (SEARCH-03)')
})
```

Die Stubs verwenden `it.todo` (Vitest-API) — kein `it.skip`. Die Mocks für db, s3, fetch sind deklariert aber werden in Wave 3 aktiviert. Keine Implementierung, keine `expect`-Aufrufe in dieser Datei.
  </action>
  <verify>
    <automated>cd /Users/mbieling/claude/Objekterkennung && npm test -- src/app/api/search/route.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Datei `src/app/api/search/route.test.ts` existiert: `ls src/app/api/search/route.test.ts` gibt die Datei aus
    - Datei enthält genau 9 `it.todo`-Einträge: `grep -c "it.todo" src/app/api/search/route.test.ts` gibt `9`
    - Datei enthält vi.mock für db und s3: `grep -c "vi.mock" src/app/api/search/route.test.ts` gibt `2`
    - npm test läuft ohne Compilation-Fehler durch (exit code 0 oder "todo" im Output)
    - Datei enthält `global.fetch = mockFetch`
    - Datei enthält `WORKER_URL = 'http://localhost:8000'` im beforeEach
  </acceptance_criteria>
  <done>route.test.ts existiert mit 9 it.todo-Stubs, korrekten Mock-Deklarationen und beforeEach-Reset. npm test -- route.test.ts läuft ohne Fehler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Code → Produktions-Code | Tests mocken alle externen Dependencies (db, s3, fetch) — kein echter Netzwerkzugriff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-00 | Tampering | route.test.ts | accept | Nur Test-Code, keine Produktionsdaten; vi.mock verhindert echte S3/DB-Calls |
</threat_model>

<verification>
```bash
cd /Users/mbieling/claude/Objekterkennung
npm test -- src/app/api/search/route.test.ts
```
Erwartet: 9 todo-Tests, 0 failures, exit 0.
</verification>

<success_criteria>
- src/app/api/search/route.test.ts existiert
- 9 it.todo-Stubs vorhanden (grep -c "it.todo" == 9)
- vi.mock für '@/lib/db' und '@/lib/s3' vorhanden
- global.fetch = mockFetch vorhanden
- npm test -- src/app/api/search/route.test.ts läuft ohne Compilation-Fehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/06-search-pipeline/06-01-SUMMARY.md` erstellen.
</output>
