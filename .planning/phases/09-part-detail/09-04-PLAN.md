---
phase: 09-part-detail
plan: 04
type: execute
wave: 3
depends_on:
  - "09-03"
files_modified:
  - tests/phase-09-part-detail.spec.ts
autonomous: false
requirements:
  - DETAIL-01
  - DETAIL-02
must_haves:
  truths:
    - "Alle 4 Playwright-Stubs aus Wave 0 sind aktiviert und grün (oder mit klarem Skip-Grund dokumentiert)"
    - "Roadmap Phase-9-Success-Criteria SC1, SC2, SC3 sind durch Human-Verify bestätigt"
    - "Klicken auf ein Suchergebnis in /search navigiert zu /parts/[id]"
    - "Download-Button zeigt 'Datei wird noch verarbeitet' für non-ready-Parts"
    - "Download-Button ist enabled für ready-Parts"
  artifacts:
    - path: "tests/phase-09-part-detail.spec.ts"
      provides: "Aktivierte Playwright E2E-Tests für Phase 9"
      contains: "test("
  key_links:
    - from: "tests/phase-09-part-detail.spec.ts"
      to: "src/app/parts/[id]/page.tsx"
      via: "page.goto('/parts/[id]')"
      pattern: "goto.*parts"
---

<objective>
Wave 3: Playwright E2E-Tests aktivieren und Human-Verify gegen Roadmap-Success-Criteria.

Purpose: Vollständige Verifikation der Phase-9-Implementierung gegen die drei ROADMAP-Erfolgskriterien (SC1: Metadaten, SC2: Thumbnails, SC3: Download).
Output: Aktivierte und grüne E2E-Tests + Human-Verify-Bestätigung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-part-detail/09-CONTEXT.md
@.planning/phases/09-part-detail/09-VALIDATION.md

<interfaces>
<!-- Phase-9-Roadmap-Success-Criteria (direkt aus ROADMAP.md) -->
SC1: Clicking any result opens a detail view showing all metadata: name, part number, project, status, and upload date
SC2: The detail view displays all generated thumbnails (all 6–8 orthographic views)
SC3: A download button delivers the original STEP file to the user's device with the correct filename and MIME type

<!-- Phase-8 E2E-Pattern (direkt aus tests/phase-08-results-ui.spec.ts) -->
Aktivierungsmuster: test.skip → test (test.skip entfernen)
Mock-Setup: page.route() für API-Abfragen
Timeout-Pattern: toBeVisible({ timeout: 10_000 })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright E2E-Tests aktivieren (test.skip → test)</name>
  <files>tests/phase-09-part-detail.spec.ts</files>
  <read_first>
    - tests/phase-09-part-detail.spec.ts — aktuellen Stub-Inhalt lesen (test.skip → test ersetzen, Mock-Fixtures prüfen)
    - tests/phase-08-results-ui.spec.ts — Aktivierungsmuster als Referenz (wie test.skip entfernt wird)
    - .planning/phases/09-part-detail/09-VALIDATION.md — Manual-Only-Verifications (Download-Dialog ist manuell)
  </read_first>
  <action>
Die Datei `tests/phase-09-part-detail.spec.ts` aktualisieren: alle 4 `test.skip` → `test` umwandeln.

Zusätzlich den Download-Test robuster machen — da `window.location.href` in Playwright-Tests keine echte Weiterleitung auslöst, den Test als Interaction-Test gestalten der prüft ob der Button klickbar ist (nicht ob der Download startet):

```typescript
// Geänderter Download-Test (test.skip → test, robuste Implementierung):
test('DETAIL-02: Download-Button ruft /download-Endpunkt auf bei Klick', async ({ page }) => {
  await setupMocks(page)

  // Download-Endpoint-Call tracken
  let downloadCalled = false
  await page.route(`**/api/parts/${TEST_PART_ID}/download`, async route => {
    downloadCalled = true
    await route.fulfill({
      status: 200,
      contentType: 'application/json',
      body: JSON.stringify({ url: 'https://s3.example.com/test.step', filename: 'Flansch_M12.step' }),
    })
  })

  await page.goto(`/parts/${TEST_PART_ID}`)
  const downloadBtn = page.getByRole('button', { name: /STEP herunterladen/i })
  await expect(downloadBtn).toBeVisible({ timeout: 10_000 })
  await downloadBtn.click()

  // Kurz warten damit fetch() abgeschlossen werden kann
  await page.waitForTimeout(500)
  expect(downloadCalled).toBe(true)
})
```

Alle 4 test.skip → test. Kein test.skip darf in der finalen Datei verbleiben.
  </action>
  <verify>
    <automated>cd /Users/mbieling/claude/Objekterkennung && npm run test:e2e -- --grep "Phase 9" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Kein test.skip mehr in phase-09-Datei: `grep -c "test.skip" tests/phase-09-part-detail.spec.ts` → 0
    - Genau 4 test()-Aufrufe: `grep -c "^  test(" tests/phase-09-part-detail.spec.ts` → 4
    - E2E-Tests laufen durch: npm run test:e2e → Exit 0 (alle 4 grün oder höchstens 1 flaky mit dokumentiertem Grund)
    - npm run test:all → Exit 0 (vollständige Suite grün)
  </acceptance_criteria>
  <done>Alle 4 Playwright-Tests aktiviert und grün. npm run test:all gibt Exit 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 9 (Part Detail) vollständig implementiert:
- GET /api/parts/[id] — Metadaten-Endpoint (D-12)
- GET /api/parts/[id]/thumbnails — Presigned-URL-Array (D-13)
- GET /api/parts/[id]/download — STEP-Download-URL (D-14)
- usePartDetail.ts — Custom Hook mit parallelen API-Calls
- PartDetail.tsx — Client-Komponente mit Galerie + Metadaten + Download-Button
- /parts/[id]/page.tsx — Server-Component-Wrapper
- 11 Vitest Unit-Tests grün, 4 Playwright E2E-Tests grün
  </what-built>
  <how-to-verify>
Voraussetzung: npm run dev läuft auf localhost:3000. Ein Part mit status='ready' muss in der DB vorhanden sein.

**SC1 — Metadaten-Anzeige (ROADMAP Phase 9, Success Criterion 1):**
1. Navigiere zu http://localhost:3000/search
2. Fotografiere ein Bauteil oder lade ein Bild hoch → Suche starten
3. Klicke auf eine Ergebniskarte
4. Erwartetes Ergebnis: Detailseite öffnet sich mit allen 5 Feldern sichtbar:
   - Bauteilname als H1
   - Teilenummer (oder "—" wenn leer)
   - Projekt (oder "—" wenn leer)
   - Status als farbiger Badge (grün für "Bereit")
   - Hochgeladen-Datum im Format TT.MM.JJJJ

**SC2 — Thumbnail-Galerie (ROADMAP Phase 9, Success Criterion 2):**
5. Auf derselben Detailseite: Überprüfe Bildbereich
6. Erwartetes Ergebnis: Großes Hauptbild (320×320px auf Mobile) sichtbar
7. Erwartetes Ergebnis: Horizontale Thumbnail-Leiste mit allen Views (bis zu 8 Miniaturen) sichtbar
8. Klicke auf eine andere Miniatur → Hauptbild wechselt zur gewählten Ansicht
9. Aktive Miniatur hat sichtbaren Ring-Rahmen (Primary-Farbe)

**SC3 — STEP-Download (ROADMAP Phase 9, Success Criterion 3):**
10. Scrolle zum "STEP herunterladen"-Button am Seitenende
11. Erwartetes Ergebnis: Button ist aktiviert (nicht grau) wenn Status = "Bereit"
12. Klicke auf den Button
13. Erwartetes Ergebnis: Browser-Save-Dialog erscheint mit Dateiname "{bauteilname}.step"
14. Erwartetes Ergebnis: MIME-Type ist application/octet-stream (keine Tab-Öffnung)

**Navigation:**
15. Klicke "← Zurück zur Suche" → kehrt zur Suche zurück (router.back() oder /search)

**Non-Ready-State (wenn verfügbar):**
16. Navigiere zu /parts/[id] eines Parts mit status='processing'
17. Erwartetes Ergebnis: Download-Button grau/disabled mit Text "Datei wird noch verarbeitet"
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn alle 3 Success Criteria bestätigt sind, oder beschreibe aufgetretene Probleme</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Playwright-Tests → Next.js-App | Tests laufen gegen localhost — kein Produktionssystem involviert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Information Disclosure | Playwright-Test-Fixtures (Mock-UUID, Mock-Part-Daten) | accept | Test-UUIDs und Mock-Daten sind keine echten Produktionsdaten; nur in Test-Umgebung verwendet |
| T-09-12 | Denial of Service | window.waitForTimeout(500) im Download-Test | accept | 500ms Wartezeit ist minimal; nur in Test-Umgebung; kein Produktionsrisiko |
</threat_model>

<verification>
Phase-9-Gesamtverifikation (vor /gsd-verify-work):
- `npm run test:all` → Exit 0 (vollständige Suite, alle Tests grün)
- `grep -c "test.skip" tests/phase-09-part-detail.spec.ts` → 0
- `test -f "src/app/parts/[id]/page.tsx"` → Exit 0
- `test -f "src/app/parts/[id]/PartDetail.tsx"` → Exit 0
- `test -f src/hooks/usePartDetail.ts` → Exit 0
- `test -f src/app/api/parts/\[id\]/thumbnails/route.ts` → Exit 0
- `test -f src/app/api/parts/\[id\]/download/route.ts` → Exit 0
- Human-Verify: SC1 (Metadaten), SC2 (Thumbnails), SC3 (Download) alle bestätigt
</verification>

<success_criteria>
- Alle 4 Playwright E2E-Tests grün (kein test.skip)
- npm run test:all Exit 0 (alle Unit- + E2E-Tests grün)
- Human-Verify: Alle 3 ROADMAP-Phase-9-Success-Criteria bestätigt
- Phase 9 bereit für /gsd-verify-work
</success_criteria>

<output>
Nach Abschluss `.planning/phases/09-part-detail/09-04-SUMMARY.md` erstellen mit:
- E2E-Test-Ergebnis (4/4 grün)
- npm run test:all Ergebnis
- Human-Verify-Status (approved / issues)
- Phase-9-Abschlussstatus
</output>
