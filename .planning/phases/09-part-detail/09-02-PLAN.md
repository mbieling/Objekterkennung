---
phase: 09-part-detail
plan: 02
type: execute
wave: 1
depends_on:
  - "09-01"
files_modified:
  - src/app/api/parts/[id]/route.ts
  - src/app/api/parts/[id]/thumbnails/route.ts
  - src/app/api/parts/[id]/download/route.ts
autonomous: true
requirements:
  - DETAIL-01
  - DETAIL-02
must_haves:
  truths:
    - "GET /api/parts/[id] gibt { part: { id, name, part_number, project, status, thumbnail_count, created_at } } zurück"
    - "GET /api/parts/[id]/thumbnails gibt { urls: string[] } mit Presigned URLs für alle Views zurück"
    - "GET /api/parts/[id]/download gibt { url: string, filename: string } mit Content-Disposition-URL zurück"
    - "Alle drei Handler validieren UUID als erste Operation mit z.string().uuid()"
    - "Ungültige UUID liefert 400, nicht-existente Part-ID liefert 404"
    - "Download-Endpoint gibt 409 wenn status != ready"
  artifacts:
    - path: "src/app/api/parts/[id]/route.ts"
      provides: "GET-Handler für Metadaten (D-12)"
      contains: "export async function GET"
    - path: "src/app/api/parts/[id]/thumbnails/route.ts"
      provides: "GET-Handler für Presigned-URL-Array (D-13)"
      contains: "Promise.all"
    - path: "src/app/api/parts/[id]/download/route.ts"
      provides: "GET-Handler für Download-Presigned-URL (D-14)"
      contains: "sanitizeFilename"
  key_links:
    - from: "src/app/api/parts/[id]/thumbnails/route.ts"
      to: "src/lib/s3.ts"
      via: "BUCKET_THUMBNAILS + getSignedUrl"
      pattern: "BUCKET_THUMBNAILS"
    - from: "src/app/api/parts/[id]/download/route.ts"
      to: "src/lib/s3.ts"
      via: "BUCKET_STEPS + getSignedUrl"
      pattern: "BUCKET_STEPS"
    - from: "src/app/api/parts/[id]/route.ts"
      to: "src/lib/db.ts"
      via: "db tagged-template SQL"
      pattern: "db`SELECT"
---

<objective>
Wave 1: Drei API-Endpoints implementieren — GET /api/parts/[id] (Metadaten), GET /api/parts/[id]/thumbnails (Presigned-URL-Array), GET /api/parts/[id]/download (STEP-Download-URL).

Purpose: Client-Komponente (Wave 2) kann alle Daten laden — API-Kontrakt muss vor Hook-Implementierung stabil sein.
Output: 3 funktionsfähige API-Endpoints mit UUID-Validierung, DB-Abfragen und S3-Presigned-URLs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-part-detail/09-CONTEXT.md
@.planning/phases/09-part-detail/09-RESEARCH.md
@.planning/phases/09-part-detail/09-PATTERNS.md

<interfaces>
<!-- Direkt aus Codebase extrahiert — Executor benötigt keine weiteren Datei-Reads für Patterns -->

Aus src/app/api/parts/[id]/thumbnail/route.ts (direktes Kopier-Pattern für beide neuen Routes):
```typescript
import { NextResponse } from 'next/server'
import { GetObjectCommand, HeadObjectCommand } from '@aws-sdk/client-s3'
import { getSignedUrl } from '@aws-sdk/s3-request-presigner'
import { z } from 'zod'
import { db } from '@/lib/db'
import { s3, BUCKET_THUMBNAILS } from '@/lib/s3'

const ParamsSchema = z.object({
  id: z.string().uuid('id muss eine gültige UUID sein'),
})

export async function GET(
  _request: Request,
  { params }: { params: Promise<{ id: string }> }
): Promise<NextResponse> {
  const { id } = await params  // Next.js 16: params ist Promise

  const parsed = ParamsSchema.safeParse({ id })
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'Invalid id', details: parsed.error.flatten() },
      { status: 400 }
    )
  }
  // ... DB-Query, HeadObject, getSignedUrl ...
  return NextResponse.json({ url })
}
```

Aus src/lib/s3.ts (Bucket-Konstanten):
```typescript
export const BUCKET_THUMBNAILS = process.env.AWS_BUCKET_THUMBNAILS!
export const BUCKET_STEPS = process.env.AWS_BUCKET_STEPS!
export const s3 = new S3Client({ region: process.env.AWS_REGION! })
```

Aus src/lib/db.ts:
```typescript
import postgres from 'postgres'
export const db = postgres(process.env.DATABASE_URL!)
// Verwendung: db`SELECT ... FROM parts WHERE id = ${id} LIMIT 1`
```

Aus src/app/api/parts/[id]/route.ts (ParamsSchema bereits definiert, GET wird hinzugefügt):
```typescript
// Bestehende Datei hat: import z, import db, const ParamsSchema, export PATCH, export DELETE
// GET-Handler wird HINZUGEFÜGT — ParamsSchema bereits vorhanden, nicht doppeln
```

S3-Pfadkonventionen (aus STATE.md Accumulated Context):
- Thumbnails: `{id}/view_{i}.png` (i = 0 bis thumbnail_count-1)
- STEP-Dateien: `{id}/original.step`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/parts/[id] hinzufügen (D-12) + GET /api/parts/[id]/thumbnails erstellen (D-13)</name>
  <files>src/app/api/parts/[id]/route.ts, src/app/api/parts/[id]/thumbnails/route.ts</files>
  <read_first>
    - src/app/api/parts/[id]/route.ts — vollständige bestehende Datei lesen (PATCH/DELETE vorhanden; GET wird hinzugefügt; ParamsSchema bereits definiert)
    - src/app/api/parts/[id]/thumbnail/route.ts — vollständiges Kopier-Pattern für thumbnails/route.ts
    - src/lib/s3.ts — BUCKET_THUMBNAILS-Konstante und s3-Export-Namen verifizieren
    - src/lib/db.ts — db-Import-Pfad verifizieren
  </read_first>
  <behavior>
    - GET /api/parts/[id] mit ungültiger UUID → HTTP 400 mit { error: "Invalid id" }
    - GET /api/parts/[id] mit nicht-existenter UUID → HTTP 404 mit { error: "Part not found" }
    - GET /api/parts/[id] mit gültiger UUID → HTTP 200 mit { part: { id, name, part_number, project, status, thumbnail_count, created_at } }
    - GET /api/parts/[id]/thumbnails mit status="processing" → HTTP 200 mit { urls: [] }
    - GET /api/parts/[id]/thumbnails mit status="ready" und thumbnail_count=2 → HTTP 200 mit { urls: ["https://...", "https://..."] } (2 Presigned URLs)
    - GET /api/parts/[id]/thumbnails mit status="ready" und thumbnail_count=0 → HTTP 200 mit { urls: [] }
  </behavior>
  <action>
**Teil A — GET-Handler in src/app/api/parts/[id]/route.ts einfügen:**

Die Datei ZUERST VOLLSTÄNDIG LESEN. Dann GET-Handler VOR den bestehenden PATCH-Handler einfügen (nach den Imports und ParamsSchema, vor PATCH). ParamsSchema NICHT doppeln — es ist bereits vorhanden.

GET-Handler (exakter Code):
```typescript
export async function GET(
  _request: Request,
  { params }: { params: Promise<{ id: string }> }
): Promise<NextResponse> {
  const { id } = await params

  const parsed = ParamsSchema.safeParse({ id })
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'Invalid id', details: parsed.error.flatten() },
      { status: 400 }
    )
  }

  const rows = await db`
    SELECT id, name, part_number, project, status, thumbnail_count, created_at
    FROM parts WHERE id = ${id} LIMIT 1
  `
  if (rows.length === 0) {
    return NextResponse.json({ error: 'Part not found' }, { status: 404 })
  }

  return NextResponse.json({ part: rows[0] })
}
```

**Teil B — Neue Datei src/app/api/parts/[id]/thumbnails/route.ts erstellen:**

Vollständiger Datei-Inhalt:
```typescript
// GET /api/parts/[id]/thumbnails — D-13
// Liefert Array von Presigned S3-URLs für view_0.png bis view_{N-1}.png.
// Server-only — KEIN "use client", keine Browser-Imports.

import { NextResponse } from 'next/server'
import { GetObjectCommand, HeadObjectCommand } from '@aws-sdk/client-s3'
import { getSignedUrl } from '@aws-sdk/s3-request-presigner'
import { z } from 'zod'
import { db } from '@/lib/db'
import { s3, BUCKET_THUMBNAILS } from '@/lib/s3'

const ParamsSchema = z.object({
  id: z.string().uuid('id muss eine gültige UUID sein'),
})

export async function GET(
  _request: Request,
  { params }: { params: Promise<{ id: string }> }
): Promise<NextResponse> {
  const { id } = await params

  // 1. UUID validieren BEVOR DB-Query oder S3-Key-Konstruktion (T-09-01: Path-Traversal)
  const parsed = ParamsSchema.safeParse({ id })
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'Invalid id', details: parsed.error.flatten() },
      { status: 400 }
    )
  }

  // 2. Part-Existenz, Status und thumbnail_count prüfen
  const rows = await db`
    SELECT status, thumbnail_count FROM parts WHERE id = ${id} LIMIT 1
  `
  if (rows.length === 0) {
    return NextResponse.json({ error: 'Part not found' }, { status: 404 })
  }

  // Bei status != 'ready' oder thumbnail_count = 0: leeres Array zurückgeben (kein 409)
  // Client rendert Skeleton-Strip (D-11)
  if (rows[0].status !== 'ready' || rows[0].thumbnail_count === 0) {
    return NextResponse.json({ urls: [] })
  }

  const count: number = rows[0].thumbnail_count

  // 3. Alle Views parallel laden (Promise.all statt sequenziell — Pitfall 5)
  const results = await Promise.all(
    Array.from({ length: count }, async (_, i) => {
      const key = `${id}/view_${i}.png`
      try {
        await s3.send(new HeadObjectCommand({ Bucket: BUCKET_THUMBNAILS, Key: key }))
        const url = await getSignedUrl(
          s3,
          new GetObjectCommand({ Bucket: BUCKET_THUMBNAILS, Key: key }),
          { expiresIn: 60 }
        )
        return url
      } catch {
        // Einzelne fehlende Views überspringen — Rest trotzdem zurückgeben (Open Question 2)
        return null
      }
    })
  )

  const urls = results.filter((url): url is string => url !== null)
  return NextResponse.json({ urls })
}
```
  </action>
  <verify>
    <automated>cd /Users/mbieling/claude/Objekterkennung && npm run build 2>&1 | grep -E "error TS|Error:" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - GET-Handler in route.ts vorhanden: `grep -c "export async function GET" src/app/api/parts/\[id\]/route.ts` → 1
    - thumbnails/route.ts existiert: `test -f src/app/api/parts/\[id\]/thumbnails/route.ts`
    - UUID-Validierung als erste Operation in thumbnails: `grep -n "ParamsSchema.safeParse" src/app/api/parts/\[id\]/thumbnails/route.ts` → Zeile vor erstem `db\``
    - Promise.all verwendet (nicht sequenzieller Loop): `grep -c "Promise.all" src/app/api/parts/\[id\]/thumbnails/route.ts` → mindestens 1
    - Kein "use client" in beiden Dateien: `grep -c '"use client"' src/app/api/parts/\[id\]/route.ts src/app/api/parts/\[id\]/thumbnails/route.ts` → 0
    - npm run build ohne TS-Fehler in diesen Dateien
  </acceptance_criteria>
  <done>GET /api/parts/[id] und GET /api/parts/[id]/thumbnails implementiert und kompilieren ohne Fehler. UUID-Validierung ist erste Operation in beiden Handlern.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /api/parts/[id]/download erstellen (D-14)</name>
  <files>src/app/api/parts/[id]/download/route.ts</files>
  <read_first>
    - src/app/api/parts/[id]/thumbnail/route.ts — vollständiges Muster für Struktur und HeadObject-Pattern
    - src/lib/s3.ts — BUCKET_STEPS-Konstantenname exakt verifizieren (nicht raten)
    - .planning/phases/09-part-detail/09-CONTEXT.md — D-14 (Response-Format: { url, filename }), D-06 (sanitizeFilename-Logik)
  </read_first>
  <behavior>
    - GET /api/parts/[id]/download mit ungültiger UUID → HTTP 400
    - GET /api/parts/[id]/download mit nicht-existenter UUID → HTTP 404
    - GET /api/parts/[id]/download wenn status != "ready" → HTTP 409 mit { error: "Not ready" }
    - GET /api/parts/[id]/download wenn STEP-Datei in S3 fehlt → HTTP 404 mit { error: "STEP file missing" }
    - GET /api/parts/[id]/download für ready-Part → HTTP 200 mit { url: "https://s3.amazonaws.com/...", filename: "Flansch_M12.step" }
    - sanitizeFilename("Flansch M12") → "Flansch_M12"
    - sanitizeFilename("Part/\\*?") → "Part" (Sonderzeichen entfernt)
    - sanitizeFilename("   ") → "bauteil" (Fallback)
  </behavior>
  <action>
Neue Datei `src/app/api/parts/[id]/download/route.ts` erstellen:

```typescript
// GET /api/parts/[id]/download — D-14
// Liefert Presigned S3-URL mit Content-Disposition für direkten Browser-Download.
// Server-only — KEIN "use client", keine Browser-Imports.
// Download-URL TTL: 300s (5min) — ausreichend für STEP-Dateien bis 100MB (Pitfall 6).

import { NextResponse } from 'next/server'
import { GetObjectCommand, HeadObjectCommand } from '@aws-sdk/client-s3'
import { getSignedUrl } from '@aws-sdk/s3-request-presigner'
import { z } from 'zod'
import { db } from '@/lib/db'
import { s3, BUCKET_STEPS } from '@/lib/s3'

const ParamsSchema = z.object({
  id: z.string().uuid('id muss eine gültige UUID sein'),
})

// D-06: Dateiname sanitizen — Leerzeichen → _, Sonderzeichen entfernen, Fallback 'bauteil'
function sanitizeFilename(name: string): string {
  return (
    name
      .replace(/\s+/g, '_')
      .replace(/[^a-zA-Z0-9_\-\.]/g, '') || 'bauteil'
  )
}

export async function GET(
  _request: Request,
  { params }: { params: Promise<{ id: string }> }
): Promise<NextResponse> {
  const { id } = await params

  // 1. UUID validieren BEVOR DB-Query oder S3-Key-Konstruktion (T-09-01: Path-Traversal)
  const parsed = ParamsSchema.safeParse({ id })
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'Invalid id', details: parsed.error.flatten() },
      { status: 400 }
    )
  }

  // 2. Part-Existenz, Status und Name laden
  const rows = await db`SELECT status, name FROM parts WHERE id = ${id} LIMIT 1`
  if (rows.length === 0) {
    return NextResponse.json({ error: 'Part not found' }, { status: 404 })
  }
  // 409 wenn noch nicht verarbeitet (kein STEP in S3 zu erwarten)
  if (rows[0].status !== 'ready') {
    return NextResponse.json({ error: 'Not ready' }, { status: 409 })
  }

  // 3. Dateiname sanitizen (D-06)
  const filename = `${sanitizeFilename(rows[0].name)}.step`
  const key = `${id}/original.step`

  // 4. HeadObject — Race-Condition-Schutz (S3 hat ggf. noch kein Objekt bei race)
  try {
    await s3.send(new HeadObjectCommand({ Bucket: BUCKET_STEPS, Key: key }))
  } catch {
    return NextResponse.json({ error: 'STEP file missing' }, { status: 404 })
  }

  // 5. Presigned URL mit Content-Disposition + MIME-Type (D-05, Pitfall 3)
  let url: string
  try {
    url = await getSignedUrl(
      s3,
      new GetObjectCommand({
        Bucket: BUCKET_STEPS,
        Key: key,
        ResponseContentDisposition: `attachment; filename="${filename}"`,
        ResponseContentType: 'application/octet-stream',
      }),
      { expiresIn: 300 }  // 5min TTL — ausreichend für 100MB bei 3 Mbit/s (Pitfall 6)
    )
  } catch {
    return NextResponse.json({ error: 'Failed to generate download URL' }, { status: 500 })
  }

  return NextResponse.json({ url, filename })
}
```
  </action>
  <verify>
    <automated>cd /Users/mbieling/claude/Objekterkennung && npm run build 2>&1 | grep -E "error TS|Error:" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - download/route.ts existiert: `test -f src/app/api/parts/\[id\]/download/route.ts`
    - UUID-Validierung als erste Operation: `grep -n "ParamsSchema.safeParse" src/app/api/parts/\[id\]/download/route.ts` → Zeile vor erstem `db\``
    - sanitizeFilename-Funktion vorhanden: `grep -c "function sanitizeFilename" src/app/api/parts/\[id\]/download/route.ts` → 1
    - BUCKET_STEPS (nicht BUCKET_THUMBNAILS) verwendet: `grep -c "BUCKET_STEPS" src/app/api/parts/\[id\]/download/route.ts` → mindestens 2
    - ResponseContentDisposition vorhanden: `grep -c "ResponseContentDisposition" src/app/api/parts/\[id\]/download/route.ts` → 1
    - ResponseContentType gesetzt: `grep -c "ResponseContentType" src/app/api/parts/\[id\]/download/route.ts` → 1
    - TTL 300 (nicht 60): `grep -c "expiresIn: 300" src/app/api/parts/\[id\]/download/route.ts` → 1
    - Kein "use client": `grep -c '"use client"' src/app/api/parts/\[id\]/download/route.ts` → 0
    - npm run build ohne TS-Fehler in dieser Datei
  </acceptance_criteria>
  <done>GET /api/parts/[id]/download implementiert. sanitizeFilename, HeadObject-Guard, Content-Disposition und 300s TTL vorhanden. Kompiliert ohne Fehler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| HTTP-Client → API-Endpoints | `id`-Parameter aus URL ist nicht vertrauenswürdig — kann beliebige Strings enthalten |
| API → S3 | S3-Key wird aus `id` konstruiert — Path-Traversal ohne UUID-Validierung möglich |
| API → Neon DB | Parametrisierte SQL-Abfragen via `db` tagged-template — kein SQL-Injection-Risiko |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | GET /api/parts/[id] — URL-Parameter `id` | mitigate | `z.string().uuid()` als ERSTE Operation in allen 3 Handlern — vor DB-Query und S3-Key-Konstruktion. Ungültige UUID → HTTP 400 ohne weitere Verarbeitung. |
| T-09-02 | Tampering | GET /api/parts/[id]/thumbnails — S3-Key `{id}/view_{i}.png` | mitigate | UUID-Validierung verhindert Path-Traversal (`../other-bucket/secret`). S3-Key-Konstruktion erst nach erfolgreichem UUID-Parsen. |
| T-09-03 | Tampering | GET /api/parts/[id]/download — S3-Key `{id}/original.step` | mitigate | UUID-Validierung + HeadObject-Guard. Dateiname über sanitizeFilename bereinigt — verhindert Header-Injection via filename. |
| T-09-04 | Information Disclosure | Presigned URLs in API-Response | accept | URLs haben kurze TTL (60s/300s); nur über HTTPS; nicht gecacht; keine Logs. UUID-Namespace verhindert praktische Enumeration. |
| T-09-05 | Information Disclosure | AWS_SECRET_ACCESS_KEY / DATABASE_URL in Server-Only-Dateien | mitigate | Alle neuen Route-Dateien sind Server-only (kein `"use client"` Prefix). Kein `NEXT_PUBLIC_`-Prefix auf Secrets. |
| T-09-06 | Elevation of Privilege | STEP-Datei eines anderen Parts downloaden | accept | UUID-Validierung verhindert Traversal. UUID-Namespace (2^122 Raum) macht Enumeration praktisch unmöglich. Keine Auth in v1 (Pilot-Scope). |
</threat_model>

<verification>
Nach Wave 1 (alle 3 Endpoints implementiert):
- `npm run build` ohne TypeScript-Fehler
- `grep -c "export async function GET" src/app/api/parts/\[id\]/route.ts` → 1
- `grep -c "ParamsSchema.safeParse" src/app/api/parts/\[id\]/thumbnails/route.ts` → 1
- `grep -c "sanitizeFilename" src/app/api/parts/\[id\]/download/route.ts` → 2 (Definition + Verwendung)
- `grep -c "BUCKET_STEPS" src/app/api/parts/\[id\]/download/route.ts` → mindestens 2
- `grep -c "Promise.all" src/app/api/parts/\[id\]/thumbnails/route.ts` → 1
</verification>

<success_criteria>
- Alle 3 API-Endpoints implementiert und TypeScript-kompilierbar
- UUID-Validierung (z.string().uuid()) ist erste Operation in JEDEM Handler — keine Ausnahmen
- Promise.all für parallele Thumbnail-URL-Generierung (nicht sequenziell)
- Download-URL mit ResponseContentDisposition + ResponseContentType + expiresIn: 300
- sanitizeFilename-Funktion deckt: Leerzeichen→_, Sonderzeichen entfernen, Fallback "bauteil"
- Kein "use client" in API-Route-Dateien
</success_criteria>

<output>
Nach Abschluss `.planning/phases/09-part-detail/09-02-SUMMARY.md` erstellen mit:
- Implementierten Endpoints und deren Response-Contracts
- Bestätigtem npm run build (Exit 0)
- Sicherheitsmaßnahmen (UUID-Validierung in allen 3 Handlern bestätigt)
</output>
