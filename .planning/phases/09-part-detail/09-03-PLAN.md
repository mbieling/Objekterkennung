---
phase: 09-part-detail
plan: 03
type: execute
wave: 2
depends_on:
  - "09-02"
files_modified:
  - src/hooks/usePartDetail.ts
  - src/app/parts/[id]/PartDetail.tsx
  - src/app/parts/[id]/page.tsx
  - src/app/parts/[id]/PartDetail.test.tsx
  - src/hooks/usePartDetail.test.ts
autonomous: true
requirements:
  - DETAIL-01
  - DETAIL-02
must_haves:
  truths:
    - "Navigieren zu /parts/[id] zeigt alle 5 Metadatenfelder (Name, Teilenummer, Projekt, Status, Hochgeladen)"
    - "Status wird als farbiger Badge angezeigt (grün/blau/grau/rot per D-10)"
    - "Während des Ladens sind Skeleton-Platzhalter für Hauptbild, Thumbnail-Leiste und Metadaten sichtbar"
    - "Fehler (404) zeigt 'Bauteil nicht gefunden' mit Zurück-Button"
    - "Download-Button ist disabled mit Hinweis wenn status != ready"
    - "Download-Button setzt window.location.href auf Presigned URL bei status=ready"
    - "Thumbnail-Galerie zeigt Hauptbild + scrollbare Miniatur-Leiste; Klick wechselt Hauptbild"
    - "Alle 11 Vitest-Stubs aus Wave 0 sind aktiviert und grün"
  artifacts:
    - path: "src/hooks/usePartDetail.ts"
      provides: "Custom Hook — parallele API-Abfragen, Daten-State"
      contains: "Promise.all"
      exports: ["usePartDetail"]
    - path: "src/app/parts/[id]/PartDetail.tsx"
      provides: "Client-Komponente — Layout D-09, Galerie D-01..D-04, Badge D-10, Download D-05..D-07"
      contains: '"use client"'
    - path: "src/app/parts/[id]/page.tsx"
      provides: "Server-Component-Wrapper"
      contains: "await params"
  key_links:
    - from: "src/app/parts/[id]/PartDetail.tsx"
      to: "src/hooks/usePartDetail.ts"
      via: "import { usePartDetail }"
      pattern: "usePartDetail"
    - from: "src/app/parts/[id]/PartDetail.tsx"
      to: "/api/parts/[id]/download"
      via: "fetch in handleDownload → window.location.href"
      pattern: "window.location.href"
    - from: "src/app/parts/[id]/page.tsx"
      to: "src/app/parts/[id]/PartDetail.tsx"
      via: "import { PartDetail }"
      pattern: "PartDetail"
---

<objective>
Wave 2: Custom-Hook und Client-Komponente implementieren — usePartDetail.ts (parallele API-Calls), PartDetail.tsx (vollständige UI per UI-SPEC), page.tsx (Server-Component-Wrapper). Gleichzeitig alle Wave-0-Stubs aus 09-01 aktivieren.

Purpose: Nach Wave 1 (API stabil) kann die Client-Seite gegen den verifizierten API-Kontrakt implementiert werden.
Output: Vollständige /parts/[id]-Seite mit Galerie, Metadaten, Download-Button und aktivierten Unit-Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-part-detail/09-CONTEXT.md
@.planning/phases/09-part-detail/09-UI-SPEC.md
@.planning/phases/09-part-detail/09-PATTERNS.md

<interfaces>
<!-- API-Kontrakt aus Wave 1 (09-02) — Executor kennt die Response-Shapes ohne Codebase-Scan -->

GET /api/parts/[id] → { part: { id: string, name: string, part_number: string | null, project: string | null, status: 'pending' | 'processing' | 'ready' | 'failed', thumbnail_count: number, created_at: string } }

GET /api/parts/[id]/thumbnails → { urls: string[] }  (leer wenn status != ready oder thumbnail_count = 0)

GET /api/parts/[id]/download → { url: string, filename: string }  (nur bei status = ready)

shadcn/ui Komponenten (alle installiert):
- import { Badge } from '@/components/ui/badge'
- import { Button } from '@/components/ui/button'
- import { Skeleton } from '@/components/ui/skeleton'

Lucide Icons:
- import { ArrowLeft, Download } from 'lucide-react'

Next.js Navigation:
- import { useRouter } from 'next/navigation'
- import Link from 'next/link'

Aus src/app/admin/CatalogTable.tsx (direkt kopierbar):
```typescript
function StatusBadge({ status }: { status: Part['status'] }) {
  if (status === 'ready')
    return <Badge className="text-green-700 bg-green-50 border-green-200 hover:bg-green-50">Bereit</Badge>
  if (status === 'pending')
    return <Badge variant="secondary">Ausstehend</Badge>
  if (status === 'processing')
    return <Badge variant="outline" className="text-blue-600 border-blue-300">Wird verarbeitet…</Badge>
  if (status === 'failed')
    return <Badge variant="destructive">Fehlgeschlagen</Badge>
  return null
}

function formatDate(iso: string): string {
  return new Intl.DateTimeFormat('de-DE', {
    day: '2-digit', month: '2-digit', year: 'numeric',
  }).format(new Date(iso))
}
```

Tailwind-Klassen aus 09-UI-SPEC.md (vollständig spezifiziert — nicht erfinden):
- Page shell: "min-h-screen bg-background py-8 px-4" / "max-w-md mx-auto"
- Back link: "flex items-center gap-1 text-sm text-muted-foreground hover:text-foreground transition-colors min-h-[44px] mb-6"
- Main image container: "relative w-[320px] h-[320px] md:w-[480px] md:h-[480px] rounded-lg overflow-hidden bg-muted mx-auto mb-3"
- Main image: "w-full h-full object-contain"
- Thumbnail strip wrapper: "flex gap-2 overflow-x-auto pb-2 mb-6"
- Thumbnail button: "relative w-16 h-16 shrink-0 rounded-md overflow-hidden bg-muted cursor-pointer ring-2 ring-transparent transition-all focus-visible:ring-ring data-[active=true]:ring-primary"
- H1: "text-2xl font-semibold leading-tight mb-4"
- Metadata dl: "space-y-3 mb-6"
- Metadata row: "flex justify-between items-baseline gap-4"
- Metadata dt: "text-sm text-muted-foreground shrink-0"
- Metadata dd: "text-sm font-normal text-right"
- Download button: "w-full gap-2" (shadcn Button default variant)
- Download hint: "text-xs text-center text-muted-foreground"
- Error state: "text-center py-16"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: usePartDetail.ts + page.tsx implementieren</name>
  <files>src/hooks/usePartDetail.ts, src/app/parts/[id]/page.tsx</files>
  <read_first>
    - src/hooks/use-part-status.ts — vollständiges Muster für Hook-Struktur (Interface, useState, useEffect, cleanup)
    - src/app/search/page.tsx — Server-Component-Wrapper-Pattern (params await, Client-Component-Pass-Down)
    - .planning/phases/09-part-detail/09-PATTERNS.md — Abschnitt "usePartDetail.ts" und "page.tsx" für konkrete Patterns
  </read_first>
  <action>
**Datei 1: `src/hooks/usePartDetail.ts`**

```typescript
// Custom Hook für Part-Detail-Seite — D-03, D-12, D-13, DETAIL-01, DETAIL-02
// Parallele API-Anfragen: GET /api/parts/[id] + GET /api/parts/[id]/thumbnails
'use client'

import { useState, useEffect } from 'react'

export interface Part {
  id: string
  name: string
  part_number: string | null
  project: string | null
  status: 'pending' | 'processing' | 'ready' | 'failed'
  thumbnail_count: number
  created_at: string
}

export interface UsePartDetailResult {
  part: Part | null
  thumbnailUrls: string[]
  isLoading: boolean
  error: 'not_found' | 'error' | null
}

export function usePartDetail(id: string): UsePartDetailResult {
  const [part, setPart] = useState<Part | null>(null)
  const [thumbnailUrls, setThumbnailUrls] = useState<string[]>([])
  const [isLoading, setIsLoading] = useState(true)
  const [error, setError] = useState<'not_found' | 'error' | null>(null)

  useEffect(() => {
    if (!id) return

    setIsLoading(true)
    setPart(null)
    setThumbnailUrls([])
    setError(null)

    // D-03: Alle Presigned URLs in einer API-Anfrage (parallel)
    Promise.all([
      fetch(`/api/parts/${id}`).then(r => {
        if (r.status === 404) return Promise.reject(404)
        if (!r.ok) return Promise.reject('error')
        return r.json()
      }),
      fetch(`/api/parts/${id}/thumbnails`).then(r =>
        r.ok ? r.json() : { urls: [] }
      ),
    ])
      .then(([partData, thumbData]) => {
        setPart(partData.part)
        setThumbnailUrls(thumbData.urls ?? [])
      })
      .catch(err => {
        setError(err === 404 ? 'not_found' : 'error')
      })
      .finally(() => {
        setIsLoading(false)
      })
  }, [id])  // NUR [id] — kein thumbnailUrls im Deps-Array (Endlosloop-Pitfall)

  return { part, thumbnailUrls, isLoading, error }
}
```

**Datei 2: `src/app/parts/[id]/page.tsx`**

```typescript
import type { Metadata } from 'next'
import { PartDetail } from './PartDetail'

export const metadata: Metadata = {
  title: 'Bauteil-Details — Bauteil-Finder',
}

export default async function PartDetailPage({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  const { id } = await params  // Next.js 16: params ist Promise
  return (
    <main className="min-h-screen bg-background py-8 px-4">
      <div className="max-w-md mx-auto">
        <PartDetail id={id} />
      </div>
    </main>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/mbieling/claude/Objekterkennung && npm run build 2>&1 | grep -E "error TS|Error:" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - usePartDetail.ts existiert: `test -f src/hooks/usePartDetail.ts`
    - page.tsx existiert: `test -f "src/app/parts/[id]/page.tsx"`
    - Hook hat "use client": `grep -c '"use client"' src/hooks/usePartDetail.ts` → 1
    - page.tsx ist Server-Component (kein "use client"): `grep -c '"use client"' "src/app/parts/[id]/page.tsx"` → 0
    - Promise.all im Hook: `grep -c "Promise.all" src/hooks/usePartDetail.ts` → 1
    - Nur [id] im Deps-Array (kein thumbnailUrls): `grep "}, \[id\]" src/hooks/usePartDetail.ts` → vorhanden
    - await params in page.tsx: `grep -c "await params" "src/app/parts/[id]/page.tsx"` → 1
    - npm run build ohne Fehler
  </acceptance_criteria>
  <done>usePartDetail.ts und page.tsx implementiert. Hook nutzt Promise.all mit [id]-only-Dependency. Server-Component awaitet params korrekt.</done>
</task>

<task type="auto">
  <name>Task 2: PartDetail.tsx implementieren + Wave-0-Stubs aktivieren</name>
  <files>src/app/parts/[id]/PartDetail.tsx, src/app/parts/[id]/PartDetail.test.tsx, src/hooks/usePartDetail.test.ts</files>
  <read_first>
    - src/app/admin/CatalogTable.tsx — StatusBadge (Zeilen 104-133) und formatDate (Zeilen 135-145) direkt kopieren
    - src/app/search/SearchResultCard.tsx — Thumbnail-Loading-Pattern mit onError-Handler
    - .planning/phases/09-part-detail/09-UI-SPEC.md — vollständige Tailwind-Klassen, alle Komponenten-Specs (Abschnitte 1-10)
    - src/app/parts/[id]/PartDetail.test.tsx — aktuellen Stub-Inhalt lesen (wird aktiviert, nicht überschrieben)
    - src/hooks/usePartDetail.test.ts — aktuellen Stub-Inhalt lesen (wird aktiviert, nicht überschrieben)
    - src/hooks/use-part-status.test.ts — mockFetchResponse-Pattern für Test-Aktivierung
  </read_first>
  <action>
**Teil A — `src/app/parts/[id]/PartDetail.tsx` erstellen:**

Vollständige Client-Komponente mit allen D-01..D-11-Entscheidungen:

```typescript
'use client'

import { useState } from 'react'
import Link from 'next/link'
import { useRouter } from 'next/navigation'
import { ArrowLeft, Download } from 'lucide-react'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Skeleton } from '@/components/ui/skeleton'
import { usePartDetail, type Part } from '@/hooks/usePartDetail'

// D-10: StatusBadge — exakt wie in CatalogTable.tsx (direkt kopiert)
function StatusBadge({ status }: { status: Part['status'] }) {
  if (status === 'ready')
    return <Badge className="text-green-700 bg-green-50 border-green-200 hover:bg-green-50">Bereit</Badge>
  if (status === 'pending')
    return <Badge variant="secondary">Ausstehend</Badge>
  if (status === 'processing')
    return <Badge variant="outline" className="text-blue-600 border-blue-300">Wird verarbeitet…</Badge>
  if (status === 'failed')
    return <Badge variant="destructive">Fehlgeschlagen</Badge>
  return null
}

// Datum-Formatierung — exakt wie in CatalogTable.tsx
function formatDate(iso: string): string {
  return new Intl.DateTimeFormat('de-DE', {
    day: '2-digit',
    month: '2-digit',
    year: 'numeric',
  }).format(new Date(iso))
}

interface PartDetailProps {
  id: string
}

export function PartDetail({ id }: PartDetailProps) {
  const router = useRouter()
  const { part, thumbnailUrls, isLoading, error } = usePartDetail(id)
  const [activeIndex, setActiveIndex] = useState(0)
  const [isDownloading, setIsDownloading] = useState(false)

  // D-08: router.back() wenn History vorhanden, sonst /search
  function handleBack() {
    if (typeof window !== 'undefined' && window.history.length > 1) {
      router.back()
    } else {
      router.push('/search')
    }
  }

  // D-05: Presigned URL holen → window.location.href (kein Next.js-Proxy)
  async function handleDownload() {
    if (!part || part.status !== 'ready') return
    setIsDownloading(true)
    try {
      const res = await fetch(`/api/parts/${id}/download`)
      if (!res.ok) return
      const { url } = await res.json()
      window.location.href = url
    } finally {
      setIsDownloading(false)
    }
  }

  // Skeleton-Layout während Laden (D-04)
  if (isLoading) {
    return (
      <>
        <Skeleton className="h-6 w-32 mb-6" />
        <Skeleton className="w-[320px] h-[320px] md:w-[480px] md:h-[480px] rounded-lg mx-auto mb-3" />
        <div className="flex gap-2 mb-6">
          {Array.from({ length: 6 }).map((_, i) => (
            <Skeleton key={i} className="w-16 h-16 shrink-0 rounded-md" />
          ))}
        </div>
        <Skeleton className="h-8 w-3/4 mb-4" />
        <div className="space-y-3 mb-6">
          {Array.from({ length: 4 }).map((_, i) => (
            <Skeleton key={i} className="h-5 w-full" />
          ))}
        </div>
        <Skeleton className="h-10 w-full" />
      </>
    )
  }

  // Fehler-State (DETAIL-01: 404 oder Netzwerkfehler)
  if (error || !part) {
    return (
      <div className="text-center py-16">
        <p className="text-muted-foreground mb-4">Bauteil nicht gefunden.</p>
        <Link href="/search">
          <Button variant="outline">Zurück zur Suche</Button>
        </Link>
      </div>
    )
  }

  // Anzahl der Skeleton-Items für Thumbnail-Strip (D-11: Fallback 6 wenn thumbnail_count = 0)
  const skeletonCount = part.thumbnail_count > 0 ? part.thumbnail_count : 6
  const hasUrls = thumbnailUrls.length > 0

  return (
    <>
      {/* D-08: Back Link */}
      <button
        onClick={handleBack}
        className="flex items-center gap-1 text-sm text-muted-foreground hover:text-foreground transition-colors min-h-[44px] mb-6"
      >
        <ArrowLeft className="h-4 w-4" />
        Zurück zur Suche
      </button>

      {/* D-01, D-02: Hauptbild */}
      <div className="relative w-[320px] h-[320px] md:w-[480px] md:h-[480px] rounded-lg overflow-hidden bg-muted mx-auto mb-3">
        {hasUrls ? (
          <img
            src={thumbnailUrls[activeIndex] ?? thumbnailUrls[0]}
            alt={`${part.name} — Ansicht ${activeIndex + 1}`}
            className="w-full h-full object-contain"
            onError={e => {
              (e.currentTarget as HTMLImageElement).style.display = 'none'
            }}
          />
        ) : (
          <Skeleton className="w-full h-full" />
        )}
      </div>

      {/* D-01: Thumbnail-Leiste (horizontal scrollable) */}
      <div className="flex gap-2 overflow-x-auto pb-2 mb-6">
        {hasUrls
          ? thumbnailUrls.map((url, i) => (
              <button
                key={i}
                onClick={() => setActiveIndex(i)}
                aria-label={`Ansicht ${i + 1}`}
                aria-pressed={i === activeIndex}
                data-active={i === activeIndex}
                className="relative w-16 h-16 shrink-0 rounded-md overflow-hidden bg-muted cursor-pointer ring-2 ring-transparent transition-all focus-visible:ring-ring data-[active=true]:ring-primary"
              >
                <img
                  src={url}
                  alt={`Ansicht ${i + 1}`}
                  className="w-full h-full object-contain"
                />
              </button>
            ))
          : Array.from({ length: skeletonCount }).map((_, i) => (
              <Skeleton key={i} className="w-16 h-16 shrink-0 rounded-md" />
            ))}
      </div>

      {/* D-09: Layout — H1 Name */}
      <h1 className="text-2xl font-semibold leading-tight mb-4">{part.name}</h1>

      {/* D-09: Metadaten-Block als <dl> (DETAIL-01) */}
      <dl className="space-y-3 mb-6">
        <div className="flex justify-between items-baseline gap-4">
          <dt className="text-sm text-muted-foreground shrink-0">Teilenummer</dt>
          <dd className="text-sm font-normal text-right">{part.part_number ?? '—'}</dd>
        </div>
        <div className="flex justify-between items-baseline gap-4">
          <dt className="text-sm text-muted-foreground shrink-0">Projekt</dt>
          <dd className="text-sm font-normal text-right">{part.project ?? '—'}</dd>
        </div>
        <div className="flex justify-between items-baseline gap-4">
          <dt className="text-sm text-muted-foreground shrink-0">Status</dt>
          <dd><StatusBadge status={part.status} /></dd>
        </div>
        <div className="flex justify-between items-baseline gap-4">
          <dt className="text-sm text-muted-foreground shrink-0">Hochgeladen</dt>
          <dd className="text-sm font-normal text-right">{formatDate(part.created_at)}</dd>
        </div>
      </dl>

      {/* D-07, D-11: Download-Button (disabled wenn status != ready) */}
      {part.status === 'ready' ? (
        <Button
          className="w-full gap-2"
          onClick={handleDownload}
          disabled={isDownloading}
          aria-disabled={isDownloading}
        >
          <Download className="h-4 w-4" />
          STEP herunterladen
        </Button>
      ) : (
        <div className="space-y-2">
          <Button className="w-full gap-2" disabled aria-disabled>
            <Download className="h-4 w-4" />
            STEP herunterladen
          </Button>
          <p className="text-xs text-center text-muted-foreground">
            Datei wird noch verarbeitet
          </p>
        </div>
      )}
    </>
  )
}
```

**Teil B — PartDetail.test.tsx aktivieren (it.todo → echte Tests):**

Die Datei komplett ersetzen mit aktivierten Tests. Alle 7 it.todo aus Wave 0 werden zu echten it()-Blöcken:

Mock-Setup: `usePartDetail` mocken via `vi.mock('@/hooks/usePartDetail')`, dann `vi.mocked(usePartDetail).mockReturnValue(...)`.

**Teil C — usePartDetail.test.ts aktivieren:**

Die Datei komplett ersetzen mit aktivierten Tests. `fetch` mocken via `global.fetch = vi.fn()`, `renderHook` + `act` + `Promise.resolve()` für async-Test.
  </action>
  <verify>
    <automated>cd /Users/mbieling/claude/Objekterkennung && npm test -- --testPathPattern="PartDetail|usePartDetail" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - PartDetail.tsx existiert: `test -f "src/app/parts/[id]/PartDetail.tsx"`
    - "use client" vorhanden: `grep -c '"use client"' "src/app/parts/[id]/PartDetail.tsx"` → 1
    - window.location.href für Download (kein Next.js-Proxy): `grep -c "window.location.href" "src/app/parts/[id]/PartDetail.tsx"` → 1
    - router.back()-Fallback vorhanden: `grep -c "window.history.length" "src/app/parts/[id]/PartDetail.tsx"` → 1
    - "Datei wird noch verarbeitet" Hinweis-Text: `grep -c "Datei wird noch verarbeitet" "src/app/parts/[id]/PartDetail.tsx"` → 1
    - aria-label auf Thumbnail-Buttons: `grep -c "aria-label" "src/app/parts/[id]/PartDetail.tsx"` → mindestens 1
    - Keine it.todo-Reste in Tests: `grep -c "it.todo" "src/app/parts/[id]/PartDetail.test.tsx"` → 0
    - Keine it.todo-Reste in Hook-Tests: `grep -c "it.todo" src/hooks/usePartDetail.test.ts` → 0
    - npm test gibt Exit 0 zurück (alle aktivierten Tests grün)
    - npm run build ohne Fehler
  </acceptance_criteria>
  <done>PartDetail.tsx vollständig implementiert. Alle 11 Wave-0-Stubs aktiviert und grün. npm test und npm run build beide Exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Client → API | Alle fetch()-Calls gehen an lokale Next.js API-Routes (nicht direkt an S3/DB) |
| API-Response → window.location.href | Presigned URL aus API-Response wird direkt als Browser-Redirect verwendet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering | id-Prop in PartDetail — kommt aus Server-Component-params | accept | params werden von Next.js aus der URL geparst; UUID-Validierung findet in API-Handlers (Wave 1) statt — doppelte Validierung im Client ist unnötig |
| T-09-08 | Tampering | window.location.href = url (Presigned URL aus API) | mitigate | URL kommt von eigenem API-Endpoint (nicht aus user input); API-Endpoint validiert UUID (T-09-01) und gibt nur gültige S3-URLs zurück; Open-Redirect ausgeschlossen da URL vom eigenen Server stammt |
| T-09-09 | Information Disclosure | Presigned URL in JS-Memory während handleDownload | accept | URL wird sofort als location.href gesetzt und nicht gecacht; Browser-Memory-Scope; kein Logging |
| T-09-10 | Denial of Service | handleDownload bei schnellen Mehrfach-Klicks | mitigate | isDownloading-State disabled den Button nach erstem Klick — verhindert mehrfache parallele Download-Anfragen |
</threat_model>

<verification>
Nach Wave 2 (Hook + Component + aktivierte Tests):
- `npm test -- --testPathPattern="PartDetail|usePartDetail"` → Exit 0, alle 11 Tests grün
- `npm run build` → Exit 0, keine TypeScript-Fehler
- `grep -c "it.todo" "src/app/parts/[id]/PartDetail.test.tsx"` → 0
- `grep -c "window.location.href" "src/app/parts/[id]/PartDetail.tsx"` → 1
- `test -f "src/app/parts/[id]/page.tsx"` → Exit 0
</verification>

<success_criteria>
- usePartDetail.ts mit Promise.all, [id]-only-Deps, UsePartDetailResult-Interface
- PartDetail.tsx mit vollständigem D-09-Layout, StatusBadge, formatDate, Galerie-Interaktion, Download-Handler
- page.tsx als Server-Component mit await params
- Alle 11 it.todo-Stubs aus 09-01 aktiviert und grün (npm test Exit 0)
- npm run build Exit 0
</success_criteria>

<output>
Nach Abschluss `.planning/phases/09-part-detail/09-03-SUMMARY.md` erstellen mit:
- Implementierten Dateien und exportierten Interfaces
- Test-Ergebnis (npm test — alle N Tests grün)
- Bestätigtem npm run build (Exit 0)
- Aktivierten Test-Stubs (11 von 11)
</output>
