---
phase: 04-ingestion-ui
plan: 06
type: execute
wave: 4
depends_on: [05]
files_modified:
  - src/app/upload/page.tsx
  - src/app/page.tsx
autonomous: false
requirements:
  - INGEST-01
  - INGEST-02
must_haves:
  truths:
    - "/upload-Seite ist als Server Component erreichbar und rendert UploadForm-Client-Component (D-01)"
    - "/upload-Seite hat metadata.title 'STEP-Datei hochladen — Bauteil-Finder'"
    - "Startseite (/) zeigt einen einzelnen Button 'Teil hochladen' der auf /upload verlinkt (D-02)"
    - "Kein automatischer Redirect / → /upload (D-02 explizit)"
    - "Vollständiger Build (npm run build) ist fehlerfrei"
    - "Human-Verify-Checkpoint bestätigt: Upload-Flow visuell + funktional funktioniert"
  artifacts:
    - path: "src/app/upload/page.tsx"
      provides: "Server-Component-Wrapper für UploadForm"
      contains: "import { UploadForm }"
    - path: "src/app/page.tsx"
      provides: "Minimale Homepage mit 'Teil hochladen'-Link (D-02)"
      contains: "href=\"/upload\""
  key_links:
    - from: "src/app/upload/page.tsx"
      to: "src/app/upload/UploadForm.tsx"
      via: "import + JSX-Verwendung"
      pattern: "<UploadForm"
    - from: "src/app/page.tsx"
      to: "/upload (Next.js Routing)"
      via: "next/link"
      pattern: "href=\"/upload\""
---

<objective>
Verbinde die UploadForm-Komponente (Plan 05) mit dem Next.js-Routing — erstelle den Server-Component-Wrapper unter `/upload` und reduziere die Startseite auf einen einzigen Link gemäß D-02.

**Purpose:** Ohne page.tsx ist die UploadForm-Komponente nicht über eine URL erreichbar. Ohne den Homepage-Link weiß der Ingenieur nicht, wo er hin navigieren soll. Phase 4 endet mit einem visuellen + funktionalen Human-Verify-Checkpoint, der den Erfolg gegen die ROADMAP.md Phase-4-Success-Criteria validiert.

**Output:**
- 1 neue Server Component: `src/app/upload/page.tsx`
- 1 modifizierte Seite: `src/app/page.tsx` (vollständiger Rewrite zu minimaler Landing)
- 1 Human-Verify-Checkpoint (Phase-Abschluss-Validation)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ingestion-ui/04-CONTEXT.md
@.planning/phases/04-ingestion-ui/04-UI-SPEC.md
@.planning/phases/04-ingestion-ui/04-PATTERNS.md
@src/app/layout.tsx
@src/app/upload/UploadForm.tsx
@src/components/ui/button.tsx

<interfaces>
<!-- Existierender layout.tsx-Pattern (Server Component mit metadata) -->

From src/app/layout.tsx:
```typescript
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AI Coding Starter Kit",
  description: "Built with AI Agent Team System",
};
```

**Button-asChild-Pattern** (button.tsx — verifiziert):
```tsx
const Comp = asChild ? Slot : "button"
```
Verwendung mit Next-Link:
```tsx
<Button asChild>
  <Link href="/upload">Teil hochladen</Link>
</Button>
```

**UploadForm-Export** (Plan 05):
```typescript
export function UploadForm()  // 'use client'
```

UploadForm ist Client Component — aber `page.tsx` darf trotzdem Server Component bleiben (next.js erlaubt Server Components, die Client Components rendern, ohne selbst `'use client'` zu sein).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-Component /upload/page.tsx + Homepage-Rewrite</name>
  <files>
    src/app/upload/page.tsx,
    src/app/page.tsx
  </files>
  <read_first>
    - src/app/upload/UploadForm.tsx (Plan 05 — Bestätigung des UploadForm-Export-Namens)
    - src/app/layout.tsx (Server Component + metadata-Pattern)
    - src/app/page.tsx (CURRENT — wird komplett ersetzt)
    - .planning/phases/04-ingestion-ui/04-UI-SPEC.md (Section "Homepage Touchpoint" — exakter Markup für Homepage; Section "Layout Contract" — Page-Wrapper-Markup)
    - .planning/phases/04-ingestion-ui/04-PATTERNS.md (Section "src/app/upload/page.tsx" + "src/app/page.tsx" — vollständige Code-Vorlagen)
    - src/components/ui/button.tsx (Bestätigung des asChild-Slot-Patterns)
  </read_first>
  <action>
    **Schritt A: `src/app/upload/page.tsx` erstellen** mit folgendem exakten Inhalt (Server Component — KEIN `'use client'`):

    ```tsx
    // src/app/upload/page.tsx
    // Phase 4 — /upload Server Component Wrapper (D-01).
    // KEIN "use client" — UploadForm ist Client Component, page.tsx bleibt server-side.

    import type { Metadata } from 'next'
    import { UploadForm } from './UploadForm'

    export const metadata: Metadata = {
      title: 'STEP-Datei hochladen — Bauteil-Finder',
    }

    export default function UploadPage() {
      return (
        <main className="min-h-screen bg-background py-12 px-4">
          <div className="max-w-4xl mx-auto">
            <h1 className="text-2xl font-semibold mb-8">STEP-Datei hochladen</h1>
            <UploadForm />
          </div>
        </main>
      )
    }
    ```

    Spacing folgt UI-SPEC: `py-12` (48px vertikal), `px-4`, `max-w-4xl`, `text-2xl font-semibold mb-8`. Heading-Text exakt "STEP-Datei hochladen" gemäß Copywriting Contract.

    **Schritt B: `src/app/page.tsx` vollständig ersetzen** (D-02: minimal, Single Button/Link, KEIN Redirect):

    ```tsx
    // src/app/page.tsx
    // Phase 4 — minimale Landing-Page (D-02). Späterer Dashboard-Ausbau in Phase 5+.

    import Link from 'next/link'
    import { Button } from '@/components/ui/button'

    export default function Home() {
      return (
        <main className="min-h-screen flex items-center justify-center p-8">
          <Button asChild>
            <Link href="/upload">Teil hochladen</Link>
          </Button>
        </main>
      )
    }
    ```

    KEIN `<Image>`, KEINE Hero-Section, KEINE alten Vercel/Next.js-Logos — alle aus dem Template-Erbe sind explizit zu entfernen. D-02 schreibt vor: ein einzelner Button.

    **Schritt C: Build-Verifikation**
    Führe aus: `npm run build` — der Build muss fehlerfrei durchlaufen. Mögliche Fehler:
    - "Property 'metadata' does not exist" → fehlender `import type { Metadata }`
    - "UploadForm is not exported" → Plan 05 hat anderen Export-Namen → Plan-05-Fix nötig
    - TypeScript-Errors in UploadForm wegen fehlender Props → UploadForm muss propslos sein

    **Schritt D: Lint-Verifikation**
    `npm run lint` — keine neuen Lint-Fehler in den modifizierten Dateien.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -20 &amp;&amp; npm run lint 2>&amp;1 | tail -10 &amp;&amp; grep -c 'href="/upload"' src/app/page.tsx | grep -q "^1$" &amp;&amp; grep -c "import { UploadForm }" src/app/upload/page.tsx | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/upload/page.tsx` existiert und exportiert default-Function: `grep -c "export default function UploadPage" src/app/upload/page.tsx` ergibt `1`
    - upload/page.tsx ist KEINE Client Component: `grep -c "'use client'" src/app/upload/page.tsx` ergibt `0`
    - upload/page.tsx importiert UploadForm: `grep -c "import { UploadForm } from './UploadForm'" src/app/upload/page.tsx` ergibt `1`
    - upload/page.tsx setzt metadata.title: `grep -c "STEP-Datei hochladen — Bauteil-Finder" src/app/upload/page.tsx` ergibt `1`
    - upload/page.tsx verwendet UI-SPEC-Spacing: `grep -c "max-w-4xl" src/app/upload/page.tsx` ergibt mindestens `1`, `grep -c "py-12" src/app/upload/page.tsx` ergibt mindestens `1`
    - `src/app/page.tsx` enthält nur einen Link auf `/upload`: `grep -c 'href="/upload"' src/app/page.tsx` ergibt `1`
    - `src/app/page.tsx` zeigt deutschen Text "Teil hochladen": `grep -c "Teil hochladen" src/app/page.tsx` ergibt `1`
    - `src/app/page.tsx` enthält KEINE Vercel-/Next.js-Logos mehr: `grep -c "next.svg\|vercel.svg" src/app/page.tsx` ergibt `0`
    - `src/app/page.tsx` enthält KEINEN automatischen Redirect (D-02): `grep -c "redirect.*upload\|router.push.*upload" src/app/page.tsx` ergibt `0`
    - `src/app/page.tsx` verwendet shadcn Button: `grep -c "from '@/components/ui/button'" src/app/page.tsx` ergibt `1`
    - `npm run build` Exit-Code 0
    - `npm run lint` Exit-Code 0 (oder nur warnings, keine errors) auf modifizierten Dateien
    - `npm test` (volle Suite) Exit-Code 0 — keine Regressionen in Phase 1–3 oder Phase 4 Plänen 02–05
  </acceptance_criteria>
  <done>upload/page.tsx Server Component erstellt, Homepage minimal mit Link, Build + Lint + Tests grün.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase-4-Abschluss — Human-Verify gegen ROADMAP-Success-Criteria</name>
  <what-built>
    Phase 4 ist vollständig implementiert:
    - Migration 002_add_thumbnail_count.sql ist auf Neon-DB angewendet (Plan 01)
    - GET /api/parts/[id]/status liefert {status, thumbnail_count} (Plan 02)
    - GET /api/parts/[id]/thumbnail liefert 60s-Presigned-URL für view_0.png (Plan 03)
    - usePartStatus-Hook pollt mit variablem Intervall + 5-Min-Timeout (Plan 04)
    - UploadForm.tsx implementiert vollständigen 5-stufigen Flow mit deutscher UI-SPEC (Plan 05)
    - /upload-Seite + minimaler Homepage-Link (Plan 06)
    - Alle Vitest-Tests grün (3+5+8+6 = 22 aktive Tests)
    - npm run build fehlerfrei
  </what-built>
  <how-to-verify>
    Diese Verifikation ist menschlich, weil sie das vollständige End-to-End-Verhalten gegen die ROADMAP-Success-Criteria prüft — visuell + funktional. Drei der vier Erfolgskriterien (Status-Echtzeit-Update, Thumbnail-Anzeige, Duplikat-Inline-Fehler) brauchen einen laufenden Worker und sind nicht durch Unit-Tests vollständig abgedeckt.

    **Voraussetzungen:**
    - Docker Compose aus Phase 3 läuft: `docker-compose up -d` im Repo-Root (Worker + Redis)
    - Neon-DB ist erreichbar (DATABASE_URL in .env.local gesetzt)
    - AWS-Buckets `parts-steps` + `parts-thumbnails` sind erreichbar
    - `npm run dev` läuft auf localhost:3000

    **Verifikations-Schritte gegen Roadmap Phase-4 Success Criteria:**

    1. **SC1: User kann STEP-Datei (≤100 MB) mit Metadaten auswählen und submitten**
       - Browser → http://localhost:3000
       - Sehe genau einen Button "Teil hochladen" auf weißem Hintergrund (D-02 Verifikation)
       - Klick führt zu http://localhost:3000/upload (kein Redirect; URL ist /upload)
       - Form-Card "Bauteil-Daten" links sichtbar
       - Status-Tracker rechts INITIAL UNSICHTBAR (D-03)
       - Datei `testdata/sample.step` (Phase 2) auswählen
       - Bezeichnung "Smoke Test 04-06" eingeben, andere Felder leer lassen
       - "Hochladen" klicken
       - **Erwartung:** Submit-Button zeigt Loader2-Spinner + Text "Wird hochgeladen…"; Status-Tracker rechts erscheint mit Progress-Bar

    2. **SC2: Status-Indicator updated in Echtzeit (pending → processing → ready)**
       - Beobachte rechte Spalte in den nächsten 30–120s
       - Badge wechselt von "Ausstehend" → "Wird verarbeitet…" → "Bereit"
       - Description-Text wechselt analog ("Warte auf Worker…" → "3D-Ansichten werden berechnet…" → "Verarbeitung abgeschlossen. Vorschau wird geladen…")
       - Network-Tab zeigt /api/parts/[id]/status-Calls alle ~2s in den ersten 30s, danach alle ~5s
       - **Erwartung:** Polling stoppt automatisch bei "Bereit"; keine weiteren Calls

    3. **SC3: Thumbnail erscheint ohne Page-Reload nach `ready`**
       - Sobald Badge "Bereit" zeigt, erscheint Skeleton, dann das geladene Thumbnail (Frontansicht)
       - **Erwartung:** Bild ist 192×192 px (`w-48 h-48`), `object-contain`, mit `border` und `rounded-md`
       - **D-10 Positiv-Verifikation (delegiert von Plan 04-05 Test 6):** Nach Thumbnail-Load erscheint "Neuer Upload"-Button. Klick auf den Button → Formular ist vollständig zurückgesetzt (Bezeichnung leer, Datei-Input geleert, Status-Tracker rechts ausgeblendet, Form-Felder wieder editierbar). Diese End-to-End-Verifikation ersetzt den im Vitest fragilen Hook+Network-Flow-Mock-Test.
       - **OQ3 RESOLVED Verifikation:** Wenn ein vorheriger Upload `failed` war (z. B. durch Worker-Fehler oder Timeout), erscheint KEIN "Neuer Upload"-Button — der User muss die Seite manuell reloaden. Dies kann optional getestet werden, indem eine fehlerhafte STEP-Datei hochgeladen wird (siehe Test-Schritt SC5 unten).

    4. **SC4: Duplikat-Upload zeigt Inline-Alert mit existing_part_id**
       - Klick "Neuer Upload" → Form ist wieder leer
       - DIESELBE `sample.step` erneut wählen, "Bezeichnung 2" eingeben, submitten
       - **Erwartung:** Inline-Alert (rote Variante) erscheint UNTER dem Datei-Input mit Text "Diese Datei existiert bereits — Teil-ID: <uuid des ersten Uploads>"
       - **Erwartung:** Form-Felder bleiben editierbar (KEIN Disabled-State)
       - **Erwartung:** Status-Tracker rechts erscheint NICHT (D-11)
       - **Erwartung:** KEIN Toast (UI-SPEC explizit)

    **Optional — Robustheit (nicht blockierend):**
    - Datei >100 MB testen → Inline-Fehler "Datei überschreitet die maximale Größe von 100 MB."
    - .jpg-Datei testen → Inline-Fehler "Nur STEP-Dateien (.step, .stp) werden akzeptiert."
    - Form ohne Bezeichnung submitten → FormMessage "Bezeichnung ist erforderlich."
    - **SC5 (OQ3 RESOLVED — failed-State, optional):** Korrupte oder ungültige STEP-Datei hochladen, sodass der Worker `status='failed'` setzt. Erwartung: Badge wechselt zu "Fehlgeschlagen" (rote Variante), Description "Die Verarbeitung ist fehlgeschlagen. Bitte prüfe die STEP-Datei und versuche es erneut.", **KEIN "Neuer Upload"-Button** sichtbar (D-10 strikt). User muss Page reloaden, um neuen Upload zu starten.

    **Falls etwas nicht passt:**
    - Frontend-Fehler (UI-SPEC-Abweichung) → /gsd-revise-plan 04 05
    - Backend-Fehler (API-Antwort falsch) → /gsd-revise-plan 04 02 oder 03
    - Polling-Verhalten falsch → /gsd-revise-plan 04 04
    - Page nicht erreichbar / Build-Fehler → /gsd-revise-plan 04 06
  </how-to-verify>
  <resume-signal>Mit "approved" antworten oder konkrete Abweichungen schildern (welche SC, was wurde gesehen vs. erwartet).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → Page | `/upload`-Route ist öffentlich erreichbar (kein Auth in Phase 4). |
| Server Component → Client Component | page.tsx → UploadForm — Hydration boundary; alle Props müssen serializable sein (UploadForm ist propslos → ok). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-23 | Information Disclosure | metadata.title in HTML-Head | accept | "STEP-Datei hochladen — Bauteil-Finder" enthält keine sensitiven Infos. SEO-Belang nicht in Phase 4. |
| T-04-24 | Tampering | Homepage-Rewrite löscht Vercel-Logos | accept | Beabsichtigte Änderung per D-02. Pure UI-Reduktion, keine Sicherheits-Auswirkung. |
| T-04-25 | Spoofing | UploadForm wird in falscher Page eingebunden | mitigate | Build-Stage prüft Import-Korrektheit. acceptance_criteria verlangt explizit `import { UploadForm }`. |
</threat_model>

<verification>
- `npm run build` Exit-Code 0
- `npm run lint` Exit-Code 0 oder nur warnings
- `npm test` volle Suite grün (Phase 1–4 alle Tests passing)
- Browser: http://localhost:3000 zeigt Single-Button-Landing
- Browser: http://localhost:3000/upload zeigt Form + (initial unsichtbaren) Status-Tracker
- Human-Verify-Checkpoint: 4 Roadmap-Success-Kriterien manuell bestätigt
</verification>

<success_criteria>
- /upload-Seite erreichbar und renderet korrekt
- Homepage zeigt Single-Button-Landing per D-02
- Build, Lint, vollständige Test-Suite grün
- Phase 4 vollständig abgeschlossen — alle 4 ROADMAP-Success-Kriterien bestätigt durch Human-Verify
- Nächster Phase-Schritt: `/gsd-progress` aktualisieren, dann `/gsd-discuss-phase 5`
</success_criteria>

<output>
After completion, create `.planning/phases/04-ingestion-ui/04-06-SUMMARY.md`
</output>
