---
phase: 04-ingestion-ui
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/002_add_thumbnail_count.sql
  - src/app/api/parts/[id]/status/route.test.ts
  - src/app/api/parts/[id]/thumbnail/route.test.ts
  - src/hooks/use-part-status.test.ts
  - src/app/upload/UploadForm.test.tsx
  - tests/phase-04-upload.spec.ts
autonomous: false
requirements:
  - INGEST-01
  - INGEST-02
must_haves:
  truths:
    - "Migration 002_add_thumbnail_count.sql existiert und ist auf der Neon-DB ausgeführt"
    - "parts.thumbnail_count-Spalte ist auf der Datenbank vorhanden mit DEFAULT 0"
    - "Vier Vitest-Test-Stubs existieren mit korrekt strukturierten describe/it-Blöcken (alle skipped)"
    - "Ein Playwright-E2E-Test-Stub existiert (test.skip)"
    - "npm test schlägt nicht fehl (skipped Tests werden nicht als Fehler gezählt)"
  artifacts:
    - path: "supabase/migrations/002_add_thumbnail_count.sql"
      provides: "DDL: ALTER TABLE parts ADD COLUMN thumbnail_count"
      contains: "alter table parts"
    - path: "src/app/api/parts/[id]/status/route.test.ts"
      provides: "Test-Stub mit allen INGEST-02 Status-Route-Tests (it.skip)"
      contains: "describe('GET /api/parts/[id]/status'"
    - path: "src/app/api/parts/[id]/thumbnail/route.test.ts"
      provides: "Test-Stub mit allen INGEST-02 Thumbnail-Route-Tests (it.skip)"
      contains: "describe('GET /api/parts/[id]/thumbnail'"
    - path: "src/hooks/use-part-status.test.ts"
      provides: "Test-Stub mit allen INGEST-02 Polling-Hook-Tests (it.skip, fake timers)"
      contains: "describe('usePartStatus'"
    - path: "src/app/upload/UploadForm.test.tsx"
      provides: "Test-Stub mit allen INGEST-01 UploadForm-Tests (it.skip)"
      contains: "describe('UploadForm'"
    - path: "tests/phase-04-upload.spec.ts"
      provides: "Playwright-E2E-Stub für Phase-4-Upload-Flow"
      contains: "test.skip"
  key_links:
    - from: "supabase/migrations/002_add_thumbnail_count.sql"
      to: "Neon-DB parts-Tabelle"
      via: "supabase db push (manual user step)"
      pattern: "thumbnail_count"
    - from: "Test-Stubs"
      to: "Wave-1/2/3-Tasks"
      via: "Spezifische it-Blöcke pro Task"
      pattern: "it.skip"
---

<objective>
Wave 0 — Schema-Vorbereitung und Test-Skelette für Phase 4 erstellen, BEVOR irgendeine API-Route oder Komponente implementiert wird.

**Purpose:** Phase 4 hängt davon ab, dass (1) die `parts.thumbnail_count`-Spalte in der DB existiert (Status-API D-05 gibt sie zurück), und (2) jeder Wave-1+ Task einen pre-existing Test-Stub hat, gegen den er implementiert werden kann (Nyquist-Konformität: kein Task ohne `<automated>` verify).

**Output:**
- 1 neue SQL-Migration: `supabase/migrations/002_add_thumbnail_count.sql`
- 4 Vitest-Test-Stubs (alle `it.skip`-Blöcke mit korrekten Test-Namen aus 04-VALIDATION.md)
- 1 Playwright-E2E-Test-Stub
- 1 Human-Checkpoint für `supabase db push` (Migration auf Neon-DB anwenden)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ingestion-ui/04-CONTEXT.md
@.planning/phases/04-ingestion-ui/04-RESEARCH.md
@.planning/phases/04-ingestion-ui/04-PATTERNS.md
@.planning/phases/04-ingestion-ui/04-UI-SPEC.md
@.planning/phases/04-ingestion-ui/04-VALIDATION.md
@supabase/migrations/001_parts_schema.sql
@src/app/api/upload/init/route.test.ts
@src/app/api/upload/confirm/route.test.ts

<interfaces>
<!-- Bestehender Test-Mock-Style aus Phase 3 — Wave-0-Stubs müssen exakt diesem Pattern folgen -->

From src/app/api/upload/init/route.test.ts (lines 7-24):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { NextResponse } from 'next/server'

vi.mock('@/lib/db', () => ({
  db: vi.fn(),
}))
vi.mock('@/lib/s3', () => ({
  s3: {},
  BUCKET_STEPS: 'mock-bucket-steps',
  BUCKET_THUMBNAILS: 'mock-bucket-thumbnails',
}))
vi.mock('@aws-sdk/s3-request-presigner', () => ({
  getSignedUrl: vi.fn().mockResolvedValue('https://s3.example.com/mock-presigned-url'),
}))

import { db } from '@/lib/db'
const mockDb = vi.mocked(db)
```

From supabase/migrations/001_parts_schema.sql (Header-Pattern Lines 1-4):
```sql
-- supabase/migrations/<NNN>_<name>.sql
-- Phase X: <Name>
-- Erstellt: <Beschreibung>
-- Einspielen: Supabase Dashboard > SQL Editor > Dateiinhalt einfügen und ausführen
```

Test-Namen-Konvention (German + Requirement-ID):
- `'gibt HTTP 409 zurück wenn SHA-256 bereits in der Datenbank existiert (INGEST-04)'`
- `'gibt HTTP 200 mit part_id und presigned_url zurück bei gültigem Init-Request'`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 002_add_thumbnail_count.sql erstellen</name>
  <files>supabase/migrations/002_add_thumbnail_count.sql</files>
  <read_first>
    - supabase/migrations/001_parts_schema.sql (Header-Pattern + lowercase-DDL-Style + RLS-Kommentar-Stil)
    - .planning/phases/04-ingestion-ui/04-PATTERNS.md (Migration-Section, Lines 452-487 — exakte DDL-Vorlage)
    - .planning/phases/04-ingestion-ui/04-RESEARCH.md (D-05 Anforderung: `{status, thumbnail_count}`)
  </read_first>
  <action>
    Erstelle `supabase/migrations/002_add_thumbnail_count.sql` mit folgendem exaktem Inhalt (lowercase-DDL-Style aus Migration 001 spiegeln):

    ```sql
    -- supabase/migrations/002_add_thumbnail_count.sql
    -- Phase 4: Ingestion UI
    -- Fügt thumbnail_count-Spalte zur parts-Tabelle hinzu (D-05)
    -- Einspielen: Supabase Dashboard > SQL Editor > Dateiinhalt einfügen und ausführen
    --   ODER: supabase db push

    -- thumbnail_count: Anzahl der vom Worker erzeugten Thumbnails (0..8).
    -- Wird vom Status-API-Endpunkt GET /api/parts/[id]/status zurückgegeben.
    -- Wird in Phase 4 noch nicht in der UI gerendert (UI zeigt nur view_0.png),
    -- aber das API-Response-Schema ist bereits durch UI-SPEC und D-05 fixiert.
    -- Worker-Update der Spalte erfolgt in Phase 5 (Admin-Katalog) oder später.

    alter table parts
      add column if not exists thumbnail_count integer not null default 0;
    ```

    KEINE Index-Erstellung — `thumbnail_count` wird in Phase 4 nicht in WHERE/ORDER BY verwendet.
    `if not exists` ist defensiv (idempotent), um Fehler bei doppeltem Push zu vermeiden.
  </action>
  <verify>
    <automated>test -f supabase/migrations/002_add_thumbnail_count.sql &amp;&amp; grep -c "alter table parts" supabase/migrations/002_add_thumbnail_count.sql | grep -q "^1$" &amp;&amp; grep -c "thumbnail_count integer not null default 0" supabase/migrations/002_add_thumbnail_count.sql | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/002_add_thumbnail_count.sql` existiert
    - Datei enthält genau eine `alter table parts`-DDL-Anweisung
    - Spalte heißt exakt `thumbnail_count` mit Typ `integer not null default 0`
    - Header-Kommentar nennt "Phase 4" und "(D-05)"
    - Datei verwendet `if not exists` (idempotent)
    - Keine Index-Erstellung in der Datei (`grep -c 'create index' supabase/migrations/002_add_thumbnail_count.sql` ergibt `0`)
  </acceptance_criteria>
  <done>Migration-Datei existiert, idempotent, lowercase-DDL-konsistent mit Migration 001.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 [BLOCKING]: Migration auf Neon-DB anwenden (supabase db push)</name>
  <what-built>Die Migrations-Datei `supabase/migrations/002_add_thumbnail_count.sql` ist im Repository erstellt.</what-built>
  <how-to-verify>
    Diese Aktion muss der Nutzer durchführen, weil supabase CLI eine authentifizierte Sitzung zur Neon-DB erfordert (Credentials sind nicht im Claude-Kontext, und die Operation modifiziert die Production-Schema-Definition).

    **Schritte:**
    1. Sicherstellen, dass `supabase` CLI installiert und gegen das Projekt verlinkt ist:
       `supabase link --project-ref <project-ref>` (falls nicht bereits geschehen)
    2. Migration anwenden:
       `supabase db push`
    3. Verifizieren via psql oder Supabase Dashboard SQL Editor:
       ```sql
       SELECT column_name, data_type, column_default
       FROM information_schema.columns
       WHERE table_name = 'parts' AND column_name = 'thumbnail_count';
       ```
       Erwartete Ausgabe: 1 Zeile mit `column_name=thumbnail_count`, `data_type=integer`, `column_default=0`.

    4. Falls `supabase db push` fehlschlägt mit "Auth required":
       `supabase login` ausführen, dann erneut `supabase db push`.

    **Warum [BLOCKING]:** Ohne die Spalte gibt der Status-API-Endpunkt (`GET /api/parts/[id]/status`, Plan 02) bei `SELECT status, thumbnail_count FROM parts` einen Postgres-Fehler "column does not exist". Wave 1 darf erst starten, wenn diese Migration angewendet ist.
  </how-to-verify>
  <resume-signal>Nach erfolgreichem `supabase db push` mit "approved" antworten oder Fehlermeldung schildern.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Vier Vitest-Test-Stubs + 1 Playwright-Stub erstellen</name>
  <files>
    src/app/api/parts/[id]/status/route.test.ts,
    src/app/api/parts/[id]/thumbnail/route.test.ts,
    src/hooks/use-part-status.test.ts,
    src/app/upload/UploadForm.test.tsx,
    tests/phase-04-upload.spec.ts
  </files>
  <read_first>
    - src/app/api/upload/init/route.test.ts (Mock-Pattern für db, s3, getSignedUrl — exakte Struktur replizieren)
    - src/app/api/upload/confirm/route.test.ts (Pattern für UUID-Validierung-Tests + 404-Tests)
    - .planning/phases/04-ingestion-ui/04-RESEARCH.md (Section "Phase Requirements → Test Map" — exakte Test-Namen)
    - .planning/phases/04-ingestion-ui/04-VALIDATION.md (Wave-0-Anforderungen)
    - .planning/phases/04-ingestion-ui/04-PATTERNS.md (Test-Stub-Section)
  </read_first>
  <action>
    Erstelle FÜNF Test-Skelett-Dateien mit `it.skip(...)` (oder `test.skip`) für jeden im Folgenden gelisteten Test-Namen. JEDER Test muss ein lauffähiges Skelett haben — KEINE Implementierungslogik. Nutze die Mock-Patterns aus Phase 3 (`init/route.test.ts`).

    **Datei 1: `src/app/api/parts/[id]/status/route.test.ts`**

    Erstelle parent-directory falls nötig: `mkdir -p src/app/api/parts/[id]/status`

    Inhalt:
    ```typescript
    // src/app/api/parts/[id]/status/route.test.ts
    // Tests für GET /api/parts/[id]/status (D-05, INGEST-02)
    // STUBS — Logik wird in Plan 02 (Wave 1) implementiert.

    import { describe, it, expect, vi, beforeEach } from 'vitest'

    vi.mock('@/lib/db', () => ({
      db: vi.fn(),
    }))

    import { db } from '@/lib/db'
    const mockDb = vi.mocked(db)

    describe('GET /api/parts/[id]/status', () => {
      beforeEach(() => {
        vi.clearAllMocks()
      })

      const validUuid = '123e4567-e89b-12d3-a456-426614174000'

      it.skip('gibt HTTP 200 mit status und thumbnail_count zurück bei vorhandener UUID (INGEST-02)', async () => {
        // STUB — Plan 02
      })

      it.skip('gibt HTTP 404 zurück für unbekannte UUID', async () => {
        // STUB — Plan 02
      })

      it.skip('gibt HTTP 400 zurück für ungültige UUID-Form', async () => {
        // STUB — Plan 02
      })
    })
    ```

    **Datei 2: `src/app/api/parts/[id]/thumbnail/route.test.ts`**

    `mkdir -p src/app/api/parts/[id]/thumbnail`

    Inhalt:
    ```typescript
    // src/app/api/parts/[id]/thumbnail/route.test.ts
    // Tests für GET /api/parts/[id]/thumbnail (D-08, INGEST-02)
    // STUBS — Logik wird in Plan 03 (Wave 1) implementiert.

    import { describe, it, expect, vi, beforeEach } from 'vitest'

    vi.mock('@/lib/db', () => ({
      db: vi.fn(),
    }))
    vi.mock('@/lib/s3', () => ({
      s3: { send: vi.fn() },
      BUCKET_STEPS: 'mock-bucket-steps',
      BUCKET_THUMBNAILS: 'mock-bucket-thumbnails',
    }))
    vi.mock('@aws-sdk/s3-request-presigner', () => ({
      getSignedUrl: vi.fn().mockResolvedValue('https://s3.example.com/mock-thumb-url'),
    }))

    import { db } from '@/lib/db'
    const mockDb = vi.mocked(db)

    describe('GET /api/parts/[id]/thumbnail', () => {
      beforeEach(() => {
        vi.clearAllMocks()
      })

      const validUuid = '123e4567-e89b-12d3-a456-426614174000'

      it.skip('gibt HTTP 200 mit Presigned-URL zurück bei status=ready (INGEST-02)', async () => {
        // STUB — Plan 03
      })

      it.skip('gibt HTTP 404 zurück für unbekannte UUID', async () => {
        // STUB — Plan 03
      })

      it.skip('gibt HTTP 409 zurück wenn status !== ready', async () => {
        // STUB — Plan 03
      })

      it.skip('gibt HTTP 400 zurück für ungültige UUID-Form', async () => {
        // STUB — Plan 03
      })

      it.skip('gibt HTTP 404 zurück wenn Thumbnail-Objekt in S3 fehlt', async () => {
        // STUB — Plan 03
      })
    })
    ```

    **Datei 3: `src/hooks/use-part-status.test.ts`**

    Inhalt:
    ```typescript
    // src/hooks/use-part-status.test.ts
    // Tests für usePartStatus-Hook (D-04, D-06, INGEST-02)
    // STUBS — Logik wird in Plan 04 (Wave 2) implementiert.

    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'

    describe('usePartStatus', () => {
      beforeEach(() => {
        vi.useFakeTimers()
        global.fetch = vi.fn() as unknown as typeof fetch
      })
      afterEach(() => {
        vi.useRealTimers()
        vi.restoreAllMocks()
      })

      it.skip('polls every 2s in first 30s', async () => {
        // STUB — Plan 04
      })

      it.skip('switches to 5s after 30s', async () => {
        // STUB — Plan 04
      })

      it.skip('stops on ready', async () => {
        // STUB — Plan 04
      })

      it.skip('stops on failed', async () => {
        // STUB — Plan 04
      })

      it.skip('timeouts after 5 minutes', async () => {
        // STUB — Plan 04
      })

      it.skip('cleans up timers on unmount', async () => {
        // STUB — Plan 04
      })

      it.skip('does not poll when partId is null', async () => {
        // STUB — Plan 04
      })

      it.skip('surfaces error only after 3 consecutive failures', async () => {
        // STUB — Plan 04
      })
    })
    ```

    **Datei 4: `src/app/upload/UploadForm.test.tsx`**

    `mkdir -p src/app/upload`

    Inhalt:
    ```tsx
    // src/app/upload/UploadForm.test.tsx
    // Tests für UploadForm-Komponente (D-09, D-10, D-11, INGEST-01)
    // STUBS — Logik wird in Plan 05 (Wave 2) implementiert.

    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'

    describe('UploadForm', () => {
      beforeEach(() => {
        global.fetch = vi.fn() as unknown as typeof fetch
      })
      afterEach(() => {
        vi.restoreAllMocks()
      })

      it.skip('validates name required', async () => {
        // STUB — Plan 05
      })

      it.skip('validates file size', async () => {
        // STUB — Plan 05
      })

      it.skip('validates file extension', async () => {
        // STUB — Plan 05
      })

      it.skip('shows duplicate alert with existing_part_id on HTTP 409', async () => {
        // STUB — Plan 05
      })

      it.skip('disables fields after submit (D-09)', async () => {
        // STUB — Plan 05
      })

      it.skip('shows reset button only after status=ready (D-10)', async () => {
        // STUB — Plan 05
      })
    })
    ```

    **Datei 5: `tests/phase-04-upload.spec.ts`**

    Inhalt:
    ```typescript
    // tests/phase-04-upload.spec.ts
    // E2E-Test für Phase 4 Ingestion UI (INGEST-01, INGEST-02)
    // STUB — manueller Test bis Live-Worker via docker-compose verfügbar.

    import { test, expect } from '@playwright/test'

    test.describe('Phase 4: Ingestion UI', () => {
      test.skip('happy path: file picker → metadata → submit → status updates → thumbnail visible', async ({ page }) => {
        // STUB — manueller Test bis Live-Worker
      })

      test.skip('duplicate file shows inline alert with existing part id', async ({ page }) => {
        // STUB — manueller Test bis Live-Worker
      })
    })
    ```

    Nach Erstellung: `npm test -- --run src/app/api/parts src/hooks/use-part-status src/app/upload` ausführen und sicherstellen, dass alle Tests als `skipped` ausgewiesen werden, NICHT als `failed`.
  </action>
  <verify>
    <automated>test -f src/app/api/parts/[id]/status/route.test.ts &amp;&amp; test -f src/app/api/parts/[id]/thumbnail/route.test.ts &amp;&amp; test -f src/hooks/use-part-status.test.ts &amp;&amp; test -f src/app/upload/UploadForm.test.tsx &amp;&amp; test -f tests/phase-04-upload.spec.ts &amp;&amp; npm test -- --run src/app/api/parts src/hooks/use-part-status src/app/upload 2>&amp;1 | grep -E "(skipped|skip)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Alle 5 Stub-Dateien existieren mit den oben spezifizierten exakten Pfaden
    - Vitest-Output enthält "skipped" für mindestens 20 Tests (3+5+8+6 = 22 Stubs)
    - Vitest-Run beendet sich mit Exit-Code 0 (keine `failed`-Zähler)
    - `grep -c "it.skip\|test.skip" src/app/api/parts/\[id\]/status/route.test.ts src/app/api/parts/\[id\]/thumbnail/route.test.ts src/hooks/use-part-status.test.ts src/app/upload/UploadForm.test.tsx tests/phase-04-upload.spec.ts` ergibt für jede Datei mindestens 2
    - Alle Test-Namen exakt aus 04-RESEARCH.md "Phase Requirements → Test Map" übernommen (German für Route-Tests, English für Hook-Tests, gemäß Vorlage)
    - Mock-Setups für `@/lib/db` als `db: vi.fn()` (NICHT `sql: vi.fn()` — `db` ist der korrekte Export aus `src/lib/db.ts`)
  </acceptance_criteria>
  <done>Fünf Stub-Dateien existieren, npm test läuft grün mit allen Tests als "skipped".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → DB | Migration-Push transformiert Schema; nur Owner-Credentials (server-only). |
| Test-Files → Codebase | Test-Stubs importieren nichts aus Production-Code (nur Vitest + Mocks); keine Produktionspfade berührt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | supabase/migrations/002_add_thumbnail_count.sql | mitigate | `if not exists` macht Migration idempotent — kein Fehler bei doppeltem Push, kein Drop einer existierenden Spalte. |
| T-04-02 | Information Disclosure | Test-Stub-Mocks | accept | Mock-UUIDs sind public Test-Fixtures; keine Echtdaten. |
| T-04-03 | Denial of Service | Migration auf Production-DB | accept | `ALTER TABLE ADD COLUMN ... DEFAULT 0` ist in Postgres ≥ 11 metadata-only (keine Tabellen-Rewrite, keine Lock-Eskalation). Neon ist auf PG 15+. |
</threat_model>

<verification>
**Phase-übergreifende Checks nach diesem Plan:**

1. `test -f supabase/migrations/002_add_thumbnail_count.sql` → existiert
2. Verifikation auf der DB: `SELECT column_name FROM information_schema.columns WHERE table_name='parts' AND column_name='thumbnail_count'` → 1 Zeile
3. `npm test` läuft fehlerfrei (skipped-Stubs werden NICHT als Fehler gezählt)
4. Keine .env-Änderungen in diesem Plan (Wave 0 ist rein Schema + Test-Skelett)
</verification>

<success_criteria>
- Migration-Datei `002_add_thumbnail_count.sql` ist im Git und auf der Neon-DB angewendet
- 4 Vitest-Stub-Dateien + 1 Playwright-Stub-Datei existieren mit allen erwarteten Tests als `it.skip` / `test.skip`
- `npm test` Exit-Code 0 (keine fehlgeschlagenen Tests)
- Wave 1 kann starten (alle Vorbedingungen erfüllt)
</success_criteria>

<output>
After completion, create `.planning/phases/04-ingestion-ui/04-01-SUMMARY.md`
</output>
