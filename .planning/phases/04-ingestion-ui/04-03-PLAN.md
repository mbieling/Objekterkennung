---
phase: 04-ingestion-ui
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/app/api/parts/[id]/thumbnail/route.ts
  - src/app/api/parts/[id]/thumbnail/route.test.ts
autonomous: true
requirements:
  - INGEST-02
must_haves:
  truths:
    - "GET /api/parts/[id]/thumbnail gibt Presigned-URL für view_0.png zurück mit 60s Lifetime (D-08)"
    - "Endpunkt prüft status='ready' BEVOR signiert wird (HTTP 409 sonst)"
    - "Endpunkt prüft S3-Object-Existenz via HeadObjectCommand (HTTP 404 wenn fehlt) — race-condition-safe"
    - "Ungültige UUIDs werden mit HTTP 400 abgelehnt BEVOR DB/S3 berührt wird"
    - "Alle 5 Wave-0-Test-Stubs sind nun aktiv (kein it.skip mehr) und grün"
  artifacts:
    - path: "src/app/api/parts/[id]/thumbnail/route.ts"
      provides: "GET-Route-Handler für Thumbnail-Presigned-URL"
      exports: ["GET"]
    - path: "src/app/api/parts/[id]/thumbnail/route.test.ts"
      provides: "Vitest-Tests aktiviert (5 Tests grün)"
      contains: "describe('GET /api/parts/[id]/thumbnail'"
  key_links:
    - from: "src/app/api/parts/[id]/thumbnail/route.ts"
      to: "@/lib/s3 (s3, BUCKET_THUMBNAILS)"
      via: "getSignedUrl(s3, new GetObjectCommand({Bucket, Key}), {expiresIn: 60})"
      pattern: "GetObjectCommand.*view_0.png"
    - from: "Browser UploadForm (Plan 05)"
      to: "GET /api/parts/[id]/thumbnail"
      via: "fetch nach status==='ready'"
      pattern: "fetch.*parts.*thumbnail"
---

<objective>
Implementiere `GET /api/parts/[id]/thumbnail` (D-08) — der Endpunkt, der einem Browser einen 60-Sekunden-Presigned-S3-URL für die Frontansicht (`view_0.png`) liefert. Wird nach `status='ready'` aufgerufen, um die Vorschau im Status-Tracker (Plan 05) zu rendern.

**Purpose:** Ohne diesen Endpunkt kann das Browser-`<img>` keine Vorschau anzeigen — Bucket ist privat (D-08), kein direkter Browser-Zugriff erlaubt.

**Output:**
- 1 neue API-Route: `src/app/api/parts/[id]/thumbnail/route.ts`
- Aktiviere alle 5 Tests in `src/app/api/parts/[id]/thumbnail/route.test.ts` (it.skip → it)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ingestion-ui/04-CONTEXT.md
@.planning/phases/04-ingestion-ui/04-RESEARCH.md
@.planning/phases/04-ingestion-ui/04-PATTERNS.md
@src/app/api/upload/init/route.ts
@src/app/api/upload/init/route.test.ts
@src/lib/s3.ts
@src/lib/db.ts

<interfaces>
<!-- S3-Client und Presigning-Pattern aus existierender init-Route -->

From src/lib/s3.ts:
```typescript
export const s3 = new S3Client({...})
export const BUCKET_THUMBNAILS = process.env.AWS_S3_BUCKET_THUMBNAILS!
// Pfadkonvention: {part_id}/original.step, {part_id}/view_0.png … view_7.png
```

From src/app/api/upload/init/route.ts (lines 76-84 — Presigned URL Pattern):
```typescript
const presignedUrl = await getSignedUrl(
  s3,
  new PutObjectCommand({
    Bucket: BUCKET_STEPS,
    Key: `${part.id}/original.step`,
    ContentType: 'application/octet-stream',
  }),
  { expiresIn: 900 }
)
```

For thumbnail GET: swap to `GetObjectCommand`, `BUCKET_THUMBNAILS`, key `${id}/view_0.png`, `expiresIn: 60` (D-08), KEIN ContentType.

HeadObjectCommand-Pattern (RESEARCH.md Pitfall 5 — kein Codebase-Analog):
```typescript
import { GetObjectCommand, HeadObjectCommand } from '@aws-sdk/client-s3'
try {
  await s3.send(new HeadObjectCommand({ Bucket: BUCKET_THUMBNAILS, Key: key }))
} catch {
  return NextResponse.json({ error: 'Thumbnail object missing' }, { status: 404 })
}
```

**S3-Key-Konvention** (STATE.md locked decision): `{part_id}/view_0.png` (NICHT `view_{name}.png`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/parts/[id]/thumbnail implementieren + Tests aktivieren</name>
  <files>
    src/app/api/parts/[id]/thumbnail/route.ts,
    src/app/api/parts/[id]/thumbnail/route.test.ts
  </files>
  <read_first>
    - src/app/api/parts/[id]/thumbnail/route.test.ts (Wave-0-Stub mit Test-Namen — Tests aktivieren)
    - src/app/api/upload/init/route.ts (Pattern für getSignedUrl + AWS-SDK-Imports)
    - src/app/api/upload/init/route.test.ts (Mock-Setup für s3, getSignedUrl, db)
    - src/app/api/upload/confirm/route.ts (Pattern für UUID-Zod-Validierung + 404-Response)
    - src/lib/s3.ts (Bestätigung: `BUCKET_THUMBNAILS` ist der korrekte Export, S3-Key-Format aus Kommentar)
    - .planning/phases/04-ingestion-ui/04-PATTERNS.md (Section "GET /api/parts/[id]/thumbnail" — Code-Vorlage)
    - .planning/phases/04-ingestion-ui/04-RESEARCH.md (Pitfall 5 — Race-Condition + HeadObject)
    - .planning/phases/04-ingestion-ui/04-UI-SPEC.md (Section "GET /api/parts/[id]/thumbnail" — Response-Schema {url: string})
  </read_first>
  <behavior>
    - Test 1: gültige UUID + DB-Zeile mit status='ready' + S3-Object existiert → HTTP 200 mit `{url: '<presigned-url>'}` (60s Lifetime)
    - Test 2: gültige UUID + KEIN DB-Eintrag → HTTP 404 mit `{error: 'Part not found'}`, KEIN getSignedUrl-Aufruf
    - Test 3: gültige UUID + DB-Zeile mit status='processing' → HTTP 409 mit `{error: 'Thumbnail not ready'}`
    - Test 4: ungültige UUID-Form → HTTP 400 mit `{error: 'Invalid id', details: ...}`, KEIN DB-Aufruf, KEIN S3-Aufruf
    - Test 5: status='ready' aber S3-HeadObject wirft Error → HTTP 404 mit `{error: 'Thumbnail object missing'}`
  </behavior>
  <action>
    **Schritt A: Datei `src/app/api/parts/[id]/thumbnail/route.ts` erstellen** mit folgendem exakten Inhalt:

    ```typescript
    // src/app/api/parts/[id]/thumbnail/route.ts
    // GET /api/parts/[id]/thumbnail — D-08, INGEST-02
    // Liefert 60-Sekunden-Presigned-S3-URL für view_0.png (Frontansicht).
    // Server-only — KEIN "use client", keine Browser-Imports.

    import { NextResponse } from 'next/server'
    import { GetObjectCommand, HeadObjectCommand } from '@aws-sdk/client-s3'
    import { getSignedUrl } from '@aws-sdk/s3-request-presigner'
    import { z } from 'zod'
    import { db } from '@/lib/db'
    import { s3, BUCKET_THUMBNAILS } from '@/lib/s3'

    // Zod-Schema für params.id — Path-Traversal-Schutz vor S3-Key-Konstruktion (security.md V5)
    const ParamsSchema = z.object({
      id: z.string().uuid('id muss eine gültige UUID sein'),
    })

    export async function GET(
      _request: Request,
      { params }: { params: Promise<{ id: string }> }   // Next.js 16: params ist Promise
    ): Promise<NextResponse> {
      const { id } = await params

      // 1. UUID validieren BEVOR DB-Query oder S3-Key-Konstruktion (Threat T-04-04)
      const parsed = ParamsSchema.safeParse({ id })
      if (!parsed.success) {
        return NextResponse.json(
          { error: 'Invalid id', details: parsed.error.flatten() },
          { status: 400 }
        )
      }

      // 2. Part-Existenz und Status-Check
      const rows = await db`
        SELECT status FROM parts WHERE id = ${id} LIMIT 1
      `
      if (rows.length === 0) {
        return NextResponse.json({ error: 'Part not found' }, { status: 404 })
      }
      if (rows[0].status !== 'ready') {
        return NextResponse.json({ error: 'Thumbnail not ready' }, { status: 409 })
      }

      // 3. S3-Key gemäß Pfadkonvention (STATE.md: view_0..view_7.png)
      const key = `${id}/view_0.png`

      // 4. HeadObject prüft Existenz BEVOR signiert wird (Pitfall 5: race condition)
      try {
        await s3.send(new HeadObjectCommand({ Bucket: BUCKET_THUMBNAILS, Key: key }))
      } catch {
        return NextResponse.json({ error: 'Thumbnail object missing' }, { status: 404 })
      }

      // 5. Presigned GET-URL — 60s Lifetime per D-08
      const url = await getSignedUrl(
        s3,
        new GetObjectCommand({ Bucket: BUCKET_THUMBNAILS, Key: key }),
        { expiresIn: 60 }
      )

      return NextResponse.json({ url })
    }
    ```

    **Schritt B: Tests aktivieren** in `src/app/api/parts/[id]/thumbnail/route.test.ts`. Erweitere das Wave-0-Mock-Setup:

    ```typescript
    // ZUSÄTZLICH zu existierenden Mocks (db, s3.send, getSignedUrl) folgendes:
    // KEIN extra Mock für @aws-sdk/client-s3 nötig — wir verwenden GetObjectCommand/HeadObjectCommand
    // als echte Konstruktoren, da nur der Output an s3.send/getSignedUrl weitergegeben wird.

    import { GetObjectCommand } from '@aws-sdk/client-s3'
    import { getSignedUrl } from '@aws-sdk/s3-request-presigner'
    import { s3 } from '@/lib/s3'

    const mockS3Send = vi.mocked(
      (s3 as unknown as { send: (...a: unknown[]) => unknown }).send
    )
    const mockGetSignedUrl = vi.mocked(getSignedUrl)
    ```

    Test-Implementierungen (jedes `it.skip` durch `it` ersetzen, Body einsetzen):

    ```typescript
    it('gibt HTTP 200 mit Presigned-URL zurück bei status=ready (INGEST-02)', async () => {
      mockDb.mockResolvedValueOnce([{ status: 'ready' }])
      mockS3Send.mockResolvedValueOnce({})  // HeadObject success
      const { GET } = await import('./route')
      const response = await GET(
        new Request(`http://localhost/api/parts/${validUuid}/thumbnail`),
        { params: Promise.resolve({ id: validUuid }) }
      )
      const data = await response.json()
      expect(response.status).toBe(200)
      expect(data.url).toContain('https://')
      expect(mockGetSignedUrl).toHaveBeenCalledWith(
        expect.anything(),
        expect.any(GetObjectCommand),
        { expiresIn: 60 }
      )
    })

    it('gibt HTTP 404 zurück für unbekannte UUID', async () => {
      mockDb.mockResolvedValueOnce([])
      const { GET } = await import('./route')
      const response = await GET(
        new Request(`http://localhost/api/parts/${validUuid}/thumbnail`),
        { params: Promise.resolve({ id: validUuid }) }
      )
      const data = await response.json()
      expect(response.status).toBe(404)
      expect(data.error).toBe('Part not found')
      expect(mockS3Send).not.toHaveBeenCalled()
      expect(mockGetSignedUrl).not.toHaveBeenCalled()
    })

    it('gibt HTTP 409 zurück wenn status !== ready', async () => {
      mockDb.mockResolvedValueOnce([{ status: 'processing' }])
      const { GET } = await import('./route')
      const response = await GET(
        new Request(`http://localhost/api/parts/${validUuid}/thumbnail`),
        { params: Promise.resolve({ id: validUuid }) }
      )
      const data = await response.json()
      expect(response.status).toBe(409)
      expect(data.error).toBe('Thumbnail not ready')
      expect(mockS3Send).not.toHaveBeenCalled()
    })

    it('gibt HTTP 400 zurück für ungültige UUID-Form', async () => {
      const { GET } = await import('./route')
      const response = await GET(
        new Request('http://localhost/api/parts/keine-uuid/thumbnail'),
        { params: Promise.resolve({ id: 'keine-uuid' }) }
      )
      expect(response.status).toBe(400)
      expect(mockDb).not.toHaveBeenCalled()
      expect(mockS3Send).not.toHaveBeenCalled()
    })

    it('gibt HTTP 404 zurück wenn Thumbnail-Objekt in S3 fehlt', async () => {
      mockDb.mockResolvedValueOnce([{ status: 'ready' }])
      mockS3Send.mockRejectedValueOnce(new Error('NoSuchKey'))  // HeadObject fail
      const { GET } = await import('./route')
      const response = await GET(
        new Request(`http://localhost/api/parts/${validUuid}/thumbnail`),
        { params: Promise.resolve({ id: validUuid }) }
      )
      const data = await response.json()
      expect(response.status).toBe(404)
      expect(data.error).toBe('Thumbnail object missing')
      expect(mockGetSignedUrl).not.toHaveBeenCalled()
    })
    ```

    **Schritt C:** `npm test -- --run "src/app/api/parts/[id]/thumbnail/route.test.ts"` ausführen → alle 5 Tests grün.
  </action>
  <verify>
    <automated>npm test -- --run "src/app/api/parts/[id]/thumbnail/route.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/parts/[id]/thumbnail/route.ts` existiert und exportiert `GET`-Funktion: `grep -c "export async function GET" src/app/api/parts/\[id\]/thumbnail/route.ts` ergibt `1`
    - Datei verwendet `params: Promise<{ id: string }>` und `await params`: `grep -c "Promise<{ id: string }>" src/app/api/parts/\[id\]/thumbnail/route.ts` ergibt mindestens `1`
    - Datei verwendet `z.string().uuid(...)`: `grep -c "z.string().uuid" src/app/api/parts/\[id\]/thumbnail/route.ts` ergibt mindestens `1`
    - Datei verwendet `BUCKET_THUMBNAILS` aus `@/lib/s3`: `grep -c "BUCKET_THUMBNAILS" src/app/api/parts/\[id\]/thumbnail/route.ts` ergibt mindestens `2` (Import + Verwendung)
    - Datei verwendet `expiresIn: 60`: `grep -c "expiresIn: 60" src/app/api/parts/\[id\]/thumbnail/route.ts` ergibt `1`
    - S3-Key folgt `${id}/view_0.png`: `grep -c "view_0.png" src/app/api/parts/\[id\]/thumbnail/route.ts` ergibt mindestens `1`
    - Datei verwendet `HeadObjectCommand` für race-safe-Check: `grep -c "HeadObjectCommand" src/app/api/parts/\[id\]/thumbnail/route.ts` ergibt mindestens `2` (Import + Verwendung)
    - Test-Datei enthält KEIN `it.skip` mehr: `grep -c "it.skip" src/app/api/parts/\[id\]/thumbnail/route.test.ts` ergibt `0`
    - Vitest-Lauf für diese Datei: 5 passed, 0 failed, 0 skipped
    - Datei verwendet KEIN `process.env.NEXT_PUBLIC_*`: `grep -c "NEXT_PUBLIC_" src/app/api/parts/\[id\]/thumbnail/route.ts` ergibt `0`
  </acceptance_criteria>
  <done>GET-Thumbnail-Route implementiert; alle 5 Tests grün; HeadObject-Race-Mitigation aktiv.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | `params.id` ist untrusted user-input; ohne Validierung in S3-Key konstruiert würde Path-Traversal möglich. |
| API → S3 | Bucket ist privat — kein direkter Browser-Zugriff. Presigned-URLs sind die einzige autorisierte Lese-Methode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | params.id → S3-Key | mitigate | `z.string().uuid()` lehnt nicht-UUID-Strings VOR der S3-Key-Konstruktion ab → HTTP 400. Verhindert `../`-Path-Traversal in Buckets. (Per <quality_gate>.) |
| T-04-09 | Information Disclosure | Presigned URL leak | mitigate | 60s Lifetime (D-08). RESEARCH.md verlangt: kein localStorage-persist (Anti-Pattern). UI rendert Foto direkt nach erhalt aus React-State. |
| T-04-10 | Information Disclosure | Direct-S3-Enumeration | mitigate | `BUCKET_THUMBNAILS` ist privat (keine public-read-Policy laut Phase 1). Nur signierte URLs → keine Listing-Möglichkeit. |
| T-04-11 | Tampering | Race condition: status='ready' aber Object noch nicht hochgeladen | mitigate | `HeadObjectCommand` vor `getSignedUrl` validiert Object-Existenz → HTTP 404 statt 200 mit broken-link-URL. |
| T-04-12 | Repudiation | Auth-Bypass | accept | Pilot ohne Auth (REQUIREMENTS.md OOS). UUIDs sind 122-bit unenumerierbar — Sicherheit durch Obscurity-via-Entropie. Auth-Nachrüstung ist Phase 10. |
</threat_model>

<verification>
- `npm test -- --run "src/app/api/parts/[id]/thumbnail/route.test.ts"` grün (5/5 passed)
- Endpunkt liefert `{url: <https-string>}` bei ready, `{error: ...}` sonst
- Bucket bleibt privat (keine bucket-policy-Änderung in diesem Plan)
</verification>

<success_criteria>
- Thumbnail-Route liefert korrekte Responses für alle fünf Pfade (200/404/409/400/404-no-object)
- Wave-0-Test-Stubs sind aktiv und grün
- Plan 05 (UploadForm) kann diesen Endpunkt nach `status='ready'` aufrufen und das `<img>` rendern
- Race-Condition zwischen Worker-Status-Update und S3-Upload abgefangen via HeadObject
</success_criteria>

<output>
After completion, create `.planning/phases/04-ingestion-ui/04-03-SUMMARY.md`
</output>
