---
phase: 04-ingestion-ui
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - src/app/upload/UploadForm.tsx
  - src/app/upload/UploadForm.test.tsx
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
must_haves:
  truths:
    - "User kann eine STEP-Datei (max. 100 MB) auswählen, Metadaten ausfüllen und das Formular abschicken (INGEST-01)"
    - "Browser berechnet SHA-256 lokal vor dem Init-Request (Architektur-Constraint, kein 100 MB an Server vor Dedup-Check)"
    - "Datei wird via XHR-PUT direkt zu S3 mit Progress-Anzeige hochgeladen (kein Content-Type-Header)"
    - "Nach erfolgreichem Confirm startet usePartStatus-Hook → Status-Tracker zeigt pending → processing → ready (D-09)"
    - "Bei HTTP 409 (Duplikat) erscheint Inline-Alert mit existing_part_id; Form bleibt editierbar (D-11)"
    - "Bei status='ready' wird Thumbnail via GET /api/parts/[id]/thumbnail geladen und gerendert (D-07, D-08)"
    - "'Neuer Upload'-Button erscheint erst nach Thumbnail-Load (D-10)"
    - "Alle 6 Wave-0-UploadForm-Tests sind aktiv und grün"
  artifacts:
    - path: "src/app/upload/UploadForm.tsx"
      provides: "Client-Komponente mit Phasen-State-Machine, react-hook-form, XHR-Upload"
      exports: ["UploadForm"]
      min_lines: 200
    - path: "src/app/upload/UploadForm.test.tsx"
      provides: "Vitest-Tests aktiviert (6 Tests grün)"
      contains: "describe('UploadForm'"
  key_links:
    - from: "src/app/upload/UploadForm.tsx"
      to: "POST /api/upload/init"
      via: "fetch mit JSON-Body {name, sha256, original_filename, file_size_bytes, part_number?, project?}"
      pattern: "fetch.*api/upload/init"
    - from: "src/app/upload/UploadForm.tsx"
      to: "S3 (presigned URL)"
      via: "XMLHttpRequest PUT mit progress-Listener (KEIN Content-Type)"
      pattern: "XMLHttpRequest\\|xhr.upload.addEventListener.*progress"
    - from: "src/app/upload/UploadForm.tsx"
      to: "POST /api/upload/confirm"
      via: "fetch mit JSON-Body {part_id}"
      pattern: "fetch.*api/upload/confirm"
    - from: "src/app/upload/UploadForm.tsx"
      to: "usePartStatus-Hook"
      via: "Hook-Aufruf mit partId nach erfolgreichem confirm"
      pattern: "usePartStatus\\("
    - from: "src/app/upload/UploadForm.tsx"
      to: "GET /api/parts/[id]/thumbnail"
      via: "fetch nach status==='ready', renderet als <img src={url}>"
      pattern: "fetch.*parts.*thumbnail"
---

<objective>
Implementiere die zentrale Client-Komponente `UploadForm.tsx` (D-09, D-10, D-11). Sie kapselt den 5-stufigen Upload-Flow als endlichen Zustandsautomat und bindet alle Vorgänger-Pläne zusammen: Init → S3-PUT → Confirm → Polling → Thumbnail.

**Purpose:** Diese Komponente macht den gesamten Phase-3-Backend-Fluss für Ingenieure nutzbar. Sie ist die einzige Komponente, in der die UI-SPEC.md vollständig manifestiert wird (Layout, Copywriting, Status-Tracker, Validierung).

**Output:**
- 1 neue Komponente: `src/app/upload/UploadForm.tsx` (`"use client"`)
- Aktiviere alle 6 Tests in `src/app/upload/UploadForm.test.tsx`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ingestion-ui/04-CONTEXT.md
@.planning/phases/04-ingestion-ui/04-RESEARCH.md
@.planning/phases/04-ingestion-ui/04-PATTERNS.md
@.planning/phases/04-ingestion-ui/04-UI-SPEC.md
@src/app/api/upload/init/route.ts
@src/app/api/upload/confirm/route.ts
@src/app/api/parts/[id]/status/route.ts
@src/app/api/parts/[id]/thumbnail/route.ts
@src/hooks/use-part-status.ts
@src/components/ui/form.tsx
@src/components/ui/button.tsx
@src/components/ui/badge.tsx

<interfaces>
<!-- Server-API-Contracts (verbindlich — UploadForm muss exakt diese Bodies senden/empfangen) -->

POST /api/upload/init Request-Body (aus init/route.ts:InitSchema):
```typescript
{
  name: string         // 1-255 chars, required
  sha256: string       // 64 hex chars
  original_filename: string  // 1-255 chars
  file_size_bytes: number    // 1..104_857_600
  part_number?: string       // ≤100, optional
  project?: string           // ≤255, optional
}
```

POST /api/upload/init Responses:
- 200: `{part_id: uuid, presigned_url: string}` (success — fortfahren mit S3-PUT)
- 409: `{error: 'Duplicate file', existing_part_id: uuid}` (D-11: Inline-Alert)
- 400: Validation-Fehler

POST /api/upload/confirm Request-Body:
```typescript
{ part_id: uuid }
```
Responses: 202 (success), 404, 502

GET /api/parts/[id]/status Response (Plan 02):
```typescript
{ status: 'pending'|'processing'|'ready'|'failed', thumbnail_count: number }
```

GET /api/parts/[id]/thumbnail Response (Plan 03):
```typescript
{ url: string }  // 60s presigned URL
```

usePartStatus-Hook (Plan 04):
```typescript
function usePartStatus(partId: string | null): {
  status: PartStatus | null
  thumbnailCount: number
  error: Error | null
  timedOut: boolean
}
```

shadcn-Komponenten (alle bereits installiert in src/components/ui/):
- Form, FormField, FormItem, FormLabel, FormControl, FormMessage
- Input, Select (mit SelectTrigger/Content/Item), Button, Badge, Alert (mit AlertDescription)
- Progress, Skeleton, Card (mit CardHeader/Title/Content), Label

**KRITISCH (RESEARCH.md Pitfall 4):** XMLHttpRequest darf KEINEN Content-Type-Header beim PUT setzen. Der init-Endpoint signiert mit `ContentType: 'application/octet-stream'` aber NICHT in signableHeaders — Browser muss `xhr.setRequestHeader('Content-Type', ...)` weglassen.

**File-Validierung-Konstanten (UI-SPEC):**
- max size: `100 * 1024 * 1024` Bytes (104_857_600)
- accept: `.step,.stp` (Regex `/\.(step|stp)$/i`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UploadForm.tsx implementieren + Tests aktivieren</name>
  <files>
    src/app/upload/UploadForm.tsx,
    src/app/upload/UploadForm.test.tsx
  </files>
  <read_first>
    - src/app/upload/UploadForm.test.tsx (Wave-0-Stub mit Test-Namen)
    - .planning/phases/04-ingestion-ui/04-UI-SPEC.md (Layout Contract + Copywriting Contract — alle deutschen Strings exakt übernehmen)
    - .planning/phases/04-ingestion-ui/04-RESEARCH.md (Pattern 1 — State-Machine; Pattern 3 — XHR-Upload; Pattern 4 — SHA-256; Pattern 5 — File-Input; Pitfall 4 — Content-Type; Pitfall 7 — Doppel-Submit)
    - .planning/phases/04-ingestion-ui/04-PATTERNS.md (UploadForm.tsx Section — Imports + Submit-Handler + Reset-Handler)
    - src/app/api/upload/init/route.ts (InitSchema — Request-Body-Vertrag)
    - src/app/api/upload/confirm/route.ts (ConfirmSchema)
    - src/components/ui/form.tsx (Form/FormField/FormControl-Pattern)
    - src/components/ui/button.tsx (Button asChild + variants)
    - src/components/ui/badge.tsx (variant: default/secondary/destructive/outline)
    - src/hooks/use-part-status.ts (Hook-Signatur — `usePartStatus(partId | null)` returns `{status, thumbnailCount, error, timedOut}`)
  </read_first>
  <behavior>
    - Test 1 (`validates name required`): Submit ohne `name` (file gewählt, andere Felder leer) → Validation-Fehler "Bezeichnung ist erforderlich." sichtbar; KEIN fetch zu `/api/upload/init` passiert.
    - Test 2 (`validates file size`): User wählt eine Pseudo-Datei mit `size: 200 * 1024 * 1024` (200 MB) → Submit zeigt "Datei überschreitet die maximale Größe von 100 MB."; KEIN fetch zu `/api/upload/init`.
    - Test 3 (`validates file extension`): User wählt eine Datei `kram.jpg` → Submit zeigt "Nur STEP-Dateien (.step, .stp) werden akzeptiert."; KEIN fetch zu `/api/upload/init`.
    - Test 4 (`shows duplicate alert with existing_part_id on HTTP 409`): Mock `/api/upload/init` → 409 mit `{existing_part_id: 'abc-123'}` → Alert sichtbar mit Text enthält "Diese Datei existiert bereits" und "abc-123"; Form-Felder bleiben enabled.
    - Test 5 (`disables fields after submit (D-09)`): Mock init → 200, Mock S3 PUT → success, Mock confirm → 202 → Erwarte: alle Form-Inputs werden disabled (entweder via attribute oder via state).
    - Test 6 (`shows reset button only after status=ready (D-10)`): Verifiziert NUR den idle-Negativ-Fall — initial ist KEIN Reset-Button im DOM. Der positive Fall (Reset-Button erscheint nach status='ready' und Form ist nach Klick zurückgesetzt) wird im Human-Verify-Checkpoint von Plan 06 manuell bestätigt; eine zuverlässige Hook+Network-Mock-Sequenz wäre fragil und würde den Phasenautomaten dupliziert reproduzieren.
  </behavior>
  <action>
    **WICHTIG (OQ2 RESOLVED — Scope-Klarstellung):** Das `status`-Feld wird in Phase 4 NICHT in das Formular aufgenommen — weder im Zod-Schema noch als Select im JSX. Begründung: Der Phase-3-Init-Endpoint akzeptiert keinen `status`-Parameter (hardcoded 'pending'). Ein nicht-funktionales UI-Feld widerspricht der Planner-Regel gegen Scope-Reduction-by-Stub. Das Feld wird in Phase 5 (Admin-Katalog, ADMIN-Scope) hinzugefügt, sobald es editierbar und persistierbar ist. Siehe `04-RESEARCH.md` → Open Questions (RESOLVED) → OQ2.

    **Schritt A: Datei `src/app/upload/UploadForm.tsx` erstellen.** Implementiere die Komponente nach folgenden Regeln. Verwende RESEARCH.md Pattern 1 als Skelett, UI-SPEC für visuelle Details.

    Pflicht-Abschnitte:

    **1. Header + Imports** (genau so):
    ```tsx
    // src/app/upload/UploadForm.tsx
    // Phase 4 — Upload-Form als Client Component (D-09, D-10, D-11, INGEST-01, INGEST-02).
    'use client'

    import { useState, useRef, useEffect } from 'react'
    import { useForm } from 'react-hook-form'
    import { zodResolver } from '@hookform/resolvers/zod'
    import { z } from 'zod'
    import { Form, FormField, FormItem, FormLabel, FormControl, FormMessage } from '@/components/ui/form'
    import { Input } from '@/components/ui/input'
    import { Button } from '@/components/ui/button'
    import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card'
    import { Progress } from '@/components/ui/progress'
    import { Badge } from '@/components/ui/badge'
    import { Alert, AlertDescription } from '@/components/ui/alert'
    import { Skeleton } from '@/components/ui/skeleton'
    import { Label } from '@/components/ui/label'
    import { AlertCircle, Loader2, Upload, CheckCircle2, XCircle, RefreshCw } from 'lucide-react'
    import { usePartStatus } from '@/hooks/use-part-status'
    ```

    **2. Type + Konstanten:**
    ```tsx
    type UploadPhase =
      | 'idle'           // Form leer, Submit enabled
      | 'hashing'        // SHA-256 läuft
      | 'initializing'   // POST /api/upload/init läuft
      | 'uploading'      // PUT zu S3 läuft (mit progress 0-100)
      | 'confirming'     // POST /api/upload/confirm läuft
      | 'polling'        // usePartStatus aktiv
      | 'ready'          // status='ready', Thumbnail laden
      | 'failed'         // status='failed' oder Polling-Timeout
      | 'duplicate'      // 409 von init — Form bleibt editierbar
      | 'error'          // Netzwerk-/Upload-Fehler

    const MAX_FILE_BYTES = 100 * 1024 * 1024        // 100 MB
    const STEP_EXT_RE = /\.(step|stp)$/i
    ```

    **3. Zod-Schema (genau aus UI-SPEC):**
    ```tsx
    const formSchema = z.object({
      name: z.string().min(1, 'Bezeichnung ist erforderlich.').max(200),
      partNumber: z.string().max(100).optional(),
      project: z.string().max(200).optional(),
      // KEIN status-Feld in Phase 4 — Init-Endpoint hardcoded 'pending'.
      // Status-Editierung kommt in Phase 5 (Admin-Katalog, ADMIN-Scope).
    })
    type FormValues = z.infer<typeof formSchema>
    ```

    **4. Helper-Funktionen** (innerhalb derselben Datei oder als private Module-Exports):
    ```tsx
    async function sha256OfFile(file: File): Promise<string> {
      const buffer = await file.arrayBuffer()
      const hashBuffer = await crypto.subtle.digest('SHA-256', buffer)
      return Array.from(new Uint8Array(hashBuffer))
        .map(b => b.toString(16).padStart(2, '0')).join('')
    }

    function uploadToS3(presignedUrl: string, file: File, onProgress: (pct: number) => void): Promise<void> {
      return new Promise((resolve, reject) => {
        const xhr = new XMLHttpRequest()
        xhr.open('PUT', presignedUrl, true)
        // KRITISCH: KEIN setRequestHeader('Content-Type', ...) — Pitfall 4 (zerstört Signatur)
        xhr.upload.addEventListener('progress', e => {
          if (e.lengthComputable) onProgress((e.loaded / e.total) * 100)
        })
        xhr.addEventListener('load', () => {
          if (xhr.status >= 200 && xhr.status < 300) resolve()
          else reject(new Error(`S3 PUT failed: HTTP ${xhr.status}`))
        })
        xhr.addEventListener('error', () => reject(new Error('Network error during S3 upload')))
        xhr.addEventListener('abort', () => reject(new Error('Upload aborted')))
        xhr.send(file)
      })
    }
    ```

    **5. State + Form-Init:**
    ```tsx
    export function UploadForm() {
      const [phase, setPhase] = useState<UploadPhase>('idle')
      const [partId, setPartId] = useState<string | null>(null)
      const [uploadPercent, setUploadPercent] = useState(0)
      const [duplicateId, setDuplicateId] = useState<string | null>(null)
      const [errorMsg, setErrorMsg] = useState<string | null>(null)
      const [thumbnailUrl, setThumbnailUrl] = useState<string | null>(null)
      const [fileError, setFileError] = useState<string | null>(null)
      const fileInputRef = useRef<HTMLInputElement>(null)

      const form = useForm<FormValues>({
        resolver: zodResolver(formSchema),
        defaultValues: { name: '', partNumber: '', project: '' },
      })

      // Hook deaktiviert sich bei partId=null automatisch
      const polledPartId = (phase === 'polling' || phase === 'ready' || phase === 'failed') ? partId : null
      const { status: polledStatus, error: pollError, timedOut } = usePartStatus(polledPartId)

      // Status-Reaktion
      useEffect(() => {
        if (polledStatus === 'ready' && phase === 'polling') {
          setPhase('ready')
          fetch(`/api/parts/${partId}/thumbnail`)
            .then(r => r.ok ? r.json() : Promise.reject(new Error(`thumb HTTP ${r.status}`)))
            .then(({ url }: { url: string }) => setThumbnailUrl(url))
            .catch(() => { /* UI-SPEC: Skeleton bleibt sichtbar, kein Fallback in Phase 4 */ })
        }
        if (polledStatus === 'failed' && phase === 'polling') setPhase('failed')
        if (timedOut && phase === 'polling') setPhase('failed')
      }, [polledStatus, phase, partId, timedOut])
      // ...
    }
    ```

    **6. Submit-Handler** (RESEARCH.md "Submit-Handler im UploadForm" — exakt diese Sequenz):
    Reihenfolge: file-validation → hashing → init → uploading (S3) → confirming → polling. Bei 409 → phase='duplicate', existing_part_id zeigen, Form NICHT disablen.

    **7. Reset-Handler** (RESEARCH.md "Reset-Handler für Neuer Upload"):
    `form.reset(...)` + `fileInputRef.current.value = ''` + alle States auf default + `setPartId(null)` (triggert Hook-Cleanup).

    **8. JSX-Struktur** (gemäß UI-SPEC Layout Contract — 2-Spalten Grid):
    ```tsx
    return (
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {/* LEFT: Form Card */}
        <Card>
          <CardHeader><CardTitle className="text-lg">Bauteil-Daten</CardTitle></CardHeader>
          <CardContent>
            {/* File Input (uncontrolled, außerhalb Form) */}
            <Label htmlFor="step-file">STEP-Datei</Label>
            <input
              id="step-file"
              type="file"
              ref={fileInputRef}
              accept=".step,.stp"
              disabled={phase !== 'idle' && phase !== 'duplicate'}
              className="..."
            />
            <p className="text-xs text-muted-foreground">Maximale Dateigröße: 100 MB</p>
            {fileError && <p className="text-sm text-destructive">{fileError}</p>}

            {/* Duplikat-Alert (D-11) */}
            {duplicateId && (
              <Alert variant="destructive">
                <AlertCircle className="h-4 w-4" />
                <AlertDescription>
                  Diese Datei existiert bereits — Teil-ID: {duplicateId}
                </AlertDescription>
              </Alert>
            )}

            <Form {...form}>
              <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
                <FormField control={form.control} name="name" render={({field}) => (
                  <FormItem>
                    <FormLabel>Bezeichnung</FormLabel>
                    <FormControl>
                      <Input placeholder="z. B. Flanschplatte 50mm" disabled={phase !== 'idle' && phase !== 'duplicate'} {...field} />
                    </FormControl>
                    <FormMessage />
                  </FormItem>
                )} />
                {/* Analog für partNumber, project (Input). KEIN status-Select in Phase 4 — siehe OQ2 in 04-RESEARCH.md (RESOLVED). */}

                <Button type="submit" disabled={phase !== 'idle' && phase !== 'duplicate'}>
                  {phase !== 'idle' && phase !== 'duplicate' ? <><Loader2 className="animate-spin" /> Wird hochgeladen…</> : <><Upload /> Hochladen</>}
                </Button>
              </form>
            </Form>
          </CardContent>
        </Card>

        {/* RIGHT: Status Tracker (D-03 — initial null, erscheint nach Submit) */}
        {(phase !== 'idle' && phase !== 'duplicate') && (
          <Card>
            <CardHeader><CardTitle className="text-lg">Verarbeitungsstatus</CardTitle></CardHeader>
            <CardContent>
              {/* Progress-Bar während uploading */}
              {phase === 'uploading' && <Progress value={uploadPercent} />}

              {/* Status-Badge — Mapping aus UI-SPEC */}
              {polledStatus && (
                <Badge variant={
                  polledStatus === 'failed' ? 'destructive'
                  : polledStatus === 'ready' ? 'outline'
                  : 'secondary'
                }>
                  {polledStatus === 'pending' && 'Ausstehend'}
                  {polledStatus === 'processing' && 'Wird verarbeitet…'}
                  {polledStatus === 'ready' && 'Bereit'}
                  {polledStatus === 'failed' && 'Fehlgeschlagen'}
                </Badge>
              )}

              {/* Status-Description */}
              {polledStatus === 'pending' && <p>Warte auf Worker…</p>}
              {polledStatus === 'processing' && <p>3D-Ansichten werden berechnet. Dies kann 30–120 Sekunden dauern.</p>}
              {polledStatus === 'ready' && <p>Verarbeitung abgeschlossen. Vorschau wird geladen…</p>}
              {polledStatus === 'failed' && <p>Die Verarbeitung ist fehlgeschlagen. Bitte prüfe die STEP-Datei und versuche es erneut.</p>}

              {/* Thumbnail */}
              {phase === 'ready' && (
                thumbnailUrl
                  ? <img src={thumbnailUrl} alt="Frontansicht" className="w-48 h-48 object-contain rounded-md border" />
                  : <Skeleton className="w-48 h-48" />
              )}

              {/* Timeout-Warnung */}
              {timedOut && <Alert><AlertDescription>Die Verarbeitung dauert länger als erwartet. Seite neu laden, um den Status zu prüfen.</AlertDescription></Alert>}

              {/* Network-Error */}
              {errorMsg && <Alert variant="destructive"><AlertCircle className="h-4 w-4" /><AlertDescription>{errorMsg}</AlertDescription></Alert>}

              {/* Reset-Button — D-10: NUR nach Thumbnail geladen */}
              {phase === 'ready' && thumbnailUrl && (
                <Button onClick={handleReset}><RefreshCw /> Neuer Upload</Button>
              )}
            </CardContent>
          </Card>
        )}
      </div>
    )
    ```

    Sämtliche deutschen Strings 1:1 aus UI-SPEC.md "Copywriting Contract" übernehmen. Nicht improvisieren.

    **Schritt B: Tests aktivieren** in `src/app/upload/UploadForm.test.tsx`. Für Test 4-6 mocke `global.fetch` und mocke `XMLHttpRequest` per `vi.stubGlobal('XMLHttpRequest', class { ... })` mit synchronem `load`-Event-Trigger. Mocke `crypto.subtle.digest` mit einem festen ArrayBuffer.

    Beispiel für Test 4 (Duplikat):
    ```tsx
    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
    import { render, screen, fireEvent, waitFor } from '@testing-library/react'
    import { UploadForm } from './UploadForm'

    function makeFile(name: string, sizeBytes: number, content = 'x'): File {
      const blob = new Blob([content.repeat(Math.max(1, Math.min(sizeBytes, 100)))], { type: 'application/octet-stream' })
      Object.defineProperty(blob, 'size', { value: sizeBytes })
      return new File([blob], name, { type: 'application/octet-stream' })
    }

    describe('UploadForm', () => {
      beforeEach(() => {
        global.fetch = vi.fn() as unknown as typeof fetch
        // crypto.subtle.digest mocken — gibt deterministischen ArrayBuffer zurück
        vi.spyOn(crypto.subtle, 'digest').mockResolvedValue(new ArrayBuffer(32))
      })
      afterEach(() => {
        vi.restoreAllMocks()
      })

      it('validates name required', async () => {
        render(<UploadForm />)
        const fileInput = screen.getByLabelText(/STEP-Datei/i) as HTMLInputElement
        Object.defineProperty(fileInput, 'files', { value: [makeFile('a.step', 1024)], writable: false })
        fireEvent.change(fileInput)
        fireEvent.click(screen.getByRole('button', { name: /Hochladen/i }))
        await waitFor(() => expect(screen.getByText('Bezeichnung ist erforderlich.')).toBeTruthy())
        expect(global.fetch).not.toHaveBeenCalled()
      })

      it('validates file size', async () => {
        render(<UploadForm />)
        const fileInput = screen.getByLabelText(/STEP-Datei/i) as HTMLInputElement
        Object.defineProperty(fileInput, 'files', { value: [makeFile('a.step', 200 * 1024 * 1024)], writable: false })
        fireEvent.change(fileInput)
        fireEvent.input(screen.getByLabelText(/Bezeichnung/i), { target: { value: 'A' } })
        fireEvent.click(screen.getByRole('button', { name: /Hochladen/i }))
        await waitFor(() => expect(screen.getByText(/überschreitet die maximale Größe von 100 MB/)).toBeTruthy())
        expect(global.fetch).not.toHaveBeenCalled()
      })

      it('validates file extension', async () => {
        render(<UploadForm />)
        const fileInput = screen.getByLabelText(/STEP-Datei/i) as HTMLInputElement
        Object.defineProperty(fileInput, 'files', { value: [makeFile('a.jpg', 1024)], writable: false })
        fireEvent.change(fileInput)
        fireEvent.input(screen.getByLabelText(/Bezeichnung/i), { target: { value: 'A' } })
        fireEvent.click(screen.getByRole('button', { name: /Hochladen/i }))
        await waitFor(() => expect(screen.getByText(/Nur STEP-Dateien/)).toBeTruthy())
        expect(global.fetch).not.toHaveBeenCalled()
      })

      it('shows duplicate alert with existing_part_id on HTTP 409', async () => {
        ;(global.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
          ok: false,
          status: 409,
          json: async () => ({ error: 'Duplicate file', existing_part_id: 'abc-123' }),
        } as Response)
        render(<UploadForm />)
        const fileInput = screen.getByLabelText(/STEP-Datei/i) as HTMLInputElement
        Object.defineProperty(fileInput, 'files', { value: [makeFile('a.step', 1024)], writable: false })
        fireEvent.change(fileInput)
        fireEvent.input(screen.getByLabelText(/Bezeichnung/i), { target: { value: 'A' } })
        fireEvent.click(screen.getByRole('button', { name: /Hochladen/i }))
        await waitFor(() => expect(screen.getByText(/Diese Datei existiert bereits/)).toBeTruthy())
        expect(screen.getByText(/abc-123/)).toBeTruthy()
        // Form bleibt editierbar
        expect((screen.getByLabelText(/Bezeichnung/i) as HTMLInputElement).disabled).toBe(false)
      })

      it('disables fields after submit (D-09)', async () => {
        // Mock init → 200, dann XHR-S3-Upload simuliert success, dann confirm → 202
        ;(global.fetch as ReturnType<typeof vi.fn>)
          .mockResolvedValueOnce({ ok: true, status: 200, json: async () => ({ part_id: 'pid-1', presigned_url: 'https://s3.example.com/x' }) } as Response)
          .mockResolvedValueOnce({ ok: true, status: 202, json: async () => ({ part_id: 'pid-1', status: 'pending' }) } as Response)
        // Mock XMLHttpRequest
        class MockXHR {
          upload = { addEventListener: vi.fn() }
          status = 200
          private listeners: Record<string, () => void> = {}
          open() {}
          setRequestHeader() {}
          addEventListener(event: string, cb: () => void) { this.listeners[event] = cb }
          send() { setTimeout(() => this.listeners['load']?.(), 0) }
        }
        vi.stubGlobal('XMLHttpRequest', MockXHR)

        render(<UploadForm />)
        const fileInput = screen.getByLabelText(/STEP-Datei/i) as HTMLInputElement
        Object.defineProperty(fileInput, 'files', { value: [makeFile('a.step', 1024)], writable: false })
        fireEvent.change(fileInput)
        fireEvent.input(screen.getByLabelText(/Bezeichnung/i), { target: { value: 'A' } })
        fireEvent.click(screen.getByRole('button', { name: /Hochladen/i }))
        await waitFor(() => expect((screen.getByLabelText(/Bezeichnung/i) as HTMLInputElement).disabled).toBe(true))
      })

      it('shows reset button only after status=ready (D-10)', async () => {
        // Test verifiziert nur den initial+ready-Zustand. Vollständiger Flow ist in E2E-Stub.
        render(<UploadForm />)
        // Initial: kein Reset-Button
        expect(screen.queryByRole('button', { name: /Neuer Upload/i })).toBeNull()
      })
    })
    ```

    **Hinweis zu Test 6:** Die vollständige Flow-Simulation bis `ready` würde Hook-Mocking erfordern und ist fragil. Halte den Test minimal: prüfe nur, dass im idle-State kein Reset-Button vorhanden ist. Vollständige End-to-End-Verifikation erfolgt im Playwright-Stub (Plan 01) und im Human-Checkpoint nach Plan 06.

    **Schritt C:** `npm test -- --run src/app/upload/UploadForm.test.tsx` → 6/6 passed.
  </action>
  <verify>
    <automated>npm test -- --run src/app/upload/UploadForm.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/upload/UploadForm.tsx` existiert mit `'use client'` Directive: `grep -c "'use client'" src/app/upload/UploadForm.tsx` ergibt mindestens `1`
    - Datei exportiert `UploadForm`: `grep -c "export function UploadForm" src/app/upload/UploadForm.tsx` ergibt `1`
    - Datei verwendet `crypto.subtle.digest('SHA-256'`: `grep -c "crypto.subtle.digest" src/app/upload/UploadForm.tsx` ergibt mindestens `1`
    - Datei verwendet `XMLHttpRequest`: `grep -c "XMLHttpRequest" src/app/upload/UploadForm.tsx` ergibt mindestens `1`
    - Datei setzt KEINEN Content-Type-Header beim PUT: `grep -c "setRequestHeader.*Content-Type" src/app/upload/UploadForm.tsx` ergibt `0`
    - Datei verwendet `xhr.upload.addEventListener('progress'`: `grep -c "xhr.upload.addEventListener" src/app/upload/UploadForm.tsx` ergibt mindestens `1`
    - Datei importiert `usePartStatus`: `grep -c "from '@/hooks/use-part-status'" src/app/upload/UploadForm.tsx` ergibt `1`
    - Datei macht POST auf `/api/upload/init`, `/api/upload/confirm` und GET auf `/api/parts/.*/thumbnail`: `grep -c "/api/upload/init\|/api/upload/confirm\|/api/parts/" src/app/upload/UploadForm.tsx` ergibt mindestens `3`
    - Datei verwendet `MAX_FILE_BYTES = 100 * 1024 * 1024`: `grep -c "MAX_FILE_BYTES = 100 \* 1024 \* 1024" src/app/upload/UploadForm.tsx` ergibt `1`
    - Datei verwendet shadcn-Imports (KEINE Re-Implementierung): `grep -c "from '@/components/ui/" src/app/upload/UploadForm.tsx` ergibt mindestens `8`
    - Datei verwendet KEIN inline-style `style={{`: `grep -c 'style={{' src/app/upload/UploadForm.tsx` ergibt `0` (Tailwind-only)
    - Test-Datei enthält KEIN `it.skip` mehr: `grep -c "it.skip" src/app/upload/UploadForm.test.tsx` ergibt `0`
    - Vitest-Lauf: 6 passed, 0 failed, 0 skipped
    - status-Select-Feld ist NICHT mehr im UploadForm.tsx vorhanden (OQ2 RESOLVED): `grep -E "status.*Select|SelectTrigger.*status|status.*z\.enum" src/app/upload/UploadForm.tsx | wc -l` ergibt `0`
    - Imports enthalten KEIN `Select` aus shadcn: `grep -c "from '@/components/ui/select'" src/app/upload/UploadForm.tsx` ergibt `0`
    - Zod-Schema enthält KEIN status-Feld: `grep -c "status:.*z\.enum" src/app/upload/UploadForm.tsx` ergibt `0`
    - Datei verwendet KEIN `process.env.NEXT_PUBLIC_*` (Client darf keine Secrets sehen): `grep -c "NEXT_PUBLIC_" src/app/upload/UploadForm.tsx` ergibt `0`
  </acceptance_criteria>
  <done>UploadForm.tsx implementiert mit allen 5 Phasen (init → upload → confirm → polling → ready); 6 Tests grün; alle deutschen Strings aus UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Browser | Datei + Metadaten-Eingabe; vollständig untrusted. |
| Browser → API | Init-Request muss Server-Schema (init/route.ts) entsprechen — Server validiert nochmal. |
| Browser → S3 | Direct-PUT mit Presigned-URL; URL ist 15-Min-Lifetime, nur 1 STEP-File-Path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Tampering | Datei-Validierung im Client | mitigate | Client-Validierung (size, extension) ist UX-only. Server-Schema in init/route.ts ist die einzige Wahrheit (file_size_bytes ≤ 100 MB, name min 1). |
| T-04-18 | Tampering | XHR-PUT Content-Type | mitigate | KEIN `setRequestHeader('Content-Type', ...)` — würde Presigned-URL-Signatur invalidieren (Pitfall 4). Code-Review per grep-Gate sicherstellen. |
| T-04-19 | Information Disclosure | Form-Inputs in Browser-Konsole | accept | Standard-Browser-Verhalten; Werte sind nicht-sensible Metadaten. |
| T-04-20 | Denial of Service | Doppel-Submit per Doppelklick | mitigate | `phase !== 'idle'` blockiert Submit-Button (Pitfall 7). `react-hook-form.formState.isSubmitting` als zusätzliche Schutzschicht. |
| T-04-21 | Tampering | XSS via existing_part_id im Alert | mitigate | React-JSX escaped automatisch — `{duplicateId}` wird als Text gerendert. UUIDs sind nur a-f0-9-, keine HTML. |
| T-04-22 | Information Disclosure | Memory-Footprint bei 100 MB SHA-256 | accept | `file.arrayBuffer()` lädt komplette Datei in Memory; akzeptabel auf Desktop (RESEARCH.md A3). Mobile/iOS riskant — ist aber Phase 7+ Scope (Mobile-Search). |
</threat_model>

<verification>
- `npm test -- --run src/app/upload/UploadForm.test.tsx` grün (6/6 passed)
- `grep -c "from '@/components/ui/" src/app/upload/UploadForm.tsx` ≥ 8 (shadcn-First-Pflicht)
- TypeScript-Build (Plan 06 mit page.tsx-Wrapper) muss fehlerfrei sein
</verification>

<success_criteria>
- UploadForm.tsx implementiert vollständigen Phasen-Automaten (idle → hashing → init → upload → confirm → polling → ready/failed/duplicate)
- Alle deutschen Strings exakt aus UI-SPEC.md übernommen
- shadcn-Komponenten exklusiv verwendet (keine custom Buttons/Inputs/etc)
- 6 Vitest-Tests grün
- Plan 06 (page.tsx + Homepage-Update) kann diese Komponente einbinden
</success_criteria>

<output>
After completion, create `.planning/phases/04-ingestion-ui/04-05-SUMMARY.md`
</output>
