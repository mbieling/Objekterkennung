---
phase: 07-camera-ui
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/app/search/CameraCapture.test.tsx
  - tests/phase-07-camera-ui.spec.ts
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-02

must_haves:
  truths:
    - "Vitest-Testdatei für CameraCapture existiert mit konkreten it.todo-Stubs für alle SEARCH-01/02-Anforderungen"
    - "Playwright-E2E-Stub existiert mit test.skip für alle Phase-7-Szenarien"
    - "getUserMedia-Mock-Helper ist in CameraCapture.test.tsx definiert"
    - "Beide Test-Kommandos laufen durch ohne Fehler (Stubs überspringen ohne Fehler)"
  artifacts:
    - path: "src/app/search/CameraCapture.test.tsx"
      provides: "Vitest Unit Test Stubs — 9 it.todo für SEARCH-01, SEARCH-02, D-05 bis D-11"
      min_lines: 80
    - path: "tests/phase-07-camera-ui.spec.ts"
      provides: "Playwright E2E Stubs — test.skip für Navigation, File-Input, Homepage-Buttons"
      min_lines: 30
  key_links:
    - from: "src/app/search/CameraCapture.test.tsx"
      to: "src/app/search/CameraCapture.tsx"
      via: "import { CameraCapture } from './CameraCapture'"
      pattern: "from.*CameraCapture"
---

<objective>
Testgerüst für Phase 7 aufbauen — Vitest-Stubs und Playwright-Stubs erstellen, bevor eine einzige Produktionszeile geschrieben wird.

Purpose: Nyquist-Compliance sicherstellen. Wave 1 (CameraCapture.tsx) kann sofort nach Implementierung gegen fertige Tests validieren. getUserMedia-Mock-Infrastruktur einmalig definieren, in allen folgenden Waves wiederverwendbar.

Output: CameraCapture.test.tsx (9 Stubs) + phase-07-camera-ui.spec.ts (test.skip-Guards)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-camera-ui/07-CONTEXT.md
@.planning/phases/07-camera-ui/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest Unit Test Stubs — CameraCapture.test.tsx</name>
  <files>src/app/search/CameraCapture.test.tsx</files>

  <read_first>
    - tests/phase-04-upload.spec.ts — test.skip-Muster im Projekt (Playwright)
    - src/app/upload/UploadForm.tsx — zeigt 'use client'-Muster + State-Machine-Typ (zum Verständnis des zu testenden Moduls)
    - .planning/phases/07-camera-ui/07-VALIDATION.md — vollständige Test-ID-Tabelle mit allen 9 Testfällen
  </read_first>

  <action>
Datei `src/app/search/CameraCapture.test.tsx` NEU erstellen. Das Verzeichnis `src/app/search/` muss zuerst angelegt werden (mkdir -p). Die Datei enthält:

1. Imports:
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import { CameraCapture } from './CameraCapture'
```

2. getUserMedia-Mock-Helper als Inline-Hilfsfunktion (wird in beforeEach gesetzt):
```typescript
function mockGetUserMedia(success: boolean) {
  const mockStream = {
    getTracks: () => [{ stop: vi.fn() }],
  } as unknown as MediaStream

  Object.defineProperty(global.navigator, 'mediaDevices', {
    value: {
      getUserMedia: success
        ? vi.fn().mockResolvedValue(mockStream)
        : vi.fn().mockRejectedValue(new DOMException('Permission denied', 'NotAllowedError')),
    },
    writable: true,
    configurable: true,
  })
}
```

3. canvas.toBlob Mock (für Capture-Tests):
```typescript
HTMLCanvasElement.prototype.toBlob = vi.fn((callback) => {
  callback(new Blob(['fake-image'], { type: 'image/jpeg' }))
})
```

4. global.fetch Mock:
```typescript
global.fetch = vi.fn()
```

5. describe-Block `Phase 7: CameraCapture` mit den folgenden 9 `it.todo`-Stubs — alle als `it.todo(...)` (kein Body, kein test.skip):

```
it.todo('SEARCH-01: Kamera-Button click ruft getUserMedia mit facingMode environment auf')
it.todo('SEARCH-01: getUserMedia success → Video-Container sichtbar mit Aufnehmen-Button')
it.todo('SEARCH-01: Aufnehmen-Button click → canvas.toBlob aufgerufen, Vorschau angezeigt (D-08)')
it.todo('SEARCH-01: Vorschau zeigt Suchen-Button + Wiederholen-Button (D-08)')
it.todo('SEARCH-01: Suchen-Button click → fetch POST /api/search mit FormData aufgerufen, Spinner sichtbar (D-09)')
it.todo('SEARCH-01+02: POST /api/search 200 → JSON in pre-Block sichtbar (D-10)')
it.todo('SEARCH-01+02: POST /api/search Fehler → Alert + Neu aufnehmen-Button sichtbar (D-11)')
it.todo('SEARCH-01: getUserMedia Fehler → Alert + File-Input eingeblendet (D-05)')
it.todo('SEARCH-02: File-Input immer sichtbar in idle-State (D-06)')
```

Hinweis: `it.todo` benötigt keinen zweiten Parameter — nur den String. Vitest markiert sie als "todo" ohne sie zu überspringen oder zu fehlern.
  </action>

  <verify>
    <automated>npm test -- --reporter=verbose src/app/search/CameraCapture.test.tsx 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - Datei `src/app/search/CameraCapture.test.tsx` existiert: `test -f src/app/search/CameraCapture.test.tsx`
    - Datei enthält getUserMedia-Mock: `grep -c "Object.defineProperty.*mediaDevices" src/app/search/CameraCapture.test.tsx` gibt >= 1
    - Datei enthält 9 it.todo-Stubs: `grep -c "it\.todo(" src/app/search/CameraCapture.test.tsx` gibt 9
    - Datei enthält canvas.toBlob Mock: `grep -c "HTMLCanvasElement.prototype.toBlob" src/app/search/CameraCapture.test.tsx` gibt >= 1
    - Datei enthält global.fetch Mock: `grep -c "global.fetch = vi.fn" src/app/search/CameraCapture.test.tsx` gibt >= 1
    - `npm test -- src/app/search/CameraCapture.test.tsx` läuft durch ohne Exit-Code != 0 (Todos zählen nicht als Fehler)
  </acceptance_criteria>

  <done>CameraCapture.test.tsx existiert mit 9 it.todo-Stubs und vollständiger Mock-Infrastruktur. `npm test` läuft ohne Fehler durch.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright E2E Stubs — phase-07-camera-ui.spec.ts</name>
  <files>tests/phase-07-camera-ui.spec.ts</files>

  <read_first>
    - tests/phase-04-upload.spec.ts — zeigt exaktes test.skip-Muster für Phase-Stubs im Projekt
    - tests/phase-05-admin.spec.ts — alternatives test.skip-Muster falls vorhanden (zum Vergleich)
    - .planning/phases/07-camera-ui/07-VALIDATION.md — Tabelle der E2E-Szenarien (7-03-01, 7-03-02)
  </read_first>

  <action>
Datei `tests/phase-07-camera-ui.spec.ts` NEU erstellen. Pattern identisch zu `tests/phase-04-upload.spec.ts` — `test.skip` mit leerem async-Body (Playwright 1.59.1 kennt kein `test.todo()`).

```typescript
// tests/phase-07-camera-ui.spec.ts
// E2E-Tests für Phase 7 Camera UI (SEARCH-01, SEARCH-02, D-02)
// Stubs — werden in Wave 3 (Plan 07-04) aktiviert.

import { test, expect } from '@playwright/test'

test.describe('Phase 7: Camera UI', () => {
  test.skip('/search Seite ist erreichbar und zeigt Kamera-Button', async ({ page }) => {
    // Wave 3: page.goto('/search') → await expect(page.getByText('Bauteil fotografieren')).toBeVisible()
  })

  test.skip('SEARCH-02: File-Input ist in idle-State sichtbar', async ({ page }) => {
    // Wave 3: page.goto('/search') → await expect(page.getByText('Foto aus Galerie wählen')).toBeVisible()
  })

  test.skip('SEARCH-02: Datei-Upload via File-Input löst Suche aus', async ({ page }) => {
    // Wave 3: File-Input mit Testbild befüllen → Spinner sichtbar → Ergebnis oder Fehler
  })

  test.skip('D-02: Homepage zeigt beide Buttons (Teil hochladen + Teil suchen)', async ({ page }) => {
    // Wave 3: page.goto('/') → beide Buttons prüfen
  })

  test.skip('D-02: Teil suchen-Button navigiert zu /search', async ({ page }) => {
    // Wave 3: page.goto('/') → click 'Teil suchen' → expect URL /search
  })

  test.skip('D-09: Spinner sichtbar während Suche läuft', async ({ page }) => {
    // Wave 3: Route-Mock für /api/search mit Delay → Spinner sichtbar
  })

  test.skip('D-10: JSON-Ergebnis in pre-Block nach erfolgreicher Suche', async ({ page }) => {
    // Wave 3: Route-Mock für /api/search 200 → pre-Block mit JSON sichtbar
  })
})
```
  </action>

  <verify>
    <automated>npx playwright test tests/phase-07-camera-ui.spec.ts --project=chromium 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - Datei `tests/phase-07-camera-ui.spec.ts` existiert: `test -f tests/phase-07-camera-ui.spec.ts`
    - Datei enthält 7 test.skip-Blöcke: `grep -c "test\.skip(" tests/phase-07-camera-ui.spec.ts` gibt 7
    - Playwright-Lauf schlägt nicht fehl (test.skip = übersprungen): `npx playwright test tests/phase-07-camera-ui.spec.ts --project=chromium` Exit-Code 0
    - Datei enthält Playwright-Import: `grep -c "from '@playwright/test'" tests/phase-07-camera-ui.spec.ts` gibt 1
  </acceptance_criteria>

  <done>phase-07-camera-ui.spec.ts existiert mit 7 test.skip-Stubs. `npx playwright test tests/phase-07-camera-ui.spec.ts --project=chromium` Exit-Code 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Code → jsdom | vi.fn()-Mocks ersetzen Browser-APIs; kein echter Kamerazugriff in Tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-W0-01 | Tampering | getUserMedia-Mock | accept | Mock ist auf Test-Scope beschränkt; kein Produktionscode betroffen |
</threat_model>

<verification>
- `npm test -- src/app/search/CameraCapture.test.tsx` → Exit-Code 0, 9 todos gelistet
- `npx playwright test tests/phase-07-camera-ui.spec.ts --project=chromium` → Exit-Code 0, alle skipped
</verification>

<success_criteria>
Wave 0 abgeschlossen wenn: Beide Testdateien existieren, beide Test-Kommandos laufen durch, getUserMedia-Mock-Infrastruktur ist in CameraCapture.test.tsx definiert, 9 Vitest-Stubs und 7 Playwright-Stubs vorhanden.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/07-camera-ui/07-01-SUMMARY.md` erstellen.
</output>
