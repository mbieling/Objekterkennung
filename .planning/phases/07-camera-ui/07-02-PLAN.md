---
phase: 07-camera-ui
plan: 02
type: execute
wave: 1
depends_on:
  - "07-01"
files_modified:
  - src/app/search/CameraCapture.tsx
  - src/app/search/page.tsx
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-02

must_haves:
  truths:
    - "Kamera-Button in idle-State startet getUserMedia mit facingMode environment (D-03, D-04)"
    - "Video-Stream läuft mit playsInline muted — Framing-Overlay sichtbar (D-07)"
    - "Aufnehmen-Button führt canvas.toBlob durch und zeigt Vorschau-Bild (D-08)"
    - "Suchen-Button sendet POST /api/search mit FormData ohne Content-Type-Header, 30s AbortController (D-09)"
    - "JSON-Antwort erscheint in pre-Block (D-10); Fehler zeigen Alert + Neu aufnehmen-Button (D-11)"
    - "getUserMedia-Fehler zeigt Alert + blendet File-Input-Button ein (D-05)"
    - "File-Input mit accept=image/* ist dauerhaft sichtbar als sekundärer Einstieg (D-06)"
    - "/search-Seite lädt als Server Component mit h1 Bauteil fotografieren und CameraCapture"
  artifacts:
    - path: "src/app/search/CameraCapture.tsx"
      provides: "Client Component — vollständige State Machine mit 7 States"
      exports: ["CameraCapture"]
      min_lines: 200
    - path: "src/app/search/page.tsx"
      provides: "Server Component Wrapper für /search-Route"
      min_lines: 20
  key_links:
    - from: "src/app/search/page.tsx"
      to: "src/app/search/CameraCapture.tsx"
      via: "import { CameraCapture } from './CameraCapture'"
      pattern: "from.*CameraCapture"
    - from: "src/app/search/CameraCapture.tsx"
      to: "/api/search"
      via: "fetch POST mit FormData"
      pattern: "fetch.*api/search"
    - from: "src/app/search/CameraCapture.tsx"
      to: "navigator.mediaDevices.getUserMedia"
      via: "startCamera()"
      pattern: "getUserMedia"
---

<objective>
CameraCapture.tsx als vollständige Client-Component implementieren und die /search-Seite als Server-Component-Wrapper erstellen.

Purpose: Kern der Phase 7 — die komplette State Machine (idle → requesting → previewing → captured → searching → result | error) mit getUserMedia, Canvas-Capture, FormData-Fetch und allen UI-Zuständen aus UI-SPEC.md.

Output: src/app/search/CameraCapture.tsx (vollständig implementiert) + src/app/search/page.tsx (Server Wrapper)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-camera-ui/07-CONTEXT.md
@.planning/phases/07-camera-ui/07-RESEARCH.md
@.planning/phases/07-camera-ui/07-UI-SPEC.md
@.planning/phases/07-camera-ui/07-01-SUMMARY.md

<interfaces>
<!-- Aus src/app/upload/UploadForm.tsx — State-Machine-Pattern zum Replizieren -->
```typescript
// State-Machine-Typ-Deklaration (Zeile 24-34)
type UploadPhase =
  | 'idle' | 'hashing' | 'initializing' | 'uploading'
  | 'confirming' | 'polling' | 'ready' | 'failed' | 'duplicate' | 'error'

// useState-Pattern
const [phase, setPhase] = useState<UploadPhase>('idle')
const [errorMessage, setErrorMessage] = useState<string | null>(null)

// useRef-Pattern für DOM-Elemente
const fileInputRef = useRef<HTMLInputElement>(null)
```

<!-- Aus src/app/api/search/route.ts — API-Contract -->
```typescript
// POST /api/search — multipart/form-data mit 'image'-Feld
// Response Shape (Phase 6 D-11):
interface SearchResponse {
  results: Array<{
    id: string
    name: string
    part_number: string | null
    project: string | null
    status: 'ready'
    similarity: number   // 0–1 Float
    created_at: string
  }>
  query: {
    threshold: number
    limit: number
    results_count: number
  }
}
```

<!-- Verfügbare shadcn/ui-Komponenten -->
```typescript
import { Button } from '@/components/ui/button'
import { Alert, AlertDescription } from '@/components/ui/alert'
import { Card, CardContent } from '@/components/ui/card'
import { Skeleton } from '@/components/ui/skeleton'
// Icons:
import { Camera, Upload, RotateCcw, Search, Loader2 } from 'lucide-react'
```

<!-- src/app/upload/page.tsx — Server-Component-Muster -->
```typescript
// Server Component ohne 'use client'
export const metadata = { title: '...' }
export default function UploadPage() {
  return (
    <main className="min-h-screen bg-background py-8 px-4">
      <div className="max-w-...">
        <h1 className="text-2xl font-semibold mb-8">...</h1>
        <ClientComponent />
      </div>
    </main>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CameraCapture.tsx — vollständige State Machine implementieren</name>
  <files>src/app/search/CameraCapture.tsx</files>

  <read_first>
    - src/app/search/CameraCapture.test.tsx — die 9 it.todo-Stubs definieren exakt, was implementiert werden muss
    - src/app/upload/UploadForm.tsx — State-Machine-Pattern (useState, useRef, useEffect, Cleanup)
    - src/hooks/use-part-status.ts — AbortController-Cleanup-Pattern
    - .planning/phases/07-camera-ui/07-UI-SPEC.md — Layout-Kontrakt pro State + Copywriting-Kontrakt
    - .planning/phases/07-camera-ui/07-RESEARCH.md — Pattern 1-6 (getUserMedia, Canvas, Stream-Cleanup, File-Input, Overlay)
  </read_first>

  <behavior>
    - Test 1 (SEARCH-01): Kamera-Button click → getUserMedia aufgerufen mit `{ video: { facingMode: { ideal: 'environment' } }, audio: false }`
    - Test 2 (SEARCH-01): getUserMedia resolved → Video-Container + Aufnehmen-Button sichtbar
    - Test 3 (SEARCH-01): Aufnehmen-Button click → canvas.toBlob('image/jpeg', 0.85) aufgerufen, img-Preview sichtbar
    - Test 4 (SEARCH-01): Vorschau-State → Suchen-Button (primary) + Wiederholen-Button (outline) sichtbar
    - Test 5 (SEARCH-01+02): Suchen-Button click → fetch('/api/search', { method: 'POST', body: FormData }) ohne Content-Type-Header, Spinner sichtbar
    - Test 6 (SEARCH-01+02): fetch 200 → pre-Block mit JSON-Inhalt sichtbar
    - Test 7 (SEARCH-01+02): fetch wirft Error → Alert (variant destructive) + Neu aufnehmen-Button sichtbar
    - Test 8 (SEARCH-01): getUserMedia rejected → Alert mit Fehlermeldung + File-Input-Trigger sichtbar
    - Test 9 (SEARCH-02): idle-State → File-Input-Trigger mit Text "Foto aus Galerie wählen" sichtbar
  </behavior>

  <action>
Datei `src/app/search/CameraCapture.tsx` NEU erstellen. Datei startet mit `'use client'`.

**Typ-Deklaration:**
```typescript
type SearchPhase =
  | 'idle'        // Startbildschirm
  | 'requesting'  // getUserMedia läuft
  | 'previewing'  // Video-Stream aktiv
  | 'captured'    // Standbild, Bestätigung
  | 'searching'   // POST /api/search läuft
  | 'result'      // Antwort vorhanden
  | 'error'       // Fehler

interface SearchResponse {
  results: Array<{
    id: string; name: string; part_number: string | null;
    project: string | null; status: 'ready'; similarity: number; created_at: string;
  }>
  query: { threshold: number; limit: number; results_count: number }
}
```

**Hilfsfunktionen (außerhalb der Komponente):**

```typescript
// D-04: facingMode ideal (nicht exact) — fällt auf Frontkamera zurück statt Hard-Error
async function startCamera(videoRef: React.RefObject<HTMLVideoElement>): Promise<MediaStream> {
  const stream = await navigator.mediaDevices.getUserMedia({
    video: { facingMode: { ideal: 'environment' } },
    audio: false,
  })
  if (videoRef.current) {
    videoRef.current.srcObject = stream
    await videoRef.current.play()
  }
  return stream
}

// Claude's Discretion: max 1024px Breite, JPEG 0.85
function captureFrame(video: HTMLVideoElement): Promise<Blob> {
  const MAX_WIDTH = 1024
  const scale = Math.min(1, MAX_WIDTH / video.videoWidth)
  const w = Math.round(video.videoWidth * scale) || MAX_WIDTH
  const h = Math.round(video.videoHeight * scale) || Math.round(MAX_WIDTH * 0.75)
  const canvas = document.createElement('canvas')
  canvas.width = w
  canvas.height = h
  canvas.getContext('2d')!.drawImage(video, 0, 0, w, h)
  return new Promise((resolve, reject) => {
    canvas.toBlob(
      (blob) => blob ? resolve(blob) : reject(new Error('canvas.toBlob lieferte null')),
      'image/jpeg',
      0.85
    )
  })
}
```

**Komponente CameraCapture:**
- `useState<SearchPhase>('idle')` für phase
- `useState<string | null>(null)` für errorMessage
- `useState<string | null>(null)` für previewUrl (objectURL vom Blob)
- `useState<SearchResponse | null>(null)` für searchResult
- `useState<Blob | null>(null)` für capturedBlob
- `useRef<HTMLVideoElement>(null)` für videoRef
- `useRef<MediaStream | null>(null)` für streamRef
- `useRef<HTMLInputElement>(null)` für fileInputRef

**useEffect für Stream-Cleanup** (Pattern 4 aus RESEARCH.md):
```typescript
useEffect(() => {
  return () => {
    if (streamRef.current) {
      streamRef.current.getTracks().forEach(track => track.stop())
      streamRef.current = null
    }
    if (previewUrl) URL.revokeObjectURL(previewUrl)
  }
}, [])
```

**handleStartCamera:**
1. `setPhase('requesting')`
2. `startCamera(videoRef)` — bei Erfolg: `streamRef.current = stream`, `setPhase('previewing')`
3. Bei Fehler: Error-Typ prüfen (NotAllowedError → "Kamerazugriff verweigert. Bitte erlaube den Kamerazugriff in den Browser-Einstellungen oder wähle ein Foto aus der Galerie." | sonst → "Kamera konnte nicht gestartet werden. Wähle ein Foto aus der Galerie."), `setErrorMessage(msg)`, `setPhase('idle')`

**handleCapture:**
1. `captureFrame(videoRef.current!)` — liefert Blob
2. Stream stoppen: `streamRef.current?.getTracks().forEach(t => t.stop())`
3. `setCapturedBlob(blob)`, `setPreviewUrl(URL.createObjectURL(blob))`, `setPhase('captured')`

**handleRetry:**
1. Vorherigen PreviewUrl revoken
2. `setPhase('idle')`, `setCapturedBlob(null)`, `setPreviewUrl(null)`, `setErrorMessage(null)`

**handleFileSelect** (für File-Input onChange):
1. `const file = e.target.files?.[0]` — prüfen ob File vorhanden
2. MIME-Typ-Check: `if (!file.type.startsWith('image/')) { setErrorMessage('Nur Bilddateien erlaubt.'); return }`
3. `setCapturedBlob(file)`, `setPreviewUrl(URL.createObjectURL(file))`, `setPhase('captured')`

**handleSearch** (D-09 — 30s Timeout):
```typescript
async function handleSearch() {
  if (!capturedBlob) return
  setPhase('searching')
  setErrorMessage(null)
  const formData = new FormData()
  formData.append('image', capturedBlob, 'capture.jpg')
  const controller = new AbortController()
  const timeoutId = setTimeout(() => controller.abort(), 30_000)
  try {
    const res = await fetch('/api/search', {
      method: 'POST',
      body: formData,
      // KEIN Content-Type-Header — Browser setzt Boundary automatisch
      signal: controller.signal,
    })
    if (!res.ok) throw new Error(`HTTP ${res.status}`)
    const data: SearchResponse = await res.json()
    setSearchResult(data)
    setPhase('result')
  } catch (err) {
    const msg = err instanceof DOMException && err.name === 'AbortError'
      ? 'Suche hat zu lange gedauert. Bitte erneut versuchen.'
      : 'Suche fehlgeschlagen. Bitte überprüfe deine Verbindung und versuche es erneut.'
    setErrorMessage(msg)
    setPhase('error')
  } finally {
    clearTimeout(timeoutId)
  }
}
```

**JSX-Rendering nach State:**

File-Input (immer vorhanden im DOM, in searching-State mit style `display:none` oder className-Bedingung):
```tsx
<input
  ref={fileInputRef}
  type="file"
  accept="image/*"
  className="hidden"
  onChange={handleFileSelect}
/>
```

File-Input-Trigger-Button (in allen States außer searching):
```tsx
<Button variant="ghost" className="w-full" onClick={() => fileInputRef.current?.click()}>
  <Upload className="mr-2 h-4 w-4" />
  Foto aus Galerie wählen
</Button>
```

**idle-State:**
```tsx
<Card>
  <CardContent className="pt-6 flex flex-col gap-4 items-center">
    {errorMessage && (
      <Alert variant="destructive">
        <AlertDescription>{errorMessage}</AlertDescription>
      </Alert>
    )}
    <Button className="w-full" onClick={handleStartCamera}>
      <Camera className="mr-2 h-4 w-4" />
      Kamera starten
    </Button>
    {FileInputTrigger}
  </CardContent>
</Card>
```

**requesting-State:**
```tsx
<div className="flex flex-col gap-4">
  <Skeleton className="w-full aspect-[4/3] max-w-sm mx-auto rounded-lg" />
  <p className="text-sm text-muted-foreground text-center">Kamera wird aktiviert...</p>
</div>
```

**previewing-State:**
```tsx
<div className="flex flex-col gap-4">
  <div className="relative w-full aspect-[4/3] max-w-sm mx-auto">
    <video
      ref={videoRef}
      className="w-full h-full object-cover rounded-lg"
      playsInline
      muted
      autoPlay
      aria-label="Kamera-Vorschau"
    />
    <div
      className="absolute inset-[10%] border-2 border-white/70 rounded-xl pointer-events-none"
      aria-hidden="true"
    />
  </div>
  <Button className="w-full h-12" onClick={handleCapture}>
    <Camera className="mr-2 h-4 w-4" />
    Aufnehmen
  </Button>
  {FileInputTrigger}
</div>
```

**captured-State:**
```tsx
<div className="flex flex-col gap-4">
  {previewUrl && (
    <img
      src={previewUrl}
      alt="Aufgenommenes Bauteil"
      className="w-full max-w-sm mx-auto rounded-lg object-cover aspect-[4/3]"
    />
  )}
  <div className="flex gap-4">
    <Button className="flex-1" onClick={handleSearch}>
      <Search className="mr-2 h-4 w-4" />
      Suchen
    </Button>
    <Button variant="outline" className="flex-1" onClick={handleRetry}>
      <RotateCcw className="mr-2 h-4 w-4" />
      Wiederholen
    </Button>
  </div>
  {FileInputTrigger}
</div>
```

**searching-State:**
```tsx
<div className="flex flex-col items-center gap-4 py-8" aria-live="polite">
  <Loader2 className="animate-spin h-8 w-8" aria-label="Suche läuft" />
  <p className="text-sm text-muted-foreground">Suche läuft...</p>
</div>
```

**result-State (D-10 Placeholder):**
```tsx
<div className="flex flex-col gap-4">
  <pre className="text-xs overflow-auto bg-muted p-4 rounded-lg max-h-96">
    {JSON.stringify(searchResult, null, 2)}
  </pre>
  <Button variant="outline" className="w-full" onClick={() => { setPhase('idle'); setSearchResult(null); setPreviewUrl(null); setCapturedBlob(null); }}>
    <RotateCcw className="mr-2 h-4 w-4" />
    Neu aufnehmen
  </Button>
</div>
```

**error-State (D-11):**
```tsx
<div className="flex flex-col gap-4">
  <Alert variant="destructive">
    <AlertDescription>{errorMessage}</AlertDescription>
  </Alert>
  <Button variant="outline" className="w-full" onClick={() => { setPhase('idle'); setErrorMessage(null); }}>
    <RotateCcw className="mr-2 h-4 w-4" />
    Neu aufnehmen
  </Button>
</div>
```

Export am Ende: `export { CameraCapture }` (named export — konsistent mit UploadForm-Pattern und CameraCapture.test.tsx-Import).
  </action>

  <verify>
    <automated>npm test -- --reporter=verbose src/app/search/CameraCapture.test.tsx 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - Datei existiert: `test -f src/app/search/CameraCapture.tsx`
    - Datei startet mit 'use client': `head -1 src/app/search/CameraCapture.tsx | grep -c "'use client'"` gibt 1
    - SearchPhase-Typ definiert: `grep -c "type SearchPhase" src/app/search/CameraCapture.tsx` gibt 1
    - getUserMedia mit facingMode environment: `grep -c "facingMode.*ideal.*environment" src/app/search/CameraCapture.tsx` gibt >= 1
    - playsInline auf video-Element: `grep -c "playsInline" src/app/search/CameraCapture.tsx` gibt >= 1
    - muted auf video-Element: `grep -c "muted" src/app/search/CameraCapture.tsx` gibt >= 1
    - AbortController mit 30000ms: `grep -c "30_000\|30000" src/app/search/CameraCapture.tsx` gibt >= 1
    - canvas.toBlob mit image/jpeg 0.85: `grep -c "toBlob.*image/jpeg.*0.85\|image/jpeg.*0.85" src/app/search/CameraCapture.tsx` gibt >= 1
    - KEIN manueller Content-Type-Header: `grep -v "// " src/app/search/CameraCapture.tsx | grep -c "Content-Type"` gibt 0
    - Framing-Overlay vorhanden: `grep -c "inset-\[10%\]" src/app/search/CameraCapture.tsx` gibt >= 1
    - File-Input accept=image/*: `grep -c 'accept="image/\*"' src/app/search/CameraCapture.tsx` gibt >= 1
    - Named export: `grep -c "export.*CameraCapture\|export function CameraCapture\|export const CameraCapture" src/app/search/CameraCapture.tsx` gibt >= 1
    - `npm run build` (oder TypeScript-Check): kein Compiler-Fehler in src/app/search/CameraCapture.tsx
  </acceptance_criteria>

  <done>CameraCapture.tsx implementiert alle 7 States vollständig. Alle CONTEXT.md-Decisions D-03 bis D-11 sind umgesetzt. TypeScript kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: /search Server Component — page.tsx</name>
  <files>src/app/search/page.tsx</files>

  <read_first>
    - src/app/upload/page.tsx — exaktes Muster für Server-Component-Wrapper im Projekt
    - src/app/search/CameraCapture.tsx — muss existieren (Task 1) vor diesem Task
    - .planning/phases/07-camera-ui/07-UI-SPEC.md — Layout-Kontrakt für /search-Seite
  </read_first>

  <action>
Datei `src/app/search/page.tsx` NEU erstellen. KEIN `'use client'` — das ist ein Server Component.

```typescript
// src/app/search/page.tsx
// Phase 7 — /search Server Component (D-01)
// Metadata + Layout-Shell; CameraCapture ist Client Component

import type { Metadata } from 'next'
import { CameraCapture } from './CameraCapture'

export const metadata: Metadata = {
  title: 'Bauteil suchen — Bauteil-Finder',
}

export default function SearchPage() {
  return (
    <main className="min-h-screen bg-background py-8 px-4">
      <div className="max-w-md mx-auto">
        <h1 className="text-2xl font-semibold mb-8">Bauteil fotografieren</h1>
        <CameraCapture />
      </div>
    </main>
  )
}
```

Exakt dieses Layout — `max-w-md` (nicht max-w-4xl wie /upload), `py-8 px-4` wie UI-SPEC, h1 "Bauteil fotografieren" (D-01 Copywriting-Kontrakt).
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - Datei existiert: `test -f src/app/search/page.tsx`
    - Kein 'use client': `grep -c "'use client'" src/app/search/page.tsx` gibt 0
    - metadata-Export mit korrektem Titel: `grep -c "Bauteil suchen" src/app/search/page.tsx` gibt 1
    - CameraCapture importiert: `grep -c "from.*CameraCapture" src/app/search/page.tsx` gibt 1
    - h1 mit korrektem Text: `grep -c "Bauteil fotografieren" src/app/search/page.tsx` gibt 1
    - max-w-md im Layout: `grep -c "max-w-md" src/app/search/page.tsx` gibt 1
    - `npm run build` kompiliert ohne Fehler
  </acceptance_criteria>

  <done>/search-Route erreichbar als Server Component. h1 "Bauteil fotografieren", CameraCapture eingebunden, TypeScript fehlerfrei.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → getUserMedia | Nutzer muss Kamera-Permission explizit gewähren; Browser erzwingt HTTPS Secure Context |
| Client → POST /api/search | Nicht-Bild-Dateien können hochgeladen werden — Validierung auf Client und Server nötig |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | handleFileSelect / File-Input | mitigate | `file.type.startsWith('image/')` Check in handleFileSelect bevor blob gesetzt wird; `accept="image/*"` als erste Verteidigungslinie am Input-Element |
| T-7-02 | DoS | captureFrame() / Canvas-Resize | accept | Canvas-Resize auf max. 1024px begrenzt Memory-Spike; kein explizites Filesize-Limit nötig — Canvas skaliert automatisch |
| T-7-03 | Spoofing | getUserMedia ohne HTTPS | accept | Browser erzwingt Secure Context (HTTPS/localhost) ohne Code-Aufwand; Vercel deployt automatisch HTTPS |
| T-7-04 | Denial of Service | AbortController Timeout | mitigate | 30s Timeout via AbortController; AbortError wird als "Suche hat zu lange gedauert" angezeigt, kein stiller Fail |
</threat_model>

<verification>
- `npm run build` ohne Fehler
- `npm test -- src/app/search/CameraCapture.test.tsx` — Stubs existieren (todos/skip), kein Exit-Code-Fehler
- `curl -s http://localhost:3000/search` gibt HTML mit "Bauteil fotografieren" zurück (wenn Dev-Server läuft)
</verification>

<success_criteria>
Wave 1 abgeschlossen wenn: CameraCapture.tsx alle 7 States implementiert, playsInline/muted/autoPlay auf video, AbortController 30s, keine Content-Type-Header beim fetch, canvas.toBlob jpeg 0.85, MIME-Typ-Check in handleFileSelect, page.tsx als Server Component mit korrektem Layout, `npm run build` grün.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/07-camera-ui/07-02-SUMMARY.md` erstellen.
</output>
