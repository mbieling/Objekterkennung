---
phase: 07-camera-ui
plan: 03
type: execute
wave: 2
depends_on:
  - "07-02"
files_modified:
  - src/app/page.tsx
  - src/app/search/CameraCapture.test.tsx
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-02

must_haves:
  truths:
    - "Homepage zeigt zwei Buttons nebeneinander: 'Teil hochladen' (primary) und 'Teil suchen' (outline) (D-02)"
    - "'Teil suchen'-Button navigiert zu /search"
    - "Alle 9 Vitest-Unit-Tests für CameraCapture sind aktiviert und grün"
    - "getUserMedia-Mock funktioniert in allen Test-Szenarien"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage mit zwei Buttons side-by-side (D-02)"
      min_lines: 15
    - path: "src/app/search/CameraCapture.test.tsx"
      provides: "9 aktive Vitest-Tests (kein it.todo mehr)"
      min_lines: 150
  key_links:
    - from: "src/app/page.tsx"
      to: "/search"
      via: "Link href='/search'"
      pattern: "href.*search"
    - from: "src/app/search/CameraCapture.test.tsx"
      to: "src/app/search/CameraCapture.tsx"
      via: "render(CameraCapture), fireEvent, screen"
      pattern: "render.*CameraCapture"
---

<objective>
Homepage mit zweitem Such-Button erweitern (D-02) und alle Vitest-Unit-Tests für CameraCapture aktivieren.

Purpose: D-02 schließt die Navigation ab — Nutzer können von der Homepage direkt zur Suche. Unit-Test-Aktivierung stellt sicher, dass die CameraCapture-Implementierung aus Wave 1 korrekt gegen alle definierten Anforderungen validiert ist.

Output: src/app/page.tsx (zwei Buttons) + src/app/search/CameraCapture.test.tsx (9 aktive Tests)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-camera-ui/07-CONTEXT.md
@.planning/phases/07-camera-ui/07-UI-SPEC.md
@.planning/phases/07-camera-ui/07-VALIDATION.md
@.planning/phases/07-camera-ui/07-02-SUMMARY.md

<interfaces>
<!-- Aus src/app/page.tsx (aktueller Stand nach Phase 4) -->
```typescript
// src/app/page.tsx — Phase 4 Stand:
import Link from 'next/link'
import { Button } from '@/components/ui/button'

export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center p-8">
      <Button asChild>
        <Link href="/upload">Teil hochladen</Link>
      </Button>
    </main>
  )
}
// Ziel (D-02): Zweiten Button "Teil suchen" → /search hinzufügen
// Layout: flex gap-4 flex-wrap justify-center (aus UI-SPEC)
```

<!-- CameraCapture Export für Tests -->
```typescript
// src/app/search/CameraCapture.tsx — named export
export { CameraCapture }
// oder: export function CameraCapture() { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Homepage — zweiten Button "Teil suchen" hinzufügen (D-02)</name>
  <files>src/app/page.tsx</files>

  <read_first>
    - src/app/page.tsx — MUSS gelesen werden; aktuellen Inhalt verstehen bevor Änderung
    - .planning/phases/07-camera-ui/07-UI-SPEC.md — Abschnitt "Homepage-Erweiterung (D-02)" für exaktes Layout
  </read_first>

  <action>
`src/app/page.tsx` lesen, dann modifizieren. Aktueller Inhalt: eine Button-Komponente mit "Teil hochladen" → /upload.

Änderung: main-Element und Button-Wrapper so anpassen, dass beide Buttons nebeneinander erscheinen.

Ziel-Inhalt:
```typescript
// src/app/page.tsx
// Phase 4 — Landing-Page. Phase 7 — zweiter Button "Teil suchen" (D-02).

import Link from 'next/link'
import { Button } from '@/components/ui/button'

export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center p-8">
      <div className="flex gap-4 flex-wrap justify-center">
        <Button asChild>
          <Link href="/upload">Teil hochladen</Link>
        </Button>
        <Button variant="outline" asChild>
          <Link href="/search">Teil suchen</Link>
        </Button>
      </div>
    </main>
  )
}
```

Exakte Klassen: `flex gap-4 flex-wrap justify-center` auf dem div-Wrapper (UI-SPEC D-02). Button 1: default (primary). Button 2: `variant="outline"`.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - Datei enthält "Teil suchen": `grep -c "Teil suchen" src/app/page.tsx` gibt 1
    - Datei enthält Link zu /search: `grep -c 'href="/search"' src/app/page.tsx` gibt 1
    - Button mit variant="outline": `grep -c 'variant="outline"' src/app/page.tsx` gibt 1
    - flex gap-4 Layout: `grep -c "flex gap-4 flex-wrap justify-center" src/app/page.tsx` gibt 1
    - Bestehender "Teil hochladen"-Button noch vorhanden: `grep -c "Teil hochladen" src/app/page.tsx` gibt 1
    - Link zu /upload noch vorhanden: `grep -c 'href="/upload"' src/app/page.tsx` gibt 1
    - `npm run build` Exit-Code 0
  </acceptance_criteria>

  <done>Homepage zeigt zwei Buttons nebeneinander: "Teil hochladen" (primary) und "Teil suchen" (outline). Beide navigieren zu den korrekten Routen. Build grün.</done>
</task>

<task type="auto">
  <name>Task 2: CameraCapture.test.tsx — alle 9 Tests aktivieren</name>
  <files>src/app/search/CameraCapture.test.tsx</files>

  <read_first>
    - src/app/search/CameraCapture.test.tsx — aktuellen Stub-Inhalt lesen (9 it.todo)
    - src/app/search/CameraCapture.tsx — Implementierung verstehen: State-Übergänge, Handler-Namen, JSX-Texte, aria-Labels
    - .planning/phases/07-camera-ui/07-VALIDATION.md — Per-Task-Verification-Map mit Test-IDs
    - .planning/phases/07-camera-ui/07-UI-SPEC.md — Copywriting-Kontrakt (exakte Texte für screen.getByText)
  </read_first>

  <action>
Datei `src/app/search/CameraCapture.test.tsx` vollständig neu schreiben — alle 9 `it.todo` ersetzen durch vollständige `it(...)` oder `test(...)` Implementierungen.

**Mock-Setup (beforeEach/afterEach):**

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { render, screen, fireEvent, waitFor, act } from '@testing-library/react'
import { CameraCapture } from './CameraCapture'

// canvas.toBlob Mock — global, einmalig
HTMLCanvasElement.prototype.toBlob = vi.fn((callback) => {
  callback(new Blob(['fake-image-data'], { type: 'image/jpeg' }))
})

// URL.createObjectURL + revokeObjectURL für preview-URL
global.URL.createObjectURL = vi.fn(() => 'blob:mock-url')
global.URL.revokeObjectURL = vi.fn()

function setupGetUserMedia(success: boolean) {
  const mockStream: MediaStream = {
    getTracks: () => [{ stop: vi.fn() }],
  } as unknown as MediaStream

  Object.defineProperty(global.navigator, 'mediaDevices', {
    value: {
      getUserMedia: success
        ? vi.fn().mockResolvedValue(mockStream)
        : vi.fn().mockRejectedValue(
            new DOMException('Permission denied', 'NotAllowedError')
          ),
    },
    writable: true,
    configurable: true,
  })
  return mockStream
}

beforeEach(() => {
  vi.clearAllMocks()
  global.fetch = vi.fn()
  // video.play mock — jsdom kennt kein echtes Video-Play
  HTMLVideoElement.prototype.play = vi.fn().mockResolvedValue(undefined)
})
```

**9 Tests:**

Test 1 — getUserMedia mit facingMode environment:
```typescript
it('SEARCH-01: Kamera-Button click ruft getUserMedia mit facingMode environment auf', async () => {
  setupGetUserMedia(true)
  render(<CameraCapture />)
  const btn = screen.getByText('Kamera starten')
  await act(async () => { fireEvent.click(btn) })
  expect(navigator.mediaDevices.getUserMedia).toHaveBeenCalledWith(
    expect.objectContaining({
      video: expect.objectContaining({ facingMode: { ideal: 'environment' } }),
      audio: false,
    })
  )
})
```

Test 2 — getUserMedia success → Video sichtbar:
```typescript
it('SEARCH-01: getUserMedia success → Video-Container + Aufnehmen-Button sichtbar', async () => {
  setupGetUserMedia(true)
  render(<CameraCapture />)
  await act(async () => { fireEvent.click(screen.getByText('Kamera starten')) })
  await waitFor(() => expect(screen.getByText('Aufnehmen')).toBeInTheDocument())
})
```

Test 3 — Capture → canvas.toBlob + Preview:
```typescript
it('SEARCH-01: Aufnehmen-Button click → canvas.toBlob aufgerufen, Vorschau angezeigt (D-08)', async () => {
  setupGetUserMedia(true)
  render(<CameraCapture />)
  await act(async () => { fireEvent.click(screen.getByText('Kamera starten')) })
  await waitFor(() => screen.getByText('Aufnehmen'))
  await act(async () => { fireEvent.click(screen.getByText('Aufnehmen')) })
  expect(HTMLCanvasElement.prototype.toBlob).toHaveBeenCalled()
  await waitFor(() => expect(screen.getByRole('img', { name: /Aufgenommenes Bauteil/i })).toBeInTheDocument())
})
```

Test 4 — Vorschau zeigt Suchen + Wiederholen:
```typescript
it('SEARCH-01: Vorschau zeigt Suchen-Button + Wiederholen-Button (D-08)', async () => {
  setupGetUserMedia(true)
  render(<CameraCapture />)
  await act(async () => { fireEvent.click(screen.getByText('Kamera starten')) })
  await waitFor(() => screen.getByText('Aufnehmen'))
  await act(async () => { fireEvent.click(screen.getByText('Aufnehmen')) })
  await waitFor(() => {
    expect(screen.getByText('Suchen')).toBeInTheDocument()
    expect(screen.getByText('Wiederholen')).toBeInTheDocument()
  })
})
```

Test 5 — Suchen → fetch aufgerufen, Spinner sichtbar:
```typescript
it('SEARCH-01+02: Suchen-Button click → fetch POST /api/search aufgerufen, Spinner sichtbar (D-09)', async () => {
  setupGetUserMedia(true)
  ;(global.fetch as ReturnType<typeof vi.fn>).mockReturnValue(new Promise(() => {})) // nie resolved
  render(<CameraCapture />)
  await act(async () => { fireEvent.click(screen.getByText('Kamera starten')) })
  await waitFor(() => screen.getByText('Aufnehmen'))
  await act(async () => { fireEvent.click(screen.getByText('Aufnehmen')) })
  await waitFor(() => screen.getByText('Suchen'))
  await act(async () => { fireEvent.click(screen.getByText('Suchen')) })
  await waitFor(() => expect(screen.getByText('Suche läuft...')).toBeInTheDocument())
  expect(global.fetch).toHaveBeenCalledWith(
    '/api/search',
    expect.objectContaining({ method: 'POST' })
  )
  // Content-Type darf NICHT manuell gesetzt sein
  const fetchCall = (global.fetch as ReturnType<typeof vi.fn>).mock.calls[0][1]
  expect(fetchCall.headers).toBeUndefined()
})
```

Test 6 — fetch 200 → JSON in pre:
```typescript
it('SEARCH-01+02: POST /api/search 200 → JSON in pre-Block sichtbar (D-10)', async () => {
  setupGetUserMedia(true)
  const mockResponse = { results: [], query: { threshold: 0.6, limit: 10, results_count: 0 } }
  ;(global.fetch as ReturnType<typeof vi.fn>).mockResolvedValue({
    ok: true,
    json: () => Promise.resolve(mockResponse),
  })
  render(<CameraCapture />)
  await act(async () => { fireEvent.click(screen.getByText('Kamera starten')) })
  await waitFor(() => screen.getByText('Aufnehmen'))
  await act(async () => { fireEvent.click(screen.getByText('Aufnehmen')) })
  await waitFor(() => screen.getByText('Suchen'))
  await act(async () => { fireEvent.click(screen.getByText('Suchen')) })
  await waitFor(() => {
    const pre = document.querySelector('pre')
    expect(pre).toBeTruthy()
    expect(pre!.textContent).toContain('"results_count": 0')
  })
})
```

Test 7 — fetch Fehler → Alert + Neu aufnehmen:
```typescript
it('SEARCH-01+02: POST /api/search Fehler → Alert + Neu aufnehmen-Button sichtbar (D-11)', async () => {
  setupGetUserMedia(true)
  ;(global.fetch as ReturnType<typeof vi.fn>).mockResolvedValue({ ok: false, status: 500 })
  render(<CameraCapture />)
  await act(async () => { fireEvent.click(screen.getByText('Kamera starten')) })
  await waitFor(() => screen.getByText('Aufnehmen'))
  await act(async () => { fireEvent.click(screen.getByText('Aufnehmen')) })
  await waitFor(() => screen.getByText('Suchen'))
  await act(async () => { fireEvent.click(screen.getByText('Suchen')) })
  await waitFor(() => {
    expect(screen.getByText('Neu aufnehmen')).toBeInTheDocument()
    expect(screen.getByText(/Suche fehlgeschlagen/i)).toBeInTheDocument()
  })
})
```

Test 8 — getUserMedia Fehler → Alert + File-Input:
```typescript
it('SEARCH-01: getUserMedia Fehler → Alert + File-Input-Trigger eingeblendet (D-05)', async () => {
  setupGetUserMedia(false)
  render(<CameraCapture />)
  await act(async () => { fireEvent.click(screen.getByText('Kamera starten')) })
  await waitFor(() => {
    expect(screen.getByText(/Kamerazugriff verweigert|Kamera konnte nicht/i)).toBeInTheDocument()
    expect(screen.getByText('Foto aus Galerie wählen')).toBeInTheDocument()
  })
})
```

Test 9 — File-Input sichtbar in idle:
```typescript
it('SEARCH-02: File-Input-Trigger mit "Foto aus Galerie wählen" in idle-State sichtbar (D-06)', () => {
  render(<CameraCapture />)
  expect(screen.getByText('Foto aus Galerie wählen')).toBeInTheDocument()
})
```
  </action>

  <verify>
    <automated>npm test -- --reporter=verbose src/app/search/CameraCapture.test.tsx 2>&1 | tail -40</automated>
  </verify>

  <acceptance_criteria>
    - Datei enthält keine it.todo mehr: `grep -c "it\.todo(" src/app/search/CameraCapture.test.tsx` gibt 0
    - Datei enthält 9 it()-Blöcke (aktive Tests): `grep -c "^\s*it(" src/app/search/CameraCapture.test.tsx` gibt 9
    - `npm test -- src/app/search/CameraCapture.test.tsx` Exit-Code 0 (alle 9 Tests grün)
    - Test-Output zeigt "9 passed" (oder analog)
    - setupGetUserMedia-Funktion vorhanden: `grep -c "setupGetUserMedia" src/app/search/CameraCapture.test.tsx` gibt >= 2
    - HTMLVideoElement.prototype.play gemockt: `grep -c "HTMLVideoElement.prototype.play" src/app/search/CameraCapture.test.tsx` gibt >= 1
  </acceptance_criteria>

  <done>Alle 9 Vitest-Unit-Tests für CameraCapture grün. Kein it.todo mehr vorhanden. `npm test` gesamte Suite grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Homepage → /search | Navigations-Link; keine sensitiven Daten im URL-Parameter |
| Test-Scope | Vi.fn()-Mocks ersetzen fetch und getUserMedia; kein echter API-Aufruf in Tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-03-01 | Information Disclosure | Homepage-Buttons | accept | Beide Routen (/upload, /search) sind nicht authentifiziert in Phase 7; keine sensitiven Daten exponiert |
| T-7-03-02 | Tampering | Test-Mock (fetch ohne Content-Type) | mitigate | Test 5 prüft explizit, dass `fetchCall.headers` undefined ist — verhindert Regression des FormData-Pitfalls |
</threat_model>

<verification>
- `npm run build` Exit-Code 0
- `npm test -- src/app/search/CameraCapture.test.tsx` → 9 passed, 0 failed
- `npm test` (gesamte Suite) → kein Regressionen
- Manuelle Sichtprüfung Homepage: zwei Buttons nebeneinander sichtbar
</verification>

<success_criteria>
Wave 2 abgeschlossen wenn: Homepage hat zwei Buttons ("Teil hochladen" primary, "Teil suchen" outline) side-by-side, alle 9 Vitest-Unit-Tests für CameraCapture grün, `npm test` gesamte Suite grün.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/07-camera-ui/07-03-SUMMARY.md` erstellen.
</output>
