---
phase: 07-camera-ui
plan: 04
type: execute
wave: 3
depends_on:
  - "07-03"
files_modified:
  - tests/phase-07-camera-ui.spec.ts
autonomous: false
requirements:
  - SEARCH-01
  - SEARCH-02

must_haves:
  truths:
    - "/search-Seite ist per Browser erreichbar und zeigt h1 Bauteil fotografieren"
    - "File-Input-Trigger 'Foto aus Galerie wählen' ist in idle-State sichtbar"
    - "Homepage zeigt beide Buttons: 'Teil hochladen' und 'Teil suchen'"
    - "'Teil suchen'-Button navigiert zu /search"
    - "Spinner erscheint nach Suche-Auslösung (gemockt via page.route)"
    - "JSON-Ergebnis erscheint in pre-Block nach gemockter 200-Antwort"
    - "Alle 7 Playwright-Tests grün (Chromium + Mobile Safari)"
    - "Human-Verify: Phase 7 Success Criteria alle erfüllt"
  artifacts:
    - path: "tests/phase-07-camera-ui.spec.ts"
      provides: "7 aktive Playwright-E2E-Tests (kein test.skip mehr)"
      min_lines: 100
  key_links:
    - from: "tests/phase-07-camera-ui.spec.ts"
      to: "/search"
      via: "page.goto('/search')"
      pattern: "goto.*search"
    - from: "tests/phase-07-camera-ui.spec.ts"
      to: "page.route('/api/search')"
      via: "Playwright route mock für fetch-Tests"
      pattern: "page\.route"
---

<objective>
Playwright E2E-Tests aktivieren und Human-Verify-Checkpoint gegen die Phase-7-Erfolgskriterien durchführen.

Purpose: Letzte Qualitäts-Gate vor Phase-Abschluss. Playwright-Tests validieren Navigation, UI-Sichtbarkeit und gemockte Such-Flows. Human-Verify bestätigt die 4 Phase-7-Success-Criteria aus dem Roadmap auf echtem Mobile-Browser.

Output: tests/phase-07-camera-ui.spec.ts (7 aktive Tests, grün) + Human-Freigabe
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-camera-ui/07-CONTEXT.md
@.planning/phases/07-camera-ui/07-VALIDATION.md
@.planning/phases/07-camera-ui/07-03-SUMMARY.md

<interfaces>
<!-- Playwright route-Mock-Pattern (für /api/search ohne echten Worker) -->
```typescript
// page.route() mockt fetch-Aufrufe im Browser-Kontext
await page.route('/api/search', async route => {
  await route.fulfill({
    status: 200,
    contentType: 'application/json',
    body: JSON.stringify({
      results: [],
      query: { threshold: 0.6, limit: 10, results_count: 0 }
    }),
  })
})
```

<!-- Playwright File-Input befüllen (SEARCH-02 Test) -->
```typescript
// Datei in File-Input setzen ohne Browser-Dialog
const fileInput = page.locator('input[type="file"]')
await fileInput.setInputFiles({
  name: 'test-part.jpg',
  mimeType: 'image/jpeg',
  buffer: Buffer.from('fake-jpeg-data'),
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright E2E-Tests aktivieren — alle 7 test.skip ersetzen</name>
  <files>tests/phase-07-camera-ui.spec.ts</files>

  <read_first>
    - tests/phase-07-camera-ui.spec.ts — aktuellen Stub-Inhalt lesen (7 test.skip)
    - tests/phase-05-admin.spec.ts — aktive Playwright-Test-Patterns im Projekt (zum Vergleich)
    - .planning/phases/07-camera-ui/07-UI-SPEC.md — Copywriting-Kontrakt (exakte Texte für Playwright-Assertions)
    - .planning/phases/07-camera-ui/07-VALIDATION.md — Tabelle mit E2E-Szenarien (7-03-01, 7-03-02)
  </read_first>

  <action>
Datei `tests/phase-07-camera-ui.spec.ts` vollständig neu schreiben. Alle 7 `test.skip` ersetzen durch aktive `test(...)`-Implementierungen.

**Wichtig:** getUserMedia-Tests sind NICHT in E2E testbar (echte Browser-Permission nötig). Nur File-Input-Flow (SEARCH-02) und Navigation sind vollständig E2E-testbar. Kamera-spezifische Tests (SEARCH-01 getUserMedia) bleiben als manuelle Tests (VALIDATION.md "Manual-Only Verifications").

```typescript
// tests/phase-07-camera-ui.spec.ts
// E2E-Tests für Phase 7 Camera UI (SEARCH-01, SEARCH-02, D-02)
// Kamera-Permission-Tests: manuell (see VALIDATION.md Manual-Only)

import { test, expect } from '@playwright/test'

test.describe('Phase 7: Camera UI', () => {

  test('/search Seite ist erreichbar und zeigt Kamera-Button', async ({ page }) => {
    await page.goto('/search')
    await expect(page.getByRole('heading', { name: 'Bauteil fotografieren' })).toBeVisible()
    await expect(page.getByText('Kamera starten')).toBeVisible()
  })

  test('SEARCH-02: File-Input-Trigger ist in idle-State sichtbar', async ({ page }) => {
    await page.goto('/search')
    await expect(page.getByText('Foto aus Galerie wählen')).toBeVisible()
  })

  test('SEARCH-02: Datei-Upload via File-Input löst Suche aus (D-06)', async ({ page }) => {
    // Route-Mock damit kein echter Worker nötig ist
    await page.route('/api/search', async route => {
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
          results: [],
          query: { threshold: 0.6, limit: 10, results_count: 0 },
        }),
      })
    })
    await page.goto('/search')
    // File-Input mit Testbild befüllen (kein Browser-Dialog nötig)
    const fileInput = page.locator('input[type="file"]')
    await fileInput.setInputFiles({
      name: 'test-part.jpg',
      mimeType: 'image/jpeg',
      buffer: Buffer.from('fake-jpeg-data'),
    })
    // Vorschau + Suchen-Button erscheinen
    await expect(page.getByText('Suchen')).toBeVisible()
    // Suchen-Button klicken
    await page.getByText('Suchen').click()
    // JSON-Ergebnis erscheint
    await expect(page.locator('pre')).toBeVisible({ timeout: 10_000 })
    await expect(page.locator('pre')).toContainText('results_count')
  })

  test('D-02: Homepage zeigt beide Buttons (Teil hochladen + Teil suchen)', async ({ page }) => {
    await page.goto('/')
    await expect(page.getByRole('link', { name: 'Teil hochladen' })).toBeVisible()
    await expect(page.getByRole('link', { name: 'Teil suchen' })).toBeVisible()
  })

  test('D-02: Teil suchen-Button navigiert zu /search', async ({ page }) => {
    await page.goto('/')
    await page.getByRole('link', { name: 'Teil suchen' }).click()
    await expect(page).toHaveURL('/search')
    await expect(page.getByRole('heading', { name: 'Bauteil fotografieren' })).toBeVisible()
  })

  test('D-09: Spinner sichtbar während Suche läuft', async ({ page }) => {
    // Route-Mock mit künstlicher Verzögerung
    await page.route('/api/search', async route => {
      await new Promise(resolve => setTimeout(resolve, 2000))
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({ results: [], query: { threshold: 0.6, limit: 10, results_count: 0 } }),
      })
    })
    await page.goto('/search')
    const fileInput = page.locator('input[type="file"]')
    await fileInput.setInputFiles({
      name: 'test-part.jpg',
      mimeType: 'image/jpeg',
      buffer: Buffer.from('fake-jpeg-data'),
    })
    await page.getByText('Suchen').click()
    // Spinner erscheint sofort nach Klick
    await expect(page.getByText('Suche läuft...')).toBeVisible({ timeout: 3_000 })
  })

  test('D-10: JSON-Ergebnis in pre-Block nach erfolgreicher Suche', async ({ page }) => {
    await page.route('/api/search', async route => {
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
          results: [
            {
              id: 'test-id-01',
              name: 'Testbauteil',
              part_number: 'TB-001',
              project: 'Testprojekt',
              status: 'ready',
              similarity: 0.87,
              created_at: '2026-01-01T00:00:00Z',
            },
          ],
          query: { threshold: 0.6, limit: 10, results_count: 1 },
        }),
      })
    })
    await page.goto('/search')
    const fileInput = page.locator('input[type="file"]')
    await fileInput.setInputFiles({
      name: 'test-part.jpg',
      mimeType: 'image/jpeg',
      buffer: Buffer.from('fake-jpeg-data'),
    })
    await page.getByText('Suchen').click()
    await expect(page.locator('pre')).toBeVisible({ timeout: 10_000 })
    await expect(page.locator('pre')).toContainText('Testbauteil')
    await expect(page.locator('pre')).toContainText('"similarity": 0.87')
  })

})
```
  </action>

  <verify>
    <automated>npx playwright test tests/phase-07-camera-ui.spec.ts --project=chromium 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - Keine test.skip mehr: `grep -c "test\.skip(" tests/phase-07-camera-ui.spec.ts` gibt 0
    - 7 aktive Tests: `grep -c "^\s*test(" tests/phase-07-camera-ui.spec.ts` gibt 7
    - page.route-Mock vorhanden: `grep -c "page\.route" tests/phase-07-camera-ui.spec.ts` gibt >= 3
    - `npx playwright test tests/phase-07-camera-ui.spec.ts --project=chromium` Exit-Code 0
    - Test-Output zeigt "7 passed" auf Chromium
    - `npx playwright test tests/phase-07-camera-ui.spec.ts` (alle Browser inkl. Mobile Safari) Exit-Code 0
  </acceptance_criteria>

  <done>Alle 7 Playwright-E2E-Tests grün auf Chromium und Mobile Safari. Kein test.skip mehr. `npm test && npx playwright test tests/phase-07-camera-ui.spec.ts` grün.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-Verify — Phase 7 Success Criteria</name>

  <what-built>
Phase 7 Camera UI vollständig implementiert:
- /search — Seite mit CameraCapture Client-Component (7 States, State Machine)
- getUserMedia mit Hinterkamera-Präferenz, Framing-Overlay, playsInline/muted für iOS
- Canvas-Capture (max 1024px, JPEG 0.85), Vorschau + Bestätigungs-Buttons
- Dauerhafter File-Upload-Fallback ("Foto aus Galerie wählen")
- POST /api/search mit FormData, 30s AbortController-Timeout
- JSON-Placeholder-Ergebnis in pre-Block
- Fehlermeldungen + "Neu aufnehmen"-Button
- Homepage mit zwei Buttons: "Teil hochladen" + "Teil suchen"
- 9 Vitest-Unit-Tests grün, 7 Playwright-E2E-Tests grün
  </what-built>

  <how-to-verify>
**Schritt 1 — Dev-Server starten:**
```bash
npm run dev
```
Server läuft auf http://localhost:3000

**Schritt 2 — SC 1: Kamera-Capture auf mobilem Browser**
Öffne http://localhost:3000/search auf einem iPhone oder Android-Gerät (gleines WLAN-Netz).

Erwartetes Verhalten:
- Seite lädt mit h1 "Bauteil fotografieren"
- "Kamera starten"-Button ist sichtbar
- Klick auf "Kamera starten" → Kamera-Berechtigung anfragen
- Nach Berechtigung: Kamera-Livestream erscheint inline (KEIN iOS-Fullscreen-Player)
- Framing-Overlay sichtbar (abgerundetes Rechteck über dem Video)
- "Aufnehmen"-Button erscheint

**Schritt 3 — SC 2: File-Upload-Fallback**
Auf der /search-Seite:
- "Foto aus Galerie wählen"-Button ist immer sichtbar
- Klick öffnet Galerie/Dateipicker
- Foto auswählen → Vorschau erscheint mit "Suchen" + "Wiederholen"-Buttons

**Schritt 4 — SC 3: Suche durchführen**
Nach Schritt 3 (Vorschau sichtbar):
- "Suchen"-Button klicken
- Spinner + "Suche läuft..."-Text erscheinen
- Nach Antwort: JSON-Daten erscheinen in pre-Block (oder Fehlermeldung wenn kein Worker läuft)
- Kein Redirect — alles bleibt auf /search

**Schritt 5 — SC 4: Framing-Overlay prüfen**
Während Kamera-Stream aktiv (Schritt 2):
- Abgerundetes Rechteck (ca. 80% der Fläche) über dem Video sichtbar
- Overlay blockiert NICHT die Kamera-Buttons darunter

**Schritt 6 — D-02: Homepage prüfen**
http://localhost:3000/ öffnen:
- "Teil hochladen"-Button sichtbar (primary)
- "Teil suchen"-Button sichtbar (outline)
- Beide Buttons nebeneinander (flex-wrap auf kleinen Screens)
- "Teil suchen" klicken → /search öffnet sich
  </how-to-verify>

  <resume-signal>
Gib "approved" ein wenn alle 4 Success Criteria bestätigt sind, oder beschreibe die aufgetretenen Probleme.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright-Test → /api/search | page.route() interceptet Fetch-Aufruf; kein echter Worker-Aufruf in Tests |
| Mobile Browser → HTTPS | getUserMedia erfordert Secure Context; localhost ist ausgenommen (Dev-Umgebung) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-04-01 | Tampering | page.route() Mock | accept | Mocks sind auf Test-Scope beschränkt; kein Einfluss auf Produktions-Fetch |
| T-7-04-02 | Information Disclosure | JSON pre-Block (D-10) | accept | Placeholder zeigt rohe API-Antwort; Phase 8 ersetzt diesen Block durch strukturierte Darstellung; keine sensitiven Daten in SearchResponse |
</threat_model>

<verification>
- `npm test` (gesamte Vitest-Suite) → Exit-Code 0
- `npx playwright test tests/phase-07-camera-ui.spec.ts --project=chromium` → 7 passed
- `npx playwright test tests/phase-07-camera-ui.spec.ts` (alle Browser) → Exit-Code 0
- Human-Verify: alle 4 Phase-7-Success-Criteria manuell bestätigt
</verification>

<success_criteria>
Phase 7 abgeschlossen wenn: Alle 7 Playwright-Tests grün (Chromium + Mobile Safari), gesamte Vitest-Suite grün, Human-Verify hat "approved" zurückgegeben für alle 4 Roadmap-Success-Criteria.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/07-camera-ui/07-04-SUMMARY.md` erstellen.
Danach STATE.md aktualisieren: Phase 7 auf "Complete" setzen, Datum eintragen.
ROADMAP.md aktualisieren: Phase 7 als abgehakt markieren, Plans-Count eintragen.
</output>
