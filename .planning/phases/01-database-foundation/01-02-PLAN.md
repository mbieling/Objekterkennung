---
phase: 01-database-foundation
plan: 02
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - src/lib/supabase.ts
  - src/lib/supabase.test.ts
  - .env.local.example
autonomous: false

must_haves:
  truths:
    - "src/lib/supabase.ts exportiert supabase (Anon-Client) und supabaseAdmin (Service Role Client)"
    - "SUPABASE_SERVICE_ROLE_KEY ist in .env.local.example dokumentiert ohne NEXT_PUBLIC_ Prefix"
    - "Der supabaseAdmin-Client hat autoRefreshToken: false und persistSession: false"
    - "src/lib/supabase.test.ts enthält einen Test der die parts-Tabelle via supabaseAdmin abfragt"
    - "Der Nutzer hat die Migration manuell eingespielt und den Test erfolgreich ausgeführt"
    - "Storage-Buckets parts-steps und parts-thumbnails existieren in Supabase Storage und sind auf privat gesetzt"
  artifacts:
    - path: "src/lib/supabase.ts"
      provides: "Supabase Anon- und Admin-Client für alle nachfolgenden Phasen"
      exports: ["supabase", "supabaseAdmin"]
    - path: "src/lib/supabase.test.ts"
      provides: "Integrations-Smoke-Test für Supabase-Verbindung"
      contains: "supabaseAdmin"
    - path: ".env.local.example"
      provides: "Dokumentation aller erforderlichen Env-Variablen"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
  key_links:
    - from: "src/lib/supabase.ts (supabaseAdmin)"
      to: "Supabase PostgreSQL (parts-Tabelle)"
      via: "SUPABASE_SERVICE_ROLE_KEY env var"
      pattern: "process.env.SUPABASE_SERVICE_ROLE_KEY"

user_setup:
  - service: supabase
    why: "Datenbankinfrastruktur für alle Phase 1 Deliverables"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard > Project Settings > API > Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard > Project Settings > API > anon / public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard > Project Settings > API > service_role key (geheim!)"
    dashboard_config:
      - task: "Supabase-Projekt anlegen (falls noch nicht vorhanden)"
        location: "https://supabase.com > New Project"
      - task: "SQL-Migration einspielen"
        location: "Supabase Dashboard > SQL Editor > Inhalt von supabase/migrations/001_parts_schema.sql einfügen und ausführen"
      - task: "Storage-Buckets anlegen: 'parts-steps' (privat) und 'parts-thumbnails' (privat)"
        location: "Supabase Dashboard > Storage > New Bucket"
---

<objective>
Der Supabase-Client in `src/lib/supabase.ts` wird aktiviert: der auskommentierte Anon-Client wird freigeschaltet und ein neuer `supabaseAdmin`-Client mit Service Role Key wird ergänzt. Die `.env.local.example` wird um `SUPABASE_SERVICE_ROLE_KEY` erweitert. Ein Integrations-Smoke-Test verifiziert nach manuellem Einspielen der Migration die Datenbankverbindung. Der abschliessende Checkpoint führt den Nutzer durch alle manuellen Schritte (Supabase-Projekt, Migration, Buckets, `.env.local`, Test).

Purpose: Nach diesem Plan können alle nachfolgenden Phasen `import { supabaseAdmin } from '@/lib/supabase'` verwenden und mit der Datenbank arbeiten.
Output: Aktivierter Supabase-Client, Integrations-Test, dokumentierte Env-Vars, verifizierte Datenbankinfrastruktur.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-foundation/01-CONTEXT.md
@.planning/phases/01-database-foundation/01-RESEARCH.md
@.planning/phases/01-database-foundation/01-PATTERNS.md

<interfaces>
<!-- Bestehender Stand von src/lib/supabase.ts (auskommentiert, Placeholder): -->
```typescript
// src/lib/supabase.ts (aktuell)
// Supabase Client Setup
// Uncomment this file when you're ready to use Supabase

/*
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!
const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!

export const supabase = createClient(supabaseUrl, supabaseAnonKey)
*/

// For now, export a placeholder to avoid import errors
export const supabase = null;
```

<!-- Ziel-Exports die nachfolgende Phasen benötigen: -->
```typescript
export const supabase   // Anon-Client (zukünftige öffentliche Lesezugriffe)
export const supabaseAdmin  // Service Role Client (alle API Routes)
```

<!-- Vitest-Konfiguration (aus vitest.config.ts): -->
```typescript
test: {
  environment: 'jsdom',
  globals: true,
  setupFiles: ['./src/test/setup.ts'],
},
resolve: {
  alias: { '@': resolve(__dirname, './src') },
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase-Client aktivieren und Admin-Client ergänzen</name>
  <files>src/lib/supabase.ts, .env.local.example</files>
  <read_first>
    - src/lib/supabase.ts (aktuellen auskommentierten Stand lesen bevor überschreiben)
    - .env.local.example (aktuellen Stand lesen bevor ergänzen)
    - .planning/phases/01-database-foundation/01-RESEARCH.md (Pattern 5: Server-seitiger Supabase Admin Client)
    - .planning/phases/01-database-foundation/01-CONTEXT.md (D-06, D-07: Service Role Key Regeln)
  </read_first>
  <action>
**src/lib/supabase.ts vollständig ersetzen** mit folgendem Inhalt (auskommentierten Block entfernen, beide Clients aktiv):

```typescript
// src/lib/supabase.ts
// SERVER-ONLY wenn supabaseAdmin verwendet wird.
// supabaseAdmin darf NIEMALS in Client-Komponenten importiert werden.
// Erlaubte Verwendungsorte: src/app/api/** (API Routes), Server Components, Server Actions.
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!
const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!

// Anon-Client — für zukünftige öffentliche Lesezugriffe mit RLS
// Aktuell ungenutzt (Phase 1); RLS ist für Pilot deaktiviert (D-06)
export const supabase = createClient(supabaseUrl, supabaseAnonKey)

// Admin-Client (service_role) — NIEMALS ins Client-Bundle
// Verwendet SUPABASE_SERVICE_ROLE_KEY (kein NEXT_PUBLIC_ Prefix — würde Key im Browser exponieren)
// Fundort des Keys: Supabase Dashboard > Project Settings > API > service_role
const supabaseServiceKey = process.env.SUPABASE_SERVICE_ROLE_KEY!
export const supabaseAdmin = createClient(supabaseUrl, supabaseServiceKey, {
  auth: {
    autoRefreshToken: false,
    persistSession: false
  }
})
```

**WICHTIG:** `SUPABASE_SERVICE_ROLE_KEY` OHNE `NEXT_PUBLIC_` Prefix — andernfalls landet der Key im Browser-Bundle.

**.env.local.example ergänzen** — bestehende Zeilen beibehalten, neue Zeile `SUPABASE_SERVICE_ROLE_KEY` hinzufügen:

```bash
# Supabase (Optional - remove if not using backend)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key (server-only — KEIN NEXT_PUBLIC_ Prefix!)
# Fundort: Supabase Dashboard > Project Settings > API > service_role key
# NIEMALS ins Client-Bundle — nur in API Routes / Server Components / Server Actions verwenden
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Add more environment variables as needed
# STRIPE_SECRET_KEY=sk_...
# SMTP_HOST=smtp.sendgrid.net
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_password
```
  </action>
  <verify>
    <automated>grep -c "export const supabaseAdmin" /Users/mbieling/claude/Objekterkennung/src/lib/supabase.ts</automated>
    <automated>grep -c "export const supabase" /Users/mbieling/claude/Objekterkennung/src/lib/supabase.ts</automated>
    <automated>grep -c "SUPABASE_SERVICE_ROLE_KEY" /Users/mbieling/claude/Objekterkennung/src/lib/supabase.ts</automated>
    <automated>grep -ic "next_public_supabase_service_role" /Users/mbieling/claude/Objekterkennung/src/lib/supabase.ts || echo "0 — korrekt, kein unsicherer Prefix"</automated>
    <automated>grep -c "autoRefreshToken: false" /Users/mbieling/claude/Objekterkennung/src/lib/supabase.ts</automated>
    <automated>grep -c "persistSession: false" /Users/mbieling/claude/Objekterkennung/src/lib/supabase.ts</automated>
    <automated>grep -c "SUPABASE_SERVICE_ROLE_KEY" /Users/mbieling/claude/Objekterkennung/.env.local.example</automated>
    <automated>grep -c "export const supabase = null" /Users/mbieling/claude/Objekterkennung/src/lib/supabase.ts || echo "0 — korrekt, kein Placeholder mehr"</automated>
  </verify>
  <done>
    src/lib/supabase.ts exportiert `supabase` (Anon-Client) und `supabaseAdmin` (Service Role Client). .env.local.example enthält SUPABASE_SERVICE_ROLE_KEY ohne NEXT_PUBLIC_ Prefix. Kein auskommentierter Placeholder mehr in supabase.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrations-Smoke-Test schreiben</name>
  <files>src/lib/supabase.test.ts</files>
  <read_first>
    - src/lib/supabase.ts (muss den aktivierten Client zeigen — nach Task 1 vollständig)
    - vitest.config.ts (Test-Environment und Alias-Konfiguration prüfen)
    - .planning/phases/01-database-foundation/01-PATTERNS.md (Ziel-Muster für supabase.test.ts)
  </read_first>
  <action>
Neue Datei `src/lib/supabase.test.ts` erstellen:

```typescript
// src/lib/supabase.test.ts
// Integrations-Smoke-Test für Phase 1: Database Foundation
//
// VORAUSSETZUNGEN für einen erfolgreichen Testlauf:
// 1. .env.local enthält NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
// 2. supabase/migrations/001_parts_schema.sql wurde im Supabase SQL Editor eingespielt
// 3. Die parts-Tabelle existiert in der Datenbank
//
// Ausführen: npm test -- src/lib/supabase.test.ts
import { describe, it, expect } from 'vitest'
import { supabaseAdmin } from './supabase'

describe('supabaseAdmin', () => {
  it('verbindet sich mit der parts-Tabelle ohne Fehler', async () => {
    const { data, error } = await supabaseAdmin
      .from('parts')
      .select('id')
      .limit(1)

    // Leere Tabelle ist OK — kein Fehler = Verbindung funktioniert
    expect(error).toBeNull()
    expect(Array.isArray(data)).toBe(true)
  })

  it('parts-Tabelle hat die erwarteten Spalten', async () => {
    // Prüft via information_schema ob alle Kernspalten vorhanden sind
    const { data, error } = await supabaseAdmin
      .rpc('get_parts_columns')
      .catch(() => ({ data: null, error: { message: 'RPC not available' } }))

    // Fallback: direkter Struktur-Test via leeren Insert (schlägt mit Spalten-Infos fehl)
    // Wenn RPC nicht verfügbar: manuell im Supabase SQL Editor prüfen (Smoke-Test in Plan 02 Checkpoint)
    if (error) {
      // RPC nicht vorhanden ist OK für Phase 1 — Tabellen-Erreichbarkeit ist ausreichend
      return
    }

    expect(data).not.toBeNull()
  })
})
```

**Hinweis:** Der Test ist ein Integrations-Test der eine echte Supabase-Verbindung benötigt. Er schlägt fehl wenn:
- `.env.local` nicht vorhanden oder `SUPABASE_SERVICE_ROLE_KEY` fehlt
- Die Migration noch nicht eingespielt wurde (parts-Tabelle existiert nicht)
- Das Supabase-Projekt offline ist

Das ist erwartetes Verhalten — der Test dokumentiert dass die Infrastruktur bereit ist.
  </action>
  <verify>
    <automated>test -f /Users/mbieling/claude/Objekterkennung/src/lib/supabase.test.ts && echo "OK"</automated>
    <automated>grep -c "from './supabase'" /Users/mbieling/claude/Objekterkennung/src/lib/supabase.test.ts</automated>
    <automated>grep -c "supabaseAdmin" /Users/mbieling/claude/Objekterkennung/src/lib/supabase.test.ts</automated>
    <automated>grep -c "from('parts')" /Users/mbieling/claude/Objekterkennung/src/lib/supabase.test.ts</automated>
    <automated>grep -c "toBeNull" /Users/mbieling/claude/Objekterkennung/src/lib/supabase.test.ts</automated>
  </verify>
  <done>
    src/lib/supabase.test.ts existiert mit einem Vitest-Test der `supabaseAdmin.from('parts').select('id').limit(1)` aufruft und `error === null` und `Array.isArray(data)` prüft.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Supabase-Infrastruktur manuell einrichten und Smoke-Tests bestätigen</name>
  <what-built>
    Plan 01 hat die SQL-Migrationsdatei geschrieben. Plan 02 Tasks 1–2 haben den Supabase-Client aktiviert und einen Integrations-Test erstellt. Jetzt muss der Nutzer:
    1. Das Supabase-Projekt prüfen/anlegen
    2. Die Migration einspielen
    3. Storage-Buckets anlegen
    4. .env.local setzen
    5. Den Integrations-Test ausführen
    6. Drei SQL-Queries als Smoke-Test im Supabase SQL Editor ausführen
  </what-built>
  <how-to-verify>
    **Schritt 1: Supabase-Projekt sicherstellen**
    - Supabase-Projekt vorhanden? Falls nicht: https://supabase.com > New Project anlegen
    - Project URL und API Keys bereithalten: Dashboard > Project Settings > API

    **Schritt 2: .env.local anlegen**
    Datei `.env.local` im Projektroot anlegen (oder bestehende ergänzen):
    ```bash
    NEXT_PUBLIC_SUPABASE_URL=https://[project-ref].supabase.co
    NEXT_PUBLIC_SUPABASE_ANON_KEY=[anon-key aus Dashboard]
    SUPABASE_SERVICE_ROLE_KEY=[service_role-key aus Dashboard]
    ```
    Fundort der Keys: Supabase Dashboard > Project Settings > API

    **Schritt 3: SQL-Migration einspielen**
    - Supabase Dashboard > SQL Editor öffnen
    - Inhalt von `supabase/migrations/001_parts_schema.sql` vollständig kopieren und einfügen
    - "Run" klicken — kein Fehler erwartet
    - Falls Fehler bei "vector": Supabase Dashboard > Database > Extensions > "vector" suchen und aktivieren, dann erneut versuchen

    **Schritt 4: Smoke-Test-Queries im SQL Editor ausführen**
    
    Query 1 — Schema-Struktur prüfen (muss 17 Zeilen zurückgeben):
    ```sql
    select column_name, data_type
    from information_schema.columns
    where table_name = 'parts'
    order by ordinal_position;
    ```
    Erwartet: 17 Zeilen mit den Spalten (id, name, part_number, project, status, sha256, original_filename, file_size_bytes, step_file_path, thumbnail_urls, embedding, embedding_model, embedding_version, is_archived, created_at, updated_at)

    Query 2 — pgvector und `<=>` Operator (kein Fehler = Erfolg, leere Tabelle ist OK):
    ```sql
    select id
    from parts
    where embedding is not null
    order by embedding <=> array_fill(0, ARRAY[768])::vector(768)
    limit 1;
    ```
    Erwartet: Kein "operator does not exist"-Fehler. Leeres Ergebnis ist korrekt.

    Query 3 — HNSW-Index aktiv (muss 1 Zeile mit "hnsw" zurückgeben):
    ```sql
    select indexname, indexdef
    from pg_indexes
    where tablename = 'parts'
      and indexdef ilike '%hnsw%';
    ```
    Erwartet: 1 Zeile mit `parts_embedding_hnsw_idx`

    **Schritt 5: Storage-Buckets anlegen**
    - Supabase Dashboard > Storage > New Bucket
    - Bucket 1: Name `parts-steps`, Public: OFF (privat)
    - Bucket 2: Name `parts-thumbnails`, Public: OFF (privat für Pilot)
    - Beide Buckets erscheinen danach in der Bucket-Liste mit dem Hinweis "Private"

    **Schritt 6: Integrations-Test ausführen**
    ```bash
    npm test -- src/lib/supabase.test.ts
    ```
    Erwartet: Test "verbindet sich mit der parts-Tabelle ohne Fehler" PASSED

    **Alle 6 Schritte erledigt?**
  </how-to-verify>
  <resume-signal>
    Tippe "done" wenn alle 6 Schritte abgeschlossen sind und der Test grün ist. Falls Fehler aufgetreten sind, beschreibe den Fehler und welcher Schritt fehlschlug — dann kann geholfen werden.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.local` → Next.js Server | Service Role Key darf nie in Client-Bundle landen |
| `src/lib/supabase.ts` → Client-Komponenten | supabaseAdmin darf nicht in Client-Komponenten importiert werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `SUPABASE_SERVICE_ROLE_KEY` in Client-Bundle | mitigate | Kein `NEXT_PUBLIC_` Prefix; Kommentar in supabase.ts und .env.local.example warnt explizit; Key nur über `process.env.SUPABASE_SERVICE_ROLE_KEY` (server-only) |
| T-02-02 | Information Disclosure | `supabaseAdmin` in Client-Komponente importiert | mitigate | Kommentar in supabase.ts: "SERVER-ONLY wenn supabaseAdmin verwendet wird. NIEMALS in Client-Komponenten importieren." Lint-Rule kann in Phase 10 ergänzt werden. |
| T-02-03 | Information Disclosure | `.env.local` versehentlich commitet | accept | `.gitignore` schließt `.env.local` aus (Next.js Standard); `.env.local.example` enthält nur Platzhalter |
</threat_model>

<verification>
Nach Abschluss aller Tasks und dem Checkpoint:

**Automatisiert (lokale Dateien):**
1. `grep -c "export const supabaseAdmin" src/lib/supabase.ts` → 1
2. `grep "NEXT_PUBLIC_SUPABASE_SERVICE_ROLE" src/lib/supabase.ts` → keine Ausgabe (kein unsicherer Prefix)
3. `grep -c "SUPABASE_SERVICE_ROLE_KEY" .env.local.example` → 1
4. `test -f src/lib/supabase.test.ts` → exit code 0

**Manuell (Supabase Dashboard):**
5. SQL-Migration erfolgreich eingespielt — Query 1 gibt 17 Spalten zurück
6. pgvector `<=>` Operator funktioniert — Query 2 wirft keinen Fehler
7. HNSW-Index aktiv — Query 3 gibt `parts_embedding_hnsw_idx` zurück
8. Buckets `parts-steps` und `parts-thumbnails` existieren in Supabase Storage und sind auf privat gesetzt

**Integrations-Test:**
9. `npm test -- src/lib/supabase.test.ts` → PASSED
</verification>

<success_criteria>
Die vier Phase-1-Erfolgskriterien aus ROADMAP.md sind erfüllt:
1. Supabase-Datenbank hat `parts`-Tabelle mit `embedding vector(768)` und aktivem HNSW-Index
2. pgvector-Extension ist aktiv und der `<=>` Cosine-Ähnlichkeits-Operator läuft ohne Fehler
3. Storage-Buckets `parts-steps` und `parts-thumbnails` existieren mit deaktivierten öffentlichen Zugriffen
4. Schema enthält `embedding_model` und `embedding_version` Spalten

Technisch:
- `npm test -- src/lib/supabase.test.ts` PASSED
- `src/lib/supabase.ts` exportiert `supabase` und `supabaseAdmin`
- Kein Secret in Quellcode oder `.env.local.example`
</success_criteria>

<output>
Nach Abschluss `.planning/phases/01-database-foundation/01-02-SUMMARY.md` erstellen mit:
- Aktivierte Exports: supabase (Anon-Client), supabaseAdmin (Service Role Client)
- Verifikationsstatus aller 4 ROADMAP Success Criteria
- Ergebnis des Integrations-Tests
- Erstellte Storage-Buckets
</output>
