---
phase: 03-ingestion-api-queue
plan: 04
type: execute
wave: 1
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/app/api/upload/confirm/route.ts
autonomous: true
requirements:
  - INGEST-04

must_haves:
  truths:
    - "Confirm-Request mit gültiger part_id löst HTTP-Aufruf auf Worker /enqueue aus"
    - "API antwortet mit HTTP 202 und { part_id, status: 'pending' }"
    - "Nicht-existierende part_id führt zu HTTP 404"
    - "Worker-Fehler (HTTP 5xx) führt zu HTTP 502 (nicht zu Datenverlust)"
  artifacts:
    - path: "src/app/api/upload/confirm/route.ts"
      provides: "POST /api/upload/confirm — Worker-Enqueue via HTTP + HTTP 202"
      exports: ["POST"]
      contains: "status: 202"
  key_links:
    - from: "src/app/api/upload/confirm/route.ts"
      to: "Worker FastAPI /enqueue"
      via: "fetch(process.env.WORKER_URL + '/enqueue', { method: 'POST' })"
      pattern: "fetch.*WORKER_URL.*enqueue"
    - from: "src/app/api/upload/confirm/route.ts"
      to: "Neon DB (parts)"
      via: "db`SELECT id FROM parts WHERE id = ${part_id}`"
      pattern: "SELECT.*FROM parts WHERE id"
---

<objective>
Der zweite und letzte Schritt des Upload-Flows wird implementiert: POST /api/upload/confirm verifiziert, dass die part_id existiert, und delegiert die Verarbeitungsaufgabe an den Python-Worker via HTTP POST auf /enqueue. Die Route antwortet mit HTTP 202 sobald der Worker den Job angenommen hat.

Purpose: Diese Route schließt den 2-Schritt-Upload-Flow (D-02). Der Browser ruft sie nach dem direkten S3-PUT auf und triggert damit den asynchronen Verarbeitungsprozess.

Output: src/app/api/upload/confirm/route.ts — vollständig implementiert, alle Vitest-Tests aus Plan 02 grün.
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ingestion-api-queue/03-CONTEXT.md
@.planning/phases/03-ingestion-api-queue/03-RESEARCH.md

<interfaces>
<!-- Typen und Exports die diese Route verwendet -->

Aus src/lib/db.ts:
```typescript
export const db = neon(databaseUrl)
// Verwendung: db`SELECT id, status FROM parts WHERE id = ${part_id} LIMIT 1`
```

Worker-FastAPI-Endpunkt (aus RESEARCH.md Pattern 3):
```python
# worker/main.py — POST /enqueue
# Erwartet: { "part_id": "uuid-string" }
# Antwortet: HTTP 202, { "task_id": "celery-task-id", "part_id": "uuid-string" }
```

Env-Var:
```typescript
process.env.WORKER_URL  // Default: 'http://localhost:8000' — via .env.local.example dokumentiert
```

Erwartetes Response-Format:
```typescript
// Erfolg: HTTP 202
{ part_id: string, status: 'pending' }

// Part nicht gefunden: HTTP 404
{ error: 'Part not found' }

// Worker nicht erreichbar: HTTP 502
{ error: 'Worker enqueue failed' }

// Ungültige UUID: HTTP 400
{ error: 'Invalid input', details: ZodFlattenedError }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /api/upload/confirm — Worker-Enqueue + HTTP 202</name>
  <files>src/app/api/upload/confirm/route.ts</files>
  <read_first>
    - src/lib/db.ts (Neon-Client — Tagged-Template-Literal-Syntax)
    - src/app/api/upload/confirm/route.test.ts (bereits existierende Tests — gegen diese implementieren)
    - src/app/api/upload/init/route.ts (Pattern-Analog — Zod-Validierung, Error-Responses)
    - .planning/phases/03-ingestion-api-queue/03-RESEARCH.md (Pattern 2: vollständiges Code-Beispiel, Zeilen 284–319)
  </read_first>
  <behavior>
    - Test 1: Gültige part_id + erreichbarer Worker → HTTP 202 mit `{ part_id, status: 'pending' }` und Worker wurde mit `{ part_id }` aufgerufen
    - Test 2: part_id nicht in DB → HTTP 404
    - Test 3: Worker antwortet mit 5xx → HTTP 502
    - Test 4: Ungültige UUID-part_id → HTTP 400 (Zod-Validierung)
  </behavior>
  <action>
Erstelle src/app/api/upload/confirm/route.ts mit folgendem Inhalt. KEIN "use client". Server-only.

```typescript
// src/app/api/upload/confirm/route.ts
// POST /api/upload/confirm — Schritt 3 des 2-Schritt-Upload-Flows (D-02)
// Verifiziert dass part_id existiert, dispatched Celery-Job via Worker HTTP /enqueue,
// antwortet mit HTTP 202.
// Server-only — KEIN "use client", keine Browser-Imports.

import { NextResponse } from 'next/server'
import { z } from 'zod'
import { db } from '@/lib/db'

// Zod-Schema — nur part_id nötig (Metadaten wurden bereits im Init-Request gespeichert)
const ConfirmSchema = z.object({
  part_id: z.string().uuid('part_id muss eine gültige UUID sein'),
})

export async function POST(request: Request): Promise<NextResponse> {
  // 1. Body parsen und validieren
  let body: unknown
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 })
  }

  const parsed = ConfirmSchema.safeParse(body)
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'Invalid input', details: parsed.error.flatten() },
      { status: 400 }
    )
  }

  const { part_id } = parsed.data

  // 2. Verifizieren dass der Part existiert (Schutz gegen Race Conditions)
  const parts = await db`
    SELECT id, status FROM parts WHERE id = ${part_id} LIMIT 1
  `
  if (parts.length === 0) {
    return NextResponse.json({ error: 'Part not found' }, { status: 404 })
  }

  // 3. Celery-Job über Worker-FastAPI /enqueue auslösen (D-04)
  // Worker läuft als separater Service (Docker Compose lokal / Railway prod)
  const workerUrl = process.env.WORKER_URL ?? 'http://localhost:8000'
  let workerResponse: Response
  try {
    workerResponse = await fetch(`${workerUrl}/enqueue`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ part_id }),
    })
  } catch (networkError) {
    // Netzwerkfehler: Worker nicht erreichbar
    return NextResponse.json(
      { error: 'Worker enqueue failed', detail: 'Worker unreachable' },
      { status: 502 }
    )
  }

  if (!workerResponse.ok) {
    return NextResponse.json(
      { error: 'Worker enqueue failed' },
      { status: 502 }
    )
  }

  // 4. HTTP 202 Accepted — Job ist in der Queue, Verarbeitung läuft asynchron
  return NextResponse.json({ part_id, status: 'pending' }, { status: 202 })
}
```

Wichtige Implementierungsdetails:
- Kein DB-UPDATE auf status in confirm — Worker setzt status von 'pending' → 'processing' → 'ready'/'failed'
- fetch() wird in try/catch gewrappt — Netzwerkfehler (Worker nicht erreichbar) ergeben HTTP 502, nicht 500
- WORKER_URL hat Default 'http://localhost:8000' — für lokale Entwicklung ohne Docker (Env-Var-Konvention aus PATTERNS.md)
- Keine Binärdaten in dieser Route — nur JSON-Dispatch (D-01: Binärdaten gehen direkt zu S3)
  </action>
  <verify>
    <automated>cd /Users/mbieling/claude/Objekterkennung && npm test -- --run src/app/api/upload/confirm/route.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/app/api/upload/confirm/route.test.ts` → alle 4 Tests grün, kein FAILED
    - `npm test -- --run src/app/api/upload/` → alle Tests (init + confirm) grün
    - `grep -c "export async function POST" src/app/api/upload/confirm/route.ts` gibt `1` zurück
    - `grep -c "use client" src/app/api/upload/confirm/route.ts` gibt `0` zurück
    - `grep -c "status: 202" src/app/api/upload/confirm/route.ts` gibt `1` zurück
    - `grep -c "status: 502" src/app/api/upload/confirm/route.ts` gibt mindestens `1` zurück (zwei Fehlerszenarien)
    - `grep -c "WORKER_URL" src/app/api/upload/confirm/route.ts` gibt `1` zurück
    - `grep -c "NEXT_PUBLIC_" src/app/api/upload/confirm/route.ts` gibt `0` zurück
  </acceptance_criteria>
  <done>POST /api/upload/confirm ist implementiert. Alle 4 Vitest-Tests laufen grün. HTTP 202 bei erfolgreichem Worker-Dispatch, HTTP 404 bei fehlender part_id, HTTP 502 bei Worker-Fehler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/upload/confirm | Nicht-authentifizierter Request mit part_id |
| /api/upload/confirm → Worker FastAPI | Interner HTTP-Aufruf — WORKER_URL darf nicht öffentlich exponiert werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | part_id in Confirm-Request | mitigate | Zod `z.string().uuid()` — ungültige UUIDs → HTTP 400; zusätzlich DB-Lookup als zweite Schicht |
| T-03-11 | Tampering | Celery-Task-Injection via part_id | mitigate | Zod-UUID-Validierung in TypeScript (Confirm) + Pydantic UUID4 in Worker /enqueue + UUID_RE-Regex in process() — drei Verteidigungsschichten |
| T-03-12 | Denial of Service | Worker-URL als SSRF-Vektor | accept | WORKER_URL ist eine server-only Env-Var; Browser hat keine Kontrolle über die Ziel-URL. Für Pilot akzeptabel. |
</threat_model>

<verification>
```bash
# Beide Route-Dateien existieren
ls /Users/mbieling/claude/Objekterkennung/src/app/api/upload/init/route.ts \
   /Users/mbieling/claude/Objekterkennung/src/app/api/upload/confirm/route.ts

# Alle Upload-Tests grün
cd /Users/mbieling/claude/Objekterkennung && npm test -- --run src/app/api/upload/

# TypeScript-Check
cd /Users/mbieling/claude/Objekterkennung && npx tsc --noEmit 2>&1 | grep "upload"
```

Erwartetes Ergebnis: Alle 9 Tests (5 init + 4 confirm) grün, keine TypeScript-Fehler.
</verification>

<success_criteria>
1. src/app/api/upload/confirm/route.ts existiert und exportiert `POST`
2. Alle 4 Tests in confirm.test.ts sind grün
3. Kombinierter Lauf: `npm test -- --run src/app/api/upload/` zeigt 9 Tests grün
4. HTTP 202 mit `{ part_id, status: 'pending' }` bei erfolgreichem Worker-Dispatch
5. HTTP 404 wenn part_id nicht in DB existiert
6. HTTP 502 wenn Worker nicht erreichbar oder mit Fehler antwortet
7. `grep -c "use client" route.ts` = 0, `grep -c "NEXT_PUBLIC_" route.ts` = 0
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/03-ingestion-api-queue/03-04-SUMMARY.md` erstellen.
</output>
