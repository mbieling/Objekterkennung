---
phase: 03-ingestion-api-queue
plan: 03
type: execute
wave: 1
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/app/api/upload/init/route.ts
autonomous: true
requirements:
  - INGEST-04

must_haves:
  truths:
    - "Zweiter Upload derselben SHA-256 wird mit HTTP 409 und existing_part_id abgewiesen"
    - "Gültiger Init-Request legt parts-Eintrag mit status='pending' an und gibt presigned_url zurück"
    - "Nur 'name' ist Pflichtfeld — part_number und project sind optional"
    - "file_size_bytes > 100 MB wird mit HTTP 400 abgewiesen"
  artifacts:
    - path: "src/app/api/upload/init/route.ts"
      provides: "POST /api/upload/init — SHA-256-Dedup + DB-Insert + Presigned S3 URL"
      exports: ["POST"]
      contains: "status: 409"
    - path: "src/app/api/upload/init/route.ts"
      provides: "Presigned S3 PUT-URL mit 900s Ablauf"
      contains: "getSignedUrl"
  key_links:
    - from: "src/app/api/upload/init/route.ts"
      to: "Neon DB (parts)"
      via: "db`SELECT sha256` + db`INSERT INTO parts`"
      pattern: "SELECT.*sha256.*FROM parts"
    - from: "src/app/api/upload/init/route.ts"
      to: "AWS S3 (BUCKET_STEPS)"
      via: "getSignedUrl(s3, PutObjectCommand)"
      pattern: "getSignedUrl"
---

<objective>
Die erste API-Route des 2-Schritt-Upload-Flows wird implementiert: POST /api/upload/init nimmt Metadaten und SHA-256-Hash entgegen, prüft auf Duplikate, legt einen parts-DB-Eintrag mit status='pending' an, und gibt eine signierte S3-PUT-URL zurück.

Purpose: Diese Route ist der Einstiegspunkt der gesamten Ingestion-Pipeline. Sie verhindert redundante Verarbeitung (INGEST-04) und entkoppelt den Datei-Upload vom Next.js-Server (D-01).

Output: src/app/api/upload/init/route.ts — vollständig implementiert, alle Vitest-Tests aus Plan 02 grün.
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ingestion-api-queue/03-CONTEXT.md
@.planning/phases/03-ingestion-api-queue/03-RESEARCH.md

<interfaces>
<!-- Typen und Exports die diese Route verwendet — aus bestehenden Dateien extrahiert -->

Aus src/lib/db.ts:
```typescript
export const db = neon(databaseUrl)
// Verwendung: db`SELECT id FROM parts WHERE sha256 = ${sha256} LIMIT 1`
// Gibt Promise<NeonQueryResult[]> zurück — Rows als Array von Objekten
```

Aus src/lib/s3.ts:
```typescript
export const s3: S3Client
export const BUCKET_STEPS: string   // z.B. 'parts-steps'
export const BUCKET_THUMBNAILS: string
// S3-Pfadkonvention: `${part_id}/original.step`
```

DB-Schema (aus supabase/migrations/001_parts_schema.sql):
```sql
CREATE TABLE parts (
  id                uuid         DEFAULT gen_random_uuid() PRIMARY KEY,
  name              text         NOT NULL,
  part_number       text,                    -- optional (D-08)
  project           text,                    -- optional (D-08)
  status            text         NOT NULL DEFAULT 'pending',
  sha256            text         NOT NULL,
  original_filename text         NOT NULL,
  file_size_bytes   bigint,
  step_file_path    text,
  thumbnail_urls    text[],
  embedding         vector(768),
  embedding_model   text,
  embedding_version text,
  is_archived       boolean      DEFAULT false,
  created_at        timestamptz  DEFAULT now(),
  updated_at        timestamptz  DEFAULT now()
);
-- Index: parts_sha256_idx ON parts(sha256)  ← für Duplikat-Lookup
```

Erwartetes Response-Format (aus RESEARCH.md Pattern 1):
```typescript
// Erfolg: HTTP 200
{ part_id: string, presigned_url: string }

// Duplikat: HTTP 409
{ error: 'Duplicate file', existing_part_id: string }

// Validierungsfehler: HTTP 400
{ error: 'Invalid input', details: ZodFlattenedError }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /api/upload/init — SHA-256-Dedup + DB-Insert + Presigned URL</name>
  <files>src/app/api/upload/init/route.ts</files>
  <read_first>
    - src/lib/db.ts (Neon-Client-Export — tagged template literal)
    - src/lib/s3.ts (S3-Client + BUCKET_STEPS Export)
    - src/app/api/upload/init/route.test.ts (bereits existierende Tests — gegen diese implementieren)
    - .planning/phases/03-ingestion-api-queue/03-RESEARCH.md (Pattern 1: vollständiges Code-Beispiel, Zeilen 222–278)
    - supabase/migrations/001_parts_schema.sql (exaktes Schema — Spaltennamen prüfen)
  </read_first>
  <behavior>
    - Test 1: SHA-256 bereits in DB → HTTP 409 mit `{ error: 'Duplicate file', existing_part_id: '...' }`
    - Test 2: Gültiger Request → HTTP 200 mit `{ part_id: uuid, presigned_url: 'https://...' }`
    - Test 3: Leeres `name`-Feld → HTTP 400
    - Test 4: sha256 nicht 64 Hex-Zeichen → HTTP 400
    - Test 5: file_size_bytes > 104857600 (100 MB) → HTTP 400
  </behavior>
  <action>
Erstelle src/app/api/upload/init/route.ts mit folgendem Inhalt. KEIN "use client". Server-only.

```typescript
// src/app/api/upload/init/route.ts
// POST /api/upload/init — Schritt 1 des 2-Schritt-Upload-Flows (D-02)
// Nimmt Metadaten + SHA-256, prüft Duplikate (INGEST-04), legt DB-Eintrag an,
// gibt Presigned S3 PUT-URL zurück.
// Server-only — KEIN "use client", keine Browser-Imports.

import { NextResponse } from 'next/server'
import { PutObjectCommand } from '@aws-sdk/client-s3'
import { getSignedUrl } from '@aws-sdk/s3-request-presigner'
import { z } from 'zod'
import { db } from '@/lib/db'
import { s3, BUCKET_STEPS } from '@/lib/s3'

// Zod-Schema für den Init-Request-Body (D-07, D-08)
const InitSchema = z.object({
  name: z.string().min(1, 'name ist Pflichtfeld').max(255),
  sha256: z
    .string()
    .length(64, 'sha256 muss 64 Hex-Zeichen lang sein')
    .regex(/^[0-9a-f]+$/i, 'sha256 muss hexadezimal sein'),
  original_filename: z.string().min(1).max(255),
  file_size_bytes: z
    .number()
    .int()
    .positive()
    .max(100 * 1024 * 1024, 'Maximale Dateigröße: 100 MB'),
  part_number: z.string().max(100).optional(),
  project: z.string().max(255).optional(),
})

export async function POST(request: Request): Promise<NextResponse> {
  // 1. Body parsen und validieren
  let body: unknown
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 })
  }

  const parsed = InitSchema.safeParse(body)
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'Invalid input', details: parsed.error.flatten() },
      { status: 400 }
    )
  }

  const { name, sha256, original_filename, file_size_bytes, part_number, project } = parsed.data

  // 2. SHA-256-Duplikat-Prüfung (INGEST-04) — vor jedem S3-Aufruf
  const existing = await db`
    SELECT id FROM parts WHERE sha256 = ${sha256} LIMIT 1
  `
  if (existing.length > 0) {
    return NextResponse.json(
      { error: 'Duplicate file', existing_part_id: existing[0].id },
      { status: 409 }
    )
  }

  // 3. parts-Eintrag anlegen (status='pending')
  const [part] = await db`
    INSERT INTO parts (
      name, sha256, original_filename, file_size_bytes,
      part_number, project, status, step_file_path
    )
    VALUES (
      ${name}, ${sha256}, ${original_filename}, ${file_size_bytes},
      ${part_number ?? null}, ${project ?? null}, 'pending', ${''}
    )
    RETURNING id
  `

  // 4. Presigned S3 PUT-URL generieren (900s = 15 Minuten — ausreichend für 100 MB)
  // Content-Type NICHT in signableHeaders — verhindert Content-Type-Mismatch (Pitfall 1)
  const presignedUrl = await getSignedUrl(
    s3,
    new PutObjectCommand({
      Bucket: BUCKET_STEPS,
      Key: `${part.id}/original.step`,
      ContentType: 'application/octet-stream',
    }),
    { expiresIn: 900 }
  )

  return NextResponse.json({ part_id: part.id, presigned_url: presignedUrl })
}
```

Wichtige Implementierungsdetails:
- SHA-256-Prüfung kommt VOR dem DB-Insert (D-03) — kein unnötiger Insert bei Duplikat
- `step_file_path` wird mit leerem String initialisiert (NOT NULL-Constraint im Schema, aber kein sinnvoller Wert vor S3-Upload)
- `part_number` und `project` sind optional (D-08) — `?? null` für saubere NULL-Speicherung
- Kein `signableHeaders: new Set(["content-type"])` — ContentType in der Signatur würde Pitfall 1 auslösen
- Kein explizites `ioredis` hier — Confirm-Route handelt Queue-Dispatch
  </action>
  <verify>
    <automated>cd /Users/mbieling/claude/Objekterkennung && npm test -- --run src/app/api/upload/init/route.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/app/api/upload/init/route.test.ts` → alle 5 Tests grün, kein FAILED
    - `grep -c "export async function POST" src/app/api/upload/init/route.ts` gibt `1` zurück
    - `grep -c "use client" src/app/api/upload/init/route.ts` gibt `0` zurück (server-only)
    - `grep -c "getSignedUrl" src/app/api/upload/init/route.ts` gibt `1` zurück
    - `grep -c "status: 409" src/app/api/upload/init/route.ts` gibt `1` zurück
    - `grep -c "Duplicate file" src/app/api/upload/init/route.ts` gibt `1` zurück
    - `grep -c "NEXT_PUBLIC_" src/app/api/upload/init/route.ts` gibt `0` zurück (keine Browser-Vars)
    - `grep -c "expiresIn: 900" src/app/api/upload/init/route.ts` gibt `1` zurück
  </acceptance_criteria>
  <done>POST /api/upload/init ist implementiert. Alle 5 Vitest-Tests aus init.test.ts laufen grün. HTTP 409 bei SHA-256-Duplikat, HTTP 200 mit part_id + presigned_url bei gültigem Request.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/upload/init | Nicht-authentifizierter Request mit Metadaten und SHA-256 |
| /api/upload/init → Neon DB | Server-seitiger DB-Aufruf mit parametrisierten Queries |
| /api/upload/init → AWS S3 | Server-seitige URL-Signierung — AWS-Credentials bleiben server-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Spoofing | SHA-256-Feld in Request-Body | accept | Browser-berechneter Hash — für Pilot akzeptabel (interne Nutzer); Phase 10 Hardening: S3 Checksum-Verifikation |
| T-03-07 | Tampering | Zod-Validierung in init/route.ts | mitigate | `z.string().length(64).regex(/^[0-9a-f]+$/i)` — verhindert injection über sha256-Feld; `z.number().max(100MB)` — verhindert oversized requests |
| T-03-08 | Elevation of Privilege | Presigned URL Missbrauch | mitigate | `expiresIn: 900` (15 Minuten) — begrenzt Missbrauchs-Fenster; URL ist spezifisch für `{part_id}/original.step` |
| T-03-09 | Information Disclosure | AWS-Credentials in Response | mitigate | Credentials sind ausschließlich in process.env (server-only); `getSignedUrl` gibt nur die signierte URL zurück, keine Credentials |
</threat_model>

<verification>
```bash
# Route-Datei existiert
ls /Users/mbieling/claude/Objekterkennung/src/app/api/upload/init/route.ts

# Server-only-Check (kein "use client")
grep -c "use client" /Users/mbieling/claude/Objekterkennung/src/app/api/upload/init/route.ts

# Tests grün
cd /Users/mbieling/claude/Objekterkennung && npm test -- --run src/app/api/upload/init/route.test.ts

# TypeScript-Compile-Check
cd /Users/mbieling/claude/Objekterkennung && npx tsc --noEmit 2>&1 | grep "upload/init"
```
</verification>

<success_criteria>
1. src/app/api/upload/init/route.ts existiert und exportiert `POST`
2. Alle 5 Tests in init.test.ts sind grün
3. HTTP 409 mit `existing_part_id` bei Duplikat-SHA-256
4. HTTP 200 mit `part_id` (UUID) und `presigned_url` (https://) bei gültigem Request
5. `grep -c "use client" route.ts` = 0 (server-only)
6. `grep -c "NEXT_PUBLIC_" route.ts` = 0 (keine Browser-Vars)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/03-ingestion-api-queue/03-03-SUMMARY.md` erstellen.
</output>
