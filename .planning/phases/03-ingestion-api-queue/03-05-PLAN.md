---
phase: 03-ingestion-api-queue
plan: 05
type: execute
wave: 2
depends_on:
  - "03-03"
  - "03-04"
files_modified:
  - worker/celery_app.py
  - worker/tasks.py
  - worker/main.py
  - worker/requirements.txt
autonomous: true
requirements:
  - INGEST-04

must_haves:
  truths:
    - "FastAPI /health gibt { status: 'ok' } zurück"
    - "FastAPI /enqueue nimmt { part_id: uuid } entgegen und schiebt Celery-Task in Redis-Queue"
    - "Celery-Worker konsumiert Task und ruft process_step.process(part_id) auf"
    - "VTK_DEFAULT_OPENGL_WINDOW ist erste Zeile in tasks.py (vor allen anderen Imports)"
  artifacts:
    - path: "worker/celery_app.py"
      provides: "Celery-Instanz mit Redis-Broker und task_acks_late=True"
      contains: "celery_app = Celery"
    - path: "worker/tasks.py"
      provides: "process_step_task Celery-Task-Wrapper"
      contains: "process_step_task"
    - path: "worker/main.py"
      provides: "FastAPI-App mit /health und /enqueue Endpunkten"
      contains: "app = FastAPI()"
    - path: "worker/requirements.txt"
      provides: "fastapi, uvicorn, celery, redis Abhängigkeiten"
      contains: "fastapi"
  key_links:
    - from: "worker/main.py:/enqueue"
      to: "worker/tasks.py:process_step_task"
      via: "process_step_task.delay(str(req.part_id))"
      pattern: "process_step_task\\.delay"
    - from: "worker/tasks.py:process_step_task"
      to: "worker/process_step.py:process()"
      via: "process(part_id)"
      pattern: "from worker\\.process_step import process"
    - from: "worker/celery_app.py"
      to: "Redis (CELERY_BROKER_URL)"
      via: "Celery(broker=BROKER_URL)"
      pattern: "CELERY_BROKER_URL"
---

<objective>
Der Python-Worker wird von einem einfachen Spike-Skript zu einem vollständigen Microservice erweitert: FastAPI-App mit /health und /enqueue Endpunkten, Celery-Task-Wrapper für die STEP-Verarbeitungs-Pipeline, und Celery-Konfiguration für Redis-Broker.

Purpose: Wave-2-Arbeit — der Worker muss die Celery-Queue konsumieren können bevor Docker Compose in Wave 3 alles zusammenfügt. Die FastAPI-App ist der Endpunkt den die Confirm-Route (Wave 1) aufruft.

Output: Vier Worker-Dateien (celery_app.py, tasks.py, main.py, aktualisierte requirements.txt) — bereit für Docker-Compose-Integration.
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ingestion-api-queue/03-CONTEXT.md
@.planning/phases/03-ingestion-api-queue/03-RESEARCH.md
@.planning/phases/02-python-worker-spike/02-CONTEXT.md

<interfaces>
<!-- Bestehende Worker-Signaturen die diese Dateien importieren/verwenden -->

Aus worker/process_step.py (nach CR-01-Fix):
```python
def validate_part_id(part_id: str) -> str: ...  # UUID-Validierung
def process(part_id: str) -> None: ...  # Vollständige Pipeline — pending→processing→ready/failed
```

Aus worker/process_step.py (Env-Var-Muster):
```python
import os
os.environ["VTK_DEFAULT_OPENGL_WINDOW"] = "vtkOSOpenGLRenderWindow"
# MUSS ERSTE ZEILE SEIN — vor allen anderen Imports (Pitfall 3)
BUCKET_STEPS = os.environ["AWS_S3_BUCKET_STEPS"]   # KeyError bei fehlender Var
```

Celery-Konfiguration (aus RESEARCH.md Pattern 3):
```python
# CELERY_BROKER_URL: 'redis://redis:6379/0' (lokal) oder 'rediss://...' (Upstash)
# task_acks_late=True: Task erst nach Abschluss aus Queue entfernen
# worker_prefetch_multiplier=1: Nur 1 Task reservieren (CPU-intensiv)
# max_retries=0: Fehler → 'failed' in DB schreiben, kein Auto-Retry
```

FastAPI /enqueue Request/Response:
```python
# POST /enqueue
# Request: { "part_id": "uuid-string" }  — Pydantic UUID4 validiert
# Response: HTTP 202, { "task_id": "celery-task-id", "part_id": "uuid-string" }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: worker/celery_app.py + worker/tasks.py erstellen</name>
  <files>
    worker/celery_app.py
    worker/tasks.py
  </files>
  <read_first>
    - worker/process_step.py (Zeilen 1–35: VTK-Env-Var-Muster + Logging-Muster + Env-Var-Konvention)
    - .planning/phases/03-ingestion-api-queue/03-RESEARCH.md (Pattern 3: celery_app.py + tasks.py Code, Zeilen 326–362)
    - .planning/phases/03-ingestion-api-queue/03-PATTERNS.md (Abschnitte "celery_app.py" und "tasks.py")
  </read_first>
  <action>
Erstelle zwei neue Dateien im worker/-Verzeichnis.

**worker/celery_app.py**:
```python
# worker/celery_app.py
# Celery-Instanz-Konfiguration für Bauteil-Finder Worker
# Broker: Redis (lokal: redis://redis:6379/0 | prod: rediss://... Upstash)
import os
from celery import Celery

BROKER_URL = os.environ["CELERY_BROKER_URL"]         # KeyError wenn fehlend — explizit
RESULT_BACKEND = os.environ.get("CELERY_RESULT_BACKEND", BROKER_URL)  # Default = Broker

celery_app = Celery("bauteil_finder", broker=BROKER_URL, backend=RESULT_BACKEND)
celery_app.conf.update(
    task_serializer="json",
    accept_content=["json"],
    result_serializer="json",
    timezone="UTC",
    enable_utc=True,
    task_acks_late=True,            # Task erst nach erfolgreichem Abschluss aus Queue entfernen
    worker_prefetch_multiplier=1,   # Nur 1 Task gleichzeitig reservieren (STEP-Processing ist CPU-intensiv)
)
```

**worker/tasks.py**:
```python
# worker/tasks.py
# Celery-Task-Wrapper für STEP-Verarbeitungs-Pipeline
#
# WARNUNG: Die folgende Zeile MUSS GANZ OBEN stehen — vor allen anderen Imports.
# Celery lädt tasks.py beim Worker-Start; process_step.py importiert OCC-Module transitiv.
# Ohne diese Zeile initialisiert VTK einen Display-basierten Window-Manager → Crash.
import os
os.environ["VTK_DEFAULT_OPENGL_WINDOW"] = "vtkOSOpenGLRenderWindow"

import logging
from worker.celery_app import celery_app
from worker.process_step import process

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s %(levelname)s [%(name)s] %(message)s",
)
logger = logging.getLogger(__name__)


@celery_app.task(name="worker.tasks.process_step", bind=True, max_retries=0)
def process_step_task(self, part_id: str) -> None:
    """Celery-Task-Wrapper für process_step.process().

    max_retries=0: Fehler werden als 'failed' in DB geschrieben — kein automatischer Retry.
    Manueller Retry erfolgt über Admin-Katalog (Phase 5).

    Args:
        part_id: UUID des parts-Eintrags (wird von FastAPI /enqueue übergeben)
    """
    logger.info(f"[{part_id}] Celery-Task gestartet")
    try:
        process(part_id)
        logger.info(f"[{part_id}] Celery-Task abgeschlossen")
    except Exception as e:
        # process() setzt bereits status='failed' in der DB bei Exceptions.
        # Hier nur Logging — kein Re-Raise damit Celery den Task als SUCCESS markiert
        # (der Fehler ist bereits in der DB dokumentiert).
        logger.error(f"[{part_id}] Celery-Task fehlgeschlagen: {e}")
```

KRITISCH: In tasks.py MUSS `os.environ["VTK_DEFAULT_OPENGL_WINDOW"] = "vtkOSOpenGLRenderWindow"` die ersten zwei ausführbaren Zeilen sein (nach dem Kommentar-Block und dem `import os`). Jede andere Reihenfolge kann zu einem Crash beim Worker-Start führen.
  </action>
  <verify>
    <automated>grep -n "VTK_DEFAULT_OPENGL_WINDOW" /Users/mbieling/claude/Objekterkennung/worker/tasks.py | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "VTK_DEFAULT_OPENGL_WINDOW" worker/tasks.py` zeigt die Zeile als eine der ersten 10 Zeilen (vor allen Import-Statements außer `import os`)
    - `grep -c "celery_app = Celery" worker/celery_app.py` gibt `1` zurück
    - `grep -c "task_acks_late=True" worker/celery_app.py` gibt `1` zurück
    - `grep -c "worker_prefetch_multiplier=1" worker/celery_app.py` gibt `1` zurück
    - `grep -c "CELERY_BROKER_URL" worker/celery_app.py` gibt `1` zurück (os.environ["CELERY_BROKER_URL"])
    - `grep -c "def process_step_task" worker/tasks.py` gibt `1` zurück
    - `grep -c "max_retries=0" worker/tasks.py` gibt `1` zurück
    - `grep -c "from worker.process_step import process" worker/tasks.py` gibt `1` zurück
    - `python -m py_compile worker/celery_app.py` gibt keinen Fehler zurück
    - `python -m py_compile worker/tasks.py` gibt keinen Fehler zurück
  </acceptance_criteria>
  <done>celery_app.py konfiguriert Celery-Instanz mit Redis-Broker und task_acks_late=True. tasks.py definiert process_step_task mit VTK-Guard als erste Zeile und max_retries=0.</done>
</task>

<task type="auto">
  <name>Task 2: worker/main.py (FastAPI) + worker/requirements.txt aktualisieren</name>
  <files>
    worker/main.py
    worker/requirements.txt
  </files>
  <read_first>
    - worker/requirements.txt (vollständig lesen — bestehende Dependencies nicht löschen)
    - worker/tasks.py (muss für Import in main.py vorhanden sein)
    - .planning/phases/03-ingestion-api-queue/03-RESEARCH.md (Pattern 3: api.py/main.py Code, Zeilen 363–384; Standard Stack Python, Zeilen 96–133)
    - .planning/phases/03-ingestion-api-queue/03-PATTERNS.md (Abschnitt "worker/main.py")
  </read_first>
  <action>
**worker/main.py** (neue Datei):
```python
# worker/main.py
# FastAPI-Endpunkte für Bauteil-Finder Worker
# Läuft als: uvicorn worker.main:app --host 0.0.0.0 --port 8000
# Endpunkte:
#   GET  /health  — Health-Check für Docker Compose + Monitoring
#   POST /enqueue — Nimmt part_id entgegen, schiebt Celery-Task in Redis-Queue

import logging
from fastapi import FastAPI
from pydantic import BaseModel, UUID4

from worker.tasks import process_step_task

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s %(levelname)s [%(name)s] %(message)s",
)
logger = logging.getLogger(__name__)

app = FastAPI(title="Bauteil-Finder Worker", version="1.0.0")


class EnqueueRequest(BaseModel):
    """Request-Body für POST /enqueue.

    part_id wird als UUID4 validiert (Pydantic) — zweite Verteidigungslinie nach CR-01-Fix
    und Zod-UUID-Validierung in der Next.js Confirm-Route.
    """
    part_id: UUID4


@app.get("/health")
def health() -> dict:
    """Health-Check-Endpunkt.

    Antwortet mit HTTP 200 wenn der FastAPI-Server läuft.
    Celery-Worker-Status wird hier nicht geprüft — separater Monitoring-Endpunkt wenn nötig.
    """
    return {"status": "ok"}


@app.post("/enqueue", status_code=202)
def enqueue(req: EnqueueRequest) -> dict:
    """Nimmt part_id entgegen und schiebt Celery-Task in die Redis-Queue.

    Die eigentliche STEP-Verarbeitung läuft asynchron im Celery-Worker.
    HTTP 202 bedeutet: Job wurde akzeptiert und wird verarbeitet.
    """
    part_id_str = str(req.part_id)
    logger.info(f"[{part_id_str}] Job in Queue eingereiht")
    task = process_step_task.delay(part_id_str)
    return {"task_id": task.id, "part_id": part_id_str}
```

**worker/requirements.txt** — füge neue Pakete hinzu (bestehende Zeilen unverändert):

LESE die bestehende requirements.txt zuerst. Dann füge am Ende folgende Zeilen an:

```
# Phase 3: FastAPI Worker-Endpunkte + Celery Queue
fastapi>=0.136.0,<1.0.0
uvicorn>=0.30.0,<1.0.0
celery>=5.6.0,<6.0.0
redis>=7.0.0,<8.0.0
```

HINWEIS: Prüfe ob fastapi, uvicorn, celery oder redis bereits in requirements.txt stehen. Wenn ja, überspringe das Hinzufügen dieser Zeilen (keine Duplikate).
  </action>
  <verify>
    <automated>grep -c "app = FastAPI" /Users/mbieling/claude/Objekterkennung/worker/main.py && grep -c "fastapi" /Users/mbieling/claude/Objekterkennung/worker/requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "app = FastAPI()" worker/main.py` gibt `1` zurück
    - `grep -c "def health" worker/main.py` gibt `1` zurück
    - `grep -c "def enqueue" worker/main.py` gibt `1` zurück
    - `grep -c "process_step_task.delay" worker/main.py` gibt `1` zurück
    - `grep -c "UUID4" worker/main.py` gibt `1` zurück (Pydantic-UUID-Validierung)
    - `grep -c "fastapi" worker/requirements.txt` gibt mindestens `1` zurück
    - `grep -c "celery" worker/requirements.txt` gibt mindestens `1` zurück
    - `grep -c "uvicorn" worker/requirements.txt` gibt mindestens `1` zurück
    - `python -m py_compile worker/main.py` gibt keinen Fehler zurück
    - Der Status-Code 202 ist in der `/enqueue`-Dekorator-Signatur: `@app.post("/enqueue", status_code=202)`
  </acceptance_criteria>
  <done>main.py definiert FastAPI-App mit /health (HTTP 200) und /enqueue (HTTP 202). requirements.txt enthält fastapi, uvicorn, celery und redis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Next.js Confirm-Route → Worker /enqueue | Interner HTTP-Aufruf — kein direkter Internetzugang zum Worker erwartet |
| Worker /enqueue → Celery-Queue (Redis) | part_id wird in Redis-Queue geschrieben und vom Celery-Worker konsumiert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Tampering | Worker /enqueue: part_id | mitigate | Pydantic UUID4 in FastAPI — dritte Schicht nach Zod (Confirm-Route) und UUID_RE (process_step.py) |
| T-03-14 | Denial of Service | Celery-Worker: CPU-Verbrauch | mitigate | worker_prefetch_multiplier=1 — nur ein Task gleichzeitig; verhindert Resource-Exhaustion durch parallele STEP-Verarbeitungen |
| T-03-15 | Information Disclosure | CELERY_BROKER_URL in Env | mitigate | os.environ["CELERY_BROKER_URL"] (KeyError wenn fehlend) — kein Hardcoding; worker/.env in .gitignore |
</threat_model>

<verification>
```bash
# Alle vier Dateien vorhanden
ls /Users/mbieling/claude/Objekterkennung/worker/celery_app.py \
   /Users/mbieling/claude/Objekterkennung/worker/tasks.py \
   /Users/mbieling/claude/Objekterkennung/worker/main.py

# Syntax-Check für alle neuen Dateien
python -m py_compile /Users/mbieling/claude/Objekterkennung/worker/celery_app.py && echo "celery_app.py OK"
python -m py_compile /Users/mbieling/claude/Objekterkennung/worker/tasks.py && echo "tasks.py OK"
python -m py_compile /Users/mbieling/claude/Objekterkennung/worker/main.py && echo "main.py OK"

# VTK-Guard ist erste ausführbare Zeile in tasks.py
grep -n "VTK_DEFAULT_OPENGL_WINDOW\|import " /Users/mbieling/claude/Objekterkennung/worker/tasks.py | head -8

# Requirements erweitert
grep -E "fastapi|celery|uvicorn|redis" /Users/mbieling/claude/Objekterkennung/worker/requirements.txt
```
</verification>

<success_criteria>
1. worker/celery_app.py: Celery-Instanz mit CELERY_BROKER_URL, task_acks_late=True, worker_prefetch_multiplier=1
2. worker/tasks.py: VTK_DEFAULT_OPENGL_WINDOW als erste Zeile, process_step_task mit max_retries=0
3. worker/main.py: FastAPI-App mit /health (200) und /enqueue (202) — Pydantic UUID4 Validierung
4. worker/requirements.txt: fastapi, uvicorn, celery, redis hinzugefügt ohne bestehende Deps zu löschen
5. Alle drei neuen Python-Dateien: `python -m py_compile` ohne Fehler
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/03-ingestion-api-queue/03-05-SUMMARY.md` erstellen.
</output>
