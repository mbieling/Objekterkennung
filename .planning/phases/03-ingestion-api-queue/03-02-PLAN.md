---
phase: 03-ingestion-api-queue
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - src/app/api/upload/init/route.test.ts
  - src/app/api/upload/confirm/route.test.ts
  - .env.local.example
  - worker/.env.example
autonomous: true
requirements:
  - INGEST-04

must_haves:
  truths:
    - "Vitest-Testdateien für init- und confirm-Route sind vorhanden und laufen grün (gegen Mocks)"
    - ".env.local.example enthält WORKER_URL und UPSTASH_REDIS_URL mit Kommentaren"
    - "worker/.env.example enthält CELERY_BROKER_URL und CELERY_RESULT_BACKEND"
  artifacts:
    - path: "src/app/api/upload/init/route.test.ts"
      provides: "Unit-Tests: SHA-256-Duplikat → 409, gültiger Init → 200"
      contains: "HTTP 409"
    - path: "src/app/api/upload/confirm/route.test.ts"
      provides: "Unit-Tests: Confirm → 202, Worker-Enqueue-Mock"
      contains: "status: 202"
    - path: ".env.local.example"
      provides: "WORKER_URL-Dokumentation"
      contains: "WORKER_URL"
    - path: "worker/.env.example"
      provides: "CELERY_BROKER_URL-Dokumentation"
      contains: "CELERY_BROKER_URL"
  key_links:
    - from: "src/app/api/upload/init/route.test.ts"
      to: "src/app/api/upload/init/route.ts"
      via: "vi.mock('@/lib/db') + vi.mock('@/lib/s3')"
      pattern: "vi\\.mock\\('@/lib/db'\\)"
---

<objective>
Vitest-Test-Stubs für die Upload-API-Routes werden erstellt, damit Wave 1 sofort gegen Tests implementieren kann. Außerdem werden die Env-Beispieldateien um die neuen Phase-3-Variablen erweitert.

Purpose: Wave-0-Aufgabe gemäß VALIDATION.md — Tests müssen vor der Implementierung existieren (Nyquist-Regel). Die Env-Dateien dokumentieren, was der Entwickler konfigurieren muss.

Output: Zwei Testdateien mit Mock-Infrastruktur (co-located neben den Route-Dateien per CLAUDE.md-Konvention), aktualisierte .env.local.example und worker/.env.example.
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ingestion-api-queue/03-RESEARCH.md
@.planning/phases/03-ingestion-api-queue/03-VALIDATION.md

<interfaces>
<!-- Bestehende Testinfrastruktur aus vitest.config.ts -->

Aus vitest.config.ts (Kontext aus PATTERNS.md):
```typescript
// environment: 'jsdom' — Web APIs (fetch, crypto) sind verfügbar
// setupFiles: ['./src/test/setup.ts']
// resolve alias: '@' → './src' (import '@/lib/db' funktioniert)
// globals: true — describe/it/expect ohne Import
```

Bestehende Neon-Client-Typen aus src/lib/db.ts:
```typescript
export const db = neon(databaseUrl)
// db ist Tagged-Template-Literal: db`SELECT ...` → Promise<NeonQueryResult[]>
```

Bestehende S3-Client-Typen aus src/lib/s3.ts:
```typescript
export const s3: S3Client
export const BUCKET_STEPS: string
export const BUCKET_THUMBNAILS: string
```

Bestehende .env.local.example-Vars (bereits vorhanden):
DATABASE_URL, AWS_REGION, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY,
AWS_S3_BUCKET_STEPS, AWS_S3_BUCKET_THUMBNAILS

Bestehende worker/.env.example-Vars (bereits vorhanden):
DATABASE_URL, AWS_REGION, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY,
AWS_S3_BUCKET_STEPS, AWS_S3_BUCKET_THUMBNAILS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest-Test-Stubs für init- und confirm-Route</name>
  <files>
    src/app/api/upload/init/route.test.ts
    src/app/api/upload/confirm/route.test.ts
  </files>
  <read_first>
    - .planning/phases/03-ingestion-api-queue/03-VALIDATION.md (Wave-0-Anforderungen und Test-Map)
    - .planning/phases/03-ingestion-api-queue/03-RESEARCH.md (Pattern 1 und Pattern 2: Route-Code-Templates)
    - .planning/phases/03-ingestion-api-queue/03-PATTERNS.md (Abschnitt "route.test.ts + confirm/route.test.ts")
  </read_first>
  <action>
Erstelle zwei Testdateien co-located neben den Route-Dateien (per CLAUDE.md: "Unit tests co-located next to source files").

**src/app/api/upload/init/route.test.ts**:
```typescript
// src/app/api/upload/init/route.test.ts
// Tests für POST /api/upload/init
// SC#1: Duplikat-SHA-256 → HTTP 409 mit existing_part_id
// SC#2: Gültiger Init → DB-Insert + Presigned URL
// INGEST-04: SHA-256-Deduplizierung

import { describe, it, expect, vi, beforeEach } from 'vitest'
import { NextResponse } from 'next/server'

// Mocks für server-only Module
vi.mock('@/lib/db', () => ({
  db: vi.fn(),
}))
vi.mock('@/lib/s3', () => ({
  s3: {},
  BUCKET_STEPS: 'mock-bucket-steps',
  BUCKET_THUMBNAILS: 'mock-bucket-thumbnails',
}))
vi.mock('@aws-sdk/s3-request-presigner', () => ({
  getSignedUrl: vi.fn().mockResolvedValue('https://s3.example.com/mock-presigned-url'),
}))

import { db } from '@/lib/db'
const mockDb = vi.mocked(db)

describe('POST /api/upload/init', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  const validBody = {
    name: 'Flansch A',
    sha256: 'a'.repeat(64),  // 64 hex chars
    original_filename: 'flansch_a.step',
    file_size_bytes: 1024 * 1024,  // 1 MB
  }

  it('gibt HTTP 409 zurück wenn SHA-256 bereits in der Datenbank existiert (INGEST-04)', async () => {
    // Arrange: DB gibt existing part zurück bei SHA-256-Lookup
    const existingId = '123e4567-e89b-12d3-a456-426614174000'
    mockDb.mockResolvedValueOnce([{ id: existingId }])  // SELECT sha256 → Treffer

    const { POST } = await import('./route')
    const request = new Request('http://localhost/api/upload/init', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(validBody),
    })

    // Act
    const response = await POST(request)
    const data = await response.json()

    // Assert
    expect(response.status).toBe(409)
    expect(data.error).toBe('Duplicate file')
    expect(data.existing_part_id).toBe(existingId)
  })

  it('gibt HTTP 200 mit part_id und presigned_url zurück bei gültigem Init-Request', async () => {
    // Arrange: kein Duplikat, Insert erfolgreich
    const newPartId = '456e7890-e89b-12d3-a456-426614174000'
    mockDb
      .mockResolvedValueOnce([])                        // SELECT sha256 → kein Treffer
      .mockResolvedValueOnce([{ id: newPartId }])       // INSERT RETURNING id

    const { POST } = await import('./route')
    const request = new Request('http://localhost/api/upload/init', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(validBody),
    })

    // Act
    const response = await POST(request)
    const data = await response.json()

    // Assert
    expect(response.status).toBe(200)
    expect(data.part_id).toBe(newPartId)
    expect(data.presigned_url).toContain('https://')
  })

  it('gibt HTTP 400 zurück bei fehlendem Pflichtfeld name', async () => {
    const { POST } = await import('./route')
    const request = new Request('http://localhost/api/upload/init', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ ...validBody, name: '' }),  // Leerer name → Zod-Fehler
    })

    const response = await POST(request)
    expect(response.status).toBe(400)
  })

  it('gibt HTTP 400 zurück wenn sha256 nicht 64 Hex-Zeichen hat', async () => {
    const { POST } = await import('./route')
    const request = new Request('http://localhost/api/upload/init', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ ...validBody, sha256: 'zu-kurz' }),
    })

    const response = await POST(request)
    expect(response.status).toBe(400)
  })

  it('gibt HTTP 400 zurück wenn file_size_bytes über 100 MB liegt', async () => {
    const { POST } = await import('./route')
    const request = new Request('http://localhost/api/upload/init', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ ...validBody, file_size_bytes: 101 * 1024 * 1024 }),
    })

    const response = await POST(request)
    expect(response.status).toBe(400)
  })
})
```

**src/app/api/upload/confirm/route.test.ts**:
```typescript
// src/app/api/upload/confirm/route.test.ts
// Tests für POST /api/upload/confirm
// SC#3: HTTP 202 bei gültigem Confirm-Request
// Worker-Enqueue: fetch auf WORKER_URL/enqueue

import { describe, it, expect, vi, beforeEach } from 'vitest'

vi.mock('@/lib/db', () => ({
  db: vi.fn(),
}))

// fetch global mocken (Worker-HTTP-Aufruf)
const mockFetch = vi.fn()
global.fetch = mockFetch

import { db } from '@/lib/db'
const mockDb = vi.mocked(db)

describe('POST /api/upload/confirm', () => {
  beforeEach(() => {
    vi.clearAllMocks()
    process.env.WORKER_URL = 'http://localhost:8000'
  })

  const validPartId = '123e4567-e89b-12d3-a456-426614174000'

  it('gibt HTTP 202 zurück und ruft Worker /enqueue auf', async () => {
    // Arrange: Part existiert in DB
    mockDb.mockResolvedValueOnce([{ id: validPartId, status: 'pending' }])
    // Worker antwortet erfolgreich
    mockFetch.mockResolvedValueOnce({ ok: true, json: async () => ({ task_id: 'abc' }) })

    const { POST } = await import('./route')
    const request = new Request('http://localhost/api/upload/confirm', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ part_id: validPartId }),
    })

    // Act
    const response = await POST(request)
    const data = await response.json()

    // Assert
    expect(response.status).toBe(202)
    expect(data.part_id).toBe(validPartId)
    expect(data.status).toBe('pending')
    // Worker wurde aufgerufen
    expect(mockFetch).toHaveBeenCalledWith(
      'http://localhost:8000/enqueue',
      expect.objectContaining({ method: 'POST' })
    )
  })

  it('gibt HTTP 404 zurück wenn part_id nicht in der Datenbank existiert', async () => {
    mockDb.mockResolvedValueOnce([])  // Kein Part gefunden

    const { POST } = await import('./route')
    const request = new Request('http://localhost/api/upload/confirm', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ part_id: validPartId }),
    })

    const response = await POST(request)
    expect(response.status).toBe(404)
  })

  it('gibt HTTP 502 zurück wenn Worker nicht erreichbar ist', async () => {
    mockDb.mockResolvedValueOnce([{ id: validPartId, status: 'pending' }])
    mockFetch.mockResolvedValueOnce({ ok: false, status: 503 })

    const { POST } = await import('./route')
    const request = new Request('http://localhost/api/upload/confirm', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ part_id: validPartId }),
    })

    const response = await POST(request)
    expect(response.status).toBe(502)
  })

  it('gibt HTTP 400 zurück bei ungültiger UUID-part_id', async () => {
    const { POST } = await import('./route')
    const request = new Request('http://localhost/api/upload/confirm', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ part_id: 'nicht-eine-uuid' }),
    })

    const response = await POST(request)
    expect(response.status).toBe(400)
  })
})
```

HINWEIS: Die Tests werden zunächst FEHLSCHLAGEN (die Route-Dateien existieren noch nicht). Das ist korrekt — Wave-1-Plans implementieren die Routes. Nach Wave 1 müssen alle Tests grün sein.
  </action>
  <verify>
    <automated>ls /Users/mbieling/claude/Objekterkennung/src/app/api/upload/init/route.test.ts /Users/mbieling/claude/Objekterkennung/src/app/api/upload/confirm/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/app/api/upload/init/route.test.ts` — Datei existiert co-located neben route.ts
    - `ls src/app/api/upload/confirm/route.test.ts` — Datei existiert co-located neben route.ts
    - `grep -c "HTTP 409" src/app/api/upload/init/route.test.ts` gibt `1` zurück
    - `grep -c "status.*202" src/app/api/upload/confirm/route.test.ts` gibt mindestens `1` zurück
    - `grep -c "vi.mock('@/lib/db')" src/app/api/upload/init/route.test.ts` gibt `1` zurück
    - `grep -c "vi.mock('@/lib/db')" src/app/api/upload/confirm/route.test.ts` gibt `1` zurück
    - Beide Dateien sind syntaktisch valides TypeScript (kein tsc-Fehler auf den Dateien allein)
  </acceptance_criteria>
  <done>Zwei Testdateien existieren co-located (init/route.test.ts, confirm/route.test.ts) mit Mock-Infrastruktur für db und s3. Tests scheitern an fehlenden Route-Implementierungen — das ist erwartet in Wave 0.</done>
</task>

<task type="auto">
  <name>Task 2: Env-Beispieldateien um Phase-3-Variablen erweitern</name>
  <files>
    .env.local.example
    worker/.env.example
  </files>
  <read_first>
    - .env.local.example (vollständig lesen — bestehende Vars nicht löschen oder ändern)
    - worker/.env.example (vollständig lesen — bestehende Vars nicht löschen oder ändern)
    - .planning/phases/03-ingestion-api-queue/03-PATTERNS.md (Abschnitt ".env.local.example" — Zeilen 401–427)
  </read_first>
  <action>
Erweitere beide Env-Beispieldateien um die neuen Phase-3-Variablen. Bestehende Zeilen bleiben vollständig unverändert.

**Zu .env.local.example hinzufügen** (ans Ende der Datei anfügen):

```bash
# ─── Phase 3: Ingestion API + Queue ──────────────────────────────────────────

# Python Worker — URL des FastAPI-Endpunkts (POST /enqueue, GET /health)
# Lokal ohne Docker: starte Worker mit: cd worker && uvicorn main:app --port 8000
# Lokal mit Docker Compose: http://localhost:8000 (Port-Mapping in docker-compose.yml)
# Produktion: https://your-worker.railway.app (Railway / Fly.io URL)
WORKER_URL=http://localhost:8000

# Upstash Redis — nur für Produktion oder wenn kein lokaler Docker läuft
# Fundort: Upstash Console > Redis > Connect > Celery
# Format: rediss://:{PASSWORD}@{HOST}:{PORT} (rediss:// = TLS-Pflicht bei Upstash)
# Lokal mit Docker Compose: nicht nötig (Worker nutzt redis://redis:6379/0 intern)
UPSTASH_REDIS_URL=rediss://:your_upstash_password@your-host.upstash.io:6379
```

**Zu worker/.env.example hinzufügen** (ans Ende der Datei anfügen):

```bash
# ─── Phase 3: Celery + Redis Queue ───────────────────────────────────────────

# Celery Broker URL — Redis-Verbindung für Task-Queue
# Lokal mit Docker Compose: redis://redis:6379/0 (internes Docker-Netzwerk)
# Lokal ohne Docker (Upstash Dev): rediss://:{PASSWORD}@{HOST}:{PORT}
# Produktion (Upstash): rediss://:{PASSWORD}@{HOST}:{PORT}?ssl_cert_reqs=required
CELERY_BROKER_URL=redis://localhost:6379/0

# Celery Result Backend — normalerweise gleiche URL wie Broker
# Speichert Task-Status (PENDING, STARTED, SUCCESS, FAILURE)
CELERY_RESULT_BACKEND=redis://localhost:6379/0
```

ACHTUNG: Beide Dateien müssen READ FIRST — niemals Blindänderungen ohne den aktuellen Inhalt zu kennen.
  </action>
  <verify>
    <automated>grep -c "WORKER_URL" /Users/mbieling/claude/Objekterkennung/.env.local.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WORKER_URL" .env.local.example` gibt `1` zurück
    - `grep -c "UPSTASH_REDIS_URL" .env.local.example` gibt `1` zurück
    - `grep -c "CELERY_BROKER_URL" worker/.env.example` gibt `1` zurück
    - `grep -c "CELERY_RESULT_BACKEND" worker/.env.example` gibt `1` zurück
    - `grep -c "DATABASE_URL" .env.local.example` gibt `1` zurück (bestehende Var noch vorhanden)
    - `grep -c "DATABASE_URL" worker/.env.example` gibt `1` zurück (bestehende Var noch vorhanden)
    - Die neuen Vars haben erklärende Kommentare mit Fundort (Upstash Console, etc.)
  </acceptance_criteria>
  <done>.env.local.example enthält WORKER_URL und UPSTASH_REDIS_URL. worker/.env.example enthält CELERY_BROKER_URL und CELERY_RESULT_BACKEND. Alle bestehenden Variablen sind unverändert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Entwickler → .env.local | Secrets werden lokal in .env.local konfiguriert, nie in .env.local.example committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | .env.local.example | mitigate | Nur Dummy-Werte in .env.local.example; echte Werte in .env.local (in .gitignore); UPSTASH_REDIS_URL mit Placeholder-Password |
| T-03-05 | Information Disclosure | worker/.env.example | mitigate | Nur lokale Entwicklungs-Defaults (localhost:6379); kein Upstash-Password in Beispieldatei |
</threat_model>

<verification>
```bash
# Testdateien vorhanden (co-located per CLAUDE.md)
ls /Users/mbieling/claude/Objekterkennung/src/app/api/upload/init/route.test.ts
ls /Users/mbieling/claude/Objekterkennung/src/app/api/upload/confirm/route.test.ts

# Env-Dateien aktualisiert
grep "WORKER_URL\|UPSTASH_REDIS_URL" /Users/mbieling/claude/Objekterkennung/.env.local.example
grep "CELERY_BROKER_URL\|CELERY_RESULT_BACKEND" /Users/mbieling/claude/Objekterkennung/worker/.env.example

# Tests laufen (erwartetes Ergebnis: FAIL wegen fehlender Routes — das ist korrekt in Wave 0)
cd /Users/mbieling/claude/Objekterkennung && npm test -- --run src/app/api/upload 2>&1 | grep -E "PASS|FAIL|Cannot find module"
```
</verification>

<success_criteria>
1. src/app/api/upload/init/route.test.ts und src/app/api/upload/confirm/route.test.ts existieren co-located neben den Route-Dateien (CLAUDE.md-konform)
2. .env.local.example enthält WORKER_URL und UPSTASH_REDIS_URL mit erklärenden Kommentaren
3. worker/.env.example enthält CELERY_BROKER_URL und CELERY_RESULT_BACKEND
4. Alle bestehenden Env-Variablen sind in beiden Dateien unverändert vorhanden
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/03-ingestion-api-queue/03-02-SUMMARY.md` erstellen.
</output>
