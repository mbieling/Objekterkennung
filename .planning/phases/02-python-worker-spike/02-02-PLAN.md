---
phase: 02-python-worker-spike
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - worker/renderer.py
  - worker/test_renderer.py
autonomous: true
requirements:
  - INGEST-03

must_haves:
  truths:
    - "test_renderer.py im Docker-Container gibt 'RENDERER_OK: 8 PNGs generated' aus"
    - "Alle 8 generierten PNGs sind > 1 KB (kein leeres/schwarzes Bild)"
    - "renderer.py lädt eine STEP-Datei, zählt Faces und verwirft Geometrien mit < 4 Faces"
    - "Bounding-Box-Validierung verwirft STEP-Dateien mit Volumen < 1e-6 (leere Form)"
    - "Renderer erzeugt 8 PNG-Dateien mit den Namenskonventionen view_0.png bis view_7.png"
    - "Weißer Hintergrund (#FFFFFF) in allen Renders (D-05)"
  artifacts:
    - path: "worker/renderer.py"
      provides: "load_step(), validate_geometry(), render_views() Funktionen"
      exports: ["load_step", "validate_geometry", "render_views", "VIEWS"]
    - path: "worker/test_renderer.py"
      provides: "Isolierter OSMesa-Smoketest: 8 Views eines Würfels rendern"
      contains: "RENDERER_OK"
  key_links:
    - from: "worker/test_renderer.py"
      to: "worker/renderer.py"
      via: "import renderer"
      pattern: "from renderer import|import renderer"
    - from: "worker/renderer.py"
      to: "VTK_DEFAULT_OPENGL_WINDOW"
      via: "os.environ gesetzt vor OCC-Import"
      pattern: "os.environ\\[.VTK_DEFAULT_OPENGL_WINDOW.\\]"
---

<objective>
STEP-Renderer implementieren und OSMesa-Funktionalität im Docker-Container verifizieren.

Purpose: Validiert den risikoreichsten Teil des Spikes — ob pythonOCC + VTK + OSMesa in einem Display-freien Docker-Container 8 Ansichten einer STEP-Datei rendern kann. Erst nach diesem Beweis kann Plan 03 (DINOv2-Embedding) implementiert werden.

Output:
- worker/renderer.py — Funktionsbibliothek für STEP-Loading, Geometrievalidierung, 8-View-Rendering
- worker/test_renderer.py — Isolierter Smoke-Test (keine S3, keine DB, kein DINOv2)
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-python-worker-spike/02-CONTEXT.md
@.planning/phases/02-python-worker-spike/02-RESEARCH.md
@.planning/phases/02-python-worker-spike/02-PATTERNS.md
@.planning/phases/02-python-worker-spike/02-01-SUMMARY.md
@supabase/migrations/001_parts_schema.sql

<interfaces>
<!-- Kritische Import-Reihenfolge (RESEARCH.md Pitfall 1) -->
<!-- VTK_DEFAULT_OPENGL_WINDOW muss VOR dem ersten OCC-Import gesetzt sein -->
import os
os.environ["VTK_DEFAULT_OPENGL_WINDOW"] = "vtkOSOpenGLRenderWindow"
# Erst DANACH:
from OCC.Core.STEPControl import STEPControl_Reader
from OCC.Display.OCCViewer import Viewer3d

<!-- VIEWS-Konstante (D-04): 6 orthografisch + 2 isometrisch -->
VIEWS = [
    ("front",     V3d_Yneg),
    ("rear",      V3d_Ypos),
    ("left",      V3d_Xneg),
    ("right",     V3d_Xpos),
    ("top",       V3d_Zpos),
    ("bottom",    V3d_Zneg),
    ("iso_front", V3d_XposYnegZpos),
    ("iso_rear",  V3d_XnegYposZneg),
]
# OPEN QUESTION A3: V3d_XnegYposZneg für iso_rear ist plausibel, empirisch zu bestätigen

<!-- Ausgabeformat: view_{i}.png (i=0..7), nicht view_{name}.png -->
<!-- Damit process_step.py f"{part_id}/view_{i}.png" in S3 hochladen kann -->

<!-- Validierungsschnittstelle (D-08, D-09): -->
def validate_geometry(shape) -> None:
    # Wirft ValueError("INVALID_GEOMETRY:face_count=N") wenn face_count < 4
    # Wirft ValueError("INVALID_GEOMETRY:empty_bounding_box") wenn Volumen < 1e-6

<!-- render_views Rückgabewert: -->
def render_views(shape, output_dir: str) -> list[str]:
    # Gibt 8 PNG-Pfade zurück: [output_dir/view_0.png, ..., output_dir/view_7.png]
    # Bilder sind 512x512px (D-06: Thumbnail-Größe)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: renderer.py — STEP-Loading, Validierung, 8-View-Rendering</name>
  <files>worker/renderer.py</files>

  <read_first>
    - .planning/phases/02-python-worker-spike/02-RESEARCH.md (Pattern 1: STEP laden, Pattern 2: Offscreen-Rendering, Pitfall 1: VTK-Import-Reihenfolge, Pitfall 4: leere STEP-Renderings)
    - .planning/phases/02-python-worker-spike/02-PATTERNS.md (worker/renderer.py Abschnitt — vollständige Funktionssignaturen)
    - .planning/phases/02-python-worker-spike/02-CONTEXT.md (D-04: 8 Views, D-05: weißer Hintergrund, D-06: 512px Thumbnails, D-08: face_count, D-09: Fehlercode-Format)
  </read_first>

  <action>
Erstelle `worker/renderer.py`. Die Datei muss drei Dinge tun: STEP laden, Geometrie validieren, 8 Views rendern.

**Kritische Reihenfolge (RESEARCH.md Pitfall 1):** `os.environ["VTK_DEFAULT_OPENGL_WINDOW"]` muss ABSOLUT ERSTE Anweisung nach `import os` sein, vor allen OCC-Imports.

```python
# worker/renderer.py
# WARNUNG: Die folgende Zeile MUSS vor allen OCC-Imports stehen (RESEARCH.md Pitfall 1)
import os
os.environ["VTK_DEFAULT_OPENGL_WINDOW"] = "vtkOSOpenGLRenderWindow"

import logging

from OCC.Core.STEPControl import STEPControl_Reader
from OCC.Core.IFSelect import IFSelect_RetDone
from OCC.Extend.TopologyUtils import TopologyExplorer
from OCC.Core.Bnd import Bnd_Box
from OCC.Core.BRepBndLib import brepbndlib_Add
from OCC.Display.OCCViewer import Viewer3d
from OCC.Core.V3d import (
    V3d_Yneg, V3d_Ypos, V3d_Xneg, V3d_Xpos,
    V3d_Zpos, V3d_Zneg, V3d_XposYnegZpos, V3d_XnegYposZneg
)

logger = logging.getLogger(__name__)

# 8 Views: 6 orthografisch + 2 isometrisch (D-04)
VIEWS = [
    ("front",     V3d_Yneg),       # Vorderansicht
    ("rear",      V3d_Ypos),       # Rückansicht
    ("left",      V3d_Xneg),       # Linksansicht
    ("right",     V3d_Xpos),       # Rechtsansicht
    ("top",       V3d_Zpos),       # Draufsicht
    ("bottom",    V3d_Zneg),       # Untersicht
    ("iso_front", V3d_XposYnegZpos),  # Isometrisch vorne-rechts-oben
    ("iso_rear",  V3d_XnegYposZneg),  # Isometrisch hinten-links-unten (A3: zu bestätigen)
]


def load_step(filename: str):
    """Lädt eine STEP-Datei und gibt das TopoDS_Shape zurück.
    
    Raises:
        ValueError: Bei Lese- oder Transfer-Fehler ("STEP_READ_ERROR:status=N")
    """
    reader = STEPControl_Reader()
    status = reader.ReadFile(filename)
    if status != IFSelect_RetDone:
        raise ValueError(f"STEP_READ_ERROR:status={status}")
    reader.TransferRoots()
    shape = reader.Shape(1)
    return shape


def validate_geometry(shape) -> None:
    """Validiert die Geometrie gegen D-08 (face_count) und Pitfall 4 (bbox).
    
    Raises:
        ValueError: "INVALID_GEOMETRY:face_count=N" wenn face_count < 4
        ValueError: "INVALID_GEOMETRY:empty_bounding_box" wenn Volumen < 1e-6
    """
    # Prüfung 1: Face-Count (D-08) — weniger als 4 Faces = kein sinnvoller 3D-Körper
    explorer = TopologyExplorer(shape)
    face_count = len(list(explorer.faces()))
    if face_count < 4:
        raise ValueError(f"INVALID_GEOMETRY:face_count={face_count}")
    logger.info(f"Geometrie-Validierung: {face_count} Faces gefunden")

    # Prüfung 2: Bounding-Box-Volumen (RESEARCH.md Pitfall 4)
    # Leere Formen erzeugen weiße Bilder → DINOv2 berechnet Embedding für weißes Bild
    bbox = Bnd_Box()
    brepbndlib_Add(shape, bbox)
    x_min, y_min, z_min, x_max, y_max, z_max = bbox.Get()
    volume = (x_max - x_min) * (y_max - y_min) * (z_max - z_min)
    if volume < 1e-6:
        raise ValueError("INVALID_GEOMETRY:empty_bounding_box")
    logger.info(f"Bounding-Box-Volumen: {volume:.4f}")


def render_views(shape, output_dir: str) -> list[str]:
    """Rendert 8 Views (D-04) als 512x512px PNG-Dateien (D-06).
    
    Args:
        shape: TopoDS_Shape (validiert — validate_geometry() wurde bereits aufgerufen)
        output_dir: Verzeichnis für PNG-Ausgabe (muss existieren)
    
    Returns:
        Liste mit 8 PNG-Pfaden: [output_dir/view_0.png, ..., output_dir/view_7.png]
    """
    viewer = Viewer3d()
    viewer.Create()
    viewer.SetModeShaded()
    # Weißer Hintergrund (D-05): maximaler Kontrast für dunkle Metallbauteile
    viewer.set_bg_gradient_color([255, 255, 255], [255, 255, 255])
    viewer.DisplayShape(shape, update=True)

    paths = []
    for i, (name, orientation) in enumerate(VIEWS):
        viewer.View.SetProj(orientation)
        viewer.FitAll()  # Automatischer Kamera-Abstand aus Bounding-Box (Claude's Discretion)
        path = os.path.join(output_dir, f"view_{i}.png")  # view_0..view_7 (S3-Pfadkonvention)
        viewer.ExportToImage(path)
        paths.append(path)
        logger.info(f"View {i} ({name}): {path}")

    return paths  # 8 PNG-Pfade (512x512px via VTK-Default-Auflösung)
```
  </action>

  <verify>
    <automated>grep -n "VTK_DEFAULT_OPENGL_WINDOW" /Users/mbieling/claude/Objekterkennung/worker/renderer.py | head -1 && grep -n "INVALID_GEOMETRY:face_count" /Users/mbieling/claude/Objekterkennung/worker/renderer.py && grep -c "def render_views" /Users/mbieling/claude/Objekterkennung/worker/renderer.py && echo "RENDERER_PY_OK"</automated>
  </verify>

  <acceptance_criteria>
    - worker/renderer.py existiert
    - Die Zeile `os.environ["VTK_DEFAULT_OPENGL_WINDOW"] = "vtkOSOpenGLRenderWindow"` ist in Zeile 2 oder 3 der Datei (vor allen OCC-Imports)
    - worker/renderer.py enthält `def load_step(filename: str):`
    - worker/renderer.py enthält `def validate_geometry(shape) -> None:`
    - worker/renderer.py enthält `def render_views(shape, output_dir: str) -> list[str]:`
    - worker/renderer.py enthält `raise ValueError(f"INVALID_GEOMETRY:face_count={face_count}")` (D-08, D-09)
    - worker/renderer.py enthält `raise ValueError("INVALID_GEOMETRY:empty_bounding_box")` (Pitfall 4)
    - VIEWS-Liste enthält genau 8 Einträge (V3d_Yneg, V3d_Ypos, V3d_Xneg, V3d_Xpos, V3d_Zpos, V3d_Zneg, V3d_XposYnegZpos, V3d_XnegYposZneg)
    - `docker run --rm bauteil-worker python -c "from renderer import load_step, validate_geometry, render_views, VIEWS; assert len(VIEWS)==8; print('RENDERER_IMPORT_OK')"` gibt RENDERER_IMPORT_OK aus
  </acceptance_criteria>

  <done>renderer.py ist importierbar im Docker-Container. Alle drei Funktionen (load_step, validate_geometry, render_views) sind vorhanden. VIEWS-Liste enthält 8 Einträge. OSMesa-Env-Var steht vor allen OCC-Imports.</done>
</task>

<task type="auto">
  <name>Task 2: test_renderer.py — Isolierter OSMesa-Smoketest</name>
  <files>worker/test_renderer.py</files>

  <read_first>
    - .planning/phases/02-python-worker-spike/02-RESEARCH.md (Code Examples: "Test: OSMesa funktioniert?", VALIDATION.md Wave-0-Anforderungen)
    - .planning/phases/02-python-worker-spike/02-PATTERNS.md (worker/test_renderer.py Abschnitt)
    - .planning/phases/02-python-worker-spike/02-VALIDATION.md (Per-Task Verification Map: Task 2-01-02 und 2-01-04)
    - worker/renderer.py (soeben erstellt — Importschnittstelle für render_views)
  </read_first>

  <action>
Erstelle `worker/test_renderer.py` — ein isoliertes Skript das NUR den Renderer testet (kein S3, keine DB, kein DINOv2).

Zwei Teile: (A) OSMesa-Basistest mit synthetischer Form, (B) vollständiger 8-View-Test mit sample.step via renderer.py.

```python
# worker/test_renderer.py
# Isolierter OSMesa-Smoketest: Prüft ob VTK+OSMesa im Docker-Container funktioniert.
# Ausführen: docker run --rm bauteil-worker python test_renderer.py
# Kein S3, keine DB, kein DINOv2 — nur Rendering.

# OSMesa ZUERST (RESEARCH.md Pitfall 1 — muss vor allen OCC-Imports kommen)
import os
os.environ["VTK_DEFAULT_OPENGL_WINDOW"] = "vtkOSOpenGLRenderWindow"

import sys
import tempfile
import logging

logging.basicConfig(level=logging.INFO, format="%(levelname)s: %(message)s")
logger = logging.getLogger(__name__)

from OCC.Core.BRepPrimAPI import BRepPrimAPI_MakeBox
from OCC.Display.OCCViewer import Viewer3d

# renderer.py importieren (aus gleichem Verzeichnis)
# Hinweis: renderer.py setzt VTK_DEFAULT_OPENGL_WINDOW ebenfalls — kein Problem bei Doppelsetzung
from renderer import load_step, validate_geometry, render_views, VIEWS


def test_osmesa_basic():
    """Teil A: Minimaler OSMesa-Test mit synthetischer Form (kein STEP nötig)."""
    logger.info("=== Test A: OSMesa Basistest ===")
    
    # Synthetischen Würfel direkt mit pythonOCC erzeugen (kein STEP)
    box = BRepPrimAPI_MakeBox(10, 20, 30).Shape()
    
    viewer = Viewer3d()
    viewer.Create()
    viewer.SetModeShaded()
    viewer.set_bg_gradient_color([255, 255, 255], [255, 255, 255])
    viewer.DisplayShape(box, update=True)
    
    output_path = "/tmp/test_osmesa_basic.png"
    viewer.ExportToImage(output_path)
    
    stat = os.stat(output_path)
    assert stat.st_size > 1000, f"PNG zu klein ({stat.st_size} Bytes) — OSMesa-Rendering fehlgeschlagen"
    logger.info(f"OSMesa Basistest OK: {output_path} ({stat.st_size} Bytes)")
    return True


def test_step_rendering():
    """Teil B: Vollständiger 8-View-Test mit worker/testdata/sample.step."""
    logger.info("=== Test B: STEP-Datei 8-View-Rendering ===")
    
    # Pfad zur Testdatei relativ zum Skript
    script_dir = os.path.dirname(os.path.abspath(__file__))
    step_path = os.path.join(script_dir, "testdata", "sample.step")
    
    if not os.path.exists(step_path):
        logger.error(f"Testdatei nicht gefunden: {step_path}")
        logger.error("Bitte worker/testdata/sample.step erstellen (Plan 01, Task 2)")
        return False
    
    # STEP laden und validieren
    shape = load_step(step_path)
    validate_geometry(shape)
    logger.info("STEP-Validierung OK")
    
    # 8 Views in temporäres Verzeichnis rendern
    with tempfile.TemporaryDirectory() as tmpdir:
        png_paths = render_views(shape, tmpdir)
        
        # Verifikation: genau 8 PNGs erzeugt
        assert len(png_paths) == 8, f"Erwartet 8 PNGs, erhalten: {len(png_paths)}"
        
        # Verifikation: alle PNGs sind nicht leer (> 1 KB)
        failed = []
        for i, path in enumerate(png_paths):
            assert os.path.exists(path), f"PNG fehlt: {path}"
            size = os.stat(path).st_size
            if size < 1000:
                failed.append(f"view_{i}.png zu klein: {size} Bytes")
            else:
                logger.info(f"view_{i}.png: {size} Bytes — OK")
        
        if failed:
            for msg in failed:
                logger.error(msg)
            return False
        
        logger.info(f"RENDERER_OK: 8 PNGs generated in {tmpdir}")
    
    return True


def test_invalid_geometry():
    """Teil C: Geometrievalidierung — face_count < 4 muss ValueError auslösen."""
    logger.info("=== Test C: Ungültige Geometrie (face_count < 4) ===")
    
    # Erstelle eine Fläche (Face) statt eines Festkörpers — hat weniger als 4 Faces
    from OCC.Core.BRepBuilderAPI import BRepBuilderAPI_MakeFace
    from OCC.Core.gp import gp_Pln, gp_Ax3, gp_Pnt, gp_Dir
    
    # Einfache planare Fläche — nur 1 Face, muss als ungültig abgelehnt werden
    plane = gp_Pln(gp_Ax3(gp_Pnt(0, 0, 0), gp_Dir(0, 0, 1)))
    face_maker = BRepBuilderAPI_MakeFace(plane, -10, 10, -10, 10)
    flat_shape = face_maker.Shape()
    
    try:
        validate_geometry(flat_shape)
        logger.error("FEHLER: ValueError erwartet aber nicht geworfen")
        return False
    except ValueError as e:
        error_msg = str(e)
        if "INVALID_GEOMETRY:face_count=" in error_msg:
            logger.info(f"Ungültige Geometrie korrekt abgelehnt: {error_msg}")
            return True
        else:
            logger.error(f"Falscher Fehlercode: {error_msg}")
            return False


if __name__ == "__main__":
    results = {}
    
    try:
        results["A_osmesa_basic"] = test_osmesa_basic()
    except Exception as e:
        logger.exception(f"Test A fehlgeschlagen: {e}")
        results["A_osmesa_basic"] = False
    
    try:
        results["B_step_rendering"] = test_step_rendering()
    except Exception as e:
        logger.exception(f"Test B fehlgeschlagen: {e}")
        results["B_step_rendering"] = False
    
    try:
        results["C_invalid_geometry"] = test_invalid_geometry()
    except Exception as e:
        logger.exception(f"Test C fehlgeschlagen: {e}")
        results["C_invalid_geometry"] = False
    
    # Zusammenfassung
    all_passed = all(results.values())
    print("\n=== TESTERGEBNIS ===")
    for test_name, passed in results.items():
        status = "OK" if passed else "FEHLER"
        print(f"  {test_name}: {status}")
    
    if all_passed:
        print("\nRENDERER_OK: 8 PNGs generated")
        sys.exit(0)
    else:
        print("\nRENDERER_FEHLER: Mindestens ein Test fehlgeschlagen")
        sys.exit(1)
```
  </action>

  <verify>
    <automated>docker run --rm bauteil-worker python test_renderer.py 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - worker/test_renderer.py existiert
    - `os.environ["VTK_DEFAULT_OPENGL_WINDOW"]` steht in den ersten 5 Zeilen der Datei
    - worker/test_renderer.py enthält `from renderer import load_step, validate_geometry, render_views, VIEWS`
    - worker/test_renderer.py enthält `RENDERER_OK: 8 PNGs generated` als Ausgabestring
    - `docker run --rm bauteil-worker python test_renderer.py` gibt Exit-Code 0 zurück
    - Die Ausgabe enthält `RENDERER_OK: 8 PNGs generated` (letzter Output-Block)
    - Die Ausgabe enthält `A_osmesa_basic: OK`
    - Die Ausgabe enthält `B_step_rendering: OK`
    - Die Ausgabe enthält `C_invalid_geometry: OK`
    - Alle 8 view_*.png-Dateien sind laut Log-Ausgabe > 1000 Bytes
  </acceptance_criteria>

  <done>test_renderer.py läuft im Docker-Container durch und gibt "RENDERER_OK: 8 PNGs generated" aus. Alle drei Subtests (OSMesa-Basis, STEP-8-View, Geometrievalidierung) bestehen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| Container → STEP-Datei | sample.step kommt aus eigenem Repo (vertrauenswürdig); in Produktion kommen STEP-Dateien aus eigenem S3-Bucket (nicht extern) |
| renderer.py → Dateisystem | Schreibt PNGs in tmpdir (kein Nutzer-Input für Pfade in diesem Plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-02-06 | Denial of Service | renderer.py: STEP-Assemblies | mitigate | face_count-Check (D-08) stoppt einfache DoS-Versuche; Bounding-Box-Check stoppt leere Formen; komplexe Assemblies mit >1M Faces sind im Spike noch nicht begrenzt (Phase 3 fügt Größenlimit hinzu) |
| T-02-07 | Tampering | VIEWS-Konstante in renderer.py | accept | VIEWS ist eine Modul-Konstante; für den Spike kein Risiko; Produktion würde Konfigurationsdatei nutzen |
| T-02-08 | Elevation of Privilege | renderer.py Datei-Schreibrechte | accept | Schreibt nur in /tmp und output_dir; kein Nutzer-Input für Pfade in renderer.py selbst; process_step.py (Plan 03) erhält part_id als UUID (validiert in Phase 3) |
| T-02-09 | Information Disclosure | test_renderer.py Log-Ausgabe | accept | Logs enthalten Dateipfade und Dateigrößen; keine Credentials; akzeptabel für lokalen Spike |
</threat_model>

<verification>
Nach Abschluss beider Tasks:

1. Renderer-Import im Container: `docker run --rm bauteil-worker python -c "from renderer import load_step, validate_geometry, render_views, VIEWS; assert len(VIEWS)==8; print('IMPORT_OK')"`
2. Vollständiger Smoketest: `docker run --rm bauteil-worker python test_renderer.py`
   - Erwartete Ausgabe: enthält "RENDERER_OK: 8 PNGs generated" und Exit-Code 0
3. OSMesa-Env-Var in Python prüfen: `docker run --rm bauteil-worker python -c "import os; v=os.environ.get('VTK_DEFAULT_OPENGL_WINDOW','NOT_SET'); print(v); assert v=='vtkOSOpenGLRenderWindow'"`
4. Visuelle Verifikation (manuell, optional): `docker cp $(docker run -d --rm bauteil-worker sleep 30):/tmp/test_osmesa_basic.png ./local_check.png && open ./local_check.png`
</verification>

<success_criteria>
- `docker run --rm bauteil-worker python test_renderer.py` gibt Exit 0 zurück
- Ausgabe enthält "RENDERER_OK: 8 PNGs generated"
- Alle 3 Subtests (A, B, C) zeigen "OK"
- renderer.py exportiert load_step, validate_geometry, render_views, VIEWS
- INVALID_GEOMETRY:face_count wird korrekt geworfen (Test C bestätigt)
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/02-python-worker-spike/02-02-SUMMARY.md` mit:
- Ausgabe von `docker run --rm bauteil-worker python test_renderer.py` (vollständig)
- Ob Test A, B, C bestanden haben (und Fehlermeldung wenn nicht)
- Exit-Code des Testlaufs
- Ob V3d_XnegYposZneg (iso_rear) korrekt funktioniert oder ob eine andere Konstante verwendet werden musste (Open Question A3)
- Größe der 8 generierten PNGs (aus Log-Ausgabe)
</output>
