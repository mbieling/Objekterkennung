---
phase: 02-python-worker-spike
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - worker/embedder.py
  - worker/process_step.py
autonomous: true
requirements:
  - INGEST-03

must_haves:
  truths:
    - "embedder.py gibt für ein 224x224px PNG einen numpy-Array der Shape (768,) zurück"
    - "mean_pool() gibt für eine Liste von 8 (768,)-Arrays einen (768,)-Array zurück"
    - "process_step.py setzt parts.status auf 'processing' als ersten DB-Schritt"
    - "process_step.py schreibt embedding (vector(768)), embedding_model='dinov2-base', embedding_version='facebook/dinov2-base', thumbnail_urls (8 S3-URLs), status='ready' nach Neon"
    - "Bei face_count < 4 setzt process_step.py parts.status auf 'failed' und loggt INVALID_GEOMETRY:face_count=N"
    - "Ein pgvector cosine-similarity Query gegen das geschriebene Embedding gibt similarity ≈ 1.0 zurück"
  artifacts:
    - path: "worker/embedder.py"
      provides: "get_embedding(), mean_pool() Funktionen; DINOv2 ViT-B/14 Inferenz"
      exports: ["get_embedding", "mean_pool"]
    - path: "worker/process_step.py"
      provides: "Vollständige Pipeline: S3 download → validate → render → embed → S3 upload → DB write"
      contains: "def process(part_id: str)"
  key_links:
    - from: "worker/process_step.py"
      to: "worker/renderer.py"
      via: "from renderer import load_step, validate_geometry, render_views"
      pattern: "from renderer import"
    - from: "worker/process_step.py"
      to: "worker/embedder.py"
      via: "from embedder import get_embedding, mean_pool"
      pattern: "from embedder import"
    - from: "worker/process_step.py"
      to: "Neon parts-Tabelle"
      via: "psycopg2 + register_vector() + UPDATE parts SET embedding=%s"
      pattern: "register_vector\\|UPDATE parts SET"
    - from: "worker/process_step.py"
      to: "S3 BUCKET_THUMBNAILS"
      via: "boto3 upload_fileobj: {part_id}/view_0.png .. view_7.png"
      pattern: "upload_fileobj\\|BUCKET_THUMBNAILS"

user_setup:
  - service: neon
    why: "Worker schreibt Embedding direkt in die Neon-Datenbank (psycopg2 direkte Verbindung)"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Dashboard > Project > Connection Details > 'Direct connection' (NICHT Pooler-URL)"
  - service: aws-s3
    why: "Worker lädt STEP-Dateien aus S3 herunter und lädt Thumbnails hoch"
    env_vars:
      - name: AWS_ACCESS_KEY_ID
        source: "AWS IAM > Users > Security credentials > Create access key"
      - name: AWS_SECRET_ACCESS_KEY
        source: "AWS IAM > Users > Security credentials > Create access key"
      - name: AWS_REGION
        source: "AWS Console > Region-Selektor (z.B. eu-central-1)"
      - name: AWS_S3_BUCKET_STEPS
        source: "Wert: 'parts-steps' (aus Phase 1)"
      - name: AWS_S3_BUCKET_THUMBNAILS
        source: "Wert: 'parts-thumbnails' (aus Phase 1)"
---

<objective>
DINOv2-Embedding-Modul und vollständige Pipeline-Integration implementieren.

Purpose: Vervollständigt den Python Worker Spike. embedder.py kapselt DINOv2 ViT-B/14 Inferenz. process_step.py verbindet alle Module (renderer + embedder + S3 + DB) zu einer ausführbaren Pipeline die einen part_id als Argument nimmt und alle fünf Phase-2-Erfolgskriterien erfüllt.

Output:
- worker/embedder.py — DINOv2 ViT-B/14 Inferenz, CLS-Token, mean_pool
- worker/process_step.py — Vollständige Pipeline (CLI-Skript)
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-python-worker-spike/02-CONTEXT.md
@.planning/phases/02-python-worker-spike/02-RESEARCH.md
@.planning/phases/02-python-worker-spike/02-PATTERNS.md
@.planning/phases/02-python-worker-spike/02-01-SUMMARY.md
@.planning/phases/02-python-worker-spike/02-02-SUMMARY.md
@supabase/migrations/001_parts_schema.sql
@src/lib/s3.ts

<interfaces>
<!-- Aus renderer.py (Plan 02, Task 1) -->
from renderer import load_step, validate_geometry, render_views, VIEWS
# load_step(filename: str) -> TopoDS_Shape
# validate_geometry(shape) -> None  # wirft ValueError bei ungültiger Geometrie
# render_views(shape, output_dir: str) -> list[str]  # 8 PNG-Pfade: view_0.png..view_7.png

<!-- S3-Pfadkonventionen (aus src/lib/s3.ts + 001_parts_schema.sql) -->
BUCKET_STEPS = os.environ["AWS_S3_BUCKET_STEPS"]      # "parts-steps"
BUCKET_THUMBNAILS = os.environ["AWS_S3_BUCKET_THUMBNAILS"]  # "parts-thumbnails"
# STEP-Datei lesen:      {part_id}/original.step  in BUCKET_STEPS
# Thumbnails schreiben:  {part_id}/view_0.png .. {part_id}/view_7.png in BUCKET_THUMBNAILS

<!-- DB-Schema (aus supabase/migrations/001_parts_schema.sql, Zeilen 11-28) -->
-- parts-Tabelle: embedding vector(768), embedding_model text, embedding_version text,
--                thumbnail_urls text[], status text, updated_at timestamptz (via Trigger)
-- Status-Werte: 'pending' | 'processing' | 'ready' | 'failed'
-- WICHTIG: register_vector(conn) MUSS vor dem ersten vector(768)-Query aufgerufen werden

<!-- DINOv2-Pattern (aus RESEARCH.md Pattern 3) -->
# Modell einmal laden beim Modulimport (nicht in Schleife — Anti-Pattern RESEARCH.md)
processor = AutoImageProcessor.from_pretrained("facebook/dinov2-base")
model = AutoModel.from_pretrained("facebook/dinov2-base")
model.eval()
# TRANSFORMERS_CACHE=/app/model_cache (via Dockerfile ENV) → kein Download zur Laufzeit

<!-- Fehlerbehandlung (aus RESEARCH.md D-09 + 02-PATTERNS.md) -->
# Format: ValueError("INVALID_GEOMETRY:face_count=N") oder ("INVALID_GEOMETRY:empty_bounding_box")
# Bei Fehler: parts.status = 'failed' in DB; kein crash ohne DB-Update
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: embedder.py — DINOv2 ViT-B/14 Inferenz</name>
  <files>worker/embedder.py</files>

  <read_first>
    - .planning/phases/02-python-worker-spike/02-RESEARCH.md (Pattern 3: DINOv2 CLS-Token Embedding, Anti-Patterns: "DINOv2 bei jeder View-Iteration neu laden")
    - .planning/phases/02-python-worker-spike/02-PATTERNS.md (worker/embedder.py Abschnitt — vollständige Implementierung)
    - .planning/phases/02-python-worker-spike/02-CONTEXT.md (D-06: 224px Input-Größe, D-07: Mean-Pool über 8 Views)
  </read_first>

  <action>
Erstelle `worker/embedder.py`. Das Modul lädt DINOv2 einmal beim Import (nicht pro View-Aufruf).

```python
# worker/embedder.py
# DINOv2 ViT-B/14 Inferenz für CAD-Part-Embeddings.
# Modell wird beim Modulimport einmalig geladen (~3s).
# TRANSFORMERS_CACHE=/app/model_cache (Dockerfile) — kein HuggingFace-Download zur Laufzeit.

import logging
import numpy as np
from PIL import Image
import torch
from transformers import AutoImageProcessor, AutoModel

logger = logging.getLogger(__name__)

# Einmaliges Laden beim Modulimport (RESEARCH.md Anti-Pattern: nicht in Schleife laden)
# TRANSFORMERS_CACHE=/app/model_cache via Dockerfile ENV — Modell ist bereits gecacht
_MODEL_NAME = "facebook/dinov2-base"
logger.info(f"Lade DINOv2-Modell: {_MODEL_NAME}")
_processor = AutoImageProcessor.from_pretrained(_MODEL_NAME)
_model = AutoModel.from_pretrained(_MODEL_NAME)
_model.eval()
logger.info("DINOv2-Modell geladen")


def get_embedding(image_path: str) -> np.ndarray:
    """Berechnet CLS-Token-Embedding (768-dim) für ein Bild.
    
    Args:
        image_path: Pfad zu einer PNG-Datei (512x512px aus renderer.py)
    
    Returns:
        numpy-Array der Shape (768,) — DINOv2 ViT-B/14 CLS-Token
    
    Preprocessing (D-06, Claude's Discretion):
        - Resize auf 224x224 VOR AutoImageProcessor (verhindert unerwartete Skalierung)
        - CLS-Token (Index 0 in last_hidden_state) — nicht mean-pool der Patch-Tokens
        - RESEARCH.md A1: CLS-Token empfohlen für globale geometrische Ähnlichkeit
    """
    # Resize auf 224x224px (D-06: DINOv2 nativer Input) vor Processor
    img = Image.open(image_path).convert("RGB").resize((224, 224))
    
    inputs = _processor(images=img, return_tensors="pt")
    
    with torch.no_grad():
        outputs = _model(**inputs)
    
    # CLS-Token: Shape [batch=1, seq_len=257, hidden=768] → Index 0 → (768,)
    # 257 = 1 CLS-Token + 256 Patch-Tokens (16x16 Patches bei 224px Input)
    cls_embedding = outputs.last_hidden_state[:, 0].squeeze().numpy()
    
    assert cls_embedding.shape == (768,), f"Unerwartete Embedding-Shape: {cls_embedding.shape}"
    return cls_embedding


def mean_pool(embeddings: list) -> np.ndarray:
    """Mean-Pool über alle 8 View-Embeddings zu einem einzigen 768-dim Vektor (D-07).
    
    Args:
        embeddings: Liste von 8 numpy-Arrays, je Shape (768,)
    
    Returns:
        numpy-Array der Shape (768,) — arithmetisches Mittel aller Views
    """
    assert len(embeddings) > 0, "Leere Embedding-Liste"
    stacked = np.stack(embeddings)  # Shape: (N, 768)
    pooled = np.mean(stacked, axis=0)  # Shape: (768,)
    assert pooled.shape == (768,), f"Unerwartete Pool-Shape: {pooled.shape}"
    return pooled
```
  </action>

  <verify>
    <!-- get_embedding() erwartet ein PNG, keine .step-Datei. Der verify-Befehl führt zuerst
         test_renderer.py aus (erzeugt /tmp/views/view_0.png), dann testet er get_embedding()
         auf diesem PNG. -->
    <automated>docker run --rm bauteil-worker bash -c "
  python test_renderer.py && \
  python -c \"
from embedder import get_embedding
import numpy as np
e = get_embedding('/tmp/views/view_0.png')
assert e.shape == (768,), f'Wrong shape: {e.shape}'
print('EMBEDDING_OK: shape=(768,)')
\"
"</automated>
  </verify>

  <acceptance_criteria>
    - worker/embedder.py existiert
    - worker/embedder.py enthält `def get_embedding(image_path: str) -> np.ndarray:`
    - worker/embedder.py enthält `def mean_pool(embeddings: list) -> np.ndarray:`
    - worker/embedder.py enthält `_MODEL_NAME = "facebook/dinov2-base"` (nicht dinov2-small oder dinov2-large)
    - worker/embedder.py enthält `outputs.last_hidden_state[:, 0]` (CLS-Token, nicht Mean-Pool der Patch-Tokens)
    - worker/embedder.py enthält `.resize((224, 224))` (D-06: 224px Input)
    - `docker run --rm bauteil-worker python -c "from embedder import get_embedding; import numpy as np; print('EMBEDDER_IMPORT_OK')"` gibt EMBEDDER_IMPORT_OK aus (kein Import-Fehler)
    - Nach Ausführung einer Rendering-Pipeline: `get_embedding(view_png_path)` gibt numpy-Array shape (768,) zurück (verifiziert durch Integration in process_step.py-Test)
    - `mean_pool([np.zeros(768), np.ones(768)])` gibt Array shape (768,) mit Werten ≈ 0.5 zurück
  </acceptance_criteria>

  <done>embedder.py ist importierbar. get_embedding() gibt (768,)-Array zurück. mean_pool() gibt (768,)-Array zurück. DINOv2-Modell wird beim Import aus TRANSFORMERS_CACHE geladen (kein Netzwerkzugriff nötig).</done>
</task>

<task type="auto">
  <name>Task 2: process_step.py — Vollständige Pipeline</name>
  <files>worker/process_step.py</files>

  <read_first>
    - .planning/phases/02-python-worker-spike/02-RESEARCH.md (Pattern 4: pgvector-Schreiben, Pattern 5: S3-Upload, Code Examples: Vollständige Struktur von process_step.py)
    - .planning/phases/02-python-worker-spike/02-PATTERNS.md (worker/process_step.py Abschnitt — vollständige Import-Struktur, Status-Update-Muster, Fehlerbehandlung)
    - supabase/migrations/001_parts_schema.sql (Zeilen 11-28: Schema-Details, Spalten die der Worker schreibt)
    - src/lib/s3.ts (Zeilen 14-16: BUCKET_STEPS, BUCKET_THUMBNAILS Konstanten und Pfadkonvention)
    - .planning/phases/02-python-worker-spike/02-CONTEXT.md (D-10: kein FastAPI/Celery, D-09: Fehlercode-Format)
    - .planning/phases/02-python-worker-spike/02-01-SUMMARY.md (welche Env-Vars verfügbar sind)
    - .planning/phases/02-python-worker-spike/02-02-SUMMARY.md (renderer.py Schnittstelle bestätigt)
  </read_first>

  <action>
Erstelle `worker/process_step.py` — das Haupt-Skript des Spikes. Nimmt eine part_id als CLI-Argument, führt die vollständige Pipeline aus.

```python
# worker/process_step.py
# Python Worker Spike — vollständige Pipeline: S3 download → render → embed → S3 upload → DB write
# Ausführen: python process_step.py <part-uuid>
# Per D-10: kein FastAPI, kein Celery — direktes Skript für Spike-Validierung

# MUSS GANZ OBEN STEHEN — vor allen OCC-Imports (RESEARCH.md Pitfall 1)
import os
os.environ["VTK_DEFAULT_OPENGL_WINDOW"] = "vtkOSOpenGLRenderWindow"

import sys
import tempfile
import logging
import numpy as np
import boto3
import psycopg2
from pgvector.psycopg2 import register_vector
from dotenv import load_dotenv

# Worker-Module (aus worker/-Verzeichnis)
from renderer import load_step, validate_geometry, render_views
from embedder import get_embedding, mean_pool

# .env-Datei laden wenn vorhanden (lokale Entwicklung)
load_dotenv()

# Logging-Konfiguration
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s %(levelname)s [%(name)s] %(message)s",
)
logger = logging.getLogger("process_step")

# S3-Bucket-Konstanten (identisch mit src/lib/s3.ts BUCKET_STEPS / BUCKET_THUMBNAILS)
BUCKET_STEPS = os.environ["AWS_S3_BUCKET_STEPS"]            # "parts-steps"
BUCKET_THUMBNAILS = os.environ["AWS_S3_BUCKET_THUMBNAILS"]  # "parts-thumbnails"


def get_s3_client():
    """Erstellt boto3 S3-Client aus Env-Vars (analog zu src/lib/s3.ts)."""
    return boto3.client(
        "s3",
        region_name=os.environ["AWS_REGION"],
        aws_access_key_id=os.environ["AWS_ACCESS_KEY_ID"],
        aws_secret_access_key=os.environ["AWS_SECRET_ACCESS_KEY"],
    )


def set_status(cur, part_id: str, status: str) -> None:
    """Setzt parts.status — updated_at via Datenbank-Trigger automatisch gesetzt."""
    cur.execute(
        "UPDATE parts SET status = %s WHERE id = %s",
        (status, part_id)
    )


def process(part_id: str) -> None:
    """Vollständige Pipeline für einen Teil-ID.
    
    Ablauf:
        1. DB: status → 'processing'
        2. S3: STEP-Datei herunterladen ({part_id}/original.step aus BUCKET_STEPS)
        3. STEP laden + Geometrie validieren (face_count ≥ 4, BBox-Volumen > 1e-6)
        4. 8 Views rendern (renderer.py) → 8 PNG-Dateien (512x512px)
        5. S3: 8 PNGs hochladen ({part_id}/view_0..7.png in BUCKET_THUMBNAILS)
        6. DINOv2: Embedding für alle 8 Views → Mean-Pool → 768-dim Vektor
        7. DB: embedding, embedding_model, embedding_version, thumbnail_urls, status='ready'
    
    Fehlerbehandlung:
        - ValueError (INVALID_GEOMETRY:*): DB status='failed', Fehlercode in Log
        - Alle anderen Exceptions: DB status='failed', vollständiger Traceback in Log
    
    Args:
        part_id: UUID des parts-Eintrags in der Datenbank (muss existieren mit status='pending')
    """
    logger.info(f"[{part_id}] Pipeline gestartet")

    conn = None
    s3 = get_s3_client()

    try:
        # DB-Verbindung öffnen
        conn = psycopg2.connect(os.environ["DATABASE_URL"])
        register_vector(conn)  # PFLICHT vor erstem vector(768)-Query (RESEARCH.md Anti-Pattern)
        cur = conn.cursor()

        # Schritt 1: Status → 'processing'
        set_status(cur, part_id, "processing")
        conn.commit()
        logger.info(f"[{part_id}] Status: processing")

        with tempfile.TemporaryDirectory() as tmpdir:
            # Schritt 2: STEP-Datei aus S3 laden
            step_key = f"{part_id}/original.step"
            step_path = os.path.join(tmpdir, "part.step")
            logger.info(f"[{part_id}] S3-Download: s3://{BUCKET_STEPS}/{step_key}")
            s3.download_file(BUCKET_STEPS, step_key, step_path)
            logger.info(f"[{part_id}] STEP-Datei heruntergeladen: {os.path.getsize(step_path)} Bytes")

            # Schritt 3: STEP laden + validieren (ValueError bei ungültiger Geometrie)
            shape = load_step(step_path)
            validate_geometry(shape)
            logger.info(f"[{part_id}] Geometrie-Validierung: OK")

            # Schritt 4: 8 Views rendern
            views_dir = os.path.join(tmpdir, "views")
            os.makedirs(views_dir, exist_ok=True)
            png_paths = render_views(shape, views_dir)
            assert len(png_paths) == 8, f"Erwartet 8 PNGs, erhalten: {len(png_paths)}"
            logger.info(f"[{part_id}] Rendering abgeschlossen: 8 PNGs in {views_dir}")

            # Schritt 5: PNGs nach S3 hochladen
            thumbnail_urls = []
            for i, local_path in enumerate(png_paths):
                s3_key = f"{part_id}/view_{i}.png"
                with open(local_path, "rb") as f:
                    s3.upload_fileobj(
                        f,
                        BUCKET_THUMBNAILS,
                        s3_key,
                        ExtraArgs={"ContentType": "image/png"}
                    )
                # URL-Format: s3://{bucket}/{key} — Phase 5 erstellt presigned URLs
                s3_url = f"s3://{BUCKET_THUMBNAILS}/{s3_key}"
                thumbnail_urls.append(s3_url)
                logger.info(f"[{part_id}] Hochgeladen: {s3_url}")

            # Schritt 6: DINOv2-Embeddings berechnen + Mean-Pool (D-07)
            logger.info(f"[{part_id}] DINOv2-Inferenz für 8 Views...")
            embeddings = [get_embedding(path) for path in png_paths]
            mean_embedding = mean_pool(embeddings)
            assert mean_embedding.shape == (768,), f"Embedding-Shape: {mean_embedding.shape}"
            logger.info(f"[{part_id}] Mean-Embedding: shape={mean_embedding.shape}, norm={np.linalg.norm(mean_embedding):.4f}")

            # Schritt 7: DB schreiben (embedding, thumbnail_urls, status='ready')
            cur.execute("""
                UPDATE parts SET
                    embedding = %s,
                    embedding_model = %s,
                    embedding_version = %s,
                    thumbnail_urls = %s,
                    status = 'ready'
                WHERE id = %s
            """, (
                mean_embedding,          # numpy(768,) → vector(768) via pgvector
                "dinov2-base",           # embedding_model
                "facebook/dinov2-base",  # embedding_version (CLAUDE.md: exakt dieser String)
                thumbnail_urls,          # list[str] → text[]
                part_id
            ))
            conn.commit()
            logger.info(f"[{part_id}] Pipeline abgeschlossen: status=ready")

    except ValueError as e:
        # Strukturierter Fehlercode (D-09): z.B. "INVALID_GEOMETRY:face_count=2"
        error_code = str(e)
        logger.error(f"[{part_id}] Geometrie-Fehler: {error_code}")
        if conn:
            try:
                cur = conn.cursor()
                set_status(cur, part_id, "failed")
                conn.commit()
                logger.info(f"[{part_id}] Status: failed (Fehlercode: {error_code})")
            except Exception as db_err:
                logger.exception(f"[{part_id}] DB-Update für failed-Status fehlgeschlagen: {db_err}")

    except Exception as e:
        logger.exception(f"[{part_id}] Unerwarteter Fehler: {e}")
        if conn:
            try:
                cur = conn.cursor()
                set_status(cur, part_id, "failed")
                conn.commit()
                logger.info(f"[{part_id}] Status: failed (unerwarteter Fehler)")
            except Exception as db_err:
                logger.exception(f"[{part_id}] DB-Update für failed-Status fehlgeschlagen: {db_err}")
        raise

    finally:
        if conn:
            conn.close()


if __name__ == "__main__":
    if len(sys.argv) != 2:
        print(f"Usage: python process_step.py <part-uuid>")
        sys.exit(1)

    part_id = sys.argv[1]
    process(part_id)
```

**Vorbereitung für den End-to-End-Test:**

Vor dem Ausführen muss ein parts-Eintrag in der Neon-Datenbank mit status='pending' und der STEP-Datei in S3 existieren. Erstelle diesen Eintrag manuell:

```sql
-- In psql oder Neon Dashboard ausführen:
INSERT INTO parts (id, name, part_number, project, status, sha256, original_filename, step_file_path)
VALUES (
    'aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee',  -- test-UUID
    'Test Würfel',
    'TEST-001',
    'Phase-2-Spike',
    'pending',
    'test-sha256-placeholder',
    'sample.step',
    'aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/original.step'
);
```

```bash
# STEP-Datei in S3 hochladen:
aws s3 cp worker/testdata/sample.step s3://parts-steps/aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/original.step

# Pipeline ausführen:
docker run --rm --env-file worker/.env bauteil-worker python process_step.py aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee
```
  </action>

  <verify>
    <!-- Automatisierter Check: Strukturelle Prüfung von process_step.py-Inhalt (kein DB-Zugriff nötig).
         Der pgvector cosine-similarity Query (ROADMAP SC#3) erfordert echte Credentials
         und ist daher als manual_verification dokumentiert. -->
    <automated>grep -n "register_vector" /Users/mbieling/claude/Objekterkennung/worker/process_step.py && grep -n "status = 'ready'" /Users/mbieling/claude/Objekterkennung/worker/process_step.py && grep -n "status = 'failed'" /Users/mbieling/claude/Objekterkennung/worker/process_step.py && grep -n "status.*processing" /Users/mbieling/claude/Objekterkennung/worker/process_step.py && grep -n "facebook/dinov2-base" /Users/mbieling/claude/Objekterkennung/worker/process_step.py && grep -n "VTK_DEFAULT_OPENGL_WINDOW" /Users/mbieling/claude/Objekterkennung/worker/process_step.py && echo "PROCESS_STEP_STRUCTURE_OK"</automated>
    <manual_verification>
      <!-- Erfordert echte Credentials in worker/.env (DATABASE_URL + AWS_*) -->
      <!-- Schritt 1: End-to-End-Test ausführen -->
      docker run --rm --env-file worker/.env bauteil-worker python process_step.py aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee

      <!-- Schritt 2: DB-Status prüfen (ROADMAP SC#3 — erfordert worker/.env) -->
      psql $DATABASE_URL -c "SELECT status, embedding IS NOT NULL AS has_embedding, array_length(thumbnail_urls, 1) AS thumb_count FROM parts WHERE id='aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee';"
      -- Erwartet: ready | true | 8

      <!-- Schritt 3: pgvector cosine-similarity Query (ROADMAP SC#3) -->
      psql $DATABASE_URL -c "SELECT id, 1-(embedding <=> (SELECT embedding FROM parts WHERE id='aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee')) AS sim FROM parts WHERE id='aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee';"
      -- Erwartet: similarity ≈ 1.0 (eigenes Embedding)
    </manual_verification>
  </verify>

  <acceptance_criteria>
    - worker/process_step.py existiert
    - Zeile 1-3: `import os` und `os.environ["VTK_DEFAULT_OPENGL_WINDOW"] = "vtkOSOpenGLRenderWindow"` stehen vor allen OCC-Imports
    - worker/process_step.py enthält `register_vector(conn)` (PFLICHT vor vector-Query)
    - worker/process_step.py enthält `status = 'processing'` als erstes DB-Update
    - worker/process_step.py enthält `status = 'ready'` im Erfolgsfall
    - worker/process_step.py enthält `status = 'failed'` in beiden except-Blöcken
    - worker/process_step.py enthält `"facebook/dinov2-base"` als embedding_version-Wert
    - worker/process_step.py enthält `"dinov2-base"` als embedding_model-Wert
    - worker/process_step.py enthält `f"{part_id}/view_{i}.png"` (S3-Pfadkonvention)
    - worker/process_step.py enthält `from renderer import load_step, validate_geometry, render_views`
    - worker/process_step.py enthält `from embedder import get_embedding, mean_pool`
    - `docker run --rm bauteil-worker python -c "import process_step; print('IMPORT_OK')"` gibt IMPORT_OK aus (kein Import-Fehler ohne Env-Vars, da Env-Vars erst in process() gelesen werden)
    - End-to-End-Test (mit echten Credentials in worker/.env): `docker run --rm --env-file worker/.env bauteil-worker python process_step.py <test-uuid>` endet ohne Exception und setzt status='ready' in der DB
    - Nach End-to-End-Test: psql-Query `SELECT status, embedding IS NOT NULL AS has_embedding, array_length(thumbnail_urls, 1) AS thumb_count FROM parts WHERE id='<test-uuid>'` gibt `ready | true | 8` zurück
    - pgvector-Ähnlichkeitsquery gibt similarity ≈ 1.0 für das eigene Embedding zurück (erfordert worker/.env — siehe manual_verification)
  </acceptance_criteria>

  <done>process_step.py verarbeitet eine part_id vollständig: status wechselt pending → processing → ready. embedding vector(768) mit embedding_model='dinov2-base' und embedding_version='facebook/dinov2-base' ist in der Neon-Datenbank. thumbnail_urls enthält 8 S3-URLs. pgvector cosine-similarity Query gibt similarity ≈ 1.0 für das eigene Part zurück (mit worker/.env).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| CLI-Argument → process() | part_id kommt als CLI-Argument; im Spike unkritisch (manuell gesetzt); Phase 3 (Celery-Queue) übergibt part_id aus eigener Queue |
| Container → Neon PostgreSQL | Alle DB-Queries via psycopg2 mit parametrisierten Statements (kein SQL-Injection-Risiko) |
| Container → AWS S3 | S3-Keys werden aus part_id konstruiert: f"{part_id}/original.step" — UUIDs sind sicher als S3-Key-Komponenten |
| STEP-Datei aus S3 → pythonOCC | STEP-Datei kommt aus eigenem S3-Bucket; nicht direkt von Nutzern hochgeladen (Phase 3 übernimmt Ingestion); Geometrievalidierung als erste Schutzschicht |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-02-10 | Denial of Service | process_step.py: STEP-Assembly-Größe | mitigate | face_count-Check (D-08) + BBox-Check (Pitfall 4) stoppen triviale DoS-Versuche; maximale Dateigröße (100 MB) wird in Phase 3 (S3-Upload) durchgesetzt; für den Spike akzeptabel |
| T-02-11 | Information Disclosure | process_step.py: Exception-Logging | accept | Fehler-Logs enthalten part_id und Geometrie-Details; keine Nutzer-Credentials in Logs; für lokalen Spike akzeptabel; Produktion würde strukturiertes Logging mit Datenmaskierung einsetzen |
| T-02-12 | Tampering | S3-Key-Konstruktion: f"{part_id}/original.step" | mitigate | part_id ist eine UUID aus der Datenbank; keine Path-Traversal-Möglichkeit da UUIDs keine "/" oder ".." enthalten können; psycopg2 parametrisierte Queries verhindern SQL-Injection beim DB-Lookup |
| T-02-13 | Information Disclosure | DATABASE_URL in Env-Var | mitigate | DATABASE_URL wird via `--env-file worker/.env` übergeben (nicht hardcodiert); worker/.env ist in .gitignore; `os.environ["DATABASE_URL"]` wirft KeyError bei fehlendem Wert (Fail-Fast, kein Fallback) |
| T-02-14 | Denial of Service | DINOv2 CPU-Inferenz: 8 Views × 0.5-2s | accept | CPU-Inferenz dauert 4-16s gesamt; für Spike akzeptabel; Phase 3 (Celery + Redis) entkoppelt dies vom HTTP-Lifecycle; kein gleichzeitiger Mehrfach-Aufruf im Spike |
</threat_model>

<verification>
Nach Abschluss beider Tasks:

**Automatisierte Prüfungen (kein .env nötig):**
1. Strukturprüfung: `grep -c "register_vector\|status.*ready\|status.*failed\|VTK_DEFAULT_OPENGL_WINDOW" worker/process_step.py` (Erwartung: ≥4)
2. Import-Test: `docker run --rm bauteil-worker python -c "import process_step; print('IMPORT_OK')"`
3. Embedder-Import: `docker run --rm bauteil-worker python -c "from embedder import get_embedding, mean_pool; import numpy as np; v=mean_pool([np.zeros(768), np.ones(768)]); assert v.shape==(768,); print('EMBEDDER_POOL_OK')"`

**Integration (echte Credentials in worker/.env erforderlich):**
4. End-to-End-Test: `docker run --rm --env-file worker/.env bauteil-worker python process_step.py <test-uuid>`
5. DB-Verifikation via psql: `SELECT status, embedding IS NOT NULL, array_length(thumbnail_urls,1) FROM parts WHERE id='<uuid>'` → Erwartet: `ready | t | 8`
6. pgvector-Ähnlichkeit: Cosine-Query mit eigenem Embedding → similarity ≈ 1.0 (RESEARCH.md Verifikationsquery)
</verification>

<success_criteria>
Alle 5 Phase-2-Erfolgskriterien aus ROADMAP.md müssen erfüllt sein:
1. STEP-Datei → 8 orthografische PNG-Thumbnails (test_renderer.py: RENDERER_OK, ROADMAP SC#1)
2. DINOv2 ViT-B/14 → 768-dim Embedding (mean_pool über 8 Views) (ROADMAP SC#2)
3. Embedding in Neon geschrieben + pgvector cosine-similarity Query gibt es zurück (ROADMAP SC#3)
4. face_count < 4 → parts.status='failed' + INVALID_GEOMETRY:face_count=N in Log (ROADMAP SC#4)
5. ROADMAP SC#5 (FastAPI health endpoint + Celery) ist per D-10 deferred auf Phase 3 — NICHT in diesem Spike
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/02-python-worker-spike/02-03-SUMMARY.md` mit:
- Ausgabe des End-to-End-Tests (letzten 20 Log-Zeilen)
- Ergebnis der DB-Verifikation: status, embedding IS NOT NULL, array_length(thumbnail_urls, 1)
- Cosine-Similarity-Wert des eigenen Embeddings (erwartet: ≈ 1.0)
- DINOv2-Inferenzzeit für 8 Views (aus Log-Zeitstempel berechnen)
- Irgendwelche Überraschungen bei der pgvector-Integration
- Ob alle 4 implementierten Erfolgskriterien (SC#1-4) erfüllt sind
</output>
