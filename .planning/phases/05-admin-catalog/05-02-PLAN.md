---
phase: 05-admin-catalog
plan: 02
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - src/app/api/parts/route.ts
autonomous: true
requirements:
  - ADMIN-01

must_haves:
  truths:
    - "GET /api/parts gibt alle parts-Zeilen zurück (ohne embedding-Feld)"
    - "Response-Shape: { parts: Array<{ id, name, part_number, project, status, thumbnail_count, created_at }> }"
  artifacts:
    - path: "src/app/api/parts/route.ts"
      provides: "GET /api/parts — alle Teile ohne embedding"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/parts/route.ts"
      to: "src/lib/db.ts"
      via: "db Tagged Template"
      pattern: "db`SELECT.*FROM parts"
---

<objective>
GET /api/parts Endpunkt — liefert alle Teile für die Admin-Tabelle.

Purpose: CatalogTable (Wave 2) fetcht beim Page-Load einmalig alle Teile. Alle Filter/Pagination laufen dann client-seitig. Das embedding-Feld (768-dim) wird bewusst nicht zurückgegeben — zu groß für die Admin-UI.

Output: `src/app/api/parts/route.ts` mit GET-Handler, der alle parts-Zeilen abfragt und als JSON-Array zurückgibt.
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-admin-catalog/05-RESEARCH.md
@.planning/phases/05-admin-catalog/05-PATTERNS.md
@.planning/phases/05-admin-catalog/05-01-SUMMARY.md
</context>

<interfaces>
<!-- Bestehende Interfaces die dieser Plan konsumiert -->

Aus src/app/api/parts/[id]/status/route.ts (exaktes Analog):
```typescript
import { NextResponse } from 'next/server'
import { z } from 'zod'
import { db } from '@/lib/db'

export async function GET(
  _request: Request,
  { params }: { params: Promise<{ id: string }> }
): Promise<NextResponse> {
  const { id } = await params
  const parsed = ParamsSchema.safeParse({ id })
  if (!parsed.success) {
    return NextResponse.json({ error: 'Invalid id', details: parsed.error.flatten() }, { status: 400 })
  }
  const rows = await db`SELECT status, thumbnail_count FROM parts WHERE id = ${id} LIMIT 1`
  if (rows.length === 0) {
    return NextResponse.json({ error: 'Part not found' }, { status: 404 })
  }
  return NextResponse.json({ status: rows[0].status, thumbnail_count: rows[0].thumbnail_count ?? 0 })
}
```

Aus src/lib/db.ts (Tagged-Template-Pattern):
```typescript
// IMMER diesen Import verwenden:
import { db } from '@/lib/db'
// Verwendung:
const rows = await db`SELECT id, name FROM parts ORDER BY created_at DESC`
```

parts-Tabelle (relevante Spalten für GET /api/parts):
```sql
-- Spalten die zurückgegeben werden (embedding ausgeschlossen):
id            uuid PK
name          text NOT NULL
part_number   text nullable
project       text nullable
status        text NOT NULL  -- 'pending'|'processing'|'ready'|'failed'|'archived'
thumbnail_count  integer default 0
created_at    timestamptz
-- is_archived: boolean — wird in Phase 5 NICHT zurückgegeben (D-10: nur status zählt)
-- embedding: vector(768) — wird NICHT zurückgegeben (zu groß)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/parts implementieren und Tests aktivieren</name>
  <files>src/app/api/parts/route.ts, src/app/api/parts/route.test.ts</files>
  <read_first>
    - src/app/api/parts/[id]/status/route.ts — exaktes Analog (Kommentar-Konvention, Import-Struktur)
    - src/app/api/parts/route.test.ts — Stub-Datei aus Wave 0 (todos aktivieren)
    - src/lib/db.ts — Tagged-Template-Export verifizieren
  </read_first>
  <behavior>
    - Test 1: GET /api/parts gibt { parts: [{id, name, part_number, project, status, thumbnail_count, created_at}] } zurück wenn DB 2 Zeilen hat
    - Test 2: GET /api/parts gibt { parts: [] } zurück wenn DB keine Zeilen hat
    - Test 3: Response enthält KEIN embedding-Feld (nicht in SELECT)
  </behavior>
  <action>
**Schritt 1 — Tests implementieren** (route.test.ts, todos durch echte Tests ersetzen):

```typescript
// src/app/api/parts/route.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { GET } from './route'

const mockDb = vi.fn()
vi.mock('@/lib/db', () => ({ db: mockDb }))

describe('GET /api/parts', () => {
  beforeEach(() => { mockDb.mockReset() })

  it('gibt alle Teile als Array zurück', async () => {
    const fakeParts = [
      { id: '123e4567-e89b-12d3-a456-426614174000', name: 'Schraube M8', part_number: 'M8-001', project: 'Projekt A', status: 'ready', thumbnail_count: 8, created_at: '2026-05-01T10:00:00Z' },
      { id: '223e4567-e89b-12d3-a456-426614174001', name: 'Mutter M8', part_number: null, project: null, status: 'pending', thumbnail_count: 0, created_at: '2026-05-02T10:00:00Z' },
    ]
    mockDb.mockResolvedValueOnce(fakeParts)
    const response = await GET()
    const data = await response.json()
    expect(data.parts).toHaveLength(2)
    expect(data.parts[0].id).toBe('123e4567-e89b-12d3-a456-426614174000')
    expect(data.parts[0].name).toBe('Schraube M8')
  })

  it('gibt leeres Array zurück wenn keine Teile vorhanden', async () => {
    mockDb.mockResolvedValueOnce([])
    const response = await GET()
    const data = await response.json()
    expect(data.parts).toEqual([])
  })

  it('schließt das embedding-Feld aus der Response aus', async () => {
    mockDb.mockResolvedValueOnce([{ id: '123e4567-e89b-12d3-a456-426614174000', name: 'Test', status: 'ready', thumbnail_count: 0, created_at: '2026-05-01T10:00:00Z' }])
    const response = await GET()
    const data = await response.json()
    expect(data.parts[0]).not.toHaveProperty('embedding')
  })
})
```

**Schritt 2 — Implementierung** (route.ts):

```typescript
// src/app/api/parts/route.ts
// GET /api/parts — ADMIN-01: Alle Teile für Admin-Katalog abrufen
// Server-only — KEIN "use client", keine Browser-Imports.

import { NextResponse } from 'next/server'
import { db } from '@/lib/db'

export async function GET(): Promise<NextResponse> {
  const rows = await db`
    SELECT id, name, part_number, project, status, thumbnail_count, created_at
    FROM parts
    ORDER BY created_at DESC
  `
  // embedding wird NICHT zurückgegeben — 768-dim, zu groß für Admin-UI
  // is_archived wird NICHT zurückgegeben — Phase 5 nutzt ausschließlich status (D-10)
  return NextResponse.json({ parts: rows })
}
```
  </action>
  <verify>
    <automated>npm test -- --run src/app/api/parts/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/app/api/parts/route.test.ts` gibt "3 passed" aus
    - `grep "embedding" src/app/api/parts/route.ts` findet 0 Treffer im SELECT (nur im Kommentar)
    - `grep "is_archived" src/app/api/parts/route.ts` findet 0 Treffer im SELECT
    - `grep "ORDER BY created_at DESC" src/app/api/parts/route.ts` findet 1 Treffer
    - `grep "use client" src/app/api/parts/route.ts` findet 0 Treffer
    - `grep "import { db } from '@/lib/db'" src/app/api/parts/route.ts` findet 1 Treffer (kein sql-Import)
  </acceptance_criteria>
  <done>GET /api/parts gibt { parts: [...] } zurück; 3 Tests grün; embedding und is_archived nicht im SELECT</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CatalogTable (Client) → GET /api/parts | Keine User-Eingabe — reiner Read-Endpunkt ohne Parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | GET /api/parts | accept | Kein Auth in Pilot (RLS deaktiviert per D-06 CONTEXT.md); embedding-Feld bewusst exkludiert |
| T-05-02-02 | Tampering | GET /api/parts | accept | Kein Query-Parameter — keine Injection-Fläche; db Tagged-Template verhindert SQL-Injection strukturell |
</threat_model>

<verification>
```bash
# Tests grün
npm test -- --run src/app/api/parts/route.test.ts
# Erwartet: 3 passed

# Route-Datei korrekt
grep -v "^//" src/app/api/parts/route.ts | grep "embedding"
# Erwartet: kein Output (embedding nur im Kommentar)
```
</verification>

<success_criteria>
1. `npm test -- --run src/app/api/parts/route.test.ts` — alle 3 Tests grün
2. `src/app/api/parts/route.ts` exportiert `GET`-Funktion
3. Response-Shape: `{ parts: Array<{id, name, part_number, project, status, thumbnail_count, created_at}> }`
4. embedding und is_archived sind nicht im SELECT
</success_criteria>

<output>
Nach Fertigstellung: `.planning/phases/05-admin-catalog/05-02-SUMMARY.md` erstellen.
</output>
