---
phase: 05-admin-catalog
plan: 04
type: execute
wave: 2
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - src/app/admin/page.tsx
  - src/app/admin/CatalogTable.tsx
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04

must_haves:
  truths:
    - "Admin kann alle Teile in einer Tabelle mit Thumbnail, Name, Teilenummer, Projekt, Status-Badge, Erstellt-am, Aktionen-Dropdown sehen (D-01)"
    - "Status-Tabs filtern client-seitig; Standard-Tab ist 'Alle' (D-05, D-06)"
    - "Freitext-Suche filtert nach Name oder Teilenummer mit 300ms Debounce (D-04)"
    - "20 Zeilen pro Seite mit shadcn/Pagination (D-02)"
    - "Dropdown-Aktionen: Bearbeiten / Archivieren / Löschen / ↺ Neu starten (nur bei status='failed') (D-03)"
    - "Edit-Sheet öffnet von rechts; Formular mit react-hook-form + Zod; Sheet bleibt nach Speichern offen (D-07, D-08, D-09)"
    - "AlertDialog zur Löschen-Bestätigung mit korrektem Deutsch-Copy (D-11)"
    - "Optimistic Updates mit Rollback + Sonner-Toast bei Fehler"
  artifacts:
    - path: "src/app/admin/page.tsx"
      provides: "Server Component Shell für /admin-Route"
    - path: "src/app/admin/CatalogTable.tsx"
      provides: "Client-Komponente mit allen Interaktionen"
      contains: "'use client'"
  key_links:
    - from: "src/app/admin/CatalogTable.tsx"
      to: "/api/parts"
      via: "fetch in useEffect on mount"
      pattern: "fetch.*api/parts"
    - from: "src/app/admin/CatalogTable.tsx"
      to: "/api/parts/[id]/archive"
      via: "fetch POST in handleArchive"
      pattern: "api/parts.*archive"
    - from: "src/app/admin/CatalogTable.tsx"
      to: "/api/parts/[id]/retry"
      via: "fetch POST in handleRetry"
      pattern: "api/parts.*retry"
---

<objective>
Admin-Seite und CatalogTable Client-Komponente — die vollständige UI für alle vier Admin-Anforderungen.

Purpose: Alle ADMIN-01 bis ADMIN-04 Anforderungen werden in einer einzigen Client-Komponente umgesetzt: Tabelle mit Tabs/Suche/Pagination (ADMIN-01), Edit-Sheet (ADMIN-02), Archive/Delete (ADMIN-03), Retry (ADMIN-04).

Output: `src/app/admin/page.tsx` (Server Component Shell) und `src/app/admin/CatalogTable.tsx` (Client-Komponente mit gesamter Interaktionslogik).
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-admin-catalog/05-UI-SPEC.md
@.planning/phases/05-admin-catalog/05-PATTERNS.md
@.planning/phases/05-admin-catalog/05-RESEARCH.md
@.planning/phases/05-admin-catalog/05-02-SUMMARY.md
@.planning/phases/05-admin-catalog/05-03-SUMMARY.md
</context>

<interfaces>
<!-- API-Interfaces die CatalogTable konsumiert -->

GET /api/parts → { parts: Part[] }
```typescript
type Part = {
  id: string         // UUID
  name: string
  part_number: string | null
  project: string | null
  status: 'pending' | 'processing' | 'ready' | 'failed' | 'archived'
  thumbnail_count: number
  created_at: string  // ISO 8601
}
```

PATCH /api/parts/[id] → { part: Part } | { error: string }
  Body: { name?: string; part_number?: string | null; project?: string | null; status?: 'pending'|'processing'|'ready'|'failed' }

DELETE /api/parts/[id] → { deleted: string } | { error: string }

POST /api/parts/[id]/archive → { id: string; status: 'archived' } | { error: string }

POST /api/parts/[id]/retry → { part_id: string; status: 'pending' } | { error: string }
  Status 202 bei Erfolg, 409 wenn Part nicht im 'failed'-Status

GET /api/parts/[id]/thumbnail → { url: string } | { error: string }
  (bereits vorhanden aus Phase 4 — für Thumbnails in Tabelle und Sheet)

Analog: src/app/upload/UploadForm.tsx (react-hook-form + Zod, Status-Badge, Thumbnail-Pattern):
```typescript
// Zod-Schema-Pattern (aus UploadForm.tsx Zeilen 42–48):
const editSchema = z.object({
  name: z.string().min(1, 'Bezeichnung ist erforderlich.').max(200),
  part_number: z.string().max(100).optional().or(z.literal('')),
  project: z.string().max(200).optional().or(z.literal('')),
  status: z.enum(['pending', 'processing', 'ready', 'failed']),
})
type EditValues = z.infer<typeof editSchema>

// Status-Badge Farbmapping (aus UploadForm.tsx Zeilen 357–379) — EXAKT kopieren:
// ready    → text-green-700 bg-green-50 border-green-200
// pending  → variant="secondary"
// processing → variant="outline" className="text-blue-600 border-blue-300"
// failed   → variant="destructive"
// archived → variant="outline" className="text-muted-foreground border-border"
```

Analog: src/app/upload/page.tsx (Server Component Shell-Pattern):
```typescript
import type { Metadata } from 'next'
import { UploadForm } from './UploadForm'
export const metadata: Metadata = { title: '...' }
export default function UploadPage() {
  return (
    <main className="min-h-screen bg-background py-12 px-4">
      <div className="max-w-4xl mx-auto">...</div>
    </main>
  )
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Admin-Page Server Component (page.tsx)</name>
  <files>src/app/admin/page.tsx</files>
  <read_first>
    - src/app/upload/page.tsx — Server Component Shell-Pattern (identische Struktur)
    - .planning/phases/05-admin-catalog/05-UI-SPEC.md — Page Layout, max-w-7xl, Header mit Link
  </read_first>
  <action>
Erstelle `src/app/admin/page.tsx` als dünne Server Component ohne eigene Daten-Fetching-Logik. CatalogTable übernimmt alle Daten via client-side fetch.

```typescript
// src/app/admin/page.tsx
// Phase 5 — /admin Server Component Shell.
// KEIN "use client" — CatalogTable ist Client Component, page.tsx bleibt server-side.

import type { Metadata } from 'next'
import { CatalogTable } from './CatalogTable'

export const metadata: Metadata = {
  title: 'Teile-Katalog — Bauteil-Finder',
}

export default function AdminPage() {
  return (
    <main className="min-h-screen bg-background py-8 px-4">
      <div className="max-w-7xl mx-auto">
        <CatalogTable />
      </div>
    </main>
  )
}
```

Begründung: Tabellen brauchen mehr Breite als die Upload-Form — `max-w-7xl` statt `max-w-4xl`. Header (h1 + Link) gehört in CatalogTable, da er den State für das Sheet kennen muss (UI-SPEC Interaction Contract).
  </action>
  <verify>
    <automated>ls /Users/mbieling/claude/Objekterkennung/src/app/admin/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "use client" src/app/admin/page.tsx` findet 0 Treffer
    - `grep "CatalogTable" src/app/admin/page.tsx` findet 2 Treffer (import + JSX)
    - `grep "max-w-7xl" src/app/admin/page.tsx` findet 1 Treffer
    - `grep "metadata" src/app/admin/page.tsx` findet "Teile-Katalog — Bauteil-Finder"
  </acceptance_criteria>
  <done>src/app/admin/page.tsx existiert als Server Component mit CatalogTable-Import und max-w-7xl</done>
</task>

<task type="auto">
  <name>Task 2: CatalogTable Client-Komponente implementieren</name>
  <files>src/app/admin/CatalogTable.tsx</files>
  <read_first>
    - src/app/upload/UploadForm.tsx — react-hook-form + Zod Pattern, Status-Badge Farbmapping, Thumbnail-Skeleton (vollständige Datei lesen)
    - .planning/phases/05-admin-catalog/05-UI-SPEC.md — Table Row Anatomy, Edit Sheet, AlertDialog, Copywriting Contract, Interaction Contracts
    - .planning/phases/05-admin-catalog/05-PATTERNS.md — Abschnitt CatalogTable (Debounce, Optimistic Update, Sonner-Toast)
    - src/components/ui/table.tsx — Komponenten-Exports verifizieren
    - src/components/ui/sheet.tsx — Komponenten-Exports verifizieren
  </read_first>
  <action>
Erstelle `src/app/admin/CatalogTable.tsx` als vollständige Client-Komponente. Die Datei ist lang — implementiere alle Abschnitte vollständig ohne Abkürzungen.

**Struktur der Datei (in dieser Reihenfolge):**

1. `'use client'` — erste Zeile
2. Imports: alle shadcn-Komponenten, lucide-react Icons, react-hook-form, zod, sonner
3. `Part`-TypeDefinition
4. `editSchema` + `EditValues` Zod-Schema (kein 'archived' im Status-Enum)
5. `StatusBadge`-Hilfsfunktion (EXAKT der Farbmapping aus UploadForm.tsx + archived-Variante)
6. `formatDate`-Hilfsfunktion (Intl.DateTimeFormat, de-DE, dd.MM.yyyy HH:mm)
7. `CatalogTable`-Komponente mit vollständigem State:
   - `parts: Part[]` — alle geladenen Teile
   - `isLoading: boolean`
   - `activeTab: 'all'|'ready'|'pending'|'failed'|'archived'`
   - `searchQuery: string`
   - `currentPage: number`
   - `editPart: Part | null` — aktuell im Sheet bearbeitetes Teil
   - `deleteTarget: Part | null` — aktuell im AlertDialog zu löschendes Teil
   - `sheetOpen: boolean`
   - `alertOpen: boolean`
   - `thumbnailUrls: Record<string, string>` — gecachte Presigned-URLs
8. `useEffect` für initialen fetch von `/api/parts`
9. `useEffect` für Thumbnail-Fetch (nur ready + visible via loading="lazy")
10. `filteredParts` computed: tab-Filter + search-Filter (composable)
11. `paginatedParts` computed: 20-Zeilen-Slice
12. `tabCounts` computed: Anzahl pro Status-Kategorie
13. Debounce-Handler für Suchfeld (300ms, useRef + clearTimeout)
14. `handleEdit(part)` — Sheet öffnen, form.reset mit Part-Werten
15. `handleSave(values)` — PATCH, optimistic update, Toast, Sheet bleibt offen
16. `handleArchive(id)` — optimistic update, POST /archive, Rollback bei Fehler
17. `handleDeleteConfirm()` — DELETE nach AlertDialog-Bestätigung, optimistic remove
18. `handleRetry(id)` — optimistic update auf 'pending', POST /retry, Rollback bei Fehler
19. JSX: Page-Header (h1 + Link /upload), Tabs, Suchfeld, Tabelle, Pagination, Sheet, AlertDialog

**Kritische Implementierungsdetails:**

Fetch beim Load:
```typescript
useEffect(() => {
  setIsLoading(true)
  fetch('/api/parts')
    .then(res => res.json())
    .then(data => setParts(data.parts ?? []))
    .catch(() => toast.error('Katalog konnte nicht geladen werden.'))
    .finally(() => setIsLoading(false))
}, [])
```

Thumbnail-Fetch (lazy, nur ready-Parts):
```typescript
// Thumbnails werden per <img loading="lazy"> durch den Browser gesteuert.
// URL-Cache: fetch /api/parts/[id]/thumbnail und in thumbnailUrls speichern.
// Nur ausführen wenn thumbnailUrls[part.id] noch nicht vorhanden:
useEffect(() => {
  parts.filter(p => p.status === 'ready' && !thumbnailUrls[p.id]).forEach(part => {
    fetch(`/api/parts/${part.id}/thumbnail`)
      .then(r => r.ok ? r.json() : null)
      .then(data => {
        if (data?.url) {
          setThumbnailUrls(prev => ({ ...prev, [part.id]: data.url }))
        }
      })
  })
}, [parts])
```

Optimistic Archive mit Rollback:
```typescript
const handleArchive = async (id: string) => {
  const original = parts.find(p => p.id === id)
  if (!original) return
  setParts(prev => prev.map(p => p.id === id ? { ...p, status: 'archived' } : p))
  try {
    const res = await fetch(`/api/parts/${id}/archive`, { method: 'POST' })
    if (!res.ok) throw new Error('Archive failed')
  } catch {
    setParts(prev => prev.map(p => p.id === id ? original : p))
    toast.error('Archivieren fehlgeschlagen. Bitte erneut versuchen.')
  }
}
```

Optimistic Delete mit Rollback:
```typescript
const handleDeleteConfirm = async () => {
  if (!deleteTarget) return
  const { id } = deleteTarget
  const backup = [...parts]
  setParts(prev => prev.filter(p => p.id !== id))
  setAlertOpen(false)
  setDeleteTarget(null)
  try {
    const res = await fetch(`/api/parts/${id}`, { method: 'DELETE' })
    if (!res.ok) throw new Error('Delete failed')
  } catch {
    setParts(backup)
    toast.error('Löschen fehlgeschlagen. Bitte erneut versuchen.')
  }
}
```

Optimistic Retry mit Rollback:
```typescript
const handleRetry = async (id: string) => {
  const original = parts.find(p => p.id === id)
  if (!original) return
  setParts(prev => prev.map(p => p.id === id ? { ...p, status: 'pending' } : p))
  try {
    const res = await fetch(`/api/parts/${id}/retry`, { method: 'POST' })
    if (!res.ok) throw new Error('Retry failed')
  } catch {
    setParts(prev => prev.map(p => p.id === id ? original : p))
    toast.error('Neustart fehlgeschlagen. Bitte erneut versuchen.')
  }
}
```

Tab-Filter + Suche (composable):
```typescript
const filteredParts = parts
  .filter(p => activeTab === 'all' ? true : p.status === activeTab)
  .filter(p => {
    if (!searchQuery) return true
    const q = searchQuery.toLowerCase()
    return p.name.toLowerCase().includes(q) ||
      (p.part_number ?? '').toLowerCase().includes(q)
  })
```

Pagination (Reset bei Tab/Suche):
```typescript
const ROWS_PER_PAGE = 20
const totalPages = Math.ceil(filteredParts.length / ROWS_PER_PAGE)
const paginatedParts = filteredParts.slice(
  (currentPage - 1) * ROWS_PER_PAGE,
  currentPage * ROWS_PER_PAGE
)
// Tab-Wechsel und Suche setzen currentPage auf 1 zurück
```

Edit-Sheet nach Speichern offen halten (D-09):
```typescript
const handleSave = async (values: EditValues) => {
  if (!editPart) return
  // Optimistic update:
  setParts(prev => prev.map(p => p.id === editPart.id ? { ...p, ...values, part_number: values.part_number || null, project: values.project || null } : p))
  try {
    const res = await fetch(`/api/parts/${editPart.id}`, {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        name: values.name,
        part_number: values.part_number || null,
        project: values.project || null,
        status: values.status,
      }),
    })
    if (!res.ok) throw new Error('Save failed')
    toast.success('Änderungen gespeichert.')
    // Sheet bleibt offen (D-09) — kein setSheetOpen(false) hier
  } catch {
    // Rollback editPart-Daten
    setParts(prev => prev.map(p => p.id === editPart.id ? editPart : p))
    // Inline-Fehler (kein Toast für Save-Fehler laut UI-SPEC)
    form.setError('root', { message: 'Speichern fehlgeschlagen. Bitte erneut versuchen.' })
  }
}
```

AlertDialog Copy (D-11 + UI-SPEC Copywriting Contract):
```tsx
<AlertDialogTitle>Bauteil unwiderruflich löschen?</AlertDialogTitle>
<AlertDialogDescription>
  Dieses Teil und alle zugehörigen Dateien werden permanent gelöscht. Fortfahren?
</AlertDialogDescription>
<AlertDialogCancel>Abbrechen</AlertDialogCancel>
<AlertDialogAction className="bg-destructive text-destructive-foreground hover:bg-destructive/90">
  Endgültig löschen
</AlertDialogAction>
```

Dropdown — "↺ Neu starten" nur bei status='failed':
```tsx
{part.status === 'failed' && (
  <>
    <DropdownMenuSeparator />
    <DropdownMenuItem onClick={() => handleRetry(part.id)}>
      ↺ Neu starten
    </DropdownMenuItem>
  </>
)}
```

Paginierungs-Caption (UI-SPEC):
```tsx
<p className="text-xs text-muted-foreground">
  Zeige {Math.min((currentPage - 1) * ROWS_PER_PAGE + 1, filteredParts.length)}–{Math.min(currentPage * ROWS_PER_PAGE, filteredParts.length)} von {filteredParts.length} Teilen
</p>
```

Empty States:
```tsx
// Kein Parts in DB:
{parts.length === 0 && !isLoading && (
  <div className="text-center py-12">
    <p className="text-lg font-semibold">Noch keine Bauteile vorhanden</p>
    <p className="text-sm text-muted-foreground mt-2">Lade eine STEP-Datei hoch, um mit dem Aufbau des Katalogs zu beginnen.</p>
  </div>
)}
// Suche ohne Treffer:
{filteredParts.length === 0 && parts.length > 0 && (
  <p className="text-center py-8 text-muted-foreground">Keine Teile gefunden</p>
)}
```

Accessibility (UI-SPEC):
- `<img alt={part.name}>` für Tabellen-Thumbnails
- `<img alt={"Vorschau: " + editPart?.name}>` für Sheet-Thumbnail
- `aria-label={"Aktionen für " + part.name}` auf Dropdown-Trigger-Button
- `aria-label="Teile suchen"` auf Suchfeld
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'use client'" src/app/admin/CatalogTable.tsx` findet 1 Treffer (erste Zeile)
    - `grep "use client" src/app/admin/page.tsx` findet 0 Treffer
    - `grep "fetch.*api/parts'" src/app/admin/CatalogTable.tsx` findet mindestens 1 Treffer (initialer Load)
    - `grep "api/parts.*archive" src/app/admin/CatalogTable.tsx` findet 1 Treffer (handleArchive)
    - `grep "api/parts.*retry" src/app/admin/CatalogTable.tsx` findet 1 Treffer (handleRetry)
    - `grep "status === 'failed'" src/app/admin/CatalogTable.tsx` findet 1 Treffer (Retry-Sichtbarkeit)
    - `grep "Archivieren fehlgeschlagen" src/app/admin/CatalogTable.tsx` findet 1 Treffer
    - `grep "Löschen fehlgeschlagen" src/app/admin/CatalogTable.tsx` findet 1 Treffer
    - `grep "Neustart fehlgeschlagen" src/app/admin/CatalogTable.tsx` findet 1 Treffer
    - `grep "Endgültig löschen" src/app/admin/CatalogTable.tsx` findet 1 Treffer
    - `grep "is_archived" src/app/admin/CatalogTable.tsx` findet 0 Treffer (nur status verwendet)
    - `grep "archived" src/app/admin/CatalogTable.tsx | grep "z.enum"` findet 0 Treffer (archived nicht im Edit-Schema)
    - `npm run build` beendet sich ohne TypeScript-Fehler (Exit-Code 0)
  </acceptance_criteria>
  <done>CatalogTable.tsx implementiert; npm run build grün; alle Interactions, Toasts, Optimistic Updates vorhanden</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/parts | Alle Daten kommen vom eigenen API — keine direkte DB-Verbindung im Client |
| CatalogTable (Client) → PATCH/DELETE/archive/retry | Alle Mutations gehen über API-Routes mit UUID-Validierung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | CatalogTable — PATCH body | mitigate | Zod-Schema auf Client (editSchema) + Server-seitiges PatchSchema — doppelte Validierung |
| T-05-04-02 | Information Disclosure | CatalogTable — thumbnailUrls | accept | Presigned URLs sind zeitlich begrenzt; kein sensitiver Content (nur geometrische Renders) |
| T-05-04-03 | Denial of Service | CatalogTable — Thumbnail-Fetch | accept | `loading="lazy"` und URL-Cache verhindern Burst-Requests; Pitfall 7 adressiert |
</threat_model>

<verification>
```bash
# TypeScript-Kompilierung
npm run build
# Erwartet: Exit-Code 0, keine TS-Fehler

# Kritische Strings prüfen
grep "use client" src/app/admin/CatalogTable.tsx | head -1
# Erwartet: 'use client'

grep "is_archived" src/app/admin/CatalogTable.tsx
# Erwartet: kein Treffer

grep "Endgültig löschen" src/app/admin/CatalogTable.tsx
# Erwartet: 1 Treffer

# Route erreichbar
curl -s http://localhost:3000/admin | grep -c "Teile-Katalog"
# Erwartet: >= 1 (wenn Dev-Server läuft)
```
</verification>

<success_criteria>
1. `npm run build` läuft ohne TypeScript-Fehler durch
2. `/admin` Route existiert und rendert CatalogTable
3. Alle 4 ADMIN-Anforderungen haben Interaktionspfade in CatalogTable
4. Deutsch-Copy aus UI-SPEC Copywriting Contract ist vollständig implementiert
5. Optimistic Updates mit Rollback für Archive, Delete, Retry
6. Edit-Sheet bleibt nach Speichern offen (D-09)
7. "↺ Neu starten" nur bei status='failed' sichtbar (D-03, D-12)
</success_criteria>

<output>
Nach Fertigstellung: `.planning/phases/05-admin-catalog/05-04-SUMMARY.md` erstellen.
</output>
