---
phase: 05-admin-catalog
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/api/parts/route.test.ts
  - src/app/api/parts/[id]/route.test.ts
  - src/app/api/parts/[id]/archive/route.test.ts
  - src/app/api/parts/[id]/retry/route.test.ts
  - tests/admin-catalog.spec.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04

must_haves:
  truths:
    - "Sonner-Toasts sind im Root-Layout gemountet und erscheinen im Browser"
    - "Alle 5 Test-Stub-Dateien existieren und laufen ohne Fehler (alle Tests skipped/todo)"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Toaster-Mount nach {children}"
      contains: "Toaster"
    - path: "src/app/api/parts/route.test.ts"
      provides: "Test-Stubs für GET /api/parts (ADMIN-01)"
    - path: "src/app/api/parts/[id]/route.test.ts"
      provides: "Test-Stubs für PATCH und DELETE (ADMIN-02, ADMIN-03)"
    - path: "src/app/api/parts/[id]/archive/route.test.ts"
      provides: "Test-Stubs für POST /archive (ADMIN-03)"
    - path: "src/app/api/parts/[id]/retry/route.test.ts"
      provides: "Test-Stubs für POST /retry (ADMIN-04)"
    - path: "tests/admin-catalog.spec.ts"
      provides: "Playwright E2E Smoke-Test-Stub"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/ui/sonner.tsx"
      via: "import { Toaster } from '@/components/ui/sonner'"
      pattern: "Toaster"
---

<objective>
Wave-0-Blocker-Fixes und Test-Stubs, die alle nachfolgenden Wellen freigeben.

Purpose: (1) `<Toaster />` fehlt in `src/app/layout.tsx` — ohne diesen Fix werden alle `toast.success()` / `toast.error()`-Aufrufe der Admin-UI lautlos ignoriert. (2) Test-Stubs müssen vor den API-Routes existieren, damit Wave-1–3-Executor-Agenten nach jedem Task `npm test -- --run <datei>` ausführen können.

Output: `src/app/layout.tsx` mit Toaster-Mount; 4 Vitest-Stub-Dateien (todo-Tests, keine Implementierung); 1 Playwright-Stub.
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-catalog/05-RESEARCH.md
@.planning/phases/05-admin-catalog/05-VALIDATION.md
</context>

<interfaces>
<!-- Bestehende Interfaces, die dieser Plan berührt -->

Aus src/app/layout.tsx (aktueller Stand — 21 Zeilen):
```tsx
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "AI Coding Starter Kit",
  description: "Built with AI Agent Team System",
};

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <body className="antialiased">
        {children}
      </body>
    </html>
  );
}
```

Aus src/components/ui/sonner.tsx (exportiert):
```tsx
export { Toaster }
// Toaster-Komponente von sonner-Paket, bereits installiert
```

Vitest-Stub-Pattern aus Phase 4 (src/app/api/parts/[id]/status/route.test.ts als Vorbild):
```typescript
import { describe, it, expect, vi } from 'vitest'

vi.mock('@/lib/db', () => ({ db: vi.fn() }))

describe('GET /api/parts/[id]/status', () => {
  it.todo('gibt status zurück wenn Part existiert')
  it.todo('gibt 404 zurück wenn Part nicht existiert')
  it.todo('gibt 400 zurück bei ungültiger UUID')
})
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Toaster in Root-Layout mounten</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx — aktueller Inhalt (MUSS vor Bearbeitung gelesen werden)
    - src/components/ui/sonner.tsx — Toaster-Export verifizieren
  </read_first>
  <action>
Füge `import { Toaster } from '@/components/ui/sonner'` nach dem bestehenden `import "./globals.css"` ein.

Ergänze `<Toaster />` als letztes Kind-Element innerhalb von `<body>`, NACH `{children}`. Die finale body-Struktur muss sein:

```tsx
<body className="antialiased">
  {children}
  <Toaster />
</body>
```

KEIN "use client" hinzufügen — layout.tsx bleibt Server Component. Keine anderen Änderungen an der Datei.

Begründung: Sonner benötigt genau eine `<Toaster />`-Instanz im DOM-Tree. Ohne diesen Mount werden alle `toast.success()` / `toast.error()`-Aufrufe der Admin-UI in Wave 2–3 lautlos ignoriert (RESEARCH.md Pitfall 1 / PATTERNS.md Shared Patterns).
  </action>
  <verify>
    <automated>grep -c "Toaster" /Users/mbieling/claude/Objekterkennung/src/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Toaster" src/app/layout.tsx` findet genau 2 Treffer: den Import und das JSX-Element
    - `grep "use client" src/app/layout.tsx` findet 0 Treffer (kein 'use client' hinzugefügt)
    - `grep "children" src/app/layout.tsx` zeigt `{children}` VOR `<Toaster />`
    - `npm run build` läuft ohne TypeScript-Fehler durch (Next.js prüft imports)
  </acceptance_criteria>
  <done>layout.tsx enthält `import { Toaster }` und `&lt;Toaster /&gt;` nach `{children}` in body</done>
</task>

<task type="auto">
  <name>Task 2: Vitest-Test-Stubs für alle 4 API-Routes anlegen</name>
  <files>
    src/app/api/parts/route.test.ts,
    src/app/api/parts/[id]/route.test.ts,
    src/app/api/parts/[id]/archive/route.test.ts,
    src/app/api/parts/[id]/retry/route.test.ts
  </files>
  <read_first>
    - src/app/api/parts/[id]/status/route.test.ts — Existierendes Stub-Pattern aus Phase 4 als Analogie
    - .planning/phases/05-admin-catalog/05-VALIDATION.md — genaue Test-IDs und Anforderungs-Mapping
  </read_first>
  <action>
Erstelle folgende 4 Dateien mit `it.todo()`-Stubs. Jede Datei beginnt mit dem vi.mock-Block für `@/lib/db`. Keine Implementierung — nur Stubs.

**`src/app/api/parts/route.test.ts`:**
```typescript
// src/app/api/parts/route.test.ts
// Test-Stubs für GET /api/parts — ADMIN-01
// Wave 0: Stubs anlegen; Wave 1: Implementierungen aktivieren

import { describe, it, vi } from 'vitest'

vi.mock('@/lib/db', () => ({ db: vi.fn() }))

describe('GET /api/parts', () => {
  it.todo('gibt alle Teile als Array zurück')
  it.todo('gibt leeres Array zurück wenn keine Teile vorhanden')
  it.todo('schließt das embedding-Feld aus der Response aus')
})
```

**`src/app/api/parts/[id]/route.test.ts`:**
```typescript
// src/app/api/parts/[id]/route.test.ts
// Test-Stubs für PATCH (ADMIN-02) und DELETE (ADMIN-03)
// Wave 0: Stubs anlegen; Wave 2–3: Implementierungen aktivieren

import { describe, it, vi } from 'vitest'

vi.mock('@/lib/db', () => ({ db: vi.fn() }))
vi.mock('@/lib/s3', () => ({
  s3: { send: vi.fn() },
  BUCKET_STEPS: 'parts-steps',
  BUCKET_THUMBNAILS: 'parts-thumbnails',
}))
vi.mock('@aws-sdk/client-s3', () => ({
  DeleteObjectsCommand: vi.fn(),
}))

describe('PATCH /api/parts/[id]', () => {
  it.todo('aktualisiert name, part_number, project, status')
  it.todo('gibt 400 zurück bei ungültiger UUID')
  it.todo('gibt 404 zurück wenn Part nicht existiert')
  it.todo('lehnt status="archived" im Body ab (HTTP 400)')
})

describe('DELETE /api/parts/[id]', () => {
  it.todo('löscht S3-Objekte (parts-steps + parts-thumbnails) VOR DB-Löschung')
  it.todo('ruft DeleteObjectsCommand (plural/Batch) auf, nicht DeleteObjectCommand (singular)')
  it.todo('löscht DB-Zeile nach S3-Cleanup')
  it.todo('gibt 400 zurück bei ungültiger UUID')
  it.todo('gibt 404 zurück wenn Part nicht existiert')
})
```

**`src/app/api/parts/[id]/archive/route.test.ts`:**
```typescript
// src/app/api/parts/[id]/archive/route.test.ts
// Test-Stubs für POST /api/parts/[id]/archive — ADMIN-03 (Soft-Delete)
// Wave 0: Stubs anlegen; Wave 3: Implementierungen aktivieren

import { describe, it, vi } from 'vitest'

vi.mock('@/lib/db', () => ({ db: vi.fn() }))

describe('POST /api/parts/[id]/archive', () => {
  it.todo('setzt status="archived" in der DB')
  it.todo('schreibt is_archived-Boolean NICHT (nur status manipulieren)')
  it.todo('gibt 400 zurück bei ungültiger UUID')
  it.todo('gibt 404 zurück wenn Part nicht existiert')
})
```

**`src/app/api/parts/[id]/retry/route.test.ts`:**
```typescript
// src/app/api/parts/[id]/retry/route.test.ts
// Test-Stubs für POST /api/parts/[id]/retry — ADMIN-04
// Wave 0: Stubs anlegen; Wave 3: Implementierungen aktivieren

import { describe, it, vi } from 'vitest'

vi.mock('@/lib/db', () => ({ db: vi.fn() }))

describe('POST /api/parts/[id]/retry', () => {
  it.todo('setzt status="pending" in DB BEVOR Worker aufgerufen wird')
  it.todo('ruft WORKER_URL/enqueue auf (identisch zu confirm/route.ts)')
  it.todo('gibt 409 zurück wenn status != "failed"')
  it.todo('gibt 400 zurück bei ungültiger UUID')
  it.todo('gibt 404 zurück wenn Part nicht existiert')
  it.todo('gibt 202 zurück bei Erfolg')
})
```
  </action>
  <verify>
    <automated>npm test -- --run src/app/api/parts/route.test.ts src/app/api/parts/[id]/route.test.ts src/app/api/parts/[id]/archive/route.test.ts src/app/api/parts/[id]/retry/route.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Alle 4 Dateien existieren: `ls src/app/api/parts/route.test.ts src/app/api/parts/\[id\]/route.test.ts src/app/api/parts/\[id\]/archive/route.test.ts src/app/api/parts/\[id\]/retry/route.test.ts` gibt 0 Fehler
    - `npm test -- --run` beendet sich mit Exit-Code 0 (alle Tests sind todo, kein Fehler)
    - Jede Datei enthält `vi.mock('@/lib/db'` — `grep -l "vi.mock" src/app/api/parts/route.test.ts` findet die Datei
    - `route.test.ts` für `[id]` enthält `vi.mock('@/lib/s3'` und `vi.mock('@aws-sdk/client-s3'`
    - `grep "DeleteObjectsCommand" src/app/api/parts/\[id\]/route.test.ts` findet mindestens 1 Treffer (Batch-Delete getestet)
  </acceptance_criteria>
  <done>4 Vitest-Stub-Dateien existieren; npm test beendet sich mit Exit-Code 0</done>
</task>

<task type="auto">
  <name>Task 3: Playwright E2E Smoke-Test-Stub anlegen</name>
  <files>tests/admin-catalog.spec.ts</files>
  <read_first>
    - tests/ Verzeichnis — bestehende Playwright-Specs als Muster prüfen (z.B. tests/upload.spec.ts falls vorhanden)
    - playwright.config.ts — baseURL und Timeout-Konfiguration
  </read_first>
  <action>
Erstelle `tests/admin-catalog.spec.ts` als Playwright-Stub. Die Spec enthält nur `test.todo()`-Einträge — keine Implementierung. Wird in Wave 4 aktiviert.

```typescript
// tests/admin-catalog.spec.ts
// E2E Smoke-Tests für /admin — ADMIN-01 bis ADMIN-04
// Wave 0: Stub anlegen. Wave 4: Implementierungen aktivieren nach CatalogTable-Fertigstellung.

import { test } from '@playwright/test'

test.describe('/admin — Teile-Katalog', () => {
  test.todo('Seite lädt und zeigt Tabellen-Header: Vorschau, Bezeichnung, Teilenummer, Projekt, Status, Erstellt am')
  test.todo('Status-Tabs sind sichtbar: Alle, Bereit, Ausstehend, Fehler, Archiviert')
  test.todo('Suchfeld ist vorhanden mit Placeholder "Nach Bezeichnung oder Teilenummer suchen…"')
  test.todo('Pagination zeigt "Zeige 1–N von M Teilen"')
})
```
  </action>
  <verify>
    <automated>ls /Users/mbieling/claude/Objekterkennung/tests/admin-catalog.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/admin-catalog.spec.ts` gibt den Dateinamen aus (keine Fehlermeldung)
    - `grep "test.todo" tests/admin-catalog.spec.ts` findet mindestens 3 Treffer
    - `grep "use client" tests/admin-catalog.spec.ts` findet 0 Treffer
    - `npm run test:e2e -- --list` listet die Datei ohne Fehler auf
  </acceptance_criteria>
  <done>tests/admin-catalog.spec.ts existiert mit todo-Stubs; kein import-Fehler</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout.tsx → sonner | Nur additive Import-Ergänzung, keine User-Eingabe-Verarbeitung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W0-01 | Information Disclosure | src/app/layout.tsx | accept | Toaster rendert nur Nachrichten aus eigenem Code — keine User-Daten in Toast-Messages |
</threat_model>

<verification>
```bash
# Wave-0-Gate: alle Stubs laufen ohne Fehler
npm test -- --run
# Erwartet: alle Tests als "todo", Exit-Code 0

# Toaster-Mount verifizieren
grep "Toaster" src/app/layout.tsx
# Erwartet: 2 Treffer (import + JSX)
```
</verification>

<success_criteria>
1. `npm test -- --run` beendet sich mit Exit-Code 0 nach diesem Plan
2. `src/app/layout.tsx` enthält `<Toaster />` nach `{children}`
3. Alle 5 Test-Stub-Dateien existieren und enthalten `it.todo()` / `test.todo()` Einträge
4. Keine bestehenden Tests sind durch diesen Plan gebrochen
</success_criteria>

<output>
Nach Fertigstellung: `.planning/phases/05-admin-catalog/05-01-SUMMARY.md` erstellen.
</output>
