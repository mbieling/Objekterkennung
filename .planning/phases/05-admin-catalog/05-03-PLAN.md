---
phase: 05-admin-catalog
plan: 03
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - src/app/api/parts/[id]/route.ts
  - src/app/api/parts/[id]/archive/route.ts
  - src/app/api/parts/[id]/retry/route.ts
autonomous: true
requirements:
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04

must_haves:
  truths:
    - "PATCH /api/parts/[id] aktualisiert name, part_number, project, status in der DB"
    - "PATCH lehnt status='archived' ab (HTTP 400)"
    - "DELETE /api/parts/[id] löscht S3-Objekte (batch, plural Command) VOR der DB-Zeile"
    - "POST /api/parts/[id]/archive setzt status='archived' (kein is_archived-Boolean)"
    - "POST /api/parts/[id]/retry setzt status='pending' in DB BEVOR Worker aufgerufen wird"
    - "Alle drei Routes validieren UUID als erste Operation"
  artifacts:
    - path: "src/app/api/parts/[id]/route.ts"
      provides: "PATCH (Metadaten-Update) und DELETE (Hard-Delete)"
      exports: ["PATCH", "DELETE"]
    - path: "src/app/api/parts/[id]/archive/route.ts"
      provides: "POST /archive (Soft-Delete)"
      exports: ["POST"]
    - path: "src/app/api/parts/[id]/retry/route.ts"
      provides: "POST /retry (Reset + Enqueue)"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/parts/[id]/route.ts"
      to: "@/lib/s3"
      via: "DeleteObjectsCommand (Batch, plural)"
      pattern: "DeleteObjectsCommand"
    - from: "src/app/api/parts/[id]/retry/route.ts"
      to: "WORKER_URL/enqueue"
      via: "fetch POST — identisch zu confirm/route.ts"
      pattern: "WORKER_URL.*enqueue"
---

<objective>
Drei Action-API-Routes für PATCH, DELETE (Hard), Archive (Soft) und Retry.

Purpose: CatalogTable ruft diese Routes bei Nutzeraktionen auf. Alle vier Aktionen (ADMIN-02 bis ADMIN-04) setzen ein spezifisches Server-Action-Muster um: UUID-Validierung zuerst, dann Existenz-Check, dann Business-Logik.

Output: `src/app/api/parts/[id]/route.ts` (PATCH + DELETE), `archive/route.ts` (POST Soft-Delete), `retry/route.ts` (POST Reset + Enqueue) — alle mit aktivierten Tests.
</objective>

<execution_context>
@/Users/mbieling/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbieling/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-admin-catalog/05-RESEARCH.md
@.planning/phases/05-admin-catalog/05-PATTERNS.md
@.planning/phases/05-admin-catalog/05-01-SUMMARY.md
</context>

<interfaces>
<!-- Bestehende Interfaces die dieser Plan konsumiert -->

Aus src/app/api/parts/[id]/status/route.ts (UUID-Validierungspattern — IDENTISCH kopieren):
```typescript
const ParamsSchema = z.object({
  id: z.string().uuid('id muss eine gültige UUID sein'),
})

export async function GET(
  _request: Request,
  { params }: { params: Promise<{ id: string }> }  // Next.js 16: params ist Promise
): Promise<NextResponse> {
  const { id } = await params  // ← await ist Pflicht (Pitfall 5)
  const parsed = ParamsSchema.safeParse({ id })
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'Invalid id', details: parsed.error.flatten() },
      { status: 400 }
    )
  }
  const rows = await db`SELECT status, thumbnail_count FROM parts WHERE id = ${id} LIMIT 1`
  if (rows.length === 0) {
    return NextResponse.json({ error: 'Part not found' }, { status: 404 })
  }
  // ...
}
```

Aus src/app/api/upload/confirm/route.ts (Worker-Enqueue — EXAKT kopieren für retry):
```typescript
const workerUrl = process.env.WORKER_URL
if (workerUrl) {
  let workerResponse: Response
  try {
    workerResponse = await fetch(`${workerUrl}/enqueue`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ part_id }),
    })
  } catch {
    return NextResponse.json({ error: 'Worker enqueue failed', detail: 'Worker unreachable' }, { status: 502 })
  }
  if (!workerResponse.ok) {
    return NextResponse.json({ error: 'Worker enqueue failed' }, { status: 502 })
  }
}
return NextResponse.json({ part_id, status: 'pending' }, { status: 202 })
```

Aus src/lib/s3.ts (S3-Client-Exports):
```typescript
export const s3: S3Client  // konfiguriert mit forcePathStyle + DECOMPOSEDS3_ENDPOINT
export const BUCKET_STEPS: string    // = process.env.AWS_S3_BUCKET_STEPS!
export const BUCKET_THUMBNAILS: string  // = process.env.AWS_S3_BUCKET_THUMBNAILS!
```

S3-Key-Schema für Hard-Delete:
```
parts-steps Bucket:       {part_id}/original.step          (1 Objekt)
parts-thumbnails Bucket:  {part_id}/view_0.png ... view_7.png  (8 Objekte)
Gesamt: 9 Keys — alle via DeleteObjectsCommand (Batch, plural)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PATCH und DELETE /api/parts/[id] implementieren</name>
  <files>src/app/api/parts/[id]/route.ts, src/app/api/parts/[id]/route.test.ts</files>
  <read_first>
    - src/app/api/parts/[id]/status/route.ts — UUID-Validierungsmuster (Zeilen 10–28)
    - src/app/api/parts/[id]/route.test.ts — Stubs aus Wave 0 (todos aktivieren)
    - src/lib/s3.ts — vollständige Datei lesen (s3, BUCKET_STEPS, BUCKET_THUMBNAILS)
    - src/app/api/upload/init/route.ts — JSON-Body-Parse-Pattern
  </read_first>
  <behavior>
    - PATCH Test 1: aktualisiert name, part_number, project, status in DB und gibt 200 zurück
    - PATCH Test 2: gibt 400 zurück bei ungültiger UUID (nicht-UUID-String)
    - PATCH Test 3: gibt 404 zurück wenn Part nicht in DB gefunden
    - PATCH Test 4: gibt 400 zurück wenn body status='archived' enthält
    - DELETE Test 1: ruft DeleteObjectsCommand (plural) auf — prüft dass s3.send mit DeleteObjectsCommand aufgerufen wurde, NICHT DeleteObjectCommand
    - DELETE Test 2: löscht DB-Zeile NACH S3 (Reihenfolge: s3.send zuerst, dann db DELETE)
    - DELETE Test 3: gibt 400 zurück bei ungültiger UUID
    - DELETE Test 4: gibt 404 zurück wenn Part nicht in DB gefunden
  </behavior>
  <action>
**Schritt 1 — Tests** (route.test.ts, todos ersetzen):

```typescript
// src/app/api/parts/[id]/route.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { PATCH, DELETE } from './route'
import { NextResponse } from 'next/server'

const mockDb = vi.fn()
vi.mock('@/lib/db', () => ({ db: mockDb }))

const mockS3Send = vi.fn()
vi.mock('@/lib/s3', () => ({
  s3: { send: mockS3Send },
  BUCKET_STEPS: 'parts-steps',
  BUCKET_THUMBNAILS: 'parts-thumbnails',
}))
vi.mock('@aws-sdk/client-s3', () => ({
  DeleteObjectsCommand: vi.fn().mockImplementation((args) => ({ ...args, _type: 'DeleteObjectsCommand' })),
}))

const VALID_UUID = '123e4567-e89b-12d3-a456-426614174000'
const makeParams = (id: string) => ({ params: Promise.resolve({ id }) })

describe('PATCH /api/parts/[id]', () => {
  beforeEach(() => { mockDb.mockReset(); mockS3Send.mockReset() })

  it('aktualisiert Metadaten und gibt 200 zurück', async () => {
    mockDb
      .mockResolvedValueOnce([{ id: VALID_UUID }])  // SELECT-Check
      .mockResolvedValueOnce([{ id: VALID_UUID, name: 'Neu', status: 'ready' }])  // UPDATE RETURNING
    const request = new Request('http://localhost', {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name: 'Neu', status: 'ready' }),
    })
    const res = await PATCH(request, makeParams(VALID_UUID))
    expect(res.status).toBe(200)
  })

  it('gibt 400 zurück bei ungültiger UUID', async () => {
    const request = new Request('http://localhost', {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name: 'Test' }),
    })
    const res = await PATCH(request, makeParams('nicht-eine-uuid'))
    expect(res.status).toBe(400)
  })

  it('gibt 404 zurück wenn Part nicht existiert', async () => {
    mockDb.mockResolvedValueOnce([])
    const request = new Request('http://localhost', {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name: 'Test' }),
    })
    const res = await PATCH(request, makeParams(VALID_UUID))
    expect(res.status).toBe(404)
  })

  it('lehnt status="archived" im Body ab', async () => {
    mockDb.mockResolvedValueOnce([{ id: VALID_UUID }])
    const request = new Request('http://localhost', {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ status: 'archived' }),
    })
    const res = await PATCH(request, makeParams(VALID_UUID))
    expect(res.status).toBe(400)
  })
})

describe('DELETE /api/parts/[id]', () => {
  beforeEach(() => { mockDb.mockReset(); mockS3Send.mockReset() })

  it('ruft DeleteObjectsCommand (Batch) auf und löscht danach DB-Zeile', async () => {
    mockDb
      .mockResolvedValueOnce([{ id: VALID_UUID }])  // SELECT-Check
      .mockResolvedValueOnce(undefined)  // DELETE
    mockS3Send.mockResolvedValue({})
    const request = new Request('http://localhost', { method: 'DELETE' })
    const res = await DELETE(request, makeParams(VALID_UUID))
    expect(mockS3Send).toHaveBeenCalledTimes(2)  // 2x DeleteObjectsCommand: parts-steps + parts-thumbnails
    const s3Calls = mockS3Send.mock.calls
    // Erster Call: BUCKET_STEPS
    expect(s3Calls[0][0]).toMatchObject({ Bucket: 'parts-steps' })
    // Zweiter Call: BUCKET_THUMBNAILS
    expect(s3Calls[1][0]).toMatchObject({ Bucket: 'parts-thumbnails' })
    expect(res.status).toBe(200)
  })

  it('gibt 400 zurück bei ungültiger UUID', async () => {
    const request = new Request('http://localhost', { method: 'DELETE' })
    const res = await DELETE(request, makeParams('nicht-eine-uuid'))
    expect(res.status).toBe(400)
    expect(mockS3Send).not.toHaveBeenCalled()
  })

  it('gibt 404 zurück wenn Part nicht existiert', async () => {
    mockDb.mockResolvedValueOnce([])
    const request = new Request('http://localhost', { method: 'DELETE' })
    const res = await DELETE(request, makeParams(VALID_UUID))
    expect(res.status).toBe(404)
    expect(mockS3Send).not.toHaveBeenCalled()
  })
})
```

**Schritt 2 — Implementierung** (route.ts):

```typescript
// src/app/api/parts/[id]/route.ts
// PATCH /api/parts/[id] — ADMIN-02: Metadaten aktualisieren
// DELETE /api/parts/[id] — ADMIN-03: Hard-Delete (DB + S3)
// Server-only — KEIN "use client", keine Browser-Imports.

import { NextResponse } from 'next/server'
import { z } from 'zod'
import { db } from '@/lib/db'
import { DeleteObjectsCommand } from '@aws-sdk/client-s3'
import { s3, BUCKET_STEPS, BUCKET_THUMBNAILS } from '@/lib/s3'

const ParamsSchema = z.object({
  id: z.string().uuid('id muss eine gültige UUID sein'),
})

// 'archived' bewusst NICHT in der Enum — Archivierung nur via /archive-Route (D-10, Pitfall 4)
const PatchSchema = z.object({
  name: z.string().min(1).max(200).optional(),
  part_number: z.string().max(100).nullable().optional(),
  project: z.string().max(200).nullable().optional(),
  status: z.enum(['pending', 'processing', 'ready', 'failed']).optional(),
})

export async function PATCH(
  request: Request,
  { params }: { params: Promise<{ id: string }> }
): Promise<NextResponse> {
  const { id } = await params  // Next.js 16: params ist Promise (Pitfall 5)

  const parsedParams = ParamsSchema.safeParse({ id })
  if (!parsedParams.success) {
    return NextResponse.json(
      { error: 'Invalid id', details: parsedParams.error.flatten() },
      { status: 400 }
    )
  }

  let body: unknown
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 })
  }

  const parsedBody = PatchSchema.safeParse(body)
  if (!parsedBody.success) {
    return NextResponse.json(
      { error: 'Invalid input', details: parsedBody.error.flatten() },
      { status: 400 }
    )
  }

  // Existenz-Check
  const existing = await db`SELECT id FROM parts WHERE id = ${id} LIMIT 1`
  if (existing.length === 0) {
    return NextResponse.json({ error: 'Part not found' }, { status: 404 })
  }

  const { name, part_number, project, status } = parsedBody.data

  const updated = await db`
    UPDATE parts
    SET
      name = COALESCE(${name ?? null}, name),
      part_number = COALESCE(${part_number !== undefined ? part_number : null}, part_number),
      project = COALESCE(${project !== undefined ? project : null}, project),
      status = COALESCE(${status ?? null}, status)
    WHERE id = ${id}
    RETURNING id, name, part_number, project, status, thumbnail_count, created_at
  `

  return NextResponse.json({ part: updated[0] })
}

export async function DELETE(
  _request: Request,
  { params }: { params: Promise<{ id: string }> }
): Promise<NextResponse> {
  const { id } = await params

  const parsedParams = ParamsSchema.safeParse({ id })
  if (!parsedParams.success) {
    return NextResponse.json(
      { error: 'Invalid id', details: parsedParams.error.flatten() },
      { status: 400 }
    )
  }

  // Existenz-Check
  const existing = await db`SELECT id FROM parts WHERE id = ${id} LIMIT 1`
  if (existing.length === 0) {
    return NextResponse.json({ error: 'Part not found' }, { status: 404 })
  }

  // S3 ZUERST löschen (Pitfall 6: S3-Waisen sind harmloser als DB-Einträge ohne S3)
  // 1. STEP-Datei aus parts-steps
  await s3.send(new DeleteObjectsCommand({
    Bucket: BUCKET_STEPS,
    Delete: {
      Objects: [{ Key: `${id}/original.step` }],
      Quiet: true,  // Unterdrückt leere Error-Responses für nicht existierende Keys
    },
  }))

  // 2. Alle Thumbnails aus parts-thumbnails (view_0.png bis view_7.png = 8 Objekte)
  await s3.send(new DeleteObjectsCommand({
    Bucket: BUCKET_THUMBNAILS,
    Delete: {
      Objects: Array.from({ length: 8 }, (_, i) => ({ Key: `${id}/view_${i}.png` })),
      Quiet: true,
    },
  }))

  // 3. DB-Zeile löschen — erst NACH S3 (best-effort S3-Cleanup)
  await db`DELETE FROM parts WHERE id = ${id}`

  return NextResponse.json({ deleted: id })
}
```
  </action>
  <verify>
    <automated>npm test -- --run "src/app/api/parts/[id]/route.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run "src/app/api/parts/[id]/route.test.ts"` gibt "7 passed" aus
    - `grep "DeleteObjectsCommand" src/app/api/parts/\[id\]/route.ts` findet Treffer (plural, nicht singular)
    - `grep "DeleteObjectCommand[^s]" src/app/api/parts/\[id\]/route.ts` findet 0 Treffer (singular wird nicht verwendet)
    - `grep "'archived'" src/app/api/parts/\[id\]/route.ts | grep -v "^//"` findet 0 Treffer im PatchSchema
    - Reihenfolge im DELETE-Handler: `grep -n "s3.send\|db\`DELETE" src/app/api/parts/\[id\]/route.ts` zeigt s3.send-Zeilen VOR db DELETE-Zeile
  </acceptance_criteria>
  <done>PATCH und DELETE implementiert; 7 Tests grün; DeleteObjectsCommand (plural) bestätigt; 'archived' aus PATCH-Schema ausgeschlossen</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /archive und POST /retry implementieren</name>
  <files>
    src/app/api/parts/[id]/archive/route.ts,
    src/app/api/parts/[id]/archive/route.test.ts,
    src/app/api/parts/[id]/retry/route.ts,
    src/app/api/parts/[id]/retry/route.test.ts
  </files>
  <read_first>
    - src/app/api/parts/[id]/archive/route.test.ts — Stubs aus Wave 0
    - src/app/api/parts/[id]/retry/route.test.ts — Stubs aus Wave 0
    - src/app/api/upload/confirm/route.ts — Worker-Enqueue-Block (Zeilen 46–67, EXAKT kopieren)
    - src/app/api/parts/[id]/status/route.ts — UUID-Validierungsmuster
  </read_first>
  <behavior>
    - Archive Test 1: POST setzt status='archived' in DB (kein is_archived-Boolean-Write)
    - Archive Test 2: gibt 400 zurück bei ungültiger UUID
    - Archive Test 3: gibt 404 zurück wenn Part nicht existiert
    - Retry Test 1: setzt status='pending' in DB BEVOR Worker aufgerufen wird (Reihenfolge prüfbar via mock.calls order)
    - Retry Test 2: ruft WORKER_URL/enqueue auf und gibt 202 zurück bei Erfolg
    - Retry Test 3: gibt 409 zurück wenn status != 'failed'
    - Retry Test 4: gibt 400 zurück bei ungültiger UUID
    - Retry Test 5: gibt 404 zurück wenn Part nicht existiert
  </behavior>
  <action>
**Schritt 1 — archive/route.test.ts** (todos ersetzen):

```typescript
// src/app/api/parts/[id]/archive/route.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { POST } from './route'

const mockDb = vi.fn()
vi.mock('@/lib/db', () => ({ db: mockDb }))

const VALID_UUID = '123e4567-e89b-12d3-a456-426614174000'
const makeParams = (id: string) => ({ params: Promise.resolve({ id }) })

describe('POST /api/parts/[id]/archive', () => {
  beforeEach(() => { mockDb.mockReset() })

  it('setzt status="archived" und gibt 200 zurück', async () => {
    mockDb
      .mockResolvedValueOnce([{ id: VALID_UUID }])  // SELECT-Check
      .mockResolvedValueOnce(undefined)  // UPDATE
    const req = new Request('http://localhost', { method: 'POST' })
    const res = await POST(req, makeParams(VALID_UUID))
    const data = await res.json()
    expect(res.status).toBe(200)
    expect(data.status).toBe('archived')
    // is_archived-Boolean darf NICHT beschrieben werden (RESEARCH.md DB-Schema)
    const updateCall = mockDb.mock.calls.find(c => String(c[0]).includes('UPDATE'))
    expect(String(updateCall?.[0] ?? '')).not.toContain('is_archived')
  })

  it('gibt 400 zurück bei ungültiger UUID', async () => {
    const req = new Request('http://localhost', { method: 'POST' })
    const res = await POST(req, makeParams('keine-uuid'))
    expect(res.status).toBe(400)
  })

  it('gibt 404 zurück wenn Part nicht existiert', async () => {
    mockDb.mockResolvedValueOnce([])
    const req = new Request('http://localhost', { method: 'POST' })
    const res = await POST(req, makeParams(VALID_UUID))
    expect(res.status).toBe(404)
  })
})
```

**Schritt 2 — archive/route.ts:**

```typescript
// src/app/api/parts/[id]/archive/route.ts
// POST /api/parts/[id]/archive — ADMIN-03: Soft-Delete (setzt status='archived')
// Server-only — KEIN "use client", keine Browser-Imports.

import { NextResponse } from 'next/server'
import { z } from 'zod'
import { db } from '@/lib/db'

const ParamsSchema = z.object({
  id: z.string().uuid('id muss eine gültige UUID sein'),
})

export async function POST(
  _request: Request,
  { params }: { params: Promise<{ id: string }> }
): Promise<NextResponse> {
  const { id } = await params

  const parsed = ParamsSchema.safeParse({ id })
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'Invalid id', details: parsed.error.flatten() },
      { status: 400 }
    )
  }

  const existing = await db`SELECT id FROM parts WHERE id = ${id} LIMIT 1`
  if (existing.length === 0) {
    return NextResponse.json({ error: 'Part not found' }, { status: 404 })
  }

  // Soft-Delete: nur status schreiben — is_archived-Boolean NICHT (D-10 + RESEARCH.md DB-Schema-Anmerkung)
  await db`UPDATE parts SET status = 'archived' WHERE id = ${id}`

  return NextResponse.json({ id, status: 'archived' })
}
```

**Schritt 3 — retry/route.test.ts** (todos ersetzen):

```typescript
// src/app/api/parts/[id]/retry/route.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { POST } from './route'

const mockDb = vi.fn()
vi.mock('@/lib/db', () => ({ db: mockDb }))

const mockFetch = vi.fn()
global.fetch = mockFetch

const VALID_UUID = '123e4567-e89b-12d3-a456-426614174000'
const makeParams = (id: string) => ({ params: Promise.resolve({ id }) })

describe('POST /api/parts/[id]/retry', () => {
  beforeEach(() => {
    mockDb.mockReset()
    mockFetch.mockReset()
    process.env.WORKER_URL = 'http://worker:8000'
  })

  it('setzt status="pending" in DB BEVOR Worker aufgerufen wird und gibt 202 zurück', async () => {
    mockDb
      .mockResolvedValueOnce([{ id: VALID_UUID, status: 'failed' }])  // SELECT-Check
      .mockResolvedValueOnce(undefined)  // UPDATE status='pending'
    mockFetch.mockResolvedValueOnce({ ok: true })
    const req = new Request('http://localhost', { method: 'POST' })
    const res = await POST(req, makeParams(VALID_UUID))
    expect(res.status).toBe(202)
    // DB-Update muss VOR fetch aufgerufen worden sein (Assumption A4)
    const dbCallIndex = mockDb.mock.invocationCallOrder[1]
    const fetchCallIndex = mockFetch.mock.invocationCallOrder[0]
    expect(dbCallIndex).toBeLessThan(fetchCallIndex)
  })

  it('gibt 409 zurück wenn status != "failed"', async () => {
    mockDb.mockResolvedValueOnce([{ id: VALID_UUID, status: 'ready' }])
    const req = new Request('http://localhost', { method: 'POST' })
    const res = await POST(req, makeParams(VALID_UUID))
    expect(res.status).toBe(409)
    expect(mockFetch).not.toHaveBeenCalled()
  })

  it('gibt 400 zurück bei ungültiger UUID', async () => {
    const req = new Request('http://localhost', { method: 'POST' })
    const res = await POST(req, makeParams('keine-uuid'))
    expect(res.status).toBe(400)
  })

  it('gibt 404 zurück wenn Part nicht existiert', async () => {
    mockDb.mockResolvedValueOnce([])
    const req = new Request('http://localhost', { method: 'POST' })
    const res = await POST(req, makeParams(VALID_UUID))
    expect(res.status).toBe(404)
  })

  it('gibt 502 zurück wenn Worker nicht erreichbar', async () => {
    mockDb
      .mockResolvedValueOnce([{ id: VALID_UUID, status: 'failed' }])
      .mockResolvedValueOnce(undefined)
    mockFetch.mockRejectedValueOnce(new Error('Connection refused'))
    const req = new Request('http://localhost', { method: 'POST' })
    const res = await POST(req, makeParams(VALID_UUID))
    expect(res.status).toBe(502)
  })
})
```

**Schritt 4 — retry/route.ts:**

```typescript
// src/app/api/parts/[id]/retry/route.ts
// POST /api/parts/[id]/retry — ADMIN-04: Reset status + Worker-Enqueue
// Identisches Enqueue-Muster wie src/app/api/upload/confirm/route.ts
// Server-only — KEIN "use client", keine Browser-Imports.

import { NextResponse } from 'next/server'
import { z } from 'zod'
import { db } from '@/lib/db'

const ParamsSchema = z.object({
  id: z.string().uuid('id muss eine gültige UUID sein'),
})

export async function POST(
  _request: Request,
  { params }: { params: Promise<{ id: string }> }
): Promise<NextResponse> {
  const { id } = await params

  const parsed = ParamsSchema.safeParse({ id })
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'Invalid id', details: parsed.error.flatten() },
      { status: 400 }
    )
  }

  const existing = await db`SELECT id, status FROM parts WHERE id = ${id} LIMIT 1`
  if (existing.length === 0) {
    return NextResponse.json({ error: 'Part not found' }, { status: 404 })
  }

  if (existing[0].status !== 'failed') {
    return NextResponse.json(
      { error: 'Only failed parts can be retried' },
      { status: 409 }
    )
  }

  // DB-Update ZUERST (Assumption A4 RESEARCH.md):
  // Falls Worker-Call danach fehlschlägt, kann Admin Retry erneut auslösen.
  await db`UPDATE parts SET status = 'pending' WHERE id = ${id}`

  // Worker-Enqueue — identisches Muster zu confirm/route.ts (Zeilen 46-67)
  const workerUrl = process.env.WORKER_URL
  if (workerUrl) {
    let workerResponse: Response
    try {
      workerResponse = await fetch(`${workerUrl}/enqueue`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ part_id: id }),
      })
    } catch {
      return NextResponse.json(
        { error: 'Worker enqueue failed', detail: 'Worker unreachable' },
        { status: 502 }
      )
    }
    if (!workerResponse.ok) {
      return NextResponse.json({ error: 'Worker enqueue failed' }, { status: 502 })
    }
  }

  return NextResponse.json({ part_id: id, status: 'pending' }, { status: 202 })
}
```
  </action>
  <verify>
    <automated>npm test -- --run "src/app/api/parts/[id]/archive/route.test.ts" "src/app/api/parts/[id]/retry/route.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run "...archive/route.test.ts"` gibt "3 passed" aus
    - `npm test -- --run "...retry/route.test.ts"` gibt "5 passed" aus
    - `grep "is_archived" src/app/api/parts/\[id\]/archive/route.ts` findet 0 Treffer (nur Kommentar erlaubt)
    - `grep "status = 'archived'" src/app/api/parts/\[id\]/archive/route.ts` findet 1 Treffer
    - `grep -n "UPDATE.*pending\|fetch.*enqueue" src/app/api/parts/\[id\]/retry/route.ts` zeigt UPDATE vor fetch (niedrigere Zeilennummer)
    - `grep "409" src/app/api/parts/\[id\]/retry/route.ts` findet 1 Treffer (nur failed Parts können retried werden)
    - `grep "WORKER_URL" src/app/api/parts/\[id\]/retry/route.ts | grep "NEXT_PUBLIC"` findet 0 Treffer
  </acceptance_criteria>
  <done>archive/route.ts und retry/route.ts implementiert; 8 Tests grün; is_archived nicht geschrieben; DB-Update vor Worker-Enqueue</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CatalogTable (Client) → PATCH/DELETE/archive/retry | User-Eingaben aus Edit-Sheet und Dropdown-Aktionen |
| retry/route.ts → WORKER_URL/enqueue | Server-zu-Server-HTTP-Call, WORKER_URL ist server-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | PATCH /api/parts/[id] — params.id | mitigate | `z.string().uuid()` als erste Operation verhindert Path-Traversal in S3-Key-Konstruktion |
| T-05-03-02 | Tampering | PATCH /api/parts/[id] — body.status | mitigate | PatchSchema schließt 'archived' explizit aus — nur via /archive Route (D-10) |
| T-05-03-03 | Tampering | DELETE /api/parts/[id] — S3-Key | mitigate | UUID-Validierung vor S3-Key-Konstruktion (`${id}/original.step`); UUID enthält keine Path-Separatoren |
| T-05-03-04 | Tampering | PATCH body — Strings | mitigate | Zod `z.string().max(200)` / `.max(100)` auf alle Edit-Felder |
| T-05-03-05 | Information Disclosure | retry/route.ts — WORKER_URL | mitigate | Kein NEXT_PUBLIC_ Prefix; server-only env var |
| T-05-03-06 | Spoofing | retry/route.ts — status check | mitigate | Status-Prüfung (`status !== 'failed'` → 409) verhindert unbeabsichtigte Re-Enqueue von running/ready Parts |
</threat_model>

<verification>
```bash
# Alle neuen Tests grün
npm test -- --run "src/app/api/parts/[id]/route.test.ts" "src/app/api/parts/[id]/archive/route.test.ts" "src/app/api/parts/[id]/retry/route.test.ts"
# Erwartet: 7 passed + 3 passed + 5 passed = 15 passed total

# Batch-Delete verifizieren (plural, nicht singular)
grep "DeleteObjectsCommand" src/app/api/parts/\[id\]/route.ts
# Erwartet: Mindestens 1 Treffer

grep "DeleteObjectCommand[^s]" src/app/api/parts/\[id\]/route.ts
# Erwartet: kein Treffer

# is_archived nicht geschrieben
grep "is_archived" src/app/api/parts/\[id\]/archive/route.ts | grep -v "^//"
# Erwartet: kein Treffer
```
</verification>

<success_criteria>
1. 15 Tests grün (7 für PATCH+DELETE, 3 für archive, 5 für retry)
2. DeleteObjectsCommand (plural/Batch) wird für Hard-Delete verwendet
3. S3-Löschung erfolgt VOR DB-Löschung (Reihenfolge korrekt)
4. archive/route.ts schreibt nur status='archived', kein is_archived
5. retry/route.ts setzt DB-Status vor Worker-Enqueue-Call
6. Alle Routes: UUID-Validierung als erste Operation
</success_criteria>

<output>
Nach Fertigstellung: `.planning/phases/05-admin-catalog/05-03-SUMMARY.md` erstellen.
</output>
